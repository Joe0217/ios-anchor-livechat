# E 里程碑派对房列表增强 · 实施计划（B 档精简）

> **For agentic workers:** 按 CLAUDE.md B 档规则：合并 impl 阶段、不用 TodoWrite 仪式化、无改动不跑 xcodebuild test、spec 已存 [E-spec-派对房列表增强-202607101300.md](E-spec-派对房列表增强-202607101300.md)。

**Goal:** Party 大厅补齐搜索/语言 pill/3-tab TabView(.page)/4 项榜单入口占位；对齐 H5 用户端 `livechat-h5/src/views/party/index.vue`。

**Architecture:** PartyListMainView 顶层 View 承载 顶部 tab 条 + 语言横滑 pill + 双榜单卡 + 3-tab TabView(.page)；重构 PartyRoomListView 拆出 Party tab 内容区复用；PartyListStore 扩展 languages/activeLanguageIndex；新增 PartyFollow/Recent Store 占位遵循新抽的 PartyRoomListLike protocol；PartySearchView 独立 push 页 + PartySearchStore 3 态。

**Tech Stack:** SwiftUI, Swift 5, iOS 16+, PartyAPI (真接口 `roomList(queryParam:languageCode:)` + `languageList()`), 复用 `Image("liveRankBadge")` / `L10n.homeTopTabComingSoon` / LiveTabView.reconnectToast 模式。

---

## 文件蓝图

| 动作 | 路径 | 责任 |
|---|---|---|
| Create | Sources/Party/List/PartyRoomListLike.swift | protocol 抽 3 store 契约 |
| Create | Sources/Party/List/PartyFollowListStore.swift | Follow tab 占位 store（Coming soon 空态） |
| Create | Sources/Party/List/PartyRecentListStore.swift | Recent tab 占位 store |
| Create | Sources/Party/List/PartySearchStore.swift | 搜索 store，5 态，300ms throttle |
| Create | Sources/Party/UI/PartyListMainView.swift | 新顶层 view：顶部 tab 条 + 语言 pill + 双榜单卡 + TabView(.page) |
| Create | Sources/Party/UI/PartySearchView.swift | 搜索独立页（push） |
| Modify | Sources/Party/List/PartyListStore.swift | 加 languages / activeLanguageIndex / setLanguage / loadLanguagesIfNeeded；languageCodeProvider 语义改为"缺省 fallback" |
| Modify | Sources/Party/UI/PartyRoomListView.swift | 剥离 header / crownBadge / createRoomButton；本文件降级为"Party tab 内容区"复用组件 `PartyRoomListContent` |
| Modify | Sources/Party/PartyRoute.swift | 加 `case search` case |
| Modify | Sources/Party/UI/PartyTabRootView.swift | 挂 PartyListMainView 而非 PartyRoomListView；navigationDestination 加 .search |
| Modify | Sources/L10n.swift | 加 tab labels + search 占位文案 + coming soon 复用 |
| Modify | project.yml | 若新增文件 xcodegen 会自动扫源码目录，无需改 |

---

### Task 1: L10n 加派对房增强所需文案

**Files:**
- Modify: `Sources/L10n.swift`（找到 `enum Party` 段追加）

- [ ] **Step 1: 追加 Party 相关文案**

在 `Sources/L10n.swift` 里 `enum Party` 段落追加：

```swift
static var tabParty: String { localize("party.tab.party", comment: "派对房 Party tab") }
static var tabFollow: String { localize("party.tab.follow", comment: "派对房 Follow tab") }
static var tabRecent: String { localize("party.tab.recent", comment: "派对房 Recent tab") }
static var languageAll: String { localize("party.language.all", comment: "语言 pill All 选项") }
static var rankPartyRich: String { localize("party.rank.partyRich", comment: "PartyRich 榜卡文案") }
static var rankRoom: String { localize("party.rank.room", comment: "Room 榜卡文案") }
static var searchPlaceholder: String { localize("party.search.placeholder", comment: "搜索输入框占位") }
static var searchHint: String { localize("party.search.hint", comment: "空 query 提示") }
static var searchNoResults: String { localize("party.search.noResults", comment: "无搜索结果") }
static var comingSoon: String { localize("party.comingSoon", comment: "占位 toast 文案") }
```

同步添加到 `Sources/Resources/en.lproj/Localizable.strings` / `ar.lproj` / `tr.lproj`（3 语言）：

```
"party.tab.party" = "Party";
"party.tab.follow" = "Follow";
"party.tab.recent" = "Recent";
"party.language.all" = "All";
"party.rank.partyRich" = "PartyRich";
"party.rank.room" = "Room";
"party.search.placeholder" = "Search rooms";
"party.search.hint" = "Enter keywords to search rooms";
"party.search.noResults" = "No rooms found";
"party.comingSoon" = "Coming soon";
```

阿语/土语用 i18n skill 或对应现有翻译风格补齐（若无翻译则先英文兜底）。

- [ ] **Step 2: Commit**

```bash
git add Sources/L10n.swift Sources/Resources
git commit -m "feat: [派对房] 新增列表增强所需 L10n 文案 (Tab/语言/搜索/榜单)"
```

---

### Task 2: PartyListStore 扩展语言状态 + setLanguage

**Files:**
- Modify: `Sources/Party/List/PartyListStore.swift`

- [ ] **Step 1: 在 State 下方（约 L66）加语言相关字段**

```swift
// MARK: - 语言状态（增强新增）

/// 首个占位为 "All"（id=-1, languageCode=nil）；后续 append PartyAPI.languageList 结果。
@Published private(set) var languages: [PartyLanguage] = [.all]

@Published private(set) var activeLanguageIndex: Int = 0

/// languages 是否已从后端拉过（避免重复请求；失败保留只有 All 单项）
private var didLoadLanguages = false
```

- [ ] **Step 2: PartyLanguage 加静态构造 all**

在 `Sources/Party/Models/PartyLanguage.swift`（若无则在 PartyRoomInfo.swift 附近）扩展：

```swift
extension PartyLanguage {
    /// 前端拼接的 "All" 选项（languageCode=nil 代表不筛语言，拉全大厅）
    static let all = PartyLanguage(id: -1, languageCode: nil, languageName: L10n.Party.languageAll)
}
```

若 `PartyLanguage.id` 类型是 String 或非 Int，或 `PartyLanguage` init 签名不同，先 Read 该文件按实际字段填。

- [ ] **Step 3: 加 loadLanguagesIfNeeded + setLanguage 方法**

在 `func retryPage()` 下方：

```swift
// MARK: - 语言（增强新增）

/// 首次进入 Party tab 时拉一次语言列表。失败保留 [.all] 单项。
func loadLanguagesIfNeeded() async {
    guard !didLoadLanguages else { return }
    didLoadLanguages = true
    do {
        let list = try await PartyAPI.languageList()
        languages = [.all] + list
    } catch {
        // 保留 [.all]，静默；下次 tab 切回不再重试（避免长期失败刷屏）
        languages = [.all]
    }
}

/// 切换语言 pill → activeLanguageIndex 更新 + 触发重拉
func setLanguage(index: Int) {
    guard index >= 0, index < languages.count, index != activeLanguageIndex else { return }
    activeLanguageIndex = index
    beginRefresh()
}

/// 当前语言 code（`nil` = All）；`languageCodeProvider` 参数保留仅作 fallback，activeLanguage 优先。
private var currentLanguageCode: String? {
    guard activeLanguageIndex < languages.count else { return nil }
    return languages[activeLanguageIndex].languageCode
}
```

- [ ] **Step 4: 改 performInitial / performLoadMore 用 currentLanguageCode**

替换 `performInitial` 里的 `languageCode: languageCodeProvider(),` 为 `languageCode: currentLanguageCode ?? languageCodeProvider(),`。同样改 `performLoadMore`。

- [ ] **Step 5: Commit**

```bash
git add Sources/Party/List/PartyListStore.swift Sources/Party/Models/PartyLanguage.swift
git commit -m "feat: [派对房] PartyListStore 扩展语言状态 + setLanguage 入口"
```

---

### Task 3: 抽 PartyRoomListLike protocol + Follow/Recent Store 占位

**Files:**
- Create: `Sources/Party/List/PartyRoomListLike.swift`
- Create: `Sources/Party/List/PartyFollowListStore.swift`
- Create: `Sources/Party/List/PartyRecentListStore.swift`

- [ ] **Step 1: Create PartyRoomListLike.swift**

```swift
import Foundation

/// 3 tab store（Party/Follow/Recent）共用契约。
/// State 复用 `PartyListStore.State`（refactor 后 6 态适合空态占位 store 直接落 .loaded(rooms: [], hasMore: false)）。
@MainActor
protocol PartyRoomListLike: ObservableObject {
    var state: PartyListStore.State { get }
    func startInitial()
    func refreshAsync() async
    func loadMore()
    func retry()
    func retryPage()
}

extension PartyListStore: PartyRoomListLike {}
```

- [ ] **Step 2: Create PartyFollowListStore.swift**

```swift
import Foundation

/// Follow tab 占位 store（E 增强：接口未定，落 .loaded([], hasMore: false) 空态）。
/// 未来接入 apiGetFollowRoomList 时替换 startInitial 内实现即可。
@MainActor
final class PartyFollowListStore: ObservableObject, PartyRoomListLike {
    @Published private(set) var state: PartyListStore.State = .idle

    func startInitial() { state = .loaded(rooms: [], hasMore: false) }
    func refreshAsync() async { state = .loaded(rooms: [], hasMore: false) }
    func loadMore() {}
    func retry() {}
    func retryPage() {}
}
```

- [ ] **Step 3: Create PartyRecentListStore.swift**（同 Follow 结构）

```swift
import Foundation

@MainActor
final class PartyRecentListStore: ObservableObject, PartyRoomListLike {
    @Published private(set) var state: PartyListStore.State = .idle

    func startInitial() { state = .loaded(rooms: [], hasMore: false) }
    func refreshAsync() async { state = .loaded(rooms: [], hasMore: false) }
    func loadMore() {}
    func retry() {}
    func retryPage() {}
}
```

- [ ] **Step 4: Commit**

```bash
git add Sources/Party/List/PartyRoomListLike.swift Sources/Party/List/PartyFollowListStore.swift Sources/Party/List/PartyRecentListStore.swift
git commit -m "feat: [派对房] 抽 PartyRoomListLike protocol + Follow/Recent 占位 store"
```

---

### Task 4: PartySearchStore

**Files:**
- Create: `Sources/Party/List/PartySearchStore.swift`

- [ ] **Step 1: 编写 Store**

```swift
import Foundation

/// 派对房搜索 store（E 增强：真接口 PartyAPI.roomList(queryParam:) + 300ms throttle）。
@MainActor
final class PartySearchStore: ObservableObject {

    enum State: Equatable {
        case idle             // 无 query 提示态
        case searching(query: String)
        case result(rooms: [PartyRoomInfo], query: String)
        case empty(query: String)
        case error(message: String, query: String)
    }

    @Published private(set) var state: State = .idle
    @Published var query: String = "" {
        didSet {
            guard oldValue != query else { return }
            scheduleSearch()
        }
    }

    private var currentTask: Task<Void, Never>?

    deinit { currentTask?.cancel() }

    private func scheduleSearch() {
        currentTask?.cancel()
        let q = query.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !q.isEmpty else {
            state = .idle
            return
        }
        state = .searching(query: q)
        currentTask = Task { [weak self] in
            do {
                try await Task.sleep(nanoseconds: 300_000_000)  // 300ms throttle
                try Task.checkCancellation()
                let rooms = try await PartyAPI.roomList(
                    languageCode: nil,
                    snapshotId: nil,
                    offset: nil,
                    pageSize: 30,
                    queryParam: q,
                    version: "v2"
                )
                try Task.checkCancellation()
                await MainActor.run { [weak self] in
                    guard let self, self.query.trimmingCharacters(in: .whitespacesAndNewlines) == q else { return }
                    self.state = rooms.isEmpty ? .empty(query: q) : .result(rooms: rooms, query: q)
                }
            } catch is CancellationError {
                return
            } catch {
                await MainActor.run { [weak self] in
                    guard let self, self.query.trimmingCharacters(in: .whitespacesAndNewlines) == q else { return }
                    self.state = .error(message: error.localizedDescription, query: q)
                }
            }
        }
    }

    func retry() {
        guard case .error(_, let q) = state else { return }
        query = ""  // trigger 清 → 再赋值
        query = q
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add Sources/Party/List/PartySearchStore.swift
git commit -m "feat: [派对房] PartySearchStore 5 态 + 300ms throttle"
```

---

### Task 5: 拆分 PartyRoomListView → PartyRoomListContent 复用组件

**Files:**
- Modify: `Sources/Party/UI/PartyRoomListView.swift`

**动机**：现在 PartyRoomListView 含 header/crownBadge/createRoomButton/roomList 混在一起，PartyListMainView 需要复用它的 "roomList" 部分（3 tab 都要显示同款空态/error/loaded）。

- [ ] **Step 1: 剥离 header / crownBadge / createRoomButton**

将 PartyRoomListView 改造为**只暴露内容区**（不再持 header 和浮按钮），API 精简为：

```swift
/// 派对房列表内容区（Party tab / Follow tab / Recent tab 复用）。
/// header / 浮按钮 / 榜单卡 由父 view `PartyListMainView` 承载。
struct PartyRoomListContent<Store: PartyRoomListLike>: View {
    @ObservedObject var store: Store
    let onTapRoom: (String) -> Void
    /// 是否让 loaded([]) 走 "Coming soon" 空态（Follow/Recent tab true；Party tab false）
    let comingSoonOnEmpty: Bool

    var body: some View {
        contentArea
    }

    @ViewBuilder
    private var contentArea: some View {
        switch store.state {
        case .idle, .loading:
            loadingView
        case .loaded(let rooms, let hasMore):
            if rooms.isEmpty {
                if comingSoonOnEmpty { comingSoonView } else { emptyView }
            } else {
                roomList(rooms: rooms, hasMore: hasMore, showBottomLoader: false, pageErrorMessage: nil)
            }
        case .refreshing(let rooms):
            roomList(rooms: rooms, hasMore: false, showBottomLoader: false, pageErrorMessage: nil)
        case .loadingMore(let rooms):
            roomList(rooms: rooms, hasMore: true, showBottomLoader: true, pageErrorMessage: nil)
        case .error(let message, _):
            errorView(message: message)
        case .pageError(let rooms, let message):
            roomList(rooms: rooms, hasMore: true, showBottomLoader: false, pageErrorMessage: message)
        }
    }

    // 复用原来的 loadingView / emptyView / errorView / roomList / loadMoreSentinel...
    // 新增 comingSoonView：
    private var comingSoonView: some View {
        VStack(spacing: 14) {
            Image(systemName: "hourglass")
                .font(.system(size: 42))
                .foregroundColor(Theme.Palette.partyGreeting)
            Text(L10n.Party.comingSoon)
                .font(.system(size: 14))
                .foregroundColor(Theme.Palette.partyGreeting)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
    // ... 其余原样迁移
}
```

**PartyRoomCardView** 保持不变（同文件下方）。

**Preview** 简化：只留 loaded/empty/error 3 态即可（其他 tab 通过 store fake 相同验证）。

- [ ] **Step 2: 保留兼容：新增一个 wrapper struct 让旧 preview 不炸**

若 `PartyRoomListView_Previews` 复杂到 refactor 成本高，先删掉 preview（B 档 rule "不必要仪式化删掉"），后续 PartyListMainView Preview 补上。

- [ ] **Step 3: Commit**

```bash
git add Sources/Party/UI/PartyRoomListView.swift
git commit -m "refactor: [派对房] PartyRoomListView → PartyRoomListContent 泛型组件 (protocol-driven)"
```

---

### Task 6: PartyListMainView 新建

**Files:**
- Create: `Sources/Party/UI/PartyListMainView.swift`

- [ ] **Step 1: 编写 PartyListMainView**

```swift
import SwiftUI

/// 派对房大厅新顶层 view（E 增强）。
/// 顶部 3 tab (Party/Follow/Recent) + 搜索/榜单 icon + 语言 pill 横滑（仅 Party tab）
/// + 双榜单卡（仅 Party tab）+ TabView(.page) 3-tab 横滑内容区 + 浮动 Create Room 按钮。
struct PartyListMainView: View {
    @ObservedObject var listStore: PartyListStore
    @StateObject private var followStore = PartyFollowListStore()
    @StateObject private var recentStore = PartyRecentListStore()

    let onTapCreate: () -> Void
    let onTapRoom: (String) -> Void
    let onTapSearch: () -> Void

    @State private var activeTab: Int = 0
    @State private var toastTick: UUID?
    @Environment(\.isPartyTabActive) private var isPartyTabActive

    var body: some View {
        ZStack(alignment: .bottom) {
            Theme.Palette.partyListBackground.ignoresSafeArea()

            VStack(spacing: 0) {
                topBar
                if activeTab == 0 {
                    languagePillBar
                    rankBanner
                }
                tabContent
            }

            createRoomButton
                .padding(.bottom, 24)

            toast
        }
        .task(id: isPartyTabActive) {
            guard isPartyTabActive else { return }
            if case .idle = listStore.state { listStore.startInitial() }
            await listStore.loadLanguagesIfNeeded()
        }
        .navigationBarHidden(true)
        .safeAreaInset(edge: .bottom, spacing: 0) {
            Color.clear.frame(height: Theme.Metric.tabBarHeight)
        }
    }

    // MARK: - 顶部 3 tab + icons
    private var topBar: some View {
        HStack(alignment: .center, spacing: 0) {
            HStack(spacing: 20) {
                ForEach(0..<3, id: \.self) { i in
                    tabButton(index: i, label: tabLabel(i))
                }
            }
            Spacer(minLength: 8)
            searchIcon
            rankBadge
        }
        .padding(.horizontal, 16)
        .padding(.top, 8)
        .padding(.bottom, 8)
    }

    private func tabLabel(_ i: Int) -> String {
        switch i {
        case 0: return L10n.Party.tabParty
        case 1: return L10n.Party.tabFollow
        default: return L10n.Party.tabRecent
        }
    }

    private func tabButton(index: Int, label: String) -> some View {
        Button {
            activeTab = index
            if index == 1 { followStore.startInitial() }
            if index == 2 { recentStore.startInitial() }
        } label: {
            VStack(spacing: 4) {
                Text(label)
                    .font(.system(size: 18, weight: .bold))
                    .foregroundColor(activeTab == index ? .white : .white.opacity(0.5))
                Capsule()
                    .fill(activeTab == index
                          ? LinearGradient(colors: [Theme.Palette.brandYellow, Theme.Palette.brandPinkA],
                                           startPoint: .leading, endPoint: .trailing)
                          : LinearGradient(colors: [.clear, .clear], startPoint: .leading, endPoint: .trailing))
                    .frame(width: 10, height: 3)
            }
        }
        .buttonStyle(.plain)
    }

    private var searchIcon: some View {
        Button(action: onTapSearch) {
            Image(systemName: "magnifyingglass")
                .font(.system(size: 18, weight: .semibold))
                .foregroundColor(.white.opacity(0.85))
                .frame(width: 32, height: 32)
        }
        .buttonStyle(.plain)
        .accessibilityLabel(L10n.Party.searchPlaceholder)
    }

    private var rankBadge: some View {
        Button {
            fireToast()
        } label: {
            Image("liveRankBadge")
                .resizable()
                .scaledToFit()
                .frame(width: 56, height: 28)
                .padding(.leading, 8)
        }
        .buttonStyle(.plain)
        .accessibilityLabel(L10n.Party.rankPartyRich)
    }

    // MARK: - 语言 pill 横滑
    private var languagePillBar: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 8) {
                ForEach(Array(listStore.languages.enumerated()), id: \.offset) { idx, lang in
                    Button {
                        listStore.setLanguage(index: idx)
                    } label: {
                        Text(lang.languageName ?? "")
                            .font(.system(size: listStore.activeLanguageIndex == idx ? 15 : 14,
                                          weight: listStore.activeLanguageIndex == idx ? .semibold : .medium))
                            .foregroundColor(listStore.activeLanguageIndex == idx ? .white : .white.opacity(0.5))
                            .padding(.horizontal, 12)
                            .frame(height: 28)
                            .background {
                                if listStore.activeLanguageIndex == idx {
                                    Capsule().fill(LinearGradient(
                                        colors: [Theme.Palette.brandYellow, Theme.Palette.brandOrange, Theme.Palette.brandPinkA],
                                        startPoint: .leading, endPoint: .trailing
                                    ))
                                } else {
                                    Capsule().fill(Color.white.opacity(0.06))
                                }
                            }
                    }
                    .buttonStyle(.plain)
                }
            }
            .padding(.horizontal, 12)
        }
        .frame(height: 44)
    }

    // MARK: - 双榜单卡
    private var rankBanner: some View {
        HStack(spacing: 12) {
            rankCard(title: L10n.Party.rankPartyRich, gradient: [Theme.Palette.brandOrange, Theme.Palette.brandPinkA])
            rankCard(title: L10n.Party.rankRoom, gradient: [Theme.Palette.brandYellow, Theme.Palette.brandOrange])
        }
        .padding(.horizontal, 12)
        .padding(.bottom, 8)
    }

    private func rankCard(title: String, gradient: [Color]) -> some View {
        Button {
            fireToast()
        } label: {
            ZStack {
                RoundedRectangle(cornerRadius: 16, style: .continuous)
                    .fill(LinearGradient(colors: gradient, startPoint: .topLeading, endPoint: .bottomTrailing))
                Text(title)
                    .font(.system(size: 15, weight: .bold))
                    .foregroundColor(.white)
                    .shadow(color: .black.opacity(0.2), radius: 1)
            }
            .frame(height: 78)
        }
        .buttonStyle(.plain)
    }

    // MARK: - TabView 3 页
    private var tabContent: some View {
        TabView(selection: $activeTab) {
            PartyRoomListContent(store: listStore, onTapRoom: onTapRoom, comingSoonOnEmpty: false)
                .refreshable { await listStore.refreshAsync() }
                .tag(0)
            PartyRoomListContent(store: followStore, onTapRoom: onTapRoom, comingSoonOnEmpty: true)
                .tag(1)
            PartyRoomListContent(store: recentStore, onTapRoom: onTapRoom, comingSoonOnEmpty: true)
                .tag(2)
        }
        .tabViewStyle(.page(indexDisplayMode: .never))
    }

    // MARK: - 浮动 Create Room 按钮（保留 iOS 现有）
    private var createRoomButton: some View {
        Button(action: onTapCreate) {
            HStack(spacing: 6) {
                Image("partyCreatePlus").resizable().frame(width: 20, height: 20)
                Text(L10n.Party.listCreateRoom)
                    .font(.system(size: 15, weight: .semibold))
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 20).padding(.vertical, 12)
            .background(Capsule().fill(LinearGradient(
                colors: [Theme.Palette.partyCreateBtnA, Theme.Palette.partyCreateBtnB],
                startPoint: .leading, endPoint: .trailing
            )))
            .shadow(color: Theme.Palette.partyCreateBtnA.opacity(0.5), radius: 12, y: 4)
        }
        .buttonStyle(.plain)
    }

    // MARK: - Toast (顶部胶囊 2s 自消)
    @ViewBuilder
    private var toast: some View {
        if toastTick != nil {
            VStack {
                Text(L10n.Party.comingSoon)
                    .font(.system(size: 13, weight: .medium))
                    .foregroundStyle(.white)
                    .padding(.horizontal, 16).padding(.vertical, 8)
                    .background(Color(hex: 0x424242), in: Capsule())
                    .padding(.top, 60)
                Spacer()
            }
            .transition(.move(edge: .top).combined(with: .opacity))
            .task(id: toastTick) {
                try? await Task.sleep(nanoseconds: 2_000_000_000)
                toastTick = nil
            }
        }
    }

    private func fireToast() {
        toastTick = UUID()
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add Sources/Party/UI/PartyListMainView.swift
git commit -m "feat: [派对房] PartyListMainView 新顶层视图 (3 tab + 语言 pill + 双榜单卡 + TabView.page)"
```

---

### Task 7: PartySearchView 独立页

**Files:**
- Create: `Sources/Party/UI/PartySearchView.swift`

- [ ] **Step 1: 编写 PartySearchView**

```swift
import SwiftUI

struct PartySearchView: View {
    @StateObject private var store = PartySearchStore()
    let onTapRoom: (String) -> Void

    var body: some View {
        VStack(spacing: 0) {
            searchBar
            content
            Spacer(minLength: 0)
        }
        .background(Theme.Palette.partyListBackground.ignoresSafeArea())
        .navigationBarBackButtonHidden(true)
        .toolbar {
            ToolbarItem(placement: .navigationBarLeading) {
                BackButton()
            }
        }
        .swipeToPopEnabled()
    }

    private var searchBar: some View {
        HStack(spacing: 8) {
            Image(systemName: "magnifyingglass")
                .foregroundColor(.white.opacity(0.5))
            TextField(L10n.Party.searchPlaceholder, text: $store.query)
                .textFieldStyle(.plain)
                .foregroundColor(.white)
                .autocorrectionDisabled()
                .textInputAutocapitalization(.never)
            if !store.query.isEmpty {
                Button { store.query = "" } label: {
                    Image(systemName: "xmark.circle.fill")
                        .foregroundColor(.white.opacity(0.5))
                }
            }
        }
        .padding(.horizontal, 12).frame(height: 40)
        .background(Capsule().fill(Color.white.opacity(0.08)))
        .padding(.horizontal, 16).padding(.top, 8).padding(.bottom, 12)
    }

    @ViewBuilder
    private var content: some View {
        switch store.state {
        case .idle:
            hint(text: L10n.Party.searchHint)
        case .searching:
            VStack { ProgressView().tint(.white); }
                .frame(maxWidth: .infinity).padding(.top, 40)
        case .result(let rooms, _):
            ScrollView {
                LazyVStack(spacing: 12) {
                    ForEach(rooms, id: \.stableListId) { room in
                        Button { onTapRoom(room.id ?? "") } label: {
                            PartyRoomCardView(room: room)
                        }
                        .buttonStyle(.plain)
                    }
                }
                .padding(.horizontal, 16)
            }
        case .empty:
            hint(text: L10n.Party.searchNoResults)
        case .error(let msg, _):
            VStack(spacing: 12) {
                Text(msg).foregroundColor(.orange).multilineTextAlignment(.center)
                Button("Retry") { store.retry() }.foregroundColor(.white)
            }
            .padding(.top, 40)
        }
    }

    private func hint(text: String) -> some View {
        Text(text)
            .font(.system(size: 14))
            .foregroundColor(.white.opacity(0.5))
            .frame(maxWidth: .infinity).padding(.top, 40)
    }
}
```

**注意**：`BackButton` 若工程无现成组件，替换成自定义 `Button { dismiss() } label: { Image(systemName: "chevron.left") }` 用 `@Environment(\.dismiss)`。

- [ ] **Step 2: Commit**

```bash
git add Sources/Party/UI/PartySearchView.swift
git commit -m "feat: [派对房] PartySearchView 独立搜索页 (300ms throttle + 5 态)"
```

---

### Task 8: PartyRoute 加 search case + PartyTabRootView 挂 PartyListMainView

**Files:**
- Modify: `Sources/Party/PartyRoute.swift`
- Modify: `Sources/Party/UI/PartyTabRootView.swift`

- [ ] **Step 1: PartyRoute 加 search**

在 `PartyRoute` enum 追加：

```swift
case search
```

- [ ] **Step 2: PartyTabRootView 替换根 view**

将 `PartyRoomListView(store: listStore, ...)` 替换为：

```swift
PartyListMainView(
    listStore: listStore,
    onTapCreate: { path.append(PartyRoute.create) },
    onTapRoom: { roomId in path.append(PartyRoute.room(id: roomId, password: nil)) },
    onTapSearch: { path.append(PartyRoute.search) }
)
```

navigationDestination 追加 `.search` 分支：

```swift
case .search:
    PartySearchView(onTapRoom: { roomId in
        path.append(PartyRoute.room(id: roomId, password: nil))
    })
```

- [ ] **Step 3: Commit**

```bash
git add Sources/Party/PartyRoute.swift Sources/Party/UI/PartyTabRootView.swift
git commit -m "feat: [派对房] PartyTabRootView 挂 PartyListMainView + 加 .search 路由"
```

---

### Task 9: xcodegen 生成 + 编译验证

**Files:**
- 无源码改动

- [ ] **Step 1: 重新生成 xcodeproj**

```bash
./bin/regen.sh
```

若无脚本：

```bash
xcodegen generate && LANG=en_US.UTF-8 pod install
```

（先让用户关闭 Xcode workspace，避免 xcodegen-podinstall-binding rule 里的 pbxproj cache 覆盖问题）

- [ ] **Step 2: 命令行 build 验证**

```bash
xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
  -destination 'generic/platform=iOS' -configuration Debug \
  CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -10
```

预期：`BUILD SUCCEEDED`。若失败，按报错位置回到对应 task 修 → 再 build。

---

### Task 10: 真集成 + 真机验收（B 档合并 step 3+4）

- [ ] **Step 1: 用户 Xcode 真机 build + 逐项验证**

按 spec §3 8 F + 5 R 逐条真机点验：

**F**:
- F1 顶部 3 tab 可点 + 横滑手势切换 + 下方彩色 indicator ✓
- F2 Party tab 加载真数据 ✓
- F3 Follow/Recent tab 显 hourglass + "Coming soon" ✓
- F4 语言 pill 横滑；All 拉全大厅；切 English 拉筛选后 ✓
- F5 🔍 图标 push PartySearchView；输入 "abc" 300ms 后拉结果；back 手势可返回 ✓
- F6 3 榜单入口点击 → toast "Coming soon" 2s 自消 ✓
- F7 浮动 Create Room 按钮不变 ✓
- F8 Party tab 下拉刷新有效 ✓

**R**:
- R1 语言 pill 拉失败：仅 All（可通过断网首次进 tab 验证） ✓
- R2 搜索离线：显 error 态可 retry ✓
- R3 Follow/Recent 切走切回：保持空态 ✓
- R4 快速连点榜单入口：toast 覆盖延续 ✓
- R5 搜索页 back 无副作用 ✓

- [ ] **Step 2: 反悔归类**

若真机反悔按 CLAUDE.md B 档 rule §step 3 反悔归类（5 方向）处理：spec 漏 case / 状态机错 / View 结构错 / 通用知识缺失 / 范围扩展。

反悔 0 → 直接进 Task 11 提交总 commit + 收尾。反悔 ≥1 → 修 → 回归 → 再验。

---

### Task 11: 收尾

- [ ] **Step 1: 若真机反悔为 0，跳过 sink；反悔 ≥1，evaluate 是否升级 rule**

按 B 档 rule "step 6 事件触发"，反悔 0 → 无需沉淀新 rule；反悔 ≥1 → 判是否与现有 rule 重复 ≥50%，重复则不沉淀。

- [ ] **Step 2: 若测试有必要，加最小单测**

`PartyListStore` 已有 PreviewFake 支持；本次不新增 unit test（B 档 rule "xcodebuild test 无改动不跑"）。语言状态迁移逻辑直接靠真机验收。

- [ ] **Step 3: 最终 review**

`git log --oneline -10` 检查提交 clean。

---

## 风险与已知坑

按 CLAUDE.md rules 列举本 impl 涉及的坑：

- **swiftui-body-type-check-timeout**：PartyListMainView.body 含 4 层 + 多 modifier，若编译报 "unable to type-check"，按规则把 topBar/languagePillBar/rankBanner/tabContent/toast 分别提为 computed property（**本 plan 已按此拆好**）。
- **swiftui-background-in-shape-signature**：语言 pill 里 `.background { if...else Capsule().fill(...) }` 已用 view-based `.background { }` 正例（**本 plan 已按此写**）。
- **default-swipe-back-on-push-pages**：PartySearchView 用 `.navigationBarBackButtonHidden(true) + 自定义 back + .swipeToPopEnabled()` 三件套（**本 plan 已按此写**）。
- **prefer-shared-component-over-adhoc**：Toast 复用 LiveTabView.reconnectToast 模式（同 view 内嵌，不新建全局基建）；PartyRoomCardView 复用 Party 搜索结果。
- **cross-scene-component-reuse-preflight**：不复用 PartyCreateLanguagePickerSheet（其自持 PartyCreateRoomStore，跨场景不合适）。
- **list-refresh-preserve-items**：Party tab `.refreshable` 已配对 `refreshAsync()`（已有）。
- **xcodegen-podinstall-binding**：新增 5 个 .swift 源文件后必须跑 `./bin/regen.sh`（Task 9 已安排）。

## 预计工作量

- 新增代码：~450 行
- 修改代码：~80 行
- 提交数：约 8-10 个 commit
- 真机验收：15 min
- 反悔 buffer：预留 30 min
