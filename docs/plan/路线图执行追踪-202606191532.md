# Hily 原生 iOS 主播端 · 路线图执行追踪

> 关联：`iOS主播端-全量上线里程碑路线图-202606191532.md`
> 用途：记录 A→J 共 10 个里程碑的启动/完成/阻塞，确保首发前全部串起
> 更新规则：每个里程碑进入"启动"或"完成"或新增"阻塞"时，原地编辑本表

> **2026-07-29 状态校准**：以下“当前状态”以工作区实际源码、测试目标和已完成真机验收为准；其后的原始长记录保留为实施历史，若两者冲突，以本节为准。代码存在不等于真机验收通过；RTC、相机、提现/活体等依赖设备或服务端的链路均单列为“待真机回归”。

| 代号 | 当前状态 | 已落地范围 | 明确未完成范围 |
|---|---|---|---|
| A-D | **已完成** | 登录、直播、视频通话、直播转私 call | 无里程碑功能缺口；J 统一处理少量收尾项 |
| E | **已完成，真机验收通过** | 派对房进出房、上下麦、视频位、邀请/被踢处理、公屏、送礼、心跳 | 无 MVP 缺口 |
| F | **部分完成** | 房间管理、排麦、锁房/模式、MC 位、PartyBattle、Lucky Number、PartyCall 设置与抢占、麦时数据 | 半屏游戏及房币兑换仍未实现 |
| G | **已完成，真机验收通过** | 主态直播 PK、邀请、匹配、惩罚、重连、历史/排行 UI | 客态 PK、推荐列表等原始范围外能力不计入本里程碑 |
| H | **部分完成** | 私聊、礼物特效引擎、公共/派对礼物面板、虚拟道具、IM 分发、直播任务、通话索要礼物/举报/SS 徽章、付费跑马灯、礼物墙列表展示 | 独立礼物墙详情页/路由仍缺；礼物特效及 IM 等真机链路待回归 |
| I | **代码已实现，待真机回归** | 任务中心、直播/派对数据页、积分排行、朋友圈、资料编辑、黑名单、sapi token、原生钱包、提现账户/记录、提现密码、人脸采集及 OSS 上传 | 未发现明确代码缺口；提现和活体流程必须以真实账号、相机与服务端回归为准 |
| J | **部分完成** | 机器人通话（132/133、预录视频、Agora 推流及结算）、直播间 WheelShow 配置、ThinkingData、en/ar/tr 与 RTL 基础、匹配真人脸检测与取证上报 | 独立抽奖转盘未发现实现；Crash 上报、远程灰度/功能开关仍缺；新手任务、Big R、Party Data 等入口仍为占位页。人脸合规链路待真机回归 |

---

## 一、里程碑执行状态（历史实施记录）

| 代号 | 名称 | 状态 | 启动日期 | 完成日期 | 备注 |
|---|---|---|---|---|---|
| A | 基建骨架 + 登录闭环 | **已完成** | 已在 POC 阶段开始 | 2026-06-23 | Keychain ✓ / 错误码 1992/1006/2001 ✓ / print→Logger ✓ / 1004·1005 集中分流 ✓ / userType 路由守卫 ✓ / xhigh review 通过（2026-06-24） |
| B | 直播闭环产品化 | **已完成** | 2026-06-19 | 2026-06-21 | v5 弱网分层 10/30/5 落地；v5.2 后台中断过滤；v5.8 订阅字典模型完整 spec/plan/implement 闭环 |
| C | 1v1 通话从 POC 升生产（仅视频） | **已完成** | 2026-06-20 | 2026-06-22 | CallStore + RTM 信令 / -6 -90 蒙层 / callRate 计费心跳全跑通 |
| D | 直播 ↔ 通话切换（转私 call） | **已完成** | 2026-06-20 | 2026-06-22 | pauseForCall / resumeCall 状态机；CallView overlay；15s 回直播倒计时；v5.8 双下游字典共享采集 |
| E | 派对房 MVP | **MVP 已真机跑通；视频位远端流 v4 增量已通过；A 档 NIM 链路已验；B/D 档待验** | 2026-06-23 | — | 进房/上下麦/公屏/送礼/WS 保活全链路通；2026-06-24 spec v4 amendment 把"他人视频位远端流渲染"从 F 期提前到 E 并真机验证通过（R1/R4/R5/R7）；2026-06-24 A 档真机通过（上麦 30s+ 不掉麦 / 1008 自切麦克风 applyMediaUpdate 定向更新 / 1001 direct seatList replace）；剩 B（1040 视频位邀请 + respondInvite）/ D（1003 被踢双字段守护）/ C（2049 送礼）三档待验；Sources/Party/ 共 ~1370 行；spec/v4 amendment/计划/安卓确认 4+1 份文档已落档 |
| F | 派对房完整玩法 | 未启动 | — | — | 必须 E 完成后启动 |
| G | 直播 PK 玩法 | **M0~M6 全编码 + 多轮真机 bug 修复 (10 项) + 5 并发邀请同步 H5 + QUICK 3s/RETRY 15s 轮询 + 222 单测全过；剩 M7 真机 DoD + 邀请弹窗推荐列表扩展(spec §1.2 反悔，待拍板)** | 2026-06-23 | — | spec 通过 + 经 5-critic 复查修订 13 个 critical/high；范围收窄：仅主态 + 中断重连 + D 互斥 + .pkActive 编码档位（480×640/30fps/500kbps）；客态/推荐列表/历史/规则图/同时邀请 5 个并发 / svga 动画 / 礼物 PK 期重排全部范围外。**M0 段一（2026-06-24）落地**：(1) 新增 `Sources/PK/PKChannelDelegate.swift`（PK 频道独立 delegate）；(2) `AgoraManager` 扩展：`oppositeRemoteView` / NSLock + 三字典（pkConnections/pkDelegates/leavePKContinuations）/ `joinPKOpposite/leavePKOpposite` async + 500ms 兜底 + 兜底超时 warning 日志 / nonisolated `pkConnection(forChannel:)` + `resumeLeavePKContinuation(channel:)` 返回 Bool / `EncoderQuality` 扩 `.pkActive` (480×640/30fps/500kbps) + `.pkLow` (480×640/15fps/400kbps)；(3) `NetworkQualityMonitor` 升级 ObservableObject + 暴露 `@Published var currentLevel: NetworkQualityLevel`（normal/weakWarning/weakSevere）在 start/degrade/recover/endLive 同步；(4) `LiveRoomView` 加 M0 临时调试入口 sheet（输入对手 channelId+uid 调 joinPKOpposite/leavePKOpposite，PR 前删除）。**xcodegen + pod install + xcodebuild clean build 全过**（SDK API 签名 joinChannelEx/leaveChannelEx/setupRemoteVideoEx/didLeaveChannelExWith 在 4.5.2.9.BASIC 正确）。code-reviewer 3 Critical 实际为设计强化建议，已在字典声明加铁律注释 + 兜底 warning 日志增强可观测性；C3 回滚后 SDK 回调因 `weak owner` + `pkConnection(forChannel:)` 返回 nil 自然跳过。**M0 段二（用户真机验证）待启动**：DoD 1-3（iOS×2 互看 + 本端 CameraPreview 持续 + leave 无残留）+ DoD 4（iOS h264 ↔ H5 vp9 codec 互通 R1）。**M1 数据契约（2026-06-24）落地**：(1) `Sources/PK/PKServiceError.swift`（business/notImplemented/decode 三类）；(2) `Sources/PK/PKModels.swift`（PKStateMain 9 态 + PKType/PKInviteHandle/PKDisconnectType/PKPunishFromStatus 四 enum + PKMatchResult/PKJoinResponse/PKScoreUpdate/PKTopUser/PKInviteInfo/PKInviteAck/PKStatusBundle/PKRemoteStatus 八模型；PKTopUser 字段 `nickName` 驼峰非对称已对齐 H5 livePk.js:1740）；(3) `Sources/PK/PKService.swift`（11 必用接口全实现：startPkMatch/cancelMatch/joinPk/invitePk/handleInvite/endPk/endPunishing/getPkTop3RankList/getPkStatus/mutePkRoom/updateInviteSwitch；6 占位接口 throw notImplemented）；(4) `AttachType` 加 PK 段 6 case（97/98/99/100/-8/-9）+ String "-8"/"-9"/"97"/.../"100" 双形态兜底；(5) `Localizable.strings` en/ar/tr 三语言各 30 条 PK 文案 + `L10n.PK` 嵌套 namespace 26 个静态属性；(6) `project.yml` HilyTests target 链入 AttachType/PKModels/PKServiceError 纯数据层；(7) 单测 `AttachTypeTests`（10 case：Int 正/负、String 正/负、nil/unknown 兜底、raw round-trip）+ `PKModelsDecodeTests`（15 case：joinPk 全/缺字段、getPkTop3RankList 数组/空、PKRemoteStatus rawValue、PKStatusBundle 10/8/-1 子分发、PKInviteAck 1/2、PKScoreUpdate/InviteInfo/MatchResult、PKServiceError 行为）。**xcodebuild 主 target build + HilyTests test 51 用例全过 (0 failures)**。**M1-2b 后端协调 4 项**（2026-06-24 通过 H5 已上线源码全部消解）：(a) endTime → H5 livePk.js:685 实证后端不下发，本地 `Date.now() + pkDuration*1000` 自算（R3 消解）；(b) attachType -8/-9 → H5 走 NSNumber.intValue Int 形态（已双形态兜底，R2 消解）；(c) updateInviteSwitch → H5 未做防抖（说明幂等或前端不在意，iOS 加 `isUpdatingInviteSwitch` 防重入即可）；(d) reconcileOnReconnect → H5 syncPkStateAfterReconnect line 961-1056 仅 getPkStatus 字符串校验远端，本地 ctx 全靠内存态保留；iOS 同行为，进程被杀就当 PK 已退（R8 消解）。**M2 状态机骨架（2026-06-24）落地**：(1) `Sources/PK/PKStoreObserver.swift`（4 protocol 方法默认空实现 + @MainActor weak）；(2) `Sources/PK/PKContext.swift`（pkId/oppositeUserId/oppositeChannel/duration/endTime 内存态；remainingSeconds 计算属性；endTime 本地 `Date()+duration` 对齐 H5 行为）；(3) `Sources/PK/PKCountdownController.swift`（3 独立 Task 计时器 invite/inPK/punish；inPK 基于绝对 endAt 前后台不漂移；scheduleX 幂等覆盖旧 task；sleep 完成后显式 `Task.isCancelled` 守 race window）；(4) `Sources/PK/PKStore.swift`（@MainActor + ObservableObject + 9 态状态机；transition 9 用户操作 + 5 NIM 入口 + reconcileOnReconnect + teardown + DEBUG testOnly 钩子；callState 联动 spec §2.4 表 (idle/failed→0; matching/inviting/invited→2; starting/inPK/punishing→3; endingPK 瞬时态保持) + LiveStore.setCallState `guard .living` 双保险；weak liveStore/agora/nim/observer/networkMonitor 守 nil；NQM `$currentLevel` Combine 订阅 enter inPK 时建立 / exit endingPK 时取消，监听 weakSevere/weakWarning 切 `.pkLow` 拦截 forceEnd / 监听 normal 切回 `.pkActive`；invite switch entry 快照 + isUpdatingInviteSwitch 防重入 + userToggledInviteSwitch 尊重用户手动）；(5) project.yml HilyTests target 链入 PKContext + PKCountdownController（PKStore 因 SDK 依赖不入测，状态机集成测留 M3 真机）。**xcodebuild 主 target build + HilyTests test 57 用例全过 (0 failures)**，其中 PKCountdownControllerTests 6 case 覆盖三计时器互不干扰 / cancelAll race 兜底 / endAt 已过立即 expire / 重复 schedule 幂等覆盖 / 基于绝对时间漂移测试。**M0 段二真机验证 + M3 极简 UI 段待启动** |
| H | 礼物全景 + 虚拟道具 + IM 完善 | **H-0 用户详情页基础已完成（feature-pipeline trial #3）；H 主体未启动** | 2026-06-25 | — | **H-0（前置子任务）2026-06-26 闭环**：用户详情页（展示+关注+拉黑入口+1v1 视频通话）+ 多入口 UserProfileRoute 路由架构 + LiveTab/List 接入；spec v3（397 行，trial #2 砍 43%）+ 10 项 step 3 真机反悔批次修复（含 H5 type.ts userId NSCFNumber 类型偏移 / followType 字段名 / UI 4 处对齐 H5 / 礼物墙真渲染 / SwipeBackEnabler / CallStore.shared.callOut 接入）+ /review deep 5 维并行审查 + 5 项建议落地；BlocklistVM `.blocklistChanged` observer 跨页同步兑现 trial #2 spec §5.4；BlocklistRow AsyncImage 头像 fix（trial #2 step 1c 遗债清理）；38+31=69 单测 339/339 全过；沉淀 `.claude/rules/ios-decode-userid-compat.md` + `xcodegen-podinstall-binding.md` 更新。Sources/Profile/UserProfile/ + SwipeBackEnabler + FollowRelationNotification 抽出。详见 [archive/H-0-pipeline-checklist-用户详情页-202606261731.md](archive/H-0-pipeline-checklist-用户详情页-202606261731.md)。**H 主体（IM 私聊 / 礼物 / 举报 / 礼物墙详情）未启动** |
| I | 收益与外围 | **I-1 黑名单列表已完成（feature-pipeline trial #2）；I 主体未启动** | 2026-06-24 | — | I-1 黑名单列表 2026-06-25 闭环；trial #2 流水线全程 + spec v2 + 222 单测 + 真机零反悔（事后 trial #3 发现 step 1c 遗 review #9 头像 AsyncImage TODO，已 trial #3 修复）；含 sapi 双 token、提现活体、UTC+8 倒计时 |
| J | 机器人/转盘/埋点/i18n/Crash/首发收尾 | 未启动 | — | — | 必须最后串行，期间禁止 B-I 功能性变更 |

---

## 二、当前阻塞清单

### 阻塞 A（已全部消化）
- [x] ~~1004 挤下线 / 1005 token 失效：APIClient 层集中分流 → 触发全局登出 + 弹 alert~~（2026-06-23 落地：APIClient 单点 throw 前 post `.apiSessionInvalidated`，SessionStore observer 统一 logout + 通过 `L10n.authErrorSessionInvalidated` 提示）
- [x] ~~userType 路由守卫：未审核主播禁止进入开播；代理（userType=9）入口分流~~（2026-06-23 落地：LivePrepareView.startLive() 首行守卫，==2 才放行，==9 走 `livePrepareGuardAgent`，其余走 `livePrepareGuardUnverified`，三语已补）

### 阻塞 B
- [x] ~~§7.2 #1–9 原"开工前确认"9 项~~（B 实施过程已逐项消化）
- [x] ~~§7.3 #10 三端心跳统一~~（iOS 已定 10s/>3 次，对齐安卓）
- [x] ~~§7.3 #16 声网 codec：H5 vp9 + 原生 H.264 同频道互通~~（2026-07-22 用户确认真机验收通过）

### 阻塞 C
- [x] ~~§7.3 #14 声网 1v1 真实通话计费心跳~~（已按 H5 蓝本：起止全用户端驱动 / callRate 可开关）

### 阻塞 E
- [x] ~~§7.3 #11 派对房 90+ 接口 dev 环境可用性~~（2026-06-23 后端确认全可用）

### 阻塞 J
- [x] ~~§7.3 #12 机器人通话相关接口与客户端实现~~（2026-07-29 代码复核：NIM 132/133 路由、接听/拒绝/结束 API、预录视频、Agora 推流和结算弹窗均已存在；待真机回归）
- [ ] §7.3 #13 两套转盘的剩余接口与完整玩法：直播间 WheelShow 已实现；派对房 Super Wheel 不纳入本项；独立抽奖转盘（`expand/turntableGame/*`）未发现代码、路由或接口调用
- [x] ~~真人脸检测与合规上报~~（2026-07-29 代码复核与实现：相芯 FaceProcessor 真检测、短时美颜后帧取证、OSS 上传、`matchFaceViolationReport` 及 `toggleMatch(faceCheckStatus: 1)` 均已接入；待真机回归）
- [ ] Crash 上报与远程灰度/功能开关：未发现 Crash SDK、未捕获异常上报、Remote Config 或 Feature Flag 实现
- [ ] Work 占位入口：新手任务（Newbie）、Big R、Party Data；另有忘记密码路由和私信系统消息的部分 CTA 未实现
- [ ] A 收尾 xhigh review 复核遗留 4 项（2026-06-24 标记）：
  - 心跳收 1004/1005 主动联动 forceEnd（避免靠累积失败 >3 兜底）
  - userType==9 后端升级为 2 后客户端缓存不刷新机制
  - 开播无权限的路由层前置守卫（避免 LivePrepareView 已启动相机/美颜后才拦截，浪费 1-2s 硬件资源）
  - 其余 cleanup/altitude/UX 类 11 项（observer→Combine 简化、errorMessage 长度限制等），统一在 J 收尾 i18n + 文案审核阶段一并处理

---

## 三、关键决策变更记录

> 路线图锁定的关键决策见路线图 §2；本表只记录"路线图发布后"的新增/调整。

| 日期 | 变更内容 | 来源 | 影响里程碑 |
|---|---|---|---|
| 2026-06-19 | 心跳/endType/弱网阈值默认对齐安卓（10s/>3/4-2-6-7-5/≥10次） | 本路线图 §2 | B/D/G |
| 2026-06-20 | **弱网阈值 v5 反悔为分层 10/30/5（"连续"语义，中间一次质量 ≤4 即清零重计）**：连续 ≥10 次降帧率 30→15fps+toast / 连续 ≥30 次下播 / 降级期连续 ≥5 次质量好恢复，保留下播兜底加缓冲层 | B-LiveStore §4.5 + B-spec H5安卓二次校验 v5 修订 | B（M2 已实施） |
| 2026-06-19 | 音频通话不做（C 仅视频） | 本路线图 §2 | C |
| 2026-06-19 | 虚拟来电不做（独立来电响铃页） | 本路线图 §2 | J |
| 2026-06-19 | PartyBattle 60s 轮询不做，按 NIM 推送实现 | 本路线图 §2 | F |
| 2026-06-19 | 不预先抽 RoomSession 通用底座，事后抓 ≥3 处重复再抽 | 本路线图 §2 | 全部 |
| 2026-06-22 | **CameraManager 帧分发改 v5.8 订阅字典**：单闭包 onFrame 在直播私 call 场景被 PIP 覆写本体且 SwiftUI overlay 兄弟节点拿不到重绑时机；改 `[ObjectIdentifier: sink]` + Coordinator 静态 dismantleUIView 精准注销 | `.claude/rules/swiftui-camera-preview.md` §3 + CameraManager.swift v5.8 | B/C/D 共享相机管线 |
| 2026-06-23 | 派对房后端接口 dev 环境确认全可用，E 阻塞解除 | 后端口头确认 | E |
| 2026-06-23 | **G 范围收窄**：仅主态 PK + 中断重连 + D 互斥；**客态 PK 延后**（等"主播浏览他人房间"链路落地）；入口 UI 极简（仅随机匹配 + 输入对手 ID，不做推荐列表/历史/搜索）；礼物 PK 期重排留 H | G-spec-H5安卓代码二次校验 §1.2 + G-直播PK玩法-spec §14 | G |
| 2026-06-23 | **G 新增 AgoraManager `.pkActive` 编码档位**（480×640/30fps/500kbps），PK 期 + 弱网叠加 → `.pkLow`（480×640/15fps/400kbps）；从源头压双频道带宽预算 | G-直播PK玩法-spec §3.5 | G |
| 2026-06-23 | **G R1 codec 风险升级"中高"**：现有直播+通话完全未配置过 codec（沿用 SDK 默认 h264），H5 推 vp9，互通靠 SDK 自动协商，工程零实证；M0 阶段独立真机验证，失败时回退方案 A 后端协调 H5 改 h264 / B 私有键 setParameters / C 服务端转码 | G-直播PK玩法-spec §13 R1 | G/B/J |
| 2026-06-24 | **G spec v2 经 5-critic 并行复查修订**：(1) matching 倒计时纠正为 15s（QUICK 窗口，原 60s 错）+ RETRY 5min 二阶段；(2) `LiveStore.setCallState` 实际有 `guard state == .living`，PKStore 调用前必须显式守卫；(3) `CallStore.handleIncomingVideoCall` PK 互斥分支精确插入位置 line 656（if 块右花括号）后、line 657 前作同级 if 块；(4) `pkConnections` 字典 SDK 回调线程 ≠ MainActor → 加 NSLock + nonisolated `pkConnection(forChannel:)` 公开方法；(5) `NetworkQualityMonitor` 暴露 `@Published var currentLevel` 让 PKStore 注册监听拦截 weakSevere 切 .pkLow（而非走 LiveStore .low）；(6) 公屏 attachType=-9 修正为**主播端主动 NIM 发送**（H5 蓝本 livePk.js:705/833，非后端推）；(7) inviteStatus 3 超时不在后端推 99，是本端 60s 倒计时本地处理 → 调 handleInvite(type:3) 上报；(8) PKArenaView 跨 .starting/.inPK/.punishing 用 if 链或 .id 锁 identity + CameraPreview 唯一实例不在 PKArenaView 内新建（铁律 §1/§3）；(9) reconcileOnReconnect ctx 重建缺口（getPkStatus 仅返回字符串）→ M1 后端协调方案 A/B/C；(10) `setVideoEncoderConfiguration` 是 engine 级别，PK 期 .pkActive 也影响主频道观众端画质（接受 + M0 评估）；(11) `renewToken` 多 connection 行为 → M0-3 长跑验证；(12) `updateInviteSwitch` 幂等性 → M1 后端确认 + isUpdatingInviteSwitch 防重入；(13) M1 新增"后端协调 4 项"（endTime 单位 / attachType -8/-9 形态 / updateInviteSwitch 幂等 / reconcile ctx 重建方案）阻塞 M2 启动；新增 R8-R11 风险 | 5-critic workflow（consistency / h5Fidelity / iosCodebase / omissions 4 维度 + SDK 维度手动补） | G |
| 2026-06-24 | **A 收尾 xhigh review 通过**：15 finding 中 3 项当场修（`SessionStore.handleSessionInvalidated` 加 AppLogger.auth 日志 + 错误码 `[xxxx]` 写入 errorMessage / `LivePrepareView.startLive` catch 1004/1005 短路 return 防双错显示）；遗留 4 类 11 项进 J backlog | code-review skill workflow `a-finish-review` | A → J |
| 2026-06-24 | **callRate 路径修正 + D 漏报点补齐**：(1) CallService.callRate 真实路径 `/api/chat/callRate`（chat 域），原代码字面值写为 `/api/call/callRate` → 404；H5 蓝本 chat/index.ts 已实证；(2) CallStore.acceptIncomingFromLive 直播私 call 接听后 publish ok 路径补 `reportRate(.answered, .callee, sinceStartDuration, 0)`，对齐 accept() 同位置上报，避免 D 路径漏报；(3) endLocally answerTime 三元简化为单 `info.sinceStartDuration`（语义"等待时长"，hangup 路径 rateCategory=nil 不进此分支，三元 connectedDuration 分支理论不触发）；不补 sourcePage（H5 全局 grep 0 命中，安卓独有，铁律"信源码"）；不动 callOver dead code（避免引入未验证路径变更） | iOS 真机 callRate 404 反馈 + Workflow 三方交叉验证 H5/安卓/iOS | C/D |
| 2026-06-24 | **F1+F6 上报抢跑防御性补丁**：归因复查 Workflow 8 路并发 verifier 验证 callRate 改动正确性后，识别 2 项本会话引入的应修项：(1) **F1 reportRate 入口 state 守卫下沉**：joinRtc 失败 → endLocally → state=.ended 后，控制流回到 acceptIncomingFromLive L414 / accept() L463 仍会执行 reportRate(.answered) 抢跑（虚假 abnormal=0 污染后端接通率），守卫下沉到 reportRate 入口拦截（state ∈ .ended/.idle/.failed 时 return）；endLocally 内的 reportRate（L573）在 state=.ended 之前调，不会被误拦截；(2) **F6 acceptIncomingFromLive fire-and-forget 一度反悔，最终保留同步 await**：M2 复查发现 fire-and-forget 让出 MainActor 期间，对端 RTM hangup 入队的 handleRemote Task 会 FIFO 先于 reportRate Task 进入 MainActor 队列 → state 推到 .ended → reportRate 被 F1 入口守卫拦截 → answered 永久漏报。改回同步 await 把这次上报放在 publish/joinRtc 共同串行链路，handleRemote 必排队在后。xcodebuild clean build 通过 | 归因复查 Workflow（wqmcpohv7）+ M2 复查 + 用户授权"本会话且应该修的都进行修复" | C/D |
| 2026-06-24 | **E 派对房视频位 v4 amendment**：v3 把"他人视频位远端流"划在 F 期，真机暴露视频位上只看到自己等于残缺；提前到 E。新增 PartyRTCEngine `[seatIndex: UIView]` 池 + `bindRemoteVideo(seatIndex:uid:)` 幂等接口（换人自动清旧 uid）+ PartyRemoteVideoView UIViewRepresentable（按 seatIndex 取池稳定实例）+ PartyStore.postMikeList diff 对账（自己 uid 守卫 + 异常 userId 跳过）。摄像头关 = UI 头像覆盖、不调 muteRemoteVideoStream（对齐 H5）。**rules 沉淀**：`.claude/rules/swiftui-camera-preview.md` §2 追加"按业务 key 池化 UIView 正解模式" — F 期 PartyCall / G 期 PK 远端视频位可直接复用 | feature-pipeline trial #2 / spec v4 amendment / 真机双机验证 R1/R4/R5/R7 通过 | E（MVP 收尾）/ F/G（复用模式）|
| 2026-06-24 | **E A 档 NIM 链路真机验通过**：(1) 上麦后 ≥90s 不被服务端自动下麦 — WSHeartbeat `setPartyContext(roomId/seatIndex)` 注入 messageType=1 / 15s 心跳 expandParams 让后端 `partyRoomHeartbeat()` 持续刷新派对键 lastActiveTime，30s 超时机制不触发；(2) 自切麦克风走 1008 → `applyMediaUpdate(seatIndex:)` 定向更新对应字段，**不**走 reloadSeatListFromServer 全量退化；(3) 1001 麦位变化命中 `direct seatList replace count=N`（不 fallback），`seatOperate` 字段语义日志清晰 | 真机 dev 房 2635796 单账号 60+60s 计时 + Console 日志验证 | E（A 档）|
| 2026-06-24 | **M1 callRate answerTime 按 H5 桶映射重构**：复查发现"统一取 sinceStartDuration"会污染 cancel/reject 桶。endLocally 签名加 `answerTime: Int` 参数；14 处调用站点按 H5 useCallApi.js 分桶映射：`answered=sinceStartDuration` / `timeout=30 字面` / `主动 cancel\|reject=0` / `remoteCancel\|remoteReject=sinceStartDuration`。xcodebuild clean build 通过 | M1 复查 + 用户授权"本会话且应该修的都进行修复" | C/D |
| 2026-06-26 | **H IM `/review deep` 二次复查 + 必修 4 项落地**：Workflow 59 agents / 3 票对抗输出 11 项 finding；逐条 Read 源码 + Grep 调用方二次复查后实际**必修 4 项**：(1) **P1-1** LiveRoomView 顶层观测 `nim.onlineCount` 触发整树重算 — 抽 `ChatPresenceStore`（onlineCount 独立 @Published）+ `OnlineCountText` 子 view 内观测，本体不再订阅；`connected` 0 view 订阅去 @Published；(2) **P2-3** NIMChatroomManager.enter 失败两路径不回滚 hasJoined/delegate/roomId — 抽 `rollbackEnterFailure()` 在 IM 未登录 + enterChatroom 抛错两路径都回滚；(3) **P2-6** PartyRoomChatManager.enterChatroom callback 失败路径残留 delegate — catch 内补 chatManager/chatroomManager.remove + 清 roomId（finder 误判 IM 未登录路径，实际 add 之前 return 不需回滚）；(4) **P2-7** PartyMessageRouter 日志 .public/.private 不一致 — line 81/123/142 三处 `Array(keys), privacy: .public` 统一改 .private 与 line 49 威胁模型一致。其余 7 项二次复查降级（建议 4 / 信息 3）不修。xcodebuild build SUCCEEDED + HilyTests SUCCEEDED | `/review deep` Workflow + 二次复查 + 用户授权"本会话产生的问题才修复" | H |
| 2026-06-26 | **review skill + code-review-discipline rule 沉淀「工具产出 ≠ 二次复查」**：上轮 `/review deep` 跑完 Workflow 直接渲染 11 项报告未做二次复查 → 用户指出 finder 误判 2 项 + 理论不触发标 P1 错。沉淀：(1) `.claude/skills/review/SKILL.md` deep 流程 4/5 之间插入「🛑 强制二次复查」步骤（Read 源码 + Grep 调用方 + 按 rule §1/§2/§6 三档分级），报告必带"复查后判定"列 + 复查统计表；(2) `.claude/rules/code-review-discipline.md` 新增 §7「工具产出 ≠ 二次复查」（Workflow 内 3 票对抗 / Agent 自我确认 / static analyzer / LLM 评分均不等同），§8 历史教训补本期 | 用户 critical feedback「审查不一定正确，必须二次复查后再列」 | 全部（process）|
| 2026-07-01 | **IM 场景过滤门（IMSceneGate / IMSceneFilter）落地**：sysMsg 通道全局长连接单播消息按当前场景 drop 无关 attachType，业务正确性 > 性能。(1) **纯决策层** `IMSceneFilter.allows`（enum ActiveScenes: OptionSet .live/.call/.party；穷举式 switch AttachType 全表；chatroom/p2p/syncSysMsg 直放行；`mustReceive` 必收 44/62/-4/58 任何场景永不 drop；grace 内全放行）；(2) **副作用层** `IMSceneGate` 单例（`enter/exit(delay:)` / `openBacklogGrace` / `resetAll`；pendingExitTasks race-safe cancel+insert）；(3) **NIMService.dispatch** 加 gate 守卫 + 短路 `return`（首个 route true 立即终止，省 N-1 次 router 遍历）；`onLogin(.loginOK)` + `login()` success continuation 双触发 openBacklogGrace(10s) 防 SDK 契约不确定；(4) **4 store didSet wiring**：LiveStore.state (.living)、CallStore.state (非 .idle → 进 / 回 .idle 后 **8s exit grace** 接尾消息 callRechargeReward)、PartyStore.roomState (.joined)、SessionStore.logout 末尾 resetAll(); (5) **32 case 单测**：通道矩阵 / grace / mustReceive / live / call / liveCall 组合 / sendGift 跨 live+call / PK & 派对房 sysMsg 屏蔽 / 全局 toast / 兜底。**4 轮方案审查 + 1 轮代码复查** 发现 14 个漏洞（含 CallStore exit 8s grace + backlog grace 10s + login()双触发 + Task @MainActor 显式标 与工程约定 11/11 一致 + 4 store didSet 覆盖所有 state 写入点等）；HilyTests 494/494 全过 | 用户「主播端多场景 IM 高频，需按场景过滤，业务正确性优先」 | 全部（IM 底座）|

---

## 四、当前 spec 文档进度（2026-07-22 校准）

| 里程碑 | 当前有效文档/实现 | 状态 |
|---|---|---|
| E | `E-里程碑实施计划`、派对房列表/模式/锁房/MC 位等专项 spec | 已实现并真机验收通过 |
| F | `F-1-spec-PartyBattle-PK`、`F-1a-plan-PartyBattle基建加主态最小闭环`、`F-PartyCall-spec` | PartyBattle 与 PartyCall 已实现；半屏游戏、房币兑换未立项 |
| G | `G-直播PK玩法-spec`、`G-里程碑实施计划` | 已实现并真机验收通过 |
| H | H-1 至 H-5、礼物特效、虚拟道具、直播任务专项 spec | 主体已实现；礼物墙列表已接入，剩余独立详情页见未完成清单 |
| I | 数据统计、用户资料、任务中心、钱包/提现/活体相关实现 | 钱包、提现、活体已有原生代码；缺专项 spec，且待真机/真实服务端回归 |
| J | 机器人通话、直播间 WheelShow、ThinkingData、i18n/RTL、匹配合规人脸检测上报 | 独立抽奖转盘、Crash、远程灰度仍待 spec 与实现；人脸合规待真机回归 |

## 五、新增 spec 文档进度（历史快照）

| 里程碑 | spec 文件 | 状态 |
|---|---|---|
| B | `archive/B-LiveStore状态机-spec-202606192000.md` | **已完成** |
| B | `archive/B-NIMChatroomManager完善-spec-202606192000.md` | **已完成** |
| B | `archive/B-spec-H5安卓代码二次校验-202606192000.md` | **已完成** |
| B | `archive/B-里程碑实施计划-202606192000.md` | **已完成** |
| C | `C-CallStore与RTM信令-spec.md` | 未单独落档（实施直接合入 D 计划） |
| C | `C-充值等待蒙层-spec.md` | 未单独落档（-6/-90 蒙层已在 C 实施中跑通） |
| D | `archive/D-直播转私call状态机-spec-202606201400.md` | **已完成** |
| D | `archive/D-里程碑实施计划-202606201430.md` | **已完成** |
| E | `E-派对房MVP-spec.md` | 待写（首要任务） |
| F | `F-派对房完整玩法-spec.md` | 待写 |
| F | `F-PartyCall抢占-spec.md` | 待写（C/D/F 三方耦合最深，单独 spec） |
| G | `G-spec-H5安卓代码二次校验-202606232231.md` | **已完成** |
| G | `G-直播PK玩法-spec-202606232325.md` | **已完成** |
| G | `G-里程碑实施计划-202606232325.md` | **已完成** |
| H | `H-礼物动画队列-spec.md` | 待写 |
| H | `H-NIM-attachType全量分发-spec.md` | 待写 |
| I | `I-钱包提现与人脸活体-spec.md` | 待补文档；代码已实现，待真机/服务端回归 |
| I | `I-任务中心UTC+8-spec.md` | 待写 |
| J | `J-机器人通话-spec.md` | 待补文档；代码已实现，待真机回归 |
| J | `J-转盘两处-spec.md` | 直播间 WheelShow 已实现；独立抽奖转盘待写 spec 与实现 |

---

## 五、下一步建议

1. **A→D 已闭环**，进入 E/F/G 三轴并行阶段（按路线图 §1 依赖图允许）
2. **本周内并行**：
   - E 派对房 MVP：另开会话推进（后端 dev 接口全可用，无阻塞）
   - F 派对房完整玩法：另开会话推进 spec（implement 阶段必须 E 先完成）
   - G 直播 PK：另开会话推进 spec → plan → implement（依赖 B+D 已就绪）
3. **并行期间评估共享基建抽取时机**：
   - HeartbeatScheduler（D 完成后可抽）：B/C/D 三处心跳实现已稳定，抽时机已到
   - BeautyPipelineSharer（C 完成后可抽）：v5.8 字典已天然支持多下游，可观察是否还需要更上层抽象
4. **后续串行**：H（礼物全景 + IM 完善）→ I（收益与外围）→ J（最后串行）
