# Hily B 里程碑 · 实施计划（M0-M5 五阶段）

> **关联**：
> - 路线图：`iOS主播端-全量上线里程碑路线图-202606191532.md` §三 B
> - Spec：
>   - `B-LiveStore状态机-spec-202606192000.md`（v4 / 18 任务 / 6.5 人/天）
>   - `B-NIMChatroomManager完善-spec-202606192000.md`（v2 / 14 任务 / 5.9 人/天）
> - 二次校验：`B-spec-H5安卓代码二次校验-202606192000.md`
>
> **范围**：B 里程碑 13 项 P0 缺口全部
> **总估时**：**约 9.5 人/天**（合并去重后；按 1 人 1 周 5 工作日，约 **2 周** 单人完工）
> **提测点**：M1 / M2 / M3 / M4 各一次独立真机回归；M5 集成回归收尾
> **节奏**：M0→M5 串行；每阶段完结提交一次合并 commit + 真机 demo

---

## §1 总览

### 1.1 阶段映射

| 阶段 | 名称 | 任务数 | 估时 | 提测内容 | 阻塞依赖 |
|---|---|---|---|---|---|
| **M0** | 基础设施 + 占位文件 | 5 | 0.8 人/天 | （无单独提测，直接进 M1） | 无 |
| **M1** | 心跳 + 下播闭环 | 6 | 1.4 人/天 | **D1**：开/下播 happy path + 心跳错误码分流 | M0 |
| **M2** | 监控 + 降级 | 6 | 1.6 人/天 | **D2**：弱网/相机异常/美颜失败/token 续期 | M1 |
| **M3** | NIM 消息分发 + 公屏重连 | 8 | 2.4 人/天 | **D3**：合规消息/礼物公屏/重连/多源仲裁 | M0（不依赖 M1/M2） |
| **M4** | 开播前置 + userType + i18n + 日志 | 4 | 1.3 人/天 | **D4**：完整业务闭环 + 路由分流 | M2 + M3 |
| **M5** | 真机集成回归 | 1 | 2.0 人/天 | **D5**：32 项 DoD 全量验证 | M4 |

> **可并行点**：M3（NIM）与 M1+M2（LiveStore 媒体管线）**仅有任务 9（LiveStore.adjustOnlineCount）跨边界**——M3 任务 21 实际依赖 M0 的 T1 LiveStore 骨架，不依赖 M1/M2 的具体实现。**单人单线开发可串行 M0→M1→M2→M3→M4；双人并行可在 M0 后分头跑 M1+M2 与 M3**。

### 1.2 关键产出

- 新增源码文件 **8 个**：
  - `Sources/Live/LiveStore.swift`
  - `Sources/Live/LiveState.swift`
  - `Sources/Live/HeartbeatController.swift`
  - `Sources/Live/NetworkQualityMonitor.swift`
  - `Sources/Live/NIM/AttachType.swift`
  - `Sources/Live/NIM/LiveCustomAttachment.swift`
  - `Sources/Live/NIM/ComplianceMessageParser.swift`
  - `Sources/Live/NIM/GiftMessageParser.swift`
- 新增协议 **1 个**：`GiftAnimationQueueProtocol`（B 注 nil mock，留 H 钩子）
- 新增 View **2 个**：`AgentRestrictedView`、`MineRestrictedView`（仅占位）
- 修改源码文件 **7 个**：`LiveService` / `LiveRoomView` / `LivePrepareView` / `AgoraManager` / `CameraManager` / `FUBeautyRenderer` / `NIMChatroomManager` / `RootView`
- 新增 `Sources/Localizable.strings`（en stub，J 里程碑补全 ar/tr）

---

## §2 M0：基础设施 + 占位文件（0.8 人/天）

> **目的**：搭出空骨架让后续任务可独立填充实现；本阶段产物**不能编译运行**（方法体留 TODO），仅供后续阶段 stub。

### T1 `LiveStore.swift` 状态机骨架（0.4 人/天）
- 实现 spec [LiveStore §2.3](B-LiveStore状态机-spec-202606192000.md) 字段全集（含 `inFlightEnd` / `cameraFailureStartedAt` / `cameraFailureWatcher`）
- 实现 spec §2.4 全部入口签名；方法体仅 `fatalError("TODO M1")` 占位
- `prepare()` 实现可用（idle → preparing）；其余入口 stub

### T2 `LiveState.swift` + `ForceEndReason` + 优先级（0.15 人/天）
- spec §2.1 状态枚举完整定义
- `ForceEndReason.code: Int` 计算属性（2/4/5/6/7）
- spec §2.5b 优先级仲裁的 `priority(of:)` 私有方法签名（M1 才实现）

### T3 `Localizable.strings` stub + L10n key 注册（0.15 人/天）
- 新建 `Sources/Localizable.strings`（en 基础）
- 注册 **B 阶段使用的全部 key**（约 15 个，从两份 spec 收集）：
  - 直播错误：`forceEndDisconnected` / `forceEndBanned` / `forceEndCameraFailure` / `forceEndNoPermission` / `forceEndWeakNetwork` / `complianceWarningDefault` / `beautyUnavailableHint`
  - 开播前置：`prepareTitleEmpty` / `prepareCoverEmpty` / `prepareCooldown` / `prepareIMOffline`
  - 公屏：`imChatroomReconnecting` / `imChatroomReconnected` / `userJoined` / `sendGiftAction` / `anonymous`
- 引入 SwiftGen 或手工生成 `L10n.swift` 枚举（不引入 SwiftGen 时手工 stub `enum L10n { static let xxx = NSLocalizedString("xxx", comment: "") }`）

### T4 `AttachType.swift`（0.2 人/天）
- spec [NIM §5.2](B-NIMChatroomManager完善-spec-202606192000.md) AttachType 枚举完整实现
- `init(raw:)` 双形态解析（Int / String）+ unit test 覆盖 8 个 case + unknown

### T5 `GiftAnimationQueueProtocol.swift`（0.05 人/天）
- 1 行 protocol 定义：`func enqueue(_ gift: GiftMessage)`
- 文件可放在 `Sources/Live/NIM/`（B 阶段无实现）

---

## §3 M1：心跳 + 下播闭环（1.4 人/天）

> **提测点 D1**：开播 → 心跳运行 → 用户主动下播 / 心跳 1992 触发 endType=2 / 心跳 2001 触发 endType=6 / 心跳连续失败 >3 触发 endType=4
> **依赖**：M0 全部完成

### T6 `LiveService` 改造（0.5 人/天）
- `heartbeat(roomId:callState:) async throws -> String`：spec [LiveStore §3.3](B-LiveStore状态机-spec-202606192000.md)
  - body NSNull 守卫（`isValidJSONObject`）
  - response code 分流：`"0000"` 返回 / `"1992","1006"` 抛 `HeartbeatError.banned` / `"2001"` 抛 `.noPermission` / default 抛 `.unknown`
- `endLiveRoom(endType: Int) async throws`：spec §10.2 加 `endType` 参数
- 现有 `print` 全部换 `os.Logger`（4 处：L60/98/103/106）

### T7 定义 `HeartbeatError` 枚举（0.1 人/天）
- 文件放在 `Sources/Live/HeartbeatController.swift`（与 controller 同文件）
- 4 个 case：`banned / noPermission / invalidPayload / unknown(code:message:)`

### T8 `HeartbeatController` 实现（0.5 人/天）
- spec §3.1-3.5：10s `Task.sleep` 循环 + 失败计数 `>3 → forceEnd(.disconnected)` + 响应分流
- 持有 `weak var store: LiveStore?`
- `start()` / `stop()` 入口；`stop()` 调 `Task.cancel`

### T9 `LiveStore.forceEnd` 实现 + 优先级仲裁 + endLive 改造（0.4 人/天）
- spec §2.5：同步 CAS `tryEnterForceEnding` + `tryEnterEnding`
- spec §2.5b：优先级仲裁逻辑（**M1 完整实现**，不留到后续）
- spec §10.1：`endLive()` 内串行 teardown：`heartbeat.stop()` → `networkMonitor.stop()`(M2 才有) → `nim.leave()`(M3 才有) → `camera.stop()` → `agora.leave()` → `endLiveRoom(endType:)`
- M1 阶段 `networkMonitor/nim` 用空 mock 替代（M2/M3 时替换为真实实例）

### T10 `LiveRoomView` 接入 LiveStore（0.2 人/天）
- 把现状 L65-72 心跳 timer 删除（HeartbeatController 接管）
- 把 L178 `Task { try? await endLiveRoom }` 改为 `Task { await store.endLive(); dismiss() }`
- 把 startLive 调用改为 `await store.startLive(title:)`
- 心跳 Timer 字段清理

### T11 LiveStore.startLive 实现（M1 阶段最小可用版）（0.3 人/天）
- 复用现状 `LiveService.startLive(liveDescribe:)` 拿 LiveRoomInfo
- 串行 `agora.join(...)` + 启动 HeartbeatController
- 失败回 idle + toast（用 L10n.xxx）
- **暂不接** NIM enter（M3 才接）/ NetworkMonitor.start（M2 才接）/ CameraManager（已有可复用）

### D1 提测验收（M1 完结）

```
□ 真机开播 → 他端可见画面 + 公屏（公屏文本还是占位"[收到礼物/自定义消息]"，OK）
□ 抓包确认 /api/agora/liveHeartBeatV2 间隔严格 10s（±200ms）
□ 用户点结束直播 → 抓包确认 endLiveRoom?endType=1 → dismiss
□ 后端 mock 1992 心跳响应 → LiveStore 进 forceEnding(.banned) → endLiveRoom?endType=2 → ended
□ 后端 mock 2001 心跳响应 → endType=6
□ 拔网线 → 4 次心跳失败 → endType=4
□ 同时 mock 1992 + 拔网线（M3 之前手工 mock 一个 forceEnd(.disconnected) 触发）→ 验证优先级仲裁：endType=2 胜出
□ Console.app 过滤 subsystem:com.anchor.livechat 无裸 print（LiveService 范围）
```

---

## §4 M2：监控 + 降级（1.6 人/天）

> **提测点 D2**：弱网分层降级（v5：10 降帧率→30 下播→5 恢复）/ 相机异常下播 / 美颜失败降级 / 声网 token 续期不中断推流
> **依赖**：M1 完成

### T12 `NetworkQualityMonitor` 实现（0.5 人/天）
- spec [LiveStore §4](B-LiveStore状态机-spec-202606192000.md) 全套：一次性触发 `>=10 && !alreadyTriggered` + `start/stop/reset/pause/resume`
- 持有 `weak var store: LiveStore?` + `weak var analytics: AnalyticsClient?`
- v5 三阈值分层（全部"连续"语义，中间一次质量 ≤4 即清零重计）：**连续** 10 次触发降级 `agora.applyEncoderQuality(.low)` 30→15fps + `store.setNetworkWarning(...)` toast；**连续** 30 次触发 `store.forceEnd(.weakNetwork, sub: "network_bad_30")`；degraded 期间**连续** 5 次质量好恢复 normal（清 toast + fps 15→30）

### T13 AgoraManager 改造（0.7 人/天）
- 接 `networkQuality(uid:txQuality:rxQuality:)` → 转发 `monitor.report(...)`
- spec §7：`tokenPrivilegeWillExpire` 实现续期（调 `LiveService.getAgoraRtmToken()` → `engine.renewToken(_:)`）
- spec §7.2：`didOccurError` 109/110 同链路续期
- 续期失败 → `store.forceEnd(.disconnected, subSource: "agora_token_renew_failed")`
- 现有 print 收敛 os.Logger（约 5 处）

### T14 CameraManager 错误回调（0.5 人/天）
- spec [LiveStore §5.1](B-LiveStore状态机-spec-202606192000.md)：新增 `onError: ((CameraError) -> Void)?` callback
- spec §5.2：监听 3 类系统通知 `AVCaptureSessionRuntimeError` / `WasInterrupted` / `InterruptionEnded`
- `CameraError` 枚举 4 case：`permissionDenied` / `sessionRuntimeError(NSError)` / `wasInterrupted(reason:)` / `interruptionEnded`
- 权限拒绝立即冒到 LiveStore（不入 20s 累计）

### T15 LiveStore 接 CameraManager 错误 + 20s watcher（0.3 人/天）
- spec §5.3：`onCameraError(_:)` 入口 + `startCameraFailureWatcher()` Task 20s 后 `forceEnd(.cameraFailure)`
- `interruptionEnded` 触发 `stopCameraFailureWatcher()` 清零
- `permissionDenied` → `permissionDeniedAlert` @Published 字段 + LiveRoomView alert

### T16 BeautyRenderer fallback（0.5 人/天）
- spec [LiveStore §6.1](B-LiveStore状态机-spec-202606192000.md)：`FUBeautyRenderer.init(authPack:) throws` + setup >500ms 抛 setupTimeout
- spec §6.2：`CameraManager` 构造时 try/catch 降级 PassthroughRenderer + 调 `store.markBeautyUnavailable()`
- LiveStore 新增 `markBeautyUnavailable()` 入口：`beautyAvailable = false` + 埋点 `c_beauty_fallback`

### T17 LiveRoomView 美颜 UI 反馈（0.2 人/天）
- 美颜按钮 `disabled(!store.beautyAvailable)` + 顶部条幅 `L10n.beautyUnavailableHint`
- 权限拒绝 alert 实现（"请去设置 → Hily → 相机 允许"）

### D2 提测验收（M2 完结）

```
□ NLC Edge 持续 20s → 顶部蓝色条幅"Network unstable…" + 声网 fps 30→15（v5 §4.5 降级）
□ NLC Edge 持续 60s+ → endType=7 下播
□ NLC 关闭 → 10-15s 内条幅消失 + fps 回 30
□ Console 看到 "network degraded" / "network recovered" / "network bad ≥30 → forceEnd weakNetwork" 日志
□ 后端 mock token TTL=30s → 30s 后看到 "Agora token renewed" 日志，推流不中断
□ 直播中触发 AVCaptureSession runtimeError（模拟器卡断，物理机长按按键），20s 后 endType=5
□ **v5.2**：直播中按 Home 切后台 60s → 回 Hily → 画面 1-2s 内恢复 + **不自动下播**（reason=1 已静默过滤）
□ **v5.2**：直播中切到原生相机 app → 回 Hily → 触发 toast + 20s 内未恢复才下播（reason=3 真硬件占用）
□ 关闭相机权限重启 app → preparing 阶段 alert + 引导设置（不进 living）
□ 临时替换 FaceUnity authpack 为无效 → 开播仍能成功；美颜按钮置灰 + 顶部条幅；画面无美颜
□ 上述 4 场景的下播 endType 严格按 §11 表对应
□ Console 无裸 print（AgoraManager + CameraManager 范围）
```

---

## §5 M3：NIM 消息分发 + 公屏重连（2.4 人/天）

> **提测点 D3**：合规消息（44/61/62/63）+ 礼物（SEND_GIFT/1/4/15/18）+ 重连成功 + 重连失败 forceEnd + 多源 forceEnd 优先级仲裁
> **依赖**：M0 完成；**不依赖 M1/M2 实现**（仅依赖 LiveStore 入口已抽离，spec §12 契约）

### T18 `LiveCustomAttachment` + Decoder + 注册（0.5 人/天）
- spec [NIM §5.1](B-NIMChatroomManager完善-spec-202606192000.md)：`encodeAttachment()` 真实方法名
- spec §5.3：`LiveCustomAttachmentDecoder: NIMCustomAttachmentCoding`
- 实施第一步：**read NIMSDK header 确认 `NIMCustomAttachmentCoding` 协议方法签名**（已 v2 grep 验证），并对应注释
- NIMChatroomManager.setupOnce 加 `NIMCustomObject.registerCustomDecoder(LiveCustomAttachmentDecoder())`

### T19 `ComplianceMessageParser`（0.5 人/天）
- spec [NIM §6](B-NIMChatroomManager完善-spec-202606192000.md)：4 case 实现 + `@MainActor` 隔离
- payload 字段 `content` / `status` ⚠️ 加 TODO 注释，implement 阶段抓 dev 真实样本对齐后才能 ship

### T20 `GiftMessageParser` + `GiftAnimationQueueProtocol` 注入（0.6 人/天）
- spec [NIM §7](B-NIMChatroomManager完善-spec-202606192000.md)：解析 8 字段
- `giftCount` String→Int 容错
- `GiftAnimationQueueProtocol` 在 NIMChatroomManager 内 `weak` 持有（B 阶段 nil）
- 公屏文本格式：`"\(sender) \(L10n.sendGiftAction) \(name) × \(count)"`

### T21 NIMChatroomManager 持 weak liveStore + 字段补全（0.3 人/天）
- spec [NIM §2.3](B-NIMChatroomManager完善-spec-202606192000.md)：标 `@MainActor` + 加 `cachedNickname`/`enterAttemptCount`/`reconnecting`
- 删除 `@Published var onlineCount`（迁到 LiveStore）
- parsers 通过构造注入（依赖反转）

### T22 onRecvMessages 改造 + 拆 sub-func（0.5 人/天）
- spec [NIM §8.2](B-NIMChatroomManager完善-spec-202606192000.md)：主函数 + 3 sub-func（parseText / dispatchCustom / handleNotification）
- 每个函数 <50 行（CLAUDE.md）
- `dispatchCustom` 内 `Task { @MainActor in await complianceParser.parse(...) }` 异步分发

### T23 `connectionStateChanged` 4 态分流 + 重连（0.6 人/天）
- spec [NIM §3.1-3.3](B-NIMChatroomManager完善-spec-202606192000.md)：Entering / EnterOK / EnterFailed / LoseConnection
- 1s/2s/4s 退避：`pow(2.0, Double(enterAttemptCount - 1))` + `Task.sleep`
- >3 次失败：`liveStore?.forceEnd(.disconnected, subSource: "im_reconnect_failed")` + 埋点 `im_reconnect_failed`
- 文案走 `L10n.imChatroomReconnecting` / `imChatroomReconnected`

### T24 `leave` 复位（0.2 人/天）
- spec §4.2：清空 `messages` / 重置 `cachedNickname/enterAttemptCount/reconnecting` / `liveStore?.setOnlineCount(0)` / `liveStore = nil`

### T25 onlineCount 数据源切换（0.3 人/天）
- LiveRoomView 公屏在线人数 `@ObservedObject store.onlineCount`（之前是 `nim.onlineCount`）
- onRecvMessages 进/出房改调 `liveStore?.adjustOnlineCount(by: delta)`
- 进房成功 callback 改调 `liveStore?.setOnlineCount(chatroom?.onlineUserCount ?? 0)`

### D3 提测验收（M3 完结）

```
□ 后端推 attachType=44 → LiveStore 进 forceEnding(.disconnected) → endType=4
□ 后端推 attachType=61 → 顶部 warningToast 显示 3s 自动消失，不下播
□ 后端推 attachType=62 → endType=2
□ 后端推 attachType=63 status=1 → LiveStore.boostingExposure = true（Console 观察）
□ 后端推 SEND_GIFT / 数字 1/4/15/18 → 公屏出现"XX 送出 礼物名 × N"
□ 后端推 attachType=50 / 未知 → logger.info 留痕，不崩溃不推公屏
□ 真机切飞行模式 5s 再开 → 公屏"重连中..."→"已重连"
□ 真机长断网 >12s → 4 次重连失败 → endType=4，埋点 im_reconnect_failed 上报
□ 多源 forceEnd 抢占：mock 心跳 1992 + NIM attachType=44 同时到 → endType=2（banned 胜出）
□ 进 100 人房间，退出再进 → onlineCount 显示正常，messages 清空
□ Console 无裸 print（NIMChatroomManager 范围）
```

---

## §6 M4：开播前置 + userType + i18n 补全 + 日志收敛（1.3 人/天）

> **提测点 D4**：完整业务闭环 + userType 路由分流 + 全 print 收敛
> **依赖**：M2 + M3 完成

### T26 `LivePrepareView.checkCanLive` 4 项（0.3 人/天）
- spec [LiveStore §8.2](B-LiveStore状态机-spec-202606192000.md)：title / cover / 60s 防抖 / IM 在线
- `UserDefaults` 读 `lastEndAt`
- 任一 fail → toast（L10n.prepareXxx）+ 不触发 startLive

### T27 RootView userType 路由 + 占位 View（0.5 人/天）
- spec [LiveStore §9.2](B-LiveStore状态机-spec-202606192000.md)：2/9/其他 三路
- 新建 `AgentRestrictedView` + `MineRestrictedView` 占位（仅 Text 提示，B 阶段不接钱包/收益数据）
- 错误码统一分流：1004 挤下线 / 1005 token 失效 / 1992/1006 封禁登出（封禁 toast）—— 实现入口 `APIClient` 层，加 `handleErrorCode(_:)` 方法

### T28 全量 i18n 补完 + L10n 校验（0.2 人/天）
- 跑一遍代码搜 `L10n.` 引用，确认 `Localizable.strings` 全 key 都存在（避免 release 缺失抛空字符串）
- 中文翻译值 stub："网络异常已断连" / "账号违规已封禁" / "网络环境过差" 等（J 里程碑替换）

### T29 全量 print → os.Logger（0.3 人/天）
- grep `Sources/` 内全部 `print(` → 替换为 `logger.info/warning/error`
- 每个 module 有独立 `Logger` 实例（`Live` / `Camera` / `Beauty` / `Agora` / `Chatroom` / `Auth`）
- M1-M3 已分散收敛了一部分；本 task 是兜底扫尾

### D4 提测验收（M4 完结）

```
□ userType=2 主播账号 → 进 HomeView 可开播
□ userType=9 代理账号 → 进 AgentRestrictedView（仅占位文案）
□ 未审核账号 (userType=其他) → 进 MineRestrictedView
□ 登录态 1005 → 自动登出回 LoginView
□ 后端推 1992 封禁 → 登出 + toast"账号已封禁"
□ 开播前置：title 空 / cover 空 / 距上次下播 <60s / IM 离线 各场景 toast
□ 全局 grep `print(` 在 Sources/ 下零命中
□ Console.app 过滤 subsystem:com.anchor.livechat 全 logger 输出可见
```

---

## §7 M5：真机集成回归（2.0 人/天）

> **提测点 D5**：32 项 DoD 全量验证（B-LiveStore 15 项 + B-NIMChatroomManager 17 项）
> **依赖**：M4 完成

### T30 真机回归
- 设备：iPhone 12 / 14 各 1 台（弱网 + 双机对话场景）
- 网络：Wifi（正常）+ Network Link Conditioner（Edge / 3G）+ 飞行模式 5s
- 后端：dev 环境 + 1 名后端工程师 mock 推送 NIM 44/61/62/63 + 心跳 1992/2001
- 录屏：每个 DoD 场景 30s 录屏存档（J 里程碑灰度时回放对比）

### D5 提测验收清单（合并 32 项）

`B-LiveStore状态机-spec.md §13` 15 项 + `B-NIMChatroomManager完善-spec.md §10` 17 项，**全部通过**才能关闭 B 里程碑。

---

## §8 跨 spec 协调点

M3 任务 T21-T25 与 M1 任务 T9 之间的契约约束（避免双人并行时撞车）：

| 契约 | 来源 spec | 调用方 spec | M0 阶段需先锁定 |
|---|---|---|---|
| `LiveStore.forceEnd(reason:subSource:)` | LiveStore §2.5b | NIM §3.3 / §6.2 | ✅ T1 签名 |
| `LiveStore.warn(message:)` | LiveStore §2.4 | NIM §6.1 | ✅ T1 签名 |
| `LiveStore.markBoostingExposure(_:)` | LiveStore §2.4 | NIM §6.1 | ✅ T1 签名 |
| `LiveStore.setOnlineCount(_:)` | LiveStore §2.4 | NIM §4.2 / §8.2 | ✅ T1 签名 |
| `LiveStore.adjustOnlineCount(by:)` | LiveStore §2.4 v3 | NIM §8.2 | ✅ T1 签名 |
| `GiftAnimationQueueProtocol` | NIM §7.3 | LiveStore 不调用 | ✅ T5 协议定义 |

**双人并行原则**：M0 完成后 LiveStore.swift 入口签名已锁定；M1+M2 与 M3 分人各自实现内部逻辑，相互不冲突。**单文件冲突域**仅在 M3 T25（LiveRoomView 数据源切换）+ M1 T10（LiveRoomView 接入 LiveStore），需协调 merge 顺序（M1 先 → M3 后）。

---

## §9 commit 策略

按 git-workflow.md：`<type>: [scope] <description>`

**单任务原则**：每个 T 任务对应 1 个 commit；M0-M5 每阶段完结额外 1 个 release commit 标记。

**示例 commit 流**：
```
feat: [直播状态机] LiveStore 骨架与状态枚举
feat: [直播状态机] LiveStore 同步 CAS 与优先级仲裁
feat: [直播心跳] HeartbeatController 10s 间隔与失败计数
feat: [直播心跳] LiveService.heartbeat 改 throws 与响应码分流
feat: [直播下播] endLiveRoom 携带 endType + endLive 抢跑修复
chore: [M1] 真机 D1 提测通过                    ← M1 阶段结束
feat: [直播网络监控] NetworkQualityMonitor 一次性触发与生命周期
feat: [声网token续期] tokenPrivilegeWillExpire + 109/110 兜底
feat: [相机错误回调] CameraManager.onError 三类系统通知监听
feat: [美颜降级] FUBeautyRenderer setup 失败 fallback Passthrough
chore: [M2] 真机 D2 提测通过
feat: [IM-attach类型] AttachType 枚举与双形态解析
feat: [IM-自定义附件] LiveCustomAttachment + Decoder 注册
feat: [IM-合规消息] ComplianceMessageParser 44/61/62/63
feat: [IM-礼物消息] GiftMessageParser + GiftAnimationQueueProtocol
feat: [公屏-自定义消息分发] onRecvMessages 改造与 sub-func 拆分
feat: [公屏重连] connectionStateChanged 4 态 + 退避重连
fix: [公屏退房复位] leave 清空 messages 与 onlineCount
refactor: [公屏管理] onlineCount 字段迁移到 LiveStore
chore: [M3] 真机 D3 提测通过
feat: [开播前置校验] LivePrepareView checkCanLive 四项
feat: [userType路由] RootView 三路分流 + 占位 View
feat: [i18n-公屏] Localizable.strings 全 key 补全
refactor: [*] print 全量收敛为 os.Logger
chore: [M4] 真机 D4 提测通过
chore: [M5] 真机 D5 集成回归通过，关闭 B 里程碑
```

**总 commit 数**：约 26 个（22 任务 + 5 个 chore 阶段标记 + 1-2 个 hotfix）

---

## §10 风险与未决项

| 风险 | 阶段 | 应对 |
|---|---|---|
| NIMSDK API 命名 v2 已验证，但 SDK 升级后可能改 | M3 T18 第一步 | implement 第一步 read NIMSDK header；若 SDK 升级再次 grep 验证 |
| attachType 61 `content` / 63 `status` 字段名未对齐 | M3 T19 | implement 阶段后端推真实样本到 dev 抓包；ship 前必须确认 |
| 后端心跳 endType=2001 是否真存在 | M1 T6 | 等路线图 §7.3 #10 三端心跳统一对齐；spec 已设兼容路径 |
| FU authpack bundleId 绑定有效期 | M2 T16 | 联系相芯确认 dev/prod 双绑定到期日 |
| 真机回归遇到声网 codec 互通（H5 vp9 vs iOS h264） | M5 D5 | 见路线图 §7.3 #16；本里程碑暂按 dev 验证通过为准，prod 上线前必须官方确认 |
| 后端 Mock NIM 44/61/62/63 推送工具未就绪 | M3 D3 | 后端工程师配合手工推送（dev 1-2 次/天联调） |
| 双人并行时 LiveRoomView merge 冲突 | M1 T10 + M3 T25 | merge 顺序：M1 先；M3 在 M1 之上 rebase；commit 粒度细方便 cherry-pick |
| Localizable.strings 中文 stub 与最终翻译不一致 | M4 T28 | en 默认值用于 B 阶段调试；J 里程碑接专业翻译 |

---

## §11 验证方式

每阶段提测点（D1-D5）对应一次真机 demo + DoD 表逐项打勾。

**关键不可绕过**：
- M1 D1：心跳失败 >3 次必须真机验证（手工拔网线 / Mock 心跳错误），不能仅 unit test
- M2 D2：弱网分层（v5 §4.5：10 降级/30 下播/5 恢复）必须真机 NLC Edge 验证 ≥1 分钟，覆盖三档状态切换
- M3 D3：多源 forceEnd 优先级仲裁必须 mock 心跳 1992 + NIM 44 同时到，验证 endType=2
- M5 D5：32 项 DoD 全量通过，每项录屏存档

**单元测试**（implement 阶段穿插）：
- `LiveStore.tryEnterForceEnding` 优先级仲裁（5 reason × 5 已 inFlight reason = 25 组合）
- `AttachType.init(raw:)` 双形态解析（8 case + unknown × Int/String）
- `NetworkQualityMonitor.report` v5 分层（10 次降级触发一次 / 30 次 forceEnd 触发一次 / 11-29 不重复触发 degrade / 5 次质量好恢复 normal / 已 ended 后所有 report 静默）
- `GiftMessageParser.parse` 字段缺失容错（giftCount 字符串/数字/缺失三态）

---

## §12 下一步

1. **即时**：本 plan 落档审阅；用户确认后开始 M0 task
2. **M0 启动前**：再次扫一遍 `B-LiveStore状态机-spec.md` 与 `B-NIMChatroomManager完善-spec.md` 是否有 v4/v2 后的遗漏修订（路线图 §六 实现纪律）
3. **M1 启动**：spec [LiveStore §14](B-LiveStore状态机-spec-202606192000.md) 任务 1 → 实施 → commit；按 commit 流单线推进
4. **每阶段结束**：打 `chore: [Mx] 真机 Dx 提测通过` 标签提交 + 录屏存档
5. **B 关闭后**：路线图 §三 D 启动 → 直播 ↔ 通话切换 spec

---

> **本 plan 版本**：v1 2026-06-19（首发）；后续每完成 1 阶段更新一次状态行 + 实际工时对比估时
