# H5 vs iOS 通话 UI 对齐 gap 全景与 Wave 推进计划

> 时间：2026-07-07 22:29
> 来源：Workflow `wf_64ff5ab7-ef1`（5 phase pipeline · 6 agents · 24 min · 587k tokens）
> 蓝本：`/Users/joe/Desktop/HN/anchor-livechat-h5/src/components/g-faceTime/` 12 组件 + topBar + topBarIncome + messageScroller + askForGiftTips + giftFloatTips + livingCallAnimation + noFacePop + waitRechargeLock + waitRechargeTips + emptyRoomCountdownPop
> iOS 现状：CallView.swift + CallStore.swift + CallModels.swift + CallConstants.swift
> critic 扫过维度：视觉细节 / 边界态（网络/后台/权限/scenePhase/键盘/isShowAll 联动）/ 微交互（haptic/长按/双击/tapGesture 冒泡阻断）/ 无声失败（SVGA 静默/try? 吞异常/callChatBubble 无消费/waitState-alert 并发）/ iOS TODO 占位 / H5 微行为 / 音频路由/CallKit/proximity/系统电话打断 / waitState 与 HUD/切换 UI 显隐/PIP 尺寸 / LivingCallAnimation 起效条件 / H5 蓝本无参考的 iOS 平台强需求

---

## 一、统计（43 gap · 100% pre-existing 缺口）

| tier | 数量 | 核心特征 |
|---|---|---|
| **P0 必对齐** | **4** | 高频用户可见 + 阻塞变现/正常闭环 |
| **P1 建议对齐** | **15** | 中频可见 + 交互差异明显 / iOS 平台强需求 |
| **P2 建议低优** | **23** | 低频触发 / 视觉细节 / iOS TODO 占位 |
| NA | 1 | headwearRatio 1.35 已对齐 |
| DEAD | 0 | 无死代码 |

**本轮 review 的核心结论**：C-1 + C-3 只做了 3 按钮 + 弱网 toast + 异常自检 alert，占 H5 全部 UI 元素的 **~10%**。其余 90% 通话中的用户可见交互仍缺。

---

## 二、43 gap 分档明细

### P0（4）· 阻塞级 · 立即启动子里程碑

| id | 标题 | fix 工时 | 依赖 |
|---|---|---|---|
| gap-002 | 公屏弹幕 `MessageScroller` 未实现 | A档 6-8h | NIM 通话通道（H 里程碑扩展） |
| gap-004 | 礼物请求 `askForGift` 按钮+面板+15s 冷却+提示 | A档 8-10h | askFor 接口 + GiftPickerSheet |
| gap-009 | 直播私call 300s 收益横幅 `privateCallTips` | B档 3h | 无 |
| gap-011 | 充值等待锁定态 60s→5s 兜底自动挂断 + `WaitRechargeTips` | A档 8-10h | sysMsg -6 payload 含 waitBonus |

### P1（15）· 应对齐

| id | 标题 | fix | 依赖 |
|---|---|---|---|
| gap-001 | 点屏切 chrome 显隐 `switchShowAll` | B档 2h | 无 |
| gap-003 | 通话内文本输入 + IM 发送 attachType=-1 | A档 4-6h | gap-002 + NIM 通话通道 |
| gap-005 | 通话反馈弹窗 `CFeedbackPopup type='call'` | B档 3-4h | 反馈接口 |
| gap-006 | 礼物 SVGA/MP4 全屏动画 `chatSvgaCanvas` | B档 3h（接线） | J SVGA 播放器 + H GiftRouter |
| gap-007 | 小礼物飘条 `GiftFloatTips` 3s 自清 | B档 2h | sysMsg 15 payload 含 giftImg |
| gap-010 | 远端摄像头 off fallback 占位（黑屏兜底）| B档 3h | AgoraManager delegate 补 remoteVideoStateChanged |
| gap-012 | 充值成功 `Congrats!` 弹窗 | B档 3h | gap-011 |
| gap-014 | 远端文字气泡带翻译（双行） | B档 3h | sysMsg -1 payload 含 translation |
| gap-016 | 无人脸检测弹窗 `NoFacePop` 通话侧挂载 | B档 3h | FaceDetectionServiceStub 实装 |
| gap-025 | 输入框键盘 adaptive + 安全区适配 | B档 2h（附随 gap-003）| gap-003 |
| gap-critic-001 | 弹窗打开时禁止 switchShowAll + @click.stop 冒泡 | B档 1h（附随 gap-001）| gap-001 |
| gap-critic-002 | 输入 blur 300ms 延迟 + enterkeyhint=send + maxlength=200 | B档 1h（附随 gap-003） | gap-003 |
| gap-critic-003 | 远端视频轨迹 off 触发 remotePIPShowState.CLOSE 双写副作用 | B档 3h（附随 gap-010） | gap-010 |
| gap-critic-004 | **AVAudioSession 系统电话打断 + proximity sensor**（iOS 平台强需求） | B档 3h | 无 |
| gap-critic-005 | **app 切后台/前台 通话推流暂停+音频保持**（iOS 平台强需求）| B档 3h | 无 |

### P2（23）· 建议低优

| id | 标题 | fix |
|---|---|---|
| gap-008 | 直播私call 双头像动画 `LivingCallAnimation` 1s 旋转 | B档 3h |
| gap-013 | 空房间 10s 圆环倒计时 `emptyRoomDetector`（iOS 用 alert 替代） | B档 4h |
| gap-015 | `chatBubble` 自定义气泡样式（border-image） | B档 3-4h |
| gap-017 | 远端全屏 vs 本端 PIP 主副对换（充值锁定期用） | B档 4h |
| gap-018 | 顶部关闭按钮 X + 二次确认 alert | B档 2h |
| gap-019 | CountdownRing 主叫端已对齐 H5 tenSecondsCB 语义 | NA |
| gap-020 | 通话进入 5s loading toast | B档 1-2h |
| gap-021 | SVGA 头像框 J 里程碑接入 | B档 2h（依赖 J） |
| gap-022 | 顶部两档收入胶囊值合并显示 callWaitBonusTotal | B档 1-2h |
| gap-023 | 顶部信息卡 头像 40 + 昵称 + 国家标签 | B档 1-2h |
| gap-024 | onBeforeUnmount 清理（callEndReport / finishAndUpdateCall） | B档 1-2h |
| gap-026 | isMicMuted 持久化 across 通话 | NA（H5 无此行为） |
| gap-028 | sysMsg -3 NIM 辅助信令通道挂载 | A档 4-6h |
| gap-029 | 收入 pill 视觉微调（bgOpacity/cornerRadius）| B档 1h |
| gap-030 | GRemotePIP 远端小窗（充值锁定合并） | 与 gap-017 合并 |
| gap-critic-006 | callChatBubble 已入 store 但 UI 未消费 | 与 gap-015 合并 |
| gap-critic-007 | NIM -3 已定义 enum 但 handler 空挂 | 与 gap-028 合并 |
| gap-critic-008 | callInfoStats 通话内统计字段占位（CallModels:37） | B档 1-2h |
| gap-critic-009 | RtmReconnect login completion 理论防御 TODO | B档 1h |
| gap-critic-010 | RemoteAvatar SVGA 后缀静默不渲染 | 与 gap-021 合并 |
| gap-critic-011 | **haptic feedback iOS 惯例全项缺失** | B档 1h |
| gap-critic-012 | askDisableTimer 15s 冷却切后台 backlog 处理 | 与 gap-004 合并 |
| gap-critic-013 | waitState HUD 胶囊与 abnormal alert 并发互斥 | 与 gap-011 合并 |

### NA（1）
- gap-027 · headwearRatio 1.35 已对齐

---

## 三、Wave 推进计划（自主制定）

按用户"UI 先其他按顺序" + 依赖图 + 阻塞项排序，拆成 6 波：

### **Wave 1 · UI 补齐（最独立，纯 UI + 状态机小改，无外部依赖）· 本轮启动**
| gap | 工时 |
|---|---|
| gap-009 P0 · 300s 私call 横幅 | 3h |
| gap-001 + gap-critic-001 · 点屏清屏 + 弹窗守卫 | 2-3h |
| gap-018 P2 · 顶部关闭按钮 + 二次确认 | 2h |
| gap-023 P2 · 顶部信息卡（头像 40 + 昵称 + 国家胶囊） | 1-2h |
| gap-020 P2 · 进入 5s loading toast | 1-2h |
| gap-critic-011 · haptic feedback（挂 CircleButton） | 1h |
| **合计** | **~11h · B 档批处理**（Spec 一份 + impl 一 shot） |

### **Wave 2 · iOS 平台强需求（AVAudioSession + 后台管理）**
| gap | 工时 |
|---|---|
| gap-critic-004 · AVAudioSession + proximity | 3h |
| gap-critic-005 · 切后台推流暂停 + 音频保持 | 3h |
| **合计** | **~6h · B 档独立 spec**（涉及系统 notification 订阅） |

### **Wave 3 · P0 大件（充值锁定 60s 流程 · A 档独立 spec）**
| gap | 工时 |
|---|---|
| gap-011 · 充值锁定 60s→5s 兜底自动挂断 + WaitRechargeTips | 8-10h |
| gap-012 · 充值成功 Congrats! 弹窗 | 3h |
| gap-critic-013 · waitState-alert 并发互斥 | 1h |
| gap-017+gap-030 · 主副画面对换（PAY 期）| 4h |
| **合计** | **~16-18h · A 档独立 spec**（6 sysMsg 时序 + 状态机扩展 + 新 UI 3 层） |

### **Wave 4 · P0 大件（索要礼物 · A 档独立 spec）**
| gap | 工时 |
|---|---|
| gap-004 · askForGift 按钮 + 面板 + 15s 冷却 + 提示 | 8-10h |
| gap-critic-012 · 15s 冷却切后台 backlog 处理 | 1h（附随） |
| **合计** | **~9-11h · A 档独立 spec**（需 askFor 接口对齐 + GiftPickerSheet 复用验证） |

### **Wave 5 · 远端/AgoraManager 补齐（需 SDK delegate 扩展）**
| gap | 工时 |
|---|---|
| gap-010 + gap-critic-003 · 远端摄像头 off fallback + remotePIPShowState 双写 | 3-4h |
| gap-022 · callWaitBonusTotal 合并收入胶囊 | 1-2h |
| gap-024 · endLocally 补 callEndReport 埋点 + finishAndUpdateCall | 1-2h |
| **合计** | **~6-8h · B 档小批**（需 AgoraManager delegate 补 remoteVideoStateChanged） |

### **Wave 6 · H 里程碑联动 UI（依赖 H 通话消息通道）**
| gap | 工时 |
|---|---|
| gap-002 · 公屏弹幕 MessageScroller | 6-8h |
| gap-003 + gap-025 + gap-critic-002 · 通话内输入 + 键盘 + 微交互 | 5-7h |
| gap-014 · 翻译气泡（双行） | 3h |
| gap-006 · SVGA 大礼物（依赖 J SVGA 播放器） | 3h（接线） |
| gap-007 · 小礼物飘条 | 2h |
| gap-015 + gap-critic-006 · chatBubble border-image | 3-4h |
| gap-005 · 反馈弹窗 CFeedbackPopup type='call' | 3-4h |
| **合计** | **~25-30h · A 档独立 spec 或多个 B 档子里程碑**（阻塞项：H NIM 通话通道 + askFor 接口 + FeedbackType='call'） |

### **Wave 7 · J 里程碑收尾（视觉 + 平台）**
| gap | 备注 |
|---|---|
| gap-008 · 双头像会合动画 | J 视觉批 |
| gap-013 · emptyRoomDetector 圆环倒计时 | 与 iOS callAbnormalReason 对齐判据后决定是否重做 |
| gap-016 · 无人脸检测通话侧挂载 | 需 FaceDetectionService 实装 |
| gap-021 + gap-critic-010 · SVGA 头像框 | J SVGAPlayer 基建 |
| gap-028 + gap-critic-007 · sysMsg -3 NIM 辅助信令 | A档 独立 |
| gap-029 · pill 视觉微调 | J 视觉批 |
| gap-critic-008 · callInfoStats 埋点 | J 埋点批 |
| gap-critic-009 · RtmReconnect TODO 兜底 | J 稳定性批 |

---

## 四、下一步（本轮启动 Wave 1）

**推荐路径**：Wave 1（~11h · B 档批处理）→ Wave 2（AVAudioSession 平台强需求）→ 用户签字 → Wave 3 大件（充值锁定 A 档 spec）...

**Wave 1 一 shot 覆盖 6 gap** ：
- gap-009 P0 · 300s 私 call 横幅
- gap-001 + critic-001 · 点屏清屏
- gap-018 · 顶部关闭 X 按钮 + 二次确认
- gap-023 · 头像 + 国家信息卡
- gap-020 · 5s loading
- gap-critic-011 · haptic feedback

**Wave 1 spec 特征**：B 档降级批处理，涉及 6 项独立 UI 补齐，共享一份 spec；每项独立可测。

准备启动 Wave 1 spec + impl。
