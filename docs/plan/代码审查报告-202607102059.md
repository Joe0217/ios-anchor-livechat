# Hily iOS 主播端 代码审查报告

> 审查时间：2026-07-10 20:59
> 审查范围：**session** — Task 1 PK 贡献榜 · Task 2 头像迁移 · 调整 1 PK 图标 · 调整 2 等级公共组件
> 审查方式：5 维度并行 finder + 3 票对抗验证 / 由 `/review deep session` 自动生成
> 审查依据：[CLAUDE.md](../../CLAUDE.md) + [.claude/rules/](../../.claude/rules/)
> 审查文件：26 个 Swift 源文件

## 一、概览

| finder 原始 severity | 数量 | 复查后判定 |
|---|---|---|
| P0（必须改） | 0 | — |
| P1（应该改） | 1 | 建议（历史遗留，本次 diff 未引入） |
| P2（可以改） | 6 | 建议（历史遗留，本次 diff 未引入） |
| **总计** | **7** | **本次 diff 0 必修** |

**核心结论**：Workflow 3 票对抗验证保留的 7 项全部通过复查（无 finder 幻觉），但**全部属于历史遗留代码问题**——本次会话的 diff 只涉及组件迁移 + PK sheet 新增 + 私 call 联动，**未引入任何一条报告的问题**。按 [code-review-discipline.md §1/§3](../../.claude/rules/code-review-discipline.md)：无本次 diff 触发的必修项，7 项 flag 给独立 cleanup 议题。

---

## 二、必修（P0）

（无）

---

## 三、建议（原始 P1，复查后降为历史遗留 · 独立议题）

### #1 · 公屏 rows 硬编码英文文案（L10n key 已存在未接入）

- **文件**：[RowEnterRoom.swift:23](../../Sources/PublicChat/UI/Rows/RowEnterRoom.swift#L23) · [RowGift.swift:23](../../Sources/PublicChat/UI/Rows/RowGift.swift#L23) · [RowWheelRes.swift:27/34](../../Sources/PublicChat/UI/Rows/RowWheelRes.swift#L27) · [RowWinnerBroadcast.swift:57](../../Sources/PublicChat/UI/Rows/RowWinnerBroadcast.swift#L57) · [RowOfficialBoostEnter.swift:19-23](../../Sources/PublicChat/UI/Rows/RowOfficialBoostEnter.swift#L19)
- **维度**：iOS 平台规范 / i18n
- **风险**：违反 CLAUDE.md "字符串走 Localizable.strings" 铁律，AR/TR 用户看到句中英文孤立单词
- **finder 原始声明**：L10n key 已存在（`publicScreenEnteredRoom` / `publicScreenSentAction` / `publicScreenWheelHit` / `publicScreenWheelOnTheWheel` / `publicScreenLuckyBySending`），iOS 侧未接入
- **复查证据**：grep [L10n.swift:903-909](../../Sources/L10n.swift#L903) 确认 5 处 key **真实存在** —— finder 主张属实
- **本次 diff 关联**：本次会话在这几个 Row 内**只替换了 `PublicChatLevelBadge` → `UserLevelBadge`**，未涉及 `Text` 硬编码。历史遗留代码。
- **复查后判定**：**建议 · 独立议题**——需要单独 i18n cleanup 一次接入 5 处 key + 补 2 处新 key（`publicScreenOfficialBoost` / `publicScreenWinnerGot`），三语翻译一并加

---

## 四、建议（原始 P2，复查后同为历史遗留）

### #2 · CallView askFor 错误 log 用 `privacy: .public` 泄露 error message

- **文件**：[CallView.swift:452](../../Sources/Call/CallView.swift#L452)
- **风险**：`err=\(error.localizedDescription, privacy: .public)` 与项目其他 error catch（`privacy: .private`）不一致，可能通过 os_log 泄露后端调试 message
- **本次 diff 关联**：本次只改了 [CallView.swift:1420](../../Sources/Call/CallView.swift#L1420) 处 `UserLevelBadge` 替换，L452 log 未动
- **复查后判定**：**建议 · 独立议题**（改为 `.private` 与 PKService 等一致）

### #3 · EnterRoomFloat onAppear Task 未 cancel 跨 item 竞态

- **文件**：[EnterRoomFloat.swift:22-29](../../Sources/Live/Marquee/EnterRoomFloat.swift#L22)
- **风险**：`onAppear` 内 4s sleep Task 不响应 item 切换，快速连击可能提前置 `phase=.leaving`
- **本次 diff 关联**：本次只改了 [EnterRoomFloat.swift:40](../../Sources/Live/Marquee/EnterRoomFloat.swift#L40) 处 `UserLevelBadge` 替换
- **复查后判定**：**建议 · 独立议题**（改用 `.task(id: item.id)` 更符合 SwiftUI 模式，无当前产品报障）

### #4 · UserWeeklyRankSheetView "BIG R" / "VIP" 硬编码文本

- **文件**：[UserWeeklyRankSheetView.swift:234/250/404/419](../../Sources/Live/Rank/UI/UserWeeklyRankSheetView.swift#L234)
- **风险**：直播间高频 badge 缺 a11y label + 未使用公共 `VIPBadge()` / `ActiveTycoonBadge()`
- **本次 diff 关联**：本次改动只涉及 3 处 **等级** 胶囊迁移到 UserLevelBadge，未动 BIG R / VIP badge
- **复查后判定**：**建议 · 独立议题**（与调整 2 精神一致但独立 cleanup）

### #5 · `Color(hex:)` extension 三处重叠定义

- **文件**：[UserLevelBadge.swift:121-129](../../Sources/Message/UI/UserLevelBadge.swift#L121) · [Theme.swift:906](../../Sources/DesignSystem/Theme.swift#L906) · [ChatColors.swift:81](../../Sources/Message/Chat/UI/Bubbles/ChatColors.swift#L81)
- **风险**：违反 [single-target-public-hygiene.md §2](../../.claude/rules/single-target-public-hygiene.md)，未来加参数会触发 `Invalid redeclaration`
- **复查证据**：grep 确认 3 处签名分别是 `init(hex: UInt32)` / `init(hex: UInt, opacity:)` / `init(hex: UInt32, alpha:)` —— overload 分辨依赖类型 label
- **本次 diff 关联**：`UserLevelBadge.swift` 的 `Color(hex:)` extension **在本次改造前就存在**——本次没引入新的重叠
- **复查后判定**：**建议 · 独立议题**（应删除 `UserLevelBadge.swift` 里的 extension，改用 Theme 的 `Color(hex: 0x64439D)` 兼容形式）

### #6 · PartyRoomListView "+100K" 徽章文本硬编码 + a11y 英文

- **文件**：[PartyRoomListView.swift:82/96](../../Sources/Party/UI/PartyRoomListView.swift#L82)
- **风险**：`Text("+100K")` + `accessibilityLabel("Party ranking")` 硬编码，AR/TR 排版失焦
- **本次 diff 关联**：本次只改了 [PartyRoomListView.swift:348](../../Sources/Party/UI/PartyRoomListView.swift#L348) 处 `avatarCircle` 迁移到 AvatarView
- **复查后判定**：**建议 · 独立议题**

### #7 · RegisterAvatarPickerView 连续换图 upload 竞态

- **文件**：[RegisterAvatarPickerView.swift:32-34](../../Sources/Register/BasicInfo/RegisterAvatarPickerView.swift#L32)
- **风险**：`onChange { Task { await upload } }` 未 cancel 旧 Task，快速二次换图 last-write-wins 不确定
- **本次 diff 关联**：本次只改了头像圆形渲染（`AsyncImage → AvatarView`），upload 逻辑未动
- **复查后判定**：**建议 · 独立议题**（理论存在，未见产品报障；改 `.task(id: pickerItem)` 或存 in-flight Task 引用）

---

## 五、复查统计

| 项 | 数量 |
|---|---|
| 原始 finder 项数 | 7 |
| 复查后必修（本次 diff 触发） | **0** |
| 复查后建议（本次 diff 触发） | **0** |
| 复查后信息（本次 diff 触发） | **0** |
| 历史遗留（本次 diff 未引入，flag 独立议题） | **7** |
| finder 误判剔除 | 0 |

**本次 diff 独立预判**（Workflow 未提，主复查预判后按 [code-review-discipline.md §8](../../.claude/rules/code-review-discipline.md) 剔除防御性洁癖）：

| 预判点 | 判决 | 理由 |
|---|---|---|
| PKRankModels 单 item `anchorId` throw → 整数组 decode 失败 | 剔除 | 理论存在 · 0 触发证据 · 修复是防御性；后端 `res \|\| []` 语义下每 item 都合规下发时无问题 |
| PKArenaView 读 AnchorInfoStore 可能空 nickname | 剔除 | sheet 只在 PK 中打开 · AnchorInfoStore 早已 loaded · header nickname 仅装饰性 |
| PKBattleTop3Contributors Button plain 无 tap 反馈 | 剔除 | 交互一致性次要问题 · 非 correctness bug · 属"能改的"不是"该改的" |
| RegisterAvatarPickerView 默认图 `person.fill` → `defaultAvatar` 视觉差异 | 剔除 | 用户上轮已认可 defaultAvatar 兜底 · 无 spec 冲突 |
| UserLevelBadge `.small` 尺寸略变（padding-based 而非 fixed frame） | 剔除 | 用户明示"Message 设计稿为准" · `.medium` 严格保留原 padding · `.small` 属新档梯度 |

---

## 六、后续建议

1. **无需为本次改动开修复 PR**——审查结论是 0 必修。
2. 上述 7 项历史遗留建议独立立一次 i18n cleanup（#1 #4 #6）+ Color extension 收敛（#5）+ 竞态守卫（#3 #7）+ 日志隐私（#2）任务。若立单独 PR，参 [feature-pipeline-complexity-tier.md](../../.claude/rules/feature-pipeline-complexity-tier.md) B 档流程。
3. Workflow 未提到 PKRankModels/PKArenaView 等本次会话真正新增的 impl，说明 finder 焦点偏历史面 —— 未来 review 可考虑 scope 限 `git diff --name-only HEAD` 收敛到"新增+修改行号"级别的 lens。

---

## 七、审查执行元数据

- Workflow Run ID：`wf_ae1d3546-011`
- 5 维度 finder：iOS 平台规范 · Swift 并发 · 内存 · SwiftUI 性能 · 安全与隐私
- 每 finding 3 票对抗验证：`correctness` · `severity-calibration` · `fix-validity`
- 保留门槛：≥2 票 `isReal=true`
- Workflow token 消耗：4.69M
- Workflow 耗时：13 分 40 秒
- 主复查耗时：包含 grep 验证 5 处 finding 声明 + 5 处预判剔除
