# Hily iOS 主播端 代码审查报告

> 审查时间：2026-07-01 22:02
> 审查范围：Sources/Live/ + Sources/Agora/ 全量（31 个 Swift 文件）
> 审查方式：5 维度并行 finder + 3 票对抗验证（Workflow, 95 agent, 7.5M tokens, 27min）→ 主流程二次复查（Read 源码 + Grep 调用方 + 按 [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§2/§6/§7 三档分级）
> 审查依据：[CLAUDE.md](../../CLAUDE.md) + [.claude/rules/](../../.claude/rules/)

## 一、概览

### 原始 finder 计数（Workflow 对抗投票后）

| 严重度 | 数量 |
|---|---:|
| P0 | 3 |
| P1 | 2 |
| P2 | 13 |
| **总计** | **18** |

### 复查后判定（按 [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §2 三档）

| 复查判定 | 数量 | 说明 |
|---|---:|---|
| **必修** | **2** | 有产品调用链路证据 + 真影响功能 / 提审 / 安全 |
| **建议** | **11** | 代码角度改进有据；无明确用户可感触发或已被兜底 |
| **信息** | **3** | 风格 / 洁癖 / 后端协议约束单端无法修 |
| **误判剔除** | **1** | 跨维度重复报告，合并 |

## 二、必修项（2）

### M-1 直播态 UI 显示中文硬编码，出海市场无 i18n（对齐路线图 J 里程碑）

- **文件**：[Sources/Agora/AgoraManager.swift:152](../../Sources/Agora/AgoraManager.swift#L152)、[Sources/Agora/AgoraManager.swift:443](../../Sources/Agora/AgoraManager.swift#L443)、[Sources/Agora/AgoraManager.swift:475](../../Sources/Agora/AgoraManager.swift#L475)
- **维度**：iOS 平台规范（i18n）
- **finder 原始 severity**：P0
- **复查后判定**：**必修**（3 票 correctness/severity/fix 全 real+keep；Read 三处硬编码属实，UI 消费链路 [LiveRoomView:256](../../Sources/Live/LiveRoomView.swift#L256) 直接 `Text(agora.message.isEmpty ? agora.state.label : agora.message)` 明确成立；违反 [CLAUDE.md](../../CLAUDE.md) "字符串走 Localizable.strings 禁止硬编码"铁律）
- **风险**：
  - L152 `message = "joinChannel 调用失败: \(ret)"`
  - L443 `self.message = "本地已加入 uid=\(uid)"`
  - L475 `self.message = "RTC 错误码: \(errorCode.rawValue)"`
  这 3 个 message 在 joinChannel 失败 / 本地加入成功 / RTC error code 三个场景写入，都通过 LiveRoomView topBar 显示给用户。阿拉伯 / 土耳其市场用户直播间顶部**永远看到中文**，命中 App Store 4.1（语言不完整）出海拒审风险
- **修复**：
  - L475 直接接现有 key：`self.message = String(format: L10n.liveRoomStatusRtcErrorFormat, errorCode.rawValue)`（[L10n.swift:457](../../Sources/L10n.swift#L457) 已声明 `liveRoomStatusRtcErrorFormat`）
  - L152 新建 L10n key `liveRoomJoinChannelFailedFormat`（**当前 grep 不存在，需新增**）+ ar/tr/en 三语补文案
  - L443 "本地已加入" 是调试信息，正确做法是**清空 message**（`message = ""`），让 UI 走 state.label = `L10n.liveRoomStatusJoined`

### M-2 DebugNetworkPanel 无 `#if DEBUG` 包裹，Release 生产包对用户显示网络内部计数

- **文件**：[Sources/Live/LiveRoomView.swift:67](../../Sources/Live/LiveRoomView.swift#L67)、[Sources/Live/LiveRoomView.swift:494-525](../../Sources/Live/LiveRoomView.swift#L494-L525)
- **维度**：iOS 平台规范 + 安全与隐私（跨 2 维度 finder 同时命中，属同一问题——本项已合并 P0-3 与 P1-1）
- **finder 原始 severity**：P0 / P1（重复）
- **复查后判定**：**必修**（3 票 correctness/severity/fix 全 keep；Read 验证 L65-67 面板挂载无 DEBUG 隔离；同文件 PK 调试入口 [L39-44](../../Sources/Live/LiveRoomView.swift#L39-L44)、[L91](../../Sources/Live/LiveRoomView.swift#L91)、[L327-330](../../Sources/Live/LiveRoomView.swift#L327-L330) 均显式 `#if DEBUG` 且注释"release 不带（避免审核侧看到内部调试入口）"，标准不一致明显）
- **风险**：Release 构建直播间顶部常驻显示 `net.normal bad=0/10→30 good=0/5 tx=0 rx=0 worst=0 total=0 agora.fps=30 cam.fps=30` 三行 monospaced 黑底 overlay。生产用户可见：(1) 弱网降级阈值 10/30；(2) agora.fps/cam.fps 档位；(3) 中英混排毫无 i18n；(4) App Store 审核 4.0"未完成品"风险
- **修复**：把 L65-67 挂载点用 `#if DEBUG ... #endif` 包起来（与 pkDebugPanel 同款）；同时 `private struct DebugNetworkPanel`（L494-525）整体加 `#if DEBUG` 减少 Release binary footprint。若产品坚持保留生产可见指示，改为小圆点 + accessibilityLabel

## 三、建议项（11）

### S-1 AgoraManager.pushFrame 直读 @MainActor 字段——与 §9 frameSnapshot 规则不对称

- **文件**：[Sources/Agora/AgoraManager.swift:158-165](../../Sources/Agora/AgoraManager.swift#L158-L165)
- **维度**：Swift 并发
- **finder 原始 severity**：P0
- **复查后判定**：**建议**（3 票 correctness/severity/fix 全 real，但 severity 降级 1/3 → P1；进一步复查：D v5.4 铁律 "leave 不 destroy" 保 sharedEngine 单例常驻已兜住 EXC_BAD_ACCESS，实际用户可感的仅是 profile 切换（`.liveBroadcasting ↔ .communication` 由 L117 触发）的 60Hz 窄窗口错帧 + Swift 6 严格并发未来编译失败，未见崩溃/画面异常投诉证据。[.claude/rules/swiftui-camera-preview.md §9](../../.claude/rules/swiftui-camera-preview.md) 已明确点名 AgoraManager 是待接入的漏网之鱼——属技术债而非阻塞 bug）
- **风险**：pushFrame nonisolated 由 CameraManager videoQueue 60Hz 调，直读 `self.engine` / `self.state` / `externalTrackId`；leave() 在 @MainActor 上 `self.engine = nil` 期间，videoQueue 上正在推的一帧可能：(a) 撞进半状态指针（sharedEngine 兜住不崩，但 profile 期短窗口错编码档位）；(b) Swift 6 迁移直接编译失败
- **修复**：照搬 PartyRTCEngine 模式加 NSLock + `frameSnapshot: (engine, active)` struct；join / didJoinChannel / leave / didOccurError 4 个写点后 updateFrameSnapshot；pushFrame 锁内取 snapshot、锁外 push；leave 先 active=false → updateFrameSnapshot → updateChannel(publish=false) → leaveChannel 关闭 inflight 帧窗口。**建议 G 里程碑 PK 抽 RTCSharedEngineGuard 时一并接入**

### S-2 LiveSettingsView 未节流的美颜订阅，slider 期间整树重算

- **文件**：[Sources/Live/LiveSettings/LiveSettingsView.swift:12](../../Sources/Live/LiveSettings/LiveSettingsView.swift#L12)、[Sources/Live/LiveSettings/LiveSettingsView.swift:84-87](../../Sources/Live/LiveSettings/LiveSettingsView.swift#L84-L87)
- **维度**：SwiftUI 性能
- **finder 原始 severity**：P1
- **复查后判定**：**建议**（3 票全 real+keep；Read 验证 L12 `@StateObject params` + L84-87 无 throttle 的 `.onReceive(params.objectWillChange)`；同工程 [LiveRoomView.BeautyPanel L451-456](../../Sources/Live/LiveRoomView.swift#L451-L456) 已用 throttle 60ms 模式证明修复方向。开播准备页非常驻热路径，无卡顿投诉——属遗留反模式回归风险）
- **风险**：`@StateObject params` 自动订阅整个 params.objectWillChange + body 内再 `.onReceive` 未 throttle → slider 30-60Hz 拖动触发 LiveSettingsView 整树重算（含 CameraPreview + LinearGradient + formCard + state switch），主线程持续吃 CPU
- **修复**：对齐 BeautyPanel v5：
  ```swift
  .onReceive(
      params.objectWillChange
          .throttle(for: 0.06, scheduler: DispatchQueue.main, latest: true)
  ) { _ in
      store.clearErrorIfNeeded()
      camera.renderer.updateParameters(params)
  }
  ```
  或把 beautySheet 抽独立子 struct + `@ObservedObject params`，主 view 持 let 不订阅

### S-3 LivePrepareView 未节流的美颜订阅——同 S-2 的旧 view 遗留

- **文件**：[Sources/Live/LivePrepareView.swift:6](../../Sources/Live/LivePrepareView.swift#L6)、[Sources/Live/LivePrepareView.swift:46-48](../../Sources/Live/LivePrepareView.swift#L46-L48)
- **维度**：SwiftUI 性能
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L6 `@StateObject params` + L46-48 无 throttle；同源反模式与 S-2 一致，属 LiveRoomView.BeautyPanel v5 修完时未同步的旧 view）
- **风险**：同 S-2，用户拖 slider 时 body 整树重算 + `DispatchQueue.main.async` 每帧入队 renderer 更新
- **修复**：同 S-2

### S-4 AgoraManager.renewToken Task 未 @MainActor + 嵌套 hop

- **文件**：[Sources/Agora/AgoraManager.swift:392-432](../../Sources/Agora/AgoraManager.swift#L392-L432)
- **维度**：Swift 并发
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（3 票 correctness=false 1/3、severity=downgrade、fix=downgrade；Read 验证 L402 `Task { [weak self] }` 无 @MainActor 属实，L411 `self.engine?.renewToken` 无锁读 engine 属实；但 sharedEngine 是进程单例不会 dealloc，renewToken 作用于 token-channel 绑定、下次 join 会重设，无"全局态污染"。属代码卫生非用户可感 bug）
- **风险**：Task 未标 @MainActor，闭包默认执行器执行；L411 `self.engine?.renewToken` 与 delegate 主队列写 `self.engine = nil` (leave 结束) 无锁；L404 `defer { Task { @MainActor in ... } }` 嵌套 hop 造成 isRenewing 复位晚于 token 应用瞬间
- **修复**：整个 Task 显式 `Task { @MainActor [weak self] in ... }`，defer 直接 `self.isRenewing = false`（已在 MainActor 上下文）。可与 S-1 一并接入 frameSnapshot 时统一改造

### S-5 AgoraManager.leave() 不清理 PK 多频道字典 + leavePKContinuations

- **文件**：[Sources/Agora/AgoraManager.swift:174-211](../../Sources/Agora/AgoraManager.swift#L174-L211)
- **维度**：内存与资源
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L174-211 leave 内确实未清 3 字典 + Grep [LiveStore.forceEnd](../../Sources/Live/LiveStore.swift#L158) 也不调 `leavePKOpposite`——异常路径确实存在。但正常业务路径（用户主动结束 PK → 主动下播）会先 leavePKOpposite；异常路径下 AgoraManager 随 @StateObject 释放会兜底）
- **风险**：forceEnd 走 `.forceEnding → teardown → LiveStore.state=.ended → LiveRoomView.onDisappear → agora.leave()` 时，若用户仍在 PK 中，`pkConnections/pkDelegates/leavePKContinuations` 会滞留至 AgoraManager 释放。极端场景（leave 后 caller 又 join 新直播）会有幽灵 continuation
- **修复**：leave() 内 `state = .idle` 前，锁内清空三字典 + resume 所有未决 continuation：
  ```swift
  pkLock.lock()
  let pendingConts = leavePKContinuations
  leavePKContinuations.removeAll()
  pkConnections.removeAll()
  pkDelegates.removeAll()
  pkLock.unlock()
  pendingConts.values.forEach { $0.resume() }
  ```

### S-6 频道 ID / roomId / uid 日志无 privacy 注解——全项目 hygiene

- **文件**：[Sources/Agora/AgoraManager.swift:144](../../Sources/Agora/AgoraManager.swift#L144)、[Sources/Agora/AgoraManager.swift:235](../../Sources/Agora/AgoraManager.swift#L235)、[Sources/Agora/AgoraManager.swift:258](../../Sources/Agora/AgoraManager.swift#L258)、[Sources/Agora/AgoraManager.swift:265](../../Sources/Agora/AgoraManager.swift#L265)、[Sources/Agora/AgoraManager.swift:439](../../Sources/Agora/AgoraManager.swift#L439)、[Sources/Agora/AgoraManager.swift:448](../../Sources/Agora/AgoraManager.swift#L448)、[Sources/Agora/AgoraManager.swift:504](../../Sources/Agora/AgoraManager.swift#L504)、[Sources/Live/LiveStore.swift:265](../../Sources/Live/LiveStore.swift#L265) 等 8 处
- **维度**：安全与隐私
- **finder 原始 severity**：P1（[AgoraManager 7 处](../../Sources/Agora/AgoraManager.swift)）+ P2（[LiveStore.pauseForCall from=](../../Sources/Live/LiveStore.swift#L265)）
- **复查后判定**：**建议**（3 票全 real，severity 全 downgrade；Read 验证多处 `logger.info("... channel=\(channelId) uid=\(uid)")` 无 privacy 注解，与 [WSHeartbeat.swift:133/142/150/158](../../Sources/Live/WSHeartbeat.swift#L133) 已用 `privacy: .public/.private` 规范不一致。Release Console.app 可抓明文，但仅数字 ID 无姓名/联系方式，需物理接机；属 hardening 而非爆炸性 bug）
- **风险**：Release binary os_log String 默认 privacy 在 Xcode 直连 / Console.app 抓取时**仍可见**；uid 是 UInt 默认 `.public`；channelId + dev Agora AppID（[AgoraConfig.swift:21](../../Sources/Agora/AgoraConfig.swift#L21) DEBUG binary literal）+ 声网 dev 环境无鉴权 → dev 环境无门槛旁听
- **修复**：所有 channelId / roomId / uid / fromUserId 插值统一加 `privacy: .private`；示例 `logger.info("joinChannel channel=\(channelId, privacy: .private) uid=\(uid, privacy: .private)")`。可考虑批量替换或改用 AppLogger 统一入口

### S-7 LiveRoomView.onDisappear guard 遗漏 `.forceEnding / .ending` 中途 dismiss 分支

- **文件**：[Sources/Live/LiveRoomView.swift:172-190](../../Sources/Live/LiveRoomView.swift#L172-L190)
- **维度**：内存与资源
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L177 `guard scenePhase != .background, store.state == .ended`；forceEnd 走 `.forceEnding → 网络请求 → .ended` 期间用户物理返回或系统触发 dismiss 会跳过 teardown）
- **风险**：中途 dismiss 时 camera.tearDown / agora.leave / nim.leave / SystemMessageRouter/pkStore observer 均未清；随后 `.onChange(of: store.state)` view 已不在 tree 也不再触发 dismiss()。摄像头持续采集（红点亮）、声网频道持续推流直到 @StateObject 随 SwiftUI 内存回收
- **修复**：把 guard 拆两阶段，`.background` 时短路；`.forceEnding/.ending/.living/.ended` 都走 teardown（endLiveRoom Task 与 view 生命周期解耦）；非 `.ended` 追加 `endLiveRoom(endType=1)` 尾包避免僵尸房

### S-8 LiveRoomView `@StateObject agora` 触发 topBar 整树重算

- **文件**：[Sources/Live/LiveRoomView.swift:17](../../Sources/Live/LiveRoomView.swift#L17)、[Sources/Live/LiveRoomView.swift:53](../../Sources/Live/LiveRoomView.swift#L53)、[Sources/Live/LiveRoomView.swift:256-262](../../Sources/Live/LiveRoomView.swift#L256-L262)
- **维度**：SwiftUI 性能
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L17 `@StateObject agora` + topBar 直读 3 个 @Published；同文件已针对 CallStore / NIMChatroomManager 拆 sub-store（callState 镜像 / ChatMessagesStore / ChatPresenceStore），agora 是唯一遗漏。对齐 [.claude/rules/swiftui-keepalive-publisher-isolation.md](../../.claude/rules/swiftui-keepalive-publisher-isolation.md) 场景）
- **风险**：远端 `didJoinedOfUid` 变 remoteUid、`didOccurError` 变 message（弱网 109/110 token 场景频发）→ LiveRoomView 整树重算 → CameraPreview updateUIView / PKArenaView identity 检查
- **修复**：把 topBar 内 agora 相关部分抽独立子 view 持 `@ObservedObject agora`（AgoraStatusText / AgoraConnectingCapsule），父 view 保留 @StateObject 生命周期归属但 body 不直接读 agora.xxx

### S-9 PKOverlayHost 恒 4 层 ZStack + 双 @ObservedObject 触发同步重算

- **文件**：[Sources/Live/LiveRoomView.swift:533-548](../../Sources/Live/LiveRoomView.swift#L533-L548)
- **维度**：SwiftUI 性能
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L537-547 ZStack 恒渲 4 个子 view + L534-535 双 @ObservedObject）
- **风险**：PK 期间 scoreUpdate 1-2s 一次触发 host body 重算 → 4 层 ZStack 全走 body 重算；SwiftUI 需算 4 个 view 的 identity 与 transition
- **修复**：按 pkStore.state 单一 if 链只渲染当前态对应 overlay（配合 [.claude/rules/swiftui-camera-preview.md §1](../../.claude/rules/swiftui-camera-preview.md) 用 if 链而非 switch）；或每 overlay 内部抽 bridge 独立订阅所需字段

### S-10 LiveStore UserDefaults "lastEndAt" 硬编码——常量已存在但未复用

- **文件**：[Sources/Live/LiveStore.swift:151](../../Sources/Live/LiveStore.swift#L151)、[Sources/Live/LiveStore.swift:170](../../Sources/Live/LiveStore.swift#L170)
- **维度**：iOS 平台规范（可维护性）
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Grep 确认 [LastEndLiveTracker.key = "lastEndAt"](../../Sources/Live/LiveSettings/LastEndLiveTracker.swift#L11) 已存在但 L151/170 未复用；Tracker 自身注释"存储 key **复用** LiveStore 已在写的"——两端硬编码分裂）
- **风险**：改 Tracker 常量会漏改 LiveStore → 60s 冷却失效；PrivacyInfo.xcprivacy 注释里 key 集分裂
- **修复**：L151/170 改为 `UserDefaults.standard.set(Date(), forKey: LastEndLiveTracker.key)`；进一步在 Tracker 加 `static func markEnded()` API 收敛写入端，LiveStore 调 `LastEndLiveTracker.markEnded()`

### S-11 LiveTimerStore 计时 Task 无 `guard let self else { return }` 出口

- **文件**：[Sources/Live/LiveStore.swift:455-464](../../Sources/Live/LiveStore.swift#L455-L464)
- **维度**：内存与资源
- **finder 原始 severity**：P2
- **复查后判定**：**建议**（Read 验证 L457-462 `while !Task.isCancelled` + `self?.elapsedSeconds += 1` 组合，无 self unwrap 出口；正常 teardown 走 stop() 会 cancel task；异常路径下（S-7 onDisappear guard 遗漏时）Task 变孤儿 nil 写空转）
- **风险**：LiveTimerStore 释放后 Task 每 1s 唤醒做 nil 写直到 stop() 或进程终止；与 S-7 是同源孤儿资源问题
- **修复**：Task.sleep 后加 `guard let self else { return }`

## 四、信息项（3，推迟或架构约束）

### I-1 SF Symbol 图标 accessibilityLabel 部分缺失

- **文件**：[Sources/Live/LiveRoomView.swift:232-236](../../Sources/Live/LiveRoomView.swift#L232-L236)、[Sources/Live/LiveRoomView.swift:264-271](../../Sources/Live/LiveRoomView.swift#L264-L271)、[Sources/Live/LiveRoomView.swift:273-283](../../Sources/Live/LiveRoomView.swift#L273-L283)
- **finder 原始 severity**：P2
- **复查后判定**：**信息**（Read 验证部分做了、部分漏；但 Text 内容已可被 VoiceOver 读到，无 App Store 4.5.5 硬拒审风险）
- **建议**：J 里程碑一并做无障碍全审时补齐 Connecting 胶囊 combine

### I-2 WSHeartbeat 握手 cipher AES-128-ECB 固定 key 无 nonce——协议约束单端无法修

- **文件**：[Sources/Live/WSHeartbeat.swift:117-135](../../Sources/Live/WSHeartbeat.swift#L117-L135)
- **finder 原始 severity**：P2
- **复查后判定**：**信息**（Read 验证 cipher 属实为 AES-128-ECB 固定 key + 无 IV/nonce → 同账号 cipher 恒定 → 理论 replay。但 wss/TLS 已挡大部分 MITM，跨端协议约束 iOS 单方无法修）
- **建议**：backlog，与后端约定加 `"ts":<epochMs>` 到 body、5min 拒 stale cipher。**当前会话不改代码**

### I-3 SSL 证书 pinning 缺失

- **文件**：[Sources/Networking/APIClient.swift:17-21](../../Sources/Networking/APIClient.swift#L17-L21)
- **finder 原始 severity**：P2
- **复查后判定**：**信息**（Read 验证 URLSession 用 `.default` 无 delegate/pinning 属实；iOS App Store 直播类 App 无 pinning 是行业常态，AES body 加密已抵御被动窃听，主动 MITM 需用户主动装 root CA。路线图 J 里程碑候选项）
- **建议**：J 里程碑收尾时按证据加固：APIClient session delegate 实现 SPKI pinning + 双 pin 轮换机制

## 五、误判剔除（1）

### 剔除 X-1 DebugNetworkPanel 未 #if DEBUG（security 维度）

- **finder 报告**：`DebugNetworkPanel 未 #if DEBUG 隔离，Release 包会暴露网络内部计数`（severity=P1，file=LiveRoomView.swift:67，dimension=安全与隐私）
- **剔除理由**：与本报告 M-2（iOS 平台规范维度 P0-3）**同一问题从两个维度同时命中**。Workflow 去重规则用 `file::title.slice(0,20)` 作 key 匹配失败（title 前 20 字符略有差异）；本次复查判定合并入 M-2

## 六、复查统计

| 阶段 | 数量 |
|---|---:|
| 原始 finder 产出 | 30（5 维度合计） |
| 3 票对抗验证通过（≥2 票 isReal） | 18 |
| 主流程二次复查后必修 | 2 |
| 二次复查后建议 | 11 |
| 二次复查后信息 | 3 |
| 二次复查后误判剔除 | 1（重复项合并） |

**复查方法遵循 [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md)**：
- §1 每条 finding Read 涉及行号验证描述与源码一致
- §2 三档分级：必修 = 有产品调用链路证据；建议 = 无具体触发但代码有改进；信息 = 风格/协议约束
- §7 Workflow 3 票对抗**不**等同于二次复查（finder agent 互投缺乏产品全局调用链视野），本轮全部重新过一遍
- §3 复查报告仅列问题，**不**私自修改源码

## 七、建议处理顺序

1. **本迭代**：M-1（i18n hardcode）+ M-2（DebugNetworkPanel 生产可见）—— 直接影响提审 / 用户可见
2. **G 里程碑 PK 抽 RTCSharedEngineGuard 时**：S-1（frameSnapshot）+ S-4（renewToken）+ S-5（leave 清 PK）—— AgoraManager 一次成型
3. **J 里程碑收尾批**：S-2/S-3（美颜 throttle）+ S-6（log privacy）+ S-8/S-9（SwiftUI 性能）+ I-1/I-2/I-3（无障碍 / 协议 / pinning）—— 集中做一次质量批
4. **随手**：S-7（onDisappear guard）+ S-10（UserDefaults 常量）+ S-11（Timer Task guard）—— 单点修复无副作用

## 参考

- [Workflow deep.workflow.js](../../.claude/skills/review/scripts/deep.workflow.js) — 5 维 finder + 3 票 verifier
- [代码审查报告-202606261825.md](代码审查报告-202606261825.md) — 上次基线（人工审查）
- [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) — 分档纪律
- [.claude/rules/swiftui-camera-preview.md](../../.claude/rules/swiftui-camera-preview.md) — §9 frameSnapshot 规则
- [.claude/rules/swiftui-keepalive-publisher-isolation.md](../../.claude/rules/swiftui-keepalive-publisher-isolation.md) — publisher 订阅隔离
