# 派对房 · 用户端 vs 主播端对比审查

> 审查时间：2026-07-17 15:19
> 审查范围：`Sources/Party/` 全量 · 对齐两份对比文档（`~/Downloads/party-room-user-vs-anchor-comparison.md` + `party-room-user-vs-anchor-differences.md`）
> 审查方式：Workflow 6 维并行 finder + 3 票对抗验证 (96 agents / 10.4M tokens) + 主流程手工二次复查（Read + Grep 当前源码）
> 审查依据：CLAUDE.md（派对房唯一以 android 为蓝本）+ .claude/rules/code-review-discipline.md + .claude/rules/uncommitted-broken-hands-off.md

---

## 元级观察 · 多会话协作现象（必读）

审查过程中触发了 [uncommitted-broken-hands-off.md](../../.claude/rules/uncommitted-broken-hands-off.md) 的多会话信号：

- `git status --short Sources/Party/` 显示 5 个 M 文件未 commit
- Workflow finder 于 15:12 抓取源码时读到"未修复"版本；verify 于 15:22 抓取时读到"已修复"版本 → 双方结论互相 reject 但都对，**是真实时序**而非幻觉
- 具体证据：`PartyRoomInfo.swift:131`、`PartyStore.swift:188-196`、`PartyRoomView.swift:1033` 在 finder → verify 期间被另一会话实时更新到正确状态

因此本报告以**最新源码（15:22 及之后）**为准，把"审查中已被修复的原 P0"单列为 §四"已被其他会话修复"，避免报告与磁盘状态发散。

---

## 一、概览（复查后 + 本轮修复）

| 分档 | 计数 | 说明 |
|---|---|---|
| **P0** | 0 | 原 3 项 P0 已在审查过程中被并行会话修复 |
| **P1** | 4 | 有真实产品调用链证据 + 影响核心功能/合规 |
|  · **本轮已修** | 3 | P1-1 / P1-3 / P1-4（本次会话，见 §二各条 "✅ 修复" 段） |
|  · 待修 | 1 | P1-2（删除公屏消息，独立小 spec） |
| **P2/建议** | 2 | 有代码路径证据但影响面小 或 修复是双保险 |
| **信息** | 3 | 属 J 里程碑既定范围 或 需追 android 源码 verify |
| **已被并行会话修复** | 3 | 见 §四 |
| **finder 误判剔除** | 4 | verify 后判非真实 bug 或改经跨里程碑范围外 |
| **android 独有 API 覆盖度评估** | 17 个接口 | 见 §六（新加节，来源：`android-anchor-party-exclusive-apis.md`） |

**审查结论**：派对房主播端权限矩阵与文档 A §1 的对齐**基本正确**（另一会话已修完关键提权 bug），但**"围绕权限的关联能力"**（关注按钮、Settings 页 owner-only 门、删消息合规能力）+ **NIM 契约细节**（userType=2）+ **主播端特有能力接入**（美颜 UI 入口）仍有明确缺口。

---

## 二、P1 · 应修（有真实产品调用链证据）

### P1-1 超管在他人房「关注按钮」UX 断链（selfRole 提权连带副作用）✅ 已修复

- **文件**：[Sources/Party/UI/PartyRoomView.swift:313](../../Sources/Party/UI/PartyRoomView.swift#L313) + [:989-993](../../Sources/Party/UI/PartyRoomView.swift#L989-L993)
- **文档参照**：文档 A §1（关注是所有非房主账号的通用能力）
- **触发场景**：平台超管进他人 Party 房 → 因 `roomInfo.isPlatformAdmin=true`，`PartyStore.selfRole` 首判提权返 `.owner`
- **现状**：
  - `isSelfRoom: store.selfRole == .owner` → 传入 `PartyRoomAnchorBar` → 顶栏关注按钮**视觉上消失**（判定为"这是自己的房间"）
  - `handleFollowTap()` 首行 `guard store.selfRole != .owner else { return }` → 即便按钮显示，tap 也被拦
- **期望**：`isSelfRoom` / follow guard 使用**真实的房主判定**：`store.roomInfo?.ownerId == store.myUserIdString`
- ✅ **修复（本次会话）**：
  1. `PartyStore.swift` 新增 [`isSelfRoomOwner`](../../Sources/Party/PartyStore.swift#L211) computed（`ownerId == myUserId`）
  2. [`PartyRoomView.swift:313`](../../Sources/Party/UI/PartyRoomView.swift#L313) `isSelfRoom` 换用 `store.isSelfRoomOwner`
  3. [`PartyRoomView.swift:989`](../../Sources/Party/UI/PartyRoomView.swift#L989) `handleFollowTap` guard 换用 `!store.isSelfRoomOwner`
  4. Sanity build ✅ SUCCEEDED
- **verify votes**：3/3 confirmed（fix-validity: 纯 iOS 侧改动，数据已在 store）

### P1-2 删除公屏消息功能整体缺失（合规红线）

- **文件**：[Sources/Party/UI/PartyMessageListView.swift](../../Sources/Party/UI/PartyMessageListView.swift)、[Sources/Party/Network/PartyAPI.swift](../../Sources/Party/Network/PartyAPI.swift)、[Sources/Party/PartyStore.swift](../../Sources/Party/PartyStore.swift)
- **文档参照**：文档 A §1 权限矩阵 + 文档 B §1.2 + §3.8
- **触发场景**：房主/房管在派对房遇到违规文本消息 → 长按 → 应弹出删除操作菜单
- **现状**：全项目 grep `deletePartyMsg|deleteMsg|revokeMsg|revokeMessage|onLongPress` 在 `Sources/Party/` 零命中 → **整条链路（UI 手势 → 权限门 → API → NIM recall 广播）不存在**
- **期望链路**：
  1. `PartyMessageListView` 里 `.text` variant 加 `.onLongPressGesture` → 权限判断（`selfRole == .owner || .admin`）→ 弹操作菜单
  2. `PartyStore` 新增 `deletePartyMessage(_ msg:) async`
  3. `PartyAPI` 补 `deletePartyMsg` 端点封装（按 [api-http-method-strict.md](../../.claude/rules/api-http-method-strict.md) rule 追 h5 `src/api/party/index.ts` 字面 path + method）
  4. NIM 侧订阅 recall 广播 → 本地移除消息
  5. 仅对 `.text` variant 允许，礼物/系统/欢迎消息 hide 长按入口
- **修复建议**：作为独立小 spec 单独立项（B 档，接口 2 个：deletePartyMsg + IM 消费；状态机 ≤3 态）
- **verify votes**：3/3 confirmed

### P1-3 派对房主播发文本时未注入 `userType=2`（跨端契约缺口）✅ 已修复

- **文件**：[Sources/Party/Chat/PartyRoomChatManager.swift:174-185](../../Sources/Party/Chat/PartyRoomChatManager.swift#L174-L185)
- **文档参照**：文档 B §1.3（明示 android `PartyRoomVM.kt:949` 固定 `remoteExtension["userType"] = 2`；用户端 `userType=1`）
- **触发场景**：主播以观众身份进他人 Party 房 → 发公屏消息 → 期望消息在其他主播端被识别为"来访主播"
- **现状**：`sendText` 构造 `ext` 时注入 `userId / nickname / userAvatar / userLevel / role / chatBubble`，**独漏 `userType=2`**
- **期望**：`ext["userType"] = 2`（主播端固定常量，与坐麦身份无关；差异文档明确"主播以观众身份进他人房其消息仍带 userType=2"）
- ✅ **修复（本次会话）**：
  - [`PartyRoomChatManager.swift:184-186`](../../Sources/Party/Chat/PartyRoomChatManager.swift#L184) 在 chatBubble 后加 `ext["userType"] = 2`，注释引用 android `PartyRoomVM.kt:949` + 差异文档 §1.3
  - 未改 `PartyPublicChatAdapter.selfEchoText` —— 本地回显 sender 结构不含 userType 字段，且本地不需要"其他端识别我"语义
  - Sanity build ✅ SUCCEEDED
- **争议记录**：verify 有 1 票 reject，理由是"iOS 项目内 party.js 蓝本注释也无 userType"。判定：
  - CLAUDE.md 明示"派对房 android 是唯一蓝本"
  - 差异文档 §1.3 明确"这是真实设计差异，不是 bug"
  - 项目内 P 权限体系已固化 userType=2/1 语义（`PartyListMainView.swift:51` 提到 userType bit）
  - 结论：跟 android 契约，加
- **verify votes**：2/3 confirmed

### P1-4 Room Settings 面板缺 owner-only 门（房管可修改房间信息）✅ 已修复

- **文件**：[Sources/Party/Settings/PartyRoomToolsSheet.swift:67](../../Sources/Party/Settings/PartyRoomToolsSheet.swift#L67)（Settings 图标无 if 门）+ [Sources/Party/Settings/PartyRoomSettingsView.swift](../../Sources/Party/Settings/PartyRoomSettingsView.swift)（内部无角色 guard）
- **文档参照**：文档 A §1 权限矩阵："修改房间信息/背景/公告 · 仅房主"（房管禁止）
- **触发场景**：房管打开管理面板 → 看到 Settings 图标 → 进入编辑页 → 修改房间名/公告/背景/语言 → 保存
- **现状**：
  - `PartyRoomToolsSheet` 的 Settings 图标在 `LazyVGrid` 里无条件显示（房管的 canManage=true 可打开 sheet 见到）
  - `PartyRoomSettingsView` 内部无 `isOwner` guard，可自由编辑并调用 `PartyRoomSettingsService.saveRoomInfo`
- **期望**：Tools sheet 里 Settings 图标包裹 `if isOwner { ... }`；Settings 页 body 加 owner-only 门（进入时若非房主，显示"只读"态或直接不允许打开）
- ✅ **修复（本次会话）**：
  - [`PartyRoomToolsSheet.swift:67-72`](../../Sources/Party/Settings/PartyRoomToolsSheet.swift#L67) Settings 图标包 `if isOwner { toolItem(...) }`
  - Header 注释同步更新（Settings 从"全部房管"移到"Owner-only 项"清单）
  - 未在 `PartyRoomSettingsView` 内部加 guard —— View 用独立 `PartyRoomSettingsStore`（无 PartyStore 引用），传参 `isOwner` 会大改；对齐 android `PartyRoomSettingActivity.kt` 也是通过入口 gate（非 view 内自持角色），入口层收口够用
  - Sanity build ✅ SUCCEEDED
- **风险提示**：房管误改房主设置对**用户端观感**造成运营事故（房间名/头像被非房主改），比 P1-1/P1-3 影响面更广
- **verify votes**：2/3 confirmed（1 票认为"后端会兜底"—— 但 UI 层门缺失属独立缺陷，符合 rule §1 有产品调用链证据判 P1）

---

## 三、P2 / 建议

### P2-5 Tools sheet Lock Room / Room Mode 只判 `if isOwner`（应显式加 `|| isPlatformAdmin` 双保险）

- **文件**：[Sources/Party/Settings/PartyRoomToolsSheet.swift:59](../../Sources/Party/Settings/PartyRoomToolsSheet.swift#L59) + [:73](../../Sources/Party/Settings/PartyRoomToolsSheet.swift#L73)
- **文档参照**：文档 A §1、文档 B §1.1（Lock/Mode 双端均 `isPlatformAdmin || OWNER`）
- **触发场景**：功能上超管已通过 `PartyStore.selfRole` 提权变 `.owner` 走通，本 finding 是"防将来 selfRole 派生优先级变更时的静默回归"
- **现状**：`if isOwner` 一层判定 —— 依赖 `PartyStore.selfRole` 提权链的隐含正确性
- **期望**：改为 `if isOwner || isPlatformAdmin`（与 MC Seat 同款：line 81 已是双保险），语义清晰 + 未来 `selfRole` 派生调整不会静默让 Lock/Mode 消失
- **建议不必修**：当前功能正确，且已有测试验证；仅建议作为长期代码卫生
- **verify votes**：verify 未直接跑此项（因 finder 阶段已被 selfRole 修复"吸收"），主流程复查确认建议保留

### P2-6 派对房内美颜面板 UI 入口缺失

- **文件**：Sources/Party/UI/（无 `BeautySettingsView` / `BeautyPanel` 调用）+ [Sources/Party/PartyStore.swift:1041-1044](../../Sources/Party/PartyStore.swift#L1041-L1044)（engine 层已 attach）
- **文档参照**：文档 A §2.1 主播端特有"美颜（FaceUnity）"
- **触发场景**：主播上视频麦 → 期望能在派对房内实时调整美颜参数（磨皮/滤镜等）
- **现状**：`BeautyPipelineSharer.shared.attach(cm.renderer, token: .party)` 已在 Party engine attach 层就位（美颜**已渲染**）；但 Party UI 无按钮触发面板打开，主播**无法在派对房内调整**
- **期望**：
  - 加浮动"美颜"按钮（对齐直播端 `LiveRoomExtraOverlays` 的美颜按钮位置）
  - 复用直播端 BeautySettingsView 前必须走 [cross-scene-component-reuse-preflight.md](../../.claude/rules/cross-scene-component-reuse-preflight.md) rule preflight（该组件自持 `@StateObject CameraManager` + `.ignoresSafeArea()` 全屏，直接 sheet 挂载会双相机 + 无法半屏 → 需新写轻量 CallBeautyPanel-类似结构）
- **建议**：作为独立 spec（B 档，涉及视频麦 UI + BeautySharer sink），非当前审查修复范围
- **verify votes**：finder 报出但未走 3 票 verify（属于"新增功能"非"已有代码 bug"）

---

## 四、已被并行会话在审查期间修复的原 P0（透明记录）

这三项在 15:12（finder 抓取时）仍存在，15:22 之后已被另一会话修复。记录以避免报告与磁盘状态背离。

### ✅ isPlatformAdmin 提权错档 · [PartyRoomInfo.swift:131](../../Sources/Party/Models/PartyRoomInfo.swift#L131)

- 旧版：`if isPlatformAdmin == true { return .admin }` + 注释"视为最高权限（admin 等价）"
- 新版：`if isPlatformAdmin == true { return .owner }  // 提权等同房主（非 admin）` + 注释引用差异文档 §4
- 影响：Lock Room / Room Mode / MC Seat 三项功能门对超管解锁

### ✅ PartyStore.selfRole 派生未纳入 isPlatformAdmin · [PartyStore.swift:188-196](../../Sources/Party/PartyStore.swift#L188-L196)

- 旧版：先 selfSeat.roomRoleType 分支 → 超管坐观众位时 selfRole=.audience，管理入口消失
- 新版：**首判** `roomInfo?.isPlatformAdmin == true → .owner`，再 selfSeat.roomRoleType，再 fallback roomInfo.selfRoleType；注释说明"seat 侧无 isPlatformAdmin 字段，超管特权只在 roomInfo 层"

### ✅ PartyRoomView tools sheet isPlatformAdmin 硬编 false · [PartyRoomView.swift:1033](../../Sources/Party/UI/PartyRoomView.swift#L1033)

- 旧版：`isPlatformAdmin: false, // TODO(F): 接入 roomInfo.isPlatformAdmin`
- 新版：`isPlatformAdmin: store.roomInfo?.isPlatformAdmin ?? false` + 注释说明"selfRole 层已提权，此参数为语义冗余安全网"

---

## 五、信息级（属 J 里程碑或需追 android 源码）

### I-7 公屏 Tab All/Chat/Gift 直接对齐 h5，未追 android filterMsgList 具体维度

- **文件**：[Sources/Party/UI/Components/PartyRoomChatTabStrip.swift](../../Sources/Party/UI/Components/PartyRoomChatTabStrip.swift)、[Sources/Party/UI/PartyMessageListView.swift](../../Sources/Party/UI/PartyMessageListView.swift)
- **文档参照**：文档 B §3.4（"h5 是 All/Chat/Gift；android filterMsgList 维度不同"—— 但未列 android 具体维度）
- **现状**：iOS 三 Tab 分类 `.all/.chat/.gift`（`.chat` 排除 gift-like，`.gift` 只 gift/luckyGift）—— 与 h5 一致
- **需追 android**：`filterMsgList()` 在 `PartyRoomActivity.kt:575` 的具体分类维度是什么？是按 `userType` 分组 / 按 attachType 分组 / 还是其他？
- **建议**：由用户或独立 agent 追 android 源码后决策 —— 若 android 也是"gift vs 非 gift"就属"两端等价，实现方式不同"（信息级）；若 android 是"用户消息 vs 系统消息"这类维度不同，则升 P2
- **verify votes**：2/3 confirmed real（severity 停 P2，因缺 android 具体证据）

### I-8 主播端特有能力 · J 里程碑范围（露脸检测 / 周任务 / 资产入口 / Party Data 看板 / 埋点）

- **文档参照**：文档 A §2.1；CLAUDE.md 里程碑路线图（J 期"埋点+i18n+Crash+收尾"）
- **现状**：grep `FaceCheck|WeekTask|PartyData|GemExchange` 在 Sources/Party/ 零命中；grep `TDAnalytics|ThinkingData` 全项目仅 Register/Live 有 J 期 TODO
- **结论**：非当前里程碑遗漏 —— 均属 J 里程碑既定范围
- **verify votes**：真实存在但严重度校准为"信息级"（rule §7 工具产出 ≠ 二次复查，主流程判定"跨里程碑基建依赖，不能在派对房 spec 范围内独立落地"）

### I-9 Party Private Call 设置端语义澄清

- **文件**：[Sources/Party/UI/PartyRoomView.swift:611](../../Sources/Party/UI/PartyRoomView.swift#L611)（房主房间开关按钮）
- **文档参照**：文档 A §2.1（`PartyPrivateCallSettingDialog.kt`：主播作为**被 call 方**配置开关与礼物）
- **现状**：iOS F-spec 已实现房主 in-room 开关 —— 用 `updatePartyPrivateCall` 端点 + `partyPrivateCallOpen` + `partyCallGiftId` 房间字段
- **需澄清**：android `PartyPrivateCallSettingDialog.kt:174` 调的**同一个 API 端点**（据 iOS 项目内安卓源码梳理文档），语义究竟是：
  - 假设 A：与 iOS F-spec 房主房间开关**同一功能**（不同 UI 载体）→ 不需要额外实现
  - 假设 B：是**主播作为被 call 方的全局开关**（房外/Mine 页配置）→ iOS 需独立入口
- **建议**：由用户或独立 agent 交叉验证安卓源码梳理文档 vs 差异文档 §2.1 表述 —— finder 有分歧（verify 3 票中 1 票认为已实现、1 票认为是全局配置端）
- **verify votes**：1/3 confirmed（弱势 finding，需产品/安卓源码进一步判定）

---

## 六、finder 误判剔除（透明记录）

| finder 原始发现 | 剔除原因 |
|---|---|
| "isPlatformAdmin 被降级为 .admin" | 已被并行会话修复（见 §四） |
| "PartyRoomView.swift:1031 硬编码 false + TODO(F)" | 已被并行会话修复（见 §四） |
| "handleFollowTap 拦截房主" | 与 P1-1 是同一 issue，合并 |
| "Party Private Call 设置端 P0/P1" | 存疑，语义未澄清，降级为信息 §I-9 |

---

## 六、android 主播端独有 API 覆盖度评估（来源：`android-anchor-party-exclusive-apis.md`）

安卓主播端 17 个独有接口，按 iOS 项目覆盖度分类。**iOS 已实现 3 组共 5 个接口**，其余分归各里程碑 backlog。

### 6.1 ✅ iOS 已实现（对齐 android）

| # | Android 端点 | Android 调用点 | iOS 实现 |
|---|---|---|---|
| 6.1.1 | `POST gem/getBalance` | `GemExchangeDiamondActivity` / `BalanceInfoActivity` 余额查询 | ✅ [`PartyBalanceService.swift:18`](../../Sources/Party/Network/PartyBalanceService.swift#L18)（路径 `/sapi/weidou/v1/client/gem/getBalance`）+ [`WorkViewModel.swift:229`](../../Sources/Work/WorkViewModel.swift#L229) —— 派对礼物面板 + 工作台钱包 |
| 6.1.2 | `POST sts/getOssUploadParam` + OSS multipart 直传 | `HttpHelper.uploadSingleFile()` 露脸截图/头像上传 | ✅ [`Sources/Core/Upload/`](../../Sources/Core/Upload/)（`PublicVideoUploader` / `ImageUploader` / `getOssUploadParam`）—— 朋友圈/头像/反馈截图/派对房背景等复用同一 |
| 6.1.3 | `POST api/anchor/party/data/board` | `PartyRoomDataActivity` 周/月看板 | ✅ [`Sources/Work/PartyData/PartyDataService.swift:23`](../../Sources/Work/PartyData/PartyDataService.swift#L23) |
| 6.1.4 | `POST api/anchor/party/data/micTimeDetail` | `PartyMicTimeDetailActivity` 麦时明细 | ✅ [`PartyDataService.swift:39`](../../Sources/Work/PartyData/PartyDataService.swift#L39) + [`PartyMicTimeDetailView.swift`](../../Sources/Work/PartyData/PartyMicTimeDetailView.swift) |
| 6.1.5 | `POST party/room/enter` / `party/seat/*` / `battle/*` 等主流程共用 | 全模块 | ✅ [`PartyAPI.swift`](../../Sources/Party/Network/PartyAPI.swift) 已覆盖 |

### 6.2 🔲 未实现 · 明确在某里程碑范围（不算当前 E 期遗漏）

| # | Android 端点 | Android 调用点 | iOS 归属里程碑 |
|---|---|---|---|
| 6.2.1 | `POST party/room/checkExistHot3` | `PartyRoomActivity.kt:2623` 热榜任务闭环 | **未定** —— 属"Hot 榜任务 + 露脸监管"整体特性，无对应 iOS 里程碑；建议归 **J 或独立特性** |
| 6.2.2 | `POST party/room/top/availableSeat` | `fetchAndShowGoTopRoomDialog()` 引导跳去热门房 | 同 6.2.1 |
| 6.2.3 | `POST party/room/partyRoomReport` | `reportToServer()` 露脸截图上报 | 同 6.2.1（`Sources/Match/MatchStore.swift` 里有 1v1 匹配的 `startFaceCheck` 系列，但派对房不同链路：android 是 topX + screenFaceLimit 计数体系，非 1v1 通话检测） |
| 6.2.4 | `POST party/room/task/getTaskInfo` | `WeekTaskDialog.kt` 周任务上麦时长换金币 | 同 6.2.1 |
| 6.2.5 | `POST party/battle/cancel` | `PartyBattleVM.kt:30` PK 选队阶段取消 | **G 里程碑（PK）**范围 |
| 6.2.6 | `POST party/battle/lastState` | `PartyPkCoolDownDialog.kt:82` 冷却期回显战绩 | **G 里程碑** |
| 6.2.7 | `POST gem/exchangeDiamond` | 宝石兑钻石 | **I 里程碑（收益/资产完整）**范围 —— iOS 目前只有 balance 读取 |
| 6.2.8 | `POST gem/exchangeCoin` | 宝石兑金币 | 同 6.2.7 |
| 6.2.9 | `POST gem/query/exchange/record` | 宝石兑换流水 | 同 6.2.7 |
| 6.2.10 | `POST diamond/flow/record` | 钻石流水 | 同 6.2.7 |
| 6.2.11 | `POST sapi/marketing/v1/client/gameTask/getRanking` | `PartyRoomRankingFragment.kt:206` 游戏任务激励排行 | **F 里程碑**（排行）范围 |
| 6.2.12 | `POST api/ranking/getRankingList` | 通用榜单 | **F 里程碑** |

### 6.3 ⚠️ 未实现 · 需 iOS 侧决策（非纯里程碑事项）

| # | Android 端点 | iOS 现状 | 决策点 |
|---|---|---|---|
| 6.3.1 | `POST party/room/checkPassword` | ❌ 未实现（走 h5 模型：本地 `lockFlag` 判断 + 密码随进房提交） | **要不要跟 android 服务端预检链路？** android 从列表点房间时**先调接口问后端是否需密码**，需要才弹密码框再进房；iOS 现状功能上等价但**多一次网络无缓解**（进房失败再回落密码框），产品/后端能否接受 h5 模型是关键——若后端对预检链路有反作弊考虑，iOS 应对齐 |
| 6.3.2 | `POST api/half/geme/anchor/list` | ❌ 未实现半屏游戏列表 | 未来做半屏游戏列表时**必须**用 `anchor/list` 端点，不要抄 h5 的 `api/half/geme/v2/list`（服务端按端下发不同游戏池；跨端复用逻辑会拉到错误的游戏集） |

### 6.4 🔇 iOS 不需要跟

| # | Android 端点 | 原因 |
|---|---|---|
| 6.4.1 | `POST api/user/v2/myUserFriend` | android 自己也是"已定义但 partyroom 业务层未见调用"，实际邀请好友走两端共用的 `party/room/getFollowInfoList`；iOS 无需 |

### 6.5 iOS 覆盖度小结

| 类别 | 计数 | 说明 |
|---|---|---|
| 已实现 | 5 | 收益基建（gem/getBalance）+ OSS 上传 + Party Data 看板 2 个 —— **正是 iOS 主播端"生产者"闭环最先落的部分** |
| 里程碑 backlog | 12 | Hot 榜/露脸 4 + PK 2 + 资产完整 4 + 榜单 2 |
| 需决策 | 2 | 密码预检链路 · 半屏游戏 anchor 端点 |
| 不需跟 | 1 | v2 好友列表（android 也遗留） |

**跨端 iOS 侧关键提醒**：
1. **半屏游戏列表**未来若做（F/G），**必须**用 `anchor/list` 端点而非 h5 的 `v2/list`
2. **密码预检**若产品定要对齐 android，需要评估：iOS `PartyRoomListVM` 类似接入 —— 但 iOS 现有 `enterRoom` 已能通过后端错误码分流密码错，链路差异不影响功能，属工程一致性诉求非 bug
3. **STS + OSS 直传**已就位 —— 未来加露脸截图上报（6.2.3）时**直接复用** `Sources/Core/Upload/`，不必新写 pipeline

---

## 七、复查统计

| 项 | 数值 |
|---|---|
| Workflow finder 原始发现（去重前） | ~30 项 |
| Workflow 3 票 verify 通过 | ~20 项 |
| 主流程二次复查（Read + Grep + rule §1/§6）后保留 | **9 项**（P1 × 4 · P2 × 2 · 信息 × 3） |
| finder 误判剔除 | 4 项（见 §六 已被替换到 §"finder 误判剔除"）|
| 审查过程中被并行会话修复 | 3 项（见 §四）|
| **本轮会话已修** | **3 项**（P1-1 / P1-3 / P1-4）|
| Android 独有 API iOS 覆盖度 | 5/17 已实现 · 12 里程碑 backlog · 2 需决策 · 1 不跟（见 §六）|

---

## 八、后续行动建议（Non-normative）

**✅ 本轮已完成**：
- P1-1 关注按钮 UX 断链（`isSelfRoomOwner` computed + 2 处 View 层替换）
- P1-3 sendText 补 `userType=2`
- P1-4 Room Settings owner-only 门（入口层 `if isOwner`）
- Sanity build ✅ `xcodebuild ... build` SUCCEEDED

**待做 · 权重 1（独立小 spec，B 档，1 天）**：
- P1-2 删除公屏消息 —— UI 手势 + PartyStore action + PartyAPI 端点 + NIM 消费

**待做 · 权重 2（独立 spec，B 档，1-2 天）**：
- P2-6 派对房内美颜面板入口 —— 涉及 `cross-scene-component-reuse-preflight` rule preflight，需新写轻量 CallBeautyPanel-类型组件

**归档 · 里程碑 backlog（见 §六）**：
- **F 期**：榜单接口（gameTaskRanks / rankingList）
- **G 期**：PK cancel/lastState + 半屏游戏 anchor 端点
- **I 期**：完整资产（gem/exchange* + diamond/flow）
- **未定/独立特性**：Hot 榜任务 + 露脸监管闭环（checkExistHot3 / availableSeat / partyRoomReport / getWeekTaskInfo）—— 属主播端最核心的独有闭环，产品若拉重要性可提前
- **J 里程碑**：埋点

**需产品/安卓源码交叉验证**：
- I-7 公屏 Tab 维度
- I-9 Party Private Call 设置端语义
- 6.3.1 密码预检链路（h5 模型 vs android 服务端预检）

---

## 附：本次审查触发的 rule / 教训

- [uncommitted-broken-hands-off.md](../../.claude/rules/uncommitted-broken-hands-off.md) —— 多会话协作触发；finder 与 verify 时序不一致的处理
- [code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§7 —— 主流程二次复查纪律（不完全信任 Workflow 内 3 票投票）
- [cross-scene-component-reuse-preflight.md](../../.claude/rules/cross-scene-component-reuse-preflight.md) —— P2-6 复用 BeautySettingsView 前必须 preflight
- [api-http-method-strict.md](../../.claude/rules/api-http-method-strict.md) —— P1-2 新增 deletePartyMsg 端点前追 h5 字面 path + method
