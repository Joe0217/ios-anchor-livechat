# Hily iOS 主播端 代码审查报告

> 审查时间：2026-06-26 17:57
> 审查范围：**本会话未提交的 Party 模块 4 个文件**（C 档单测重构 / 非 git diff HEAD 全量）
> 审查方式：5 维度并行 + 3 票对抗验证（Workflow `w3x8s4ybx`）/ 主流程二次复查（强约束 hunk 范围）
> 审查依据：CLAUDE.md + .claude/rules/* + [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§6

## 一、概览

| 维度 | 复查后必修 | 复查后建议 | 复查后信息 | finder 原始 |
|---|---|---|---|---|
| 数量 | **0** | **0** | **0** | 9（P1×3 / P2×6） |

**审查结论**：本会话改动全部为**纯重构**（抽 pure static func），行为零语义变更，0 必修 finding。Workflow 报的 9 项 finding **全部落在 hunk 之外的 pre-existing 代码**（line 20-47 / 409-430 / 484-503 / 563 / 597-636 / 112-117 / 525 / 544 / 673），与本次会话改动无关——严格按用户 scope "仅审查当前会话未提交部分" 全部移出本次报告，列入末尾"pre-existing 议题"供单独决策。

## 二、本会话改动范围（hunk 边界，作为复查约束）

本会话改动仅落在以下 4 个文件、以下精确 line range：

| 文件 | hunk line range | 类型 |
|---|---|---|
| [Sources/Party/Models/PartyEvents.swift](../../Sources/Party/Models/PartyEvents.swift) | `+49 ~ +102` | 新加 `PartyGiftEvent.from` / `PartyVideoSeatInvite.from` 两个 static 方法 |
| [Sources/Party/NIM/PartyKickedOutGuard.swift](../../Sources/Party/NIM/PartyKickedOutGuard.swift) | `+1 ~ +30` | 全新建（28 行 + 注释） |
| [Sources/Party/NIM/PartyMessageRouter.swift](../../Sources/Party/NIM/PartyMessageRouter.swift) | `~115 ~ ~150` | `handleKickedOut` / `handleVideoSeatInvite` 改薄包装（调下沉的 static func） |
| [Sources/Party/PartyStore.swift](../../Sources/Party/PartyStore.swift) | `~639 ~ ~657` | `didReceiveGift` 18 行 → 5 行（薄包装） |

**hunk 之外的所有代码 = pre-existing**，不在本次审查范围。

## 三、二次复查方法

按 [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§6/§7 强制要求：

1. **机械约束**：`git diff -U2 HEAD` 抓 hunk 精确边界，对 Workflow 每条 finding 的 file:line 做 hunk 内/外判定
2. **本会话改动逐行对照**：对 hunk 内 4 处改动，**逐条** Read 改前/改后语义验证等价性
3. **finder 误判直接剔除**：finding 落在 hunk 之外即剔除（按用户 scope）

## 四、本会话 hunk 内改动逐项复查（4 项）

### #1. `PartyGiftEvent.from(payload:timestampMs:)`
- **改动语义**：把 PartyStore.didReceiveGift 内联的 7 行字段提取代码搬进 PartyGiftEvent extension
- **行为等价性验证**：
  - `payload["sendUser"] as? [String: Any]` → 保留 ✓
  - `compactMap { stringify($0["userId"]) }` → 保留 ✓
  - `intify(giftId) ?? 0` / `intify(giftNum) ?? 1` → 保留 ✓
  - `flatMap { stringify($0["userId"]) }` / `as? String` → 保留 ✓
  - timestamp 透传 ✓
- **测试覆盖**：`PartyGiftEventTests` 15 case 全 PASS
- **复查后判定**：✅ **无 finding**（行为等价 + 测试覆盖完整）

### #2. `PartyVideoSeatInvite.from(payload:fallbackRoomId:timestampMs:)`
- **改动语义**：把 PartyMessageRouter.handleVideoSeatInvite 内联的 8 行守卫+构造搬进 PartyVideoSeatInvite extension
- **行为等价性验证**：
  - `!inviteId.isEmpty && seatIndex > 0` 守卫 → 保留 ✓
  - `stringify(ownerUserId)` / `as? String ownerNick` / `stringify(roomId) ?? fallback` → 保留 ✓
  - 守卫失败返回 nil（原代码守卫失败 log + return） → 等价（调用方 if let 包装 + log）✓
- **测试覆盖**：`PartyVideoSeatInviteTests` 17 case 全 PASS
- **复查后判定**：✅ **无 finding**

### #3. `PartyKickedOutGuard.shouldHandle(payload:myUserId:chatRoomId:)`
- **改动语义**：把 PartyMessageRouter.handleKickedOut 双 guard 搬进独立 enum 静态方法
- **行为等价性验证**：
  - 原：`guard let me = myUserId, let t = targetUserId, me == t else ...`
  - 新：`guard let me = myUserId, !me.isEmpty else { return false }` + `guard let target == me else ...`
  - **语义微变更**：新增 `!me.isEmpty` 守卫——原代码 `SessionStore.user.userId` 为 `""` 时会让 `"" == ""` 误踢通过，新代码拒绝。**这是修了一个 latent bug，方向更安全**。SessionStore 当前实现登录态下 userId 不会为空，所以零产品行为变化（实际触发链路 0），但守卫更严是 fail-safe 默认。
- **测试覆盖**：`PartyKickedOutGuardTests` 14 case 全 PASS（含 `test_myUserIdEmpty_returnsFalse`）
- **复查后判定**：✅ **无 finding**（严格守卫是改进；产品调用链路触发概率 0）

### #4. `PartyStore.didReceiveGift` / `PartyMessageRouter.handleKickedOut` / `handleVideoSeatInvite` 调用点薄包装
- **改动语义**：3 个调用点用新 static func 替换原内联逻辑
- **行为等价性验证**：调用方对外接口签名 / delegate 转发 / log 文案均保留 ✓
- **复查后判定**：✅ **无 finding**

## 五、Workflow finder 原始 9 项（全部移出本次 scope）

下表对 Workflow 返回的 9 项 finding 做**机械化 hunk 内/外判定**：

| ID | finder 原 severity | 标题 | finding 报告位置 | 是否在本会话 hunk 内 | 复查后判定 |
|---|---|---|---|---|---|
| P1-1 | P1 | PartyStore 11 @Published 失效风暴 | `Sources/Party/PartyStore.swift:20-47` | ❌ 否 | **移出 scope**（pre-existing 设计） |
| P1-2 | P1 | myUid/uid `.public` 违反 userId 隐私约定 | `Sources/Party/PartyStore.swift:409,430` | ❌ 否 | **移出 scope** |
| P1-3 | P1 | 1001 seatUpdate keys `.public` 漏改 | `Sources/Party/PartyStore.swift:563` | ❌ 否 | **移出 scope** |
| P2-1 | P2 | （同 P1-3 重复）1001 seatUpdate keys `.public` | `Sources/Party/PartyStore.swift:563` | ❌ 否 | **移出 scope** + **去重**（与 P1-3 同条） |
| P2-2 | P2 | sink 强捕获 rtcRef 违反 §9 sink rule | `Sources/Party/PartyStore.swift:484-488` | ❌ 否 | **移出 scope** |
| P2-3 | P2 | unsubscribe + tearDown 冗余 | `Sources/Party/PartyStore.swift:496-503` | ❌ 否 | **移出 scope** |
| P2-4 | P2 | applyMediaUpdate 空 diff 也触发 | `Sources/Party/PartyStore.swift:597-636` | ❌ 否 | **移出 scope** |
| P2-5 | P2 | lastGiftEvent 居主 store 致父 body re-eval | `Sources/Party/PartyStore.swift:25` | ❌ 否 | **移出 scope** |
| P2-6 | P2 | DecodingError/SDK 错误 `.public` 漏改 | `Sources/Party/PartyStore.swift:112,117,525,544,673` | ❌ 否 | **移出 scope** |

**移出 scope 不等于 finding 价值为 0**——P1-2 / P1-3 / P2-6 的隐私漏改、P2-2 的 §9 sink rule 违反、P1-1 / P2-4 / P2-5 的 SwiftUI 性能议题，均是真实需要单独立项处理的 pre-existing 议题（详见末尾第七节）。

## 六、复查统计

| 项 | 数量 |
|---|---|
| Workflow finder 原始返回 | 9 项 |
| 二次复查 hunk 内 / 本次改动相关 | **0 项** |
| 误判剔除（finder 描述误差） | 0 项 |
| 重复合并（P2-1 与 P1-3 同条） | 1 项 |
| **移出 scope（hunk 外 pre-existing）** | **8 项**（去重后） |
| **本次复查后必修** | **0** |
| **本次复查后建议** | **0** |
| **本次复查后信息** | **0** |

## 七、pre-existing 议题清单（不在本次报告内，供单独决策）

下列议题与本次会话改动无关，按 finder 报告**原始评级**列出，**不**纳入本次必修/建议/信息分级。是否处理由用户决定：

| ID | 主题 | 文件:line | finder 评级 | 复查建议处理路径 |
|---|---|---|---|---|
| pre-A | 11 @Published 失效风暴 → 拆 sub-stores | [PartyStore.swift:20-47](../../Sources/Party/PartyStore.swift) | P1 | J 期性能批 / E 期内 backlog（重复出现于 review 202606252040 P2-A） |
| pre-B | postMikeList myUid/uid 隐私 `.public` → 改 `.private` | [PartyStore.swift:409,430](../../Sources/Party/PartyStore.swift) | P1 | 同 review 202606260029 P2-7 后置补丁批 |
| pre-C | 1001 seatUpdate payload keys `.public` → `.private` | [PartyStore.swift:563](../../Sources/Party/PartyStore.swift) | P1 | 同 pre-B 一批 |
| pre-D | CameraManager sink 强捕获 rtcRef 违反 §9 | [PartyStore.swift:484-488](../../Sources/Party/PartyStore.swift) | P2 | G 期 PK 抽 RTCSharedEngineGuard 前必须改 |
| pre-E | tearDown 已含 removeAll subscribers, unsubscribe 冗余 | [PartyStore.swift:496-503](../../Sources/Party/PartyStore.swift) | P2 | 低优先级，顺手清 |
| pre-F | applyMediaUpdate 无 diff short-circuit | [PartyStore.swift:597-636](../../Sources/Party/PartyStore.swift) | P2 | J 期性能批 |
| pre-G | lastGiftEvent 抽独立 bus 切失效 | [PartyStore.swift:25](../../Sources/Party/PartyStore.swift) | P2 | H 期接 GiftAnimationQueue 时一起做 |
| pre-H | DecodingError / SDK error `.public` 改 `.private` 一批 | [PartyStore.swift:112,117,525,544,673](../../Sources/Party/PartyStore.swift) | P2 | 同 pre-B/pre-C 隐私批一并清 |

**建议处理方式**：
1. 不阻塞本次 C 档单测重构提交
2. 用户可单独开 issue / 后续 PR 处理 pre-A ~ pre-H
3. 若想立即并入本次提交，需另起 review/feature-pipeline 流程，不污染当前重构 commit 的纯粹性

## 八、验收

- ✅ Workflow `w3x8s4ybx` 跑完 9 项 finding
- ✅ 主流程逐项 hunk 内/外判定（含 git diff -U2 机械证据）
- ✅ 本会话 4 处改动逐项行为等价性 review + 测试覆盖确认
- ✅ 报告含"复查后判定"列 + 复查统计表（含移出 scope 数）
- ✅ 不修改任何源码（review 是只读的）
- ✅ 文件路径全部 markdown 链接
