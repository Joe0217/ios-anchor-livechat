# H 里程碑 IM 完善 · 实施计划

> 关联 spec：`H-IM完善-spec-202606242200.md` + `H-IM完善-spec-H5安卓代码二次校验-202606242200.md`
> 上游：B 直播 ✓ · C 通话 ✓ · D 直播↔通话 ✓ · E 派对房 MVP ✓ · G 直播 PK ✓
> 量级：M（IM 底座）· 预计 ~6.5d
> 范围：IM 底座 + 三通道路由 + AttachType 全量 + 跨里程碑重构；**礼物动画在另一会话**

---

## §1 阶段总览

| 代号 | 目标 | 主要交付 | 估时 | 提测点 | 依赖 |
|---|---|---|---|---|---|
| **M0** AttachType 全量枚举 + protocol 锁定 | 60+ case + MessageRouter + GiftMessageRouter / SystemMessageRouter / PartyMessageRouter 协议签名 + NIMServiceError | 1d | 单测全量 case Int/String raw round-trip；礼物会话 review protocol 签名通过 | — |
| **M1** NIMService 共底座 | NIMService.swift 含 setupOnce / login / logout / refreshToken / enterChatroom / exitChatroom / sysMsg + syncSysMsgs 监听 / connectionState @Published / unwrapDataField gzip helper | 1.5d | NIMServiceTests 单测覆盖 login/logout/token 刷新 + gzip 解压 + 错误码分流 | M0 |
| **M2** PKNIMRouter 改 MessageRouter | PKNIMRouter 实现 MessageRouter；保留旧 route 为 internal 调用入口；PKStore.router 类型改 MessageRouter | 0.3d | G PK 全链路真机不破 | M0 |
| **M3** PartyMessageRouter + PartyChatroomChannel 重构 | 从 PartyRoomChatManager 抽 dispatchCustom 到 PartyMessageRouter；ChatroomManager 简化到 ~350 行 | 1.5d | E 派对房用例全过：进房 / 上下麦 / 公屏 / 送礼 placeholder | M1 |
| **M4** SystemMessageRouter | 实现 sysMsg 通道分发 attachType=44/61/62/-3/-6/-1/15/18/58/90/-4；接入 LiveStore.handleForceEndFromIM + CallStore 各 helper 方法 | 1d | sysMsg mock 注入测试；C 通话用例 callIncome / callRemoteMessage 真机回归 | M1 |
| **M5** LiveChatroomChannel 重构 + LiveRoomView 接入 | NIMChatroomManager 简化为 LiveChatroomChannel ~120 行；删 IM 登录 + 注入 [MessageRouter]；LiveRoomView 注入路由数组 | 0.8d | B 直播 + G PK + 礼物 placeholder 全链路不破 | M1, M2 |
| **M6** 真机验收 + 跨里程碑回归 | DoD §7 全部 10 项；51+ 单测全过；2 端 NIM 全链路真机回归 | 0.5d | spec §7 DoD 全过 | All |

**累计**：6.6d（接近 6.5d 评估）

**关键节点**：
- M0 锁 protocol → 礼物会话立即可启动并行
- M1 完成 NIMService → 后续 M2-M5 全部可在此基础上小步迭代
- M4 sysMsg 首次接入：风险最高（R2/R7），单独留 0.5d 集成测试

---

## §2 M0 AttachType 全量 + protocol 锁定（1d）

**目标**：把 attachType 全量编码定死 + Router 协议签名确定；礼物会话基于此立刻可启动。

### 2.1 任务清单

| 任务号 | 文件 | 改动 | 自检 |
|---|---|---|---|
| M0-1 | `Sources/Live/NIM/AttachType.swift` | 从 13 case 扩到 ~60 case；新增 String case（SEND_GIFT / CP_RANK_* / active_tycoon_enter_room）+ `knownButUnhandled(raw:)` 兜底；调整 `init(raw:)` 全量分发 | Build 通过 |
| M0-2 | `Sources/Live/NIM/MessageRouter.swift`（新增） | protocol + MessageContext enum + Weak<T> wrapper | Build 通过 |
| M0-3 | `Sources/Live/NIM/GiftMessageRouter.swift`（新增） | protocol（仅签名，default impl 用 fallthrough）+ `GiftMessage` 数据模型扩展（H 礼物会话填充） | Build 通过 |
| M0-4 | `Sources/Live/NIM/SystemMessageRouter.swift`（新增） | class 骨架；TODO 标 M4 实现具体业务分发 | Build 通过 |
| M0-5 | `Sources/Live/NIM/NIMServiceError.swift`（新增） | enum + LocalizedError + errorDescription | Build 通过 |
| M0-6 | `Tests/HilyTests/AttachTypeFullTests.swift`（新增） | 60+ case 全量 raw round-trip 单测；字符串 vs Int 双形态；nil/unknown 兜底 | 全过 |
| M0-7 | `Sources/L10n.swift` + `*.lproj/Localizable.strings` | 新增 L10n.IM 8 条错误文案三语言 | 三语言一致 |

### 2.2 礼物会话 protocol review checkpoint

M0-3 完成后**立刻通知礼物会话**：本会话锁定的 `GiftMessageRouter` protocol 签名 + AttachType 礼物相关 case + `GiftMessage` 数据模型。礼物会话基于此独立实施动画/SVGA/队列。

**双方约定**：礼物会话**不动** `AttachType.swift`（避免 enum 冲突）；如需新增礼物相关 case 由本会话补。

### 2.3 commit 策略（M0）

- `feat: [IM] AttachType 全量枚举扩展（60+ case 含字符串）`
- `feat: [IM] MessageRouter / GiftMessageRouter / SystemMessageRouter 协议骨架`
- `feat: [IM] NIMServiceError 错误类型 + i18n 三语言`
- `test: [IM] AttachType 全量 case raw round-trip 单测`

---

## §3 M1 NIMService 共底座（1.5d）

**目标**：把 NIMChatroomManager 的 IM 登录 + 进退房 + GenericCustomAttachmentCoder 注册 + sysMsg/syncSysMsgs 监听 + 错误处理 全部抽到 NIMService.shared。

### 3.1 任务清单

| 任务号 | 文件 | 改动 | 自检 |
|---|---|---|---|
| M1-1 | `Sources/Live/NIM/NIMService.swift`（新增） | @MainActor singleton；shared 实例；setupOnce 注册 appKey + GenericCustomAttachmentCoder | Build 通过 |
| M1-2 | NIMService | login(account:token:) async throws；logout() async；refreshToken(_:) async；isLogined() | login/logout 单测 |
| M1-3 | NIMService | enterChatroom(roomId:nickname:retryCount:) async throws -> NIMChatroom；exitChatroom(_:) async；前置检查 + retry | 单测 |
| M1-4 | NIMService | sysMsg + syncSysMsgs delegate 注册；handleSysMsg / handleSyncSysMsgs（batch throttle）→ 分发到 routers | sysMsg mock 注入 |
| M1-5 | NIMService | connectionState @Published；on('willReconnect') / on('disconnect') 状态更新 | UI 可订阅 |
| M1-6 | NIMService | registerRouter / unregisterRouter；routers: [Weak<MessageRouter>]；分发主入口 dispatch(attachType:payload:context:) | 多 router 链路单测 |
| M1-7 | NIMService | unwrapDataField helper（从 PartyRoomChatManager L370-406 抽出）；支持 Dictionary / JSON String / Base64+gzip 三态 | 单测覆盖 3 种形态 |
| M1-8 | NIMService | mapError 错误码分流：1004 → .kickedOut；1005 → .tokenInvalid → 自动 logout + post .apiSessionInvalidated（对齐 A 收尾 backlog #1） | 错误码单测 |
| M1-9 | `Sources/Live/NIMOnlineKeeper.swift` | 内部改调 NIMService.shared.login/logout；保留外部 API 不变（B 调用点零改动） | NIMOnlineKeeperTests 不破 |
| M1-10 | `Tests/HilyTests/NIMServiceTests.swift`（新增） | 至少 15 case：setupOnce 幂等 / login 成功失败 / token 刷新 / enterChatroom / 错误码分流 / unwrapDataField 三态 / sysMsg dispatch / Weak router 弱引用 | 全过 |

### 3.2 commit 策略（M1）

- `feat: [IM] NIMService 共底座骨架 + setupOnce + login/logout`
- `feat: [IM] NIMService.enterChatroom helper（含 retry + 错误码分流）`
- `feat: [IM] NIMService sysMsg + syncSysMsgs 监听 + Router dispatch`
- `feat: [IM] NIMService unwrapDataField gzip helper`
- `refactor: [IM] NIMOnlineKeeper 内部走 NIMService（外部 API 不变）`
- `test: [IM] NIMServiceTests 15 case`

---

## §4 M2 PKNIMRouter 改 MessageRouter（0.3d）

**目标**：G 已实现的 PKNIMRouter 仅改方法签名实现 protocol，逻辑不动。

### 4.1 任务清单

| 任务号 | 文件 | 改动 |
|---|---|---|
| M2-1 | `Sources/PK/PKNIMRouter.swift` | extension PKNIMRouter: MessageRouter；route(at:payload:context:) -> Bool 路由后调旧 route(at:payload:) |
| M2-2 | `Sources/PK/PKStore.swift` | router 字段类型 lazy var 保持，PKNIMRouter 类型不变；NIMService.shared.registerRouter(router) 注入 |
| M2-3 | `Sources/Live/LiveRoomView.swift` | 删除 `nim.pkRouter = pkStore.router`；改 `NIMService.shared.registerRouter(pkStore.router)` |
| M2-4 | 真机回归 | G PK 全链路：匹配 / 邀请 / 接受 / 实时分数 / 投降 / 结束弹窗 |

### 4.2 commit（M2）

- `refactor: [直播PK] PKNIMRouter 实现 MessageRouter protocol`
- `refactor: [直播PK] PKStore router 改通过 NIMService 注册`

---

## §5 M3 PartyMessageRouter + PartyChatroomChannel 重构（1.5d）

**目标**：从 PartyRoomChatManager 抽出消息分发到 PartyMessageRouter；Channel 简化保留进退房 + delegate。

### 5.1 任务清单

| 任务号 | 文件 | 改动 | 自检 |
|---|---|---|---|
| M3-1 | `Sources/Party/NIM/PartyMessageRouter.swift`（新增） | class 实现 MessageRouter；route(at:payload:context:) 分发 15 类 PartyAttachType 到 partyStore delegate；保留 unwrapDataField 处理 | Build 通过 |
| M3-2 | `Sources/Party/Chat/PartyRoomChatManager.swift` | 删 dispatchCustom（移到 router）；删 unwrapDataField（移到 NIMService）；保留 enter/exit/didReconnect 钩子；行数 450 → ~350 | Build 通过 |
| M3-3 | `Sources/Party/PartyStore.swift` | onAppear 注入：NIMService.shared.registerRouter(partyRouter)；partyRouter.partyStore = self | E 用例不破 |
| M3-4 | `Tests/HilyTests/PartyMessageRouterTests.swift`（新增） | 15 case 分发单测（mock payload） | 全过 |
| M3-5 | 真机回归 E | 进房 / 上下麦 / 禁麦 / 公屏 / 送礼 placeholder 全过 | 真机 |

### 5.2 commit（M3）

- `feat: [派对房] PartyMessageRouter 抽出 + 15 case 分发单测`
- `refactor: [派对房] PartyRoomChatManager 简化（删 dispatch + unwrap，注入 PartyMessageRouter）`

---

## §6 M4 SystemMessageRouter 实现（1d；**最高风险**）

**目标**：首次接入 sysMsg 通道，补 C 期缺口 + 强制下播跨通道兜底。

### 6.1 任务清单

| 任务号 | 文件 | 改动 | 自检 |
|---|---|---|---|
| M4-1 | `Sources/Live/NIM/SystemMessageRouter.swift` | 实现 M0 占位的 class；分发 attachType=44/61/62/63/-3/-6/-1/15/18/58/-4/90 各 case | mock payload 单测 |
| M4-2 | `Sources/Live/LiveStore.swift` | 新增 handleForceEndFromIM(reason:)；handleComplianceWarning(text:)；handleBoostingExposure(on:) | 单测 |
| M4-3 | `Sources/Call/CallStore.swift` | 新增 5 个 helper：handleRemoteText / updateWaitState(type:) / appendCallIncome(num:) / appendGiftIncome(num:) / appendWaitBonus(num:) | C 期回归 |
| M4-4 | `Sources/Auth/SessionStore.swift`（或类似） | 新增 incrementFollow + handleAuditStatus；与 A 收尾 backlog #1 联动 | 单测 |
| M4-5 | `Sources/Live/LiveRoomView.swift` + `Sources/Call/CallView.swift` | onAppear 注入：sysRouter = SystemMessageRouter()；NIMService.registerRouter(sysRouter)；sysRouter.liveStore = store；sysRouter.callStore = callStore | — |
| M4-6 | `Tests/HilyTests/SystemMessageRouterTests.swift` | 12+ case mock 注入 sysMsg → 验 store 状态变化 | 全过 |
| M4-7 | 真机回归 C 通话 | 通话中送礼 / 充值等待 / 远端文字消息 翻译显示 | 真机 |
| M4-8 | **mock 注入测试** | NIMService 加 testOnly_simulateSysMsg(at:payload:) → 端到端验证 attachType=44 → LiveStore.state = forceEnding | 真机 + 单测 |

### 6.2 风险缓解（R2 sysMsg 历史从未接入）

**渐进式接入**：M4-1 仅分发 H 核心 12 个 attachType；未覆盖的 case 走 `case .knownButUnhandled / .unknown` 仅 logger.debug 不刷屏不影响业务。后续 I/J 期按需扩。

### 6.3 commit（M4）

- `feat: [IM] SystemMessageRouter 分发 sysMsg 通道核心 12 case`
- `feat: [直播/通话] LiveStore / CallStore 加 IM 系统消息入口（5 helper）`
- `test: [IM] SystemMessageRouterTests 12 case mock 注入`

---

## §7 M5 LiveChatroomChannel 重构 + LiveRoomView 接入（0.8d）

**目标**：NIMChatroomManager 简化；删 IM 登录 + pkRouter 单点；改用 NIMService 注入。

### 7.1 任务清单

| 任务号 | 文件 | 改动 | 自检 |
|---|---|---|---|
| M5-1 | `Sources/Live/NIMChatroomManager.swift` | 删 setupOnce + IM 登录逻辑（NIMService 接管）；保留 enter/exit + onRecvMessages delegate；删 weak pkRouter；行数 207 → ~120 | Build 通过 |
| M5-2 | NIMChatroomManager | onRecvMessages 改为：调 NIMService.shared.dispatch(at:payload:context:.liveChatroom(roomId:))；遵循 router 链式短路 | dispatch 端到端 |
| M5-3 | `Sources/Live/LiveRoomView.swift` | onAppear 改为：NIMService.shared.registerRouter(...)；ondisappear unregisterRouter；保留 nim.enter/leave | B 直播 + G PK 不破 |
| M5-4 | 真机回归 B 直播 | 公屏 / 礼物 placeholder / PK 全链路 | 真机 |

### 7.2 commit（M5）

- `refactor: [直播] NIMChatroomManager 简化（删登录+pkRouter，走 NIMService dispatch）`
- `refactor: [直播] LiveRoomView NIM router 注册改 NIMService`

---

## §8 M6 真机验收 + 跨里程碑回归（0.5d）

### 8.1 任务清单

| 任务号 | 验收 | 验证方式 |
|---|---|---|
| M6-1 | spec §7 DoD 10 项 | 真机×2 + 单测全跑 |
| M6-2 | B 直播全链路 | 进房 / 公屏 / PK / 强制下播 |
| M6-3 | C 通话全链路 | 来电 / 通话中礼物 / 翻译远端消息 / 充值奖励 |
| M6-4 | D 直播 ↔ 通话 | 直播态收私 call / 挂断后回直播 |
| M6-5 | E 派对房 | 进房 / 上下麦 / 公屏 / 送礼 placeholder |
| M6-6 | G PK 全链路 | 邀请 / 实时分数 / 投降 / 结果窗 |
| M6-7 | sysMsg 首次接入 | mock attachType=44 → LiveStore.forceEnd |
| M6-8 | syncSysMsgs 离线 | app 冷启动一次性补发不卡 |
| M6-9 | 错误码分流 | 1004/1005 → 全局 logout |
| M6-10 | 单测全过 | 70+ → 100+ 全过 |

### 8.2 commit（M6）

- `test: [IM] H 里程碑 IM 完善真机 DoD 全过`
- `docs: [IM] H 里程碑 IM 完善完成报告`

---

## §9 跨 spec 协调点

### 9.1 与礼物会话的接口锁定

| 接口 | 由本会话锁定 | 礼物会话遵守 |
|---|---|---|
| `AttachType` enum 全量 case | M0-1 完成后冻结 | 不动 AttachType.swift |
| `GiftMessageRouter` protocol | M0-3 完成后冻结签名 | 实现 enqueueGift / reorderDuringPK / playEnterAnimation / route default |
| `GiftMessage` 数据模型 | M0-3 锁定字段 | 可加 svgaUrl / mp4Url / isCombo 等扩展字段（非必需） |
| 礼物相关 attachType | M0-1 锁定（1/4/15/18/50/56/71/80/250-253/1030-1033/SEND_GIFT/active_tycoon_enter_room/CP_RANK_*）| 实现对应 handler |
| 注入入口 | NIMService.shared.registerRouter(giftRouter) | 礼物会话在 RootView 或 LiveRoomView/PartyRoomView 注入 |

**Review checkpoint**：M0-3 完成（约 1d 后）请礼物会话 review protocol 签名 + AttachType 礼物 case 完整性，签字后冻结。

### 9.2 与 A 收尾 backlog 的衔接

- 心跳 1004/1005 主动联动 forceEnd → 本会话 NIMServiceError + APIClient .apiSessionInvalidated 整合点已就绪
- 其他 3 项 backlog 留 J 阶段

### 9.3 与 J 收尾的衔接

- attachType=140/144/156/157（活动 / 猜拳 / 活动报名）H 不实现，进入 `.knownButUnhandled`；J 期统一接 i18n + 文案审核
- attachType=63/64（折扣池）同 J
- attachType=83（座驾入场）属礼物会话或 J

---

## §10 commit 策略汇总

```
<type>: [IM] <description>
```

- type: feat / fix / refactor / docs / test / chore
- scope: `[IM]` 主要；跨里程碑 `[直播/IM]` / `[通话/IM]` / `[派对房/IM]` / `[直播PK/IM]`

NEVER commit/push unless explicitly asked（项目铁律）

---

## §11 验证方式

### 11.1 单元测试覆盖率目标

- AttachType 全量 raw round-trip：每个 case 至少一对 raw→case→raw 断言（M0-6）
- NIMService：login/logout 成功失败 + token 刷新 + enterChatroom retry + unwrapDataField 三态 + dispatch 链路（M1-10）
- MessageRouter dispatch：注册 + 短路 + weak 解引用安全（M1-10）
- PartyMessageRouter 15 case 分发（M3-4）
- SystemMessageRouter 12 case 分发（M4-6）

### 11.2 真机集成测试

- B 直播：M5 完成后 + M6 验收
- C 通话：M4 完成后（callIncome / 远端文字翻译 / 充值等待）
- E 派对房：M3 完成后
- G PK：M2 完成后 + M6 验收

### 11.3 性能专项

- syncSysMsgs 离线一次性 200+ 条 → main actor 不卡（M1 batch throttle 50/50ms）
- Router 注册 / 注销链路 → 反复进退房 100 次不内存泄漏

---

## §12 风险与未决项

继承 spec §1.5 R1-R10；执行期重点关注：

- **R1 抽 NIMService 破现有调用点** → M1 先加新文件不删旧；M2-M5 逐步迁移；M6 全回归
- **R2 sysMsg 首次接入** → M4 渐进式仅 12 case；未覆盖 .knownButUnhandled
- **R5 GiftMessageRouter protocol 与礼物会话不兼容** → M0-3 完成后 review checkpoint
- **R7 C 期 -3/-6/-1/15/18 接入破现有 CallStore 行为** → M4-3 单独单测覆盖
- **R8 syncSysMsgs 大批量阻塞 main actor** → M1-4 batch 50/50ms throttle
- **R9 礼物会话与本会话并行 attachType 冲突** → M0-1 完成后**冻结 AttachType.swift**

---

## §13 下一步

1. **用户审本计划** → 拍板后启动 M0
2. **M0 完成（约 1d 后）** 通知礼物会话 protocol 已就绪可启动并行
3. **M1 完成（~2.5d）** NIMService 跑通 → M2-M5 串行小步迭代
4. **M4 sysMsg 接入** 单独配 review（最高风险）
5. **M6 全 DoD 过后**：更新追踪表 G/E 行 + H 行 → 启动 I 期 / H 礼物会话验收

---

## §14 启动检查清单（本会话开工前）

- [x] H5 蓝本盘点完成（Workflow w60wpk0cb）
- [x] 安卓 02-05 IM 章节阅读完成
- [x] iOS 现有 NIM 实施盘点完成（NIMChatroomManager + PartyRoomChatManager + AttachType + PKNIMRouter + NIMOnlineKeeper + GenericCustomAttachment + GiftMessage 占位）
- [x] spec §1 H5/安卓代码二次校验清单落档
- [x] spec 功能详档落档
- [x] 本实施计划落档
- [ ] 用户拍板启动 M0
- [ ] 礼物会话同步 M0-3 protocol 签名

**等用户确认后启动 M0**。

或：本会话先**暂停**进入 IM 实施，**继续处理 PK 已知 bug**，IM 实施待另一会话接手——按用户指示。
