# H 里程碑 · 跨场景礼物特效播放引擎 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 [`superpowers:subagent-driven-development`](推荐) 或 [`superpowers:executing-plans`](inline 执行) 按 Task 顺序落地。Step 用 checkbox（`- [ ]`）跟踪。
>
> **Spec 依据**：[`docs/plan/H-礼物特效引擎-spec-202607082140.md`](H-礼物特效引擎-spec-202607082140.md) （权威源，Plan 冲突时以 spec 为准）
>
> **相关 rules**（impl 期必读）：
> - [`.claude/rules/xcodegen-podinstall-binding.md`](../../.claude/rules/xcodegen-podinstall-binding.md) — Task 1 集成流程
> - [`.claude/rules/swiftui-camera-preview.md`](../../.claude/rules/swiftui-camera-preview.md) — Task 6/7 UIViewRepresentable / scenePhase
> - [`.claude/rules/swiftui-fullscreencover-hoist.md`](../../.claude/rules/swiftui-fullscreencover-hoist.md) — Task 6 UIWindow 隔离
> - [`.claude/rules/swiftui-keepalive-publisher-isolation.md`](../../.claude/rules/swiftui-keepalive-publisher-isolation.md) — Task 3 @Published 只暴露 current
> - [`.claude/rules/ios-decode-userid-compat.md`](../../.claude/rules/ios-decode-userid-compat.md) — Task 9 payload 双兼容
> - [`.claude/rules/session-scoped-store-refresh.md`](../../.claude/rules/session-scoped-store-refresh.md) — Task 7 logout reset
> - [`.claude/rules/root-cause-investigation.md`](../../.claude/rules/root-cause-investigation.md) — Task 12/13 真机反悔调查
> - [`.claude/rules/encoding-check.md`](../../.claude/rules/encoding-check.md) — 中文注释/字符串处理
> - [`.claude/rules/git-workflow.md`](../../.claude/rules/git-workflow.md) — commit 格式；**NEVER commit unless explicitly asked**

---

**Goal**：交付跨 4 场景（直播 / 1v1 通话 / 派对房 / P2P 私聊）的礼物特效播放引擎——SVGA + YYEVA 双 SDK 池化播放、Deque(30) 单例队列、独立 UIWindow overlay 完全解耦业务 view tree。

**Architecture**：`GiftEffectCenter @MainActor 单例` 持队列 + 场景 tag + Player 路由；独立 `UIWindow(windowLevel:.alert-1)` 挂 SwiftUI Root（Central + MicroToast 两层）；4 场景 view 通过 `.giftEffectScene(_:scopeId:)` modifier 声明活跃场景；IM 层统一走 `GiftEffectIntake.ingest` 入队。硬中断语义：场景切换立刻 stop current + clear pending。

**Tech Stack**：Swift 5、SwiftUI、UIKit（UIWindow overlay）、SVGAPlayer-iOS ~>2.5、YYEVA-iOS、XCTest、Combine、CocoaPods、xcodegen。

---

## 📁 File Structure（先决目录/文件划分）

### 新建 `Sources/GiftEffect/`

```
Sources/GiftEffect/
├── Models/
│   ├── GiftEffectScene.swift              // enum GiftEffectScene + struct GiftEffectSceneKey
│   ├── GiftEffectItem.swift               // 送礼事件模型 + Codable payload 兼容 decoder helper
│   └── MicroToastItem.swift               // 1v1 小飘窗模型
├── Center/
│   ├── GiftEffectCenter.swift             // @MainActor 单例：queue + activeKey + play 路由入口
│   ├── GiftEffectSceneModifier.swift      // .giftEffectScene(_:scopeId:) view modifier
│   └── GiftEffectOverlayWindow.swift      // UIWindow 独立层管理（show/hide/hostView 桥）
├── Players/
│   ├── GiftAnimationPlayer.swift          // protocol + 默认 no-op impl（用于测试）
│   ├── GiftPlayerRouter.swift             // URL 后缀路由 SVGA vs YYEVA
│   ├── SVGAAnimationPlayer.swift          // SVGAPlayer 池化封装
│   └── YYEVAAnimationPlayer.swift         // YYEasyMetalPlayer 池化封装
├── UI/
│   ├── GiftEffectRoot.swift               // SwiftUI root view（ZStack Central + Toast）
│   ├── CentralEffectLayer.swift           // 中央大动画层（UIViewRepresentable）
│   ├── MicroToastLayer.swift              // 底部 3s 小飘窗层
│   └── GiftPlayerHostView.swift           // UIViewRepresentable 承载 SVGA/YYEVA subview
└── IM/
    └── GiftEffectIntake.swift             // 4 场景 IM 消息统一入队入口 + payload parseItem
```

### 修改文件

| 路径 | 修改内容 |
|---|---|
| [`project.yml`](../../project.yml) | `targets.Hily.sources` 加 `Sources/GiftEffect`；`targets.HilyTests.sources` 加相应测试目录 |
| [`Podfile`](../../Podfile) | 加 `pod 'SVGAPlayer', '~> 2.5'` + `pod 'YYEVA'`（版本 Task 1 试跑锁定） |
| [`Sources/HilyApp.swift`](../../Sources/HilyApp.swift) | login 5s 后 warmupSVGA；listener isLoggedIn 触发 Window show/hide |
| [`Sources/Auth/SessionStore.swift`](../../Sources/Auth/SessionStore.swift) | `logout()` 内加 `GiftEffectCenter.shared.reset()` |
| [`Sources/Live/LiveRoomView.swift`](../../Sources/Live/LiveRoomView.swift) | root view 挂 `.giftEffectScene(.live, scopeId: String(store.roomId ?? 0))` |
| [`Sources/Call/CallView.swift`](../../Sources/Call/CallView.swift) | root view 挂 `.giftEffectScene(.call, scopeId: currentCallId)` |
| [`Sources/Party/UI/PartyRoomView.swift`](../../Sources/Party/UI/PartyRoomView.swift) | root view 挂 `.giftEffectScene(.party, scopeId: room.id ?? "")` |
| [`Sources/Message/Chat/UI/ChatDetailView.swift`](../../Sources/Message/Chat/UI/ChatDetailView.swift) | root view 挂 `.giftEffectScene(.chat, scopeId: peerYxAccid)` |
| [`Sources/Live/NIMChatroomManager.swift`](../../Sources/Live/NIMChatroomManager.swift) | Live 礼物消息（attachType 50/1）→ `GiftEffectIntake.ingest(scene: .live, ...)` |
| [`Sources/Live/NIM/NIMService.swift`](../../Sources/Live/NIM/NIMService.swift) 或 [`Sources/Call/CallStore.swift`](../../Sources/Call/CallStore.swift) | Call 礼物消息（attachType 4）→ `.call` |
| [`Sources/Party/NIM/PartyMessageRouter.swift`](../../Sources/Party/NIM/PartyMessageRouter.swift) | partyGiftCompressed gzip 解压后 → `.party` |
| [`Sources/Message/Chat/P2PChatStore.swift`](../../Sources/Message/Chat/P2PChatStore.swift) 或消息接收路径 | `attachType == "SEND_GIFT"` → `.chat` |

### 删除文件（Task 12 迁移期一次性删）

- `Sources/Live/GiftAnimation/GiftAnimationPlayerProtocol.swift`
- `Sources/Live/GiftAnimation/GiftAnimationQueue.swift`
- `Sources/Live/GiftAnimation/GiftAnimationOverlay.swift`
- `Sources/Live/NIM/GiftAnimationQueueProtocol.swift`

### 新建测试

```
Tests/HilyTests/GiftEffect/
├── GiftEffectItemDecoderTests.swift       // payload 双兼容 decoder
├── GiftEffectCenterTests.swift            // Center 状态机（enqueue/tag 过滤/上限 30/派对房 me 插队/clear）
├── GiftEffectIntakeTests.swift            // 4 场景 payload → item 解析 + 派对房 me 判定
└── FakeGiftAnimationPlayer.swift          // Player protocol 的 test double
```

---

## Task 1 · Pod 引入 + xcodegen 目录挂载

**目的**：让 Pod 依赖到位、`Sources/GiftEffect/` 目录被 xcodegen 编入 Hily target；本 Task **不写代码**只搭骨架。

**Files**:
- Modify: [`project.yml`](../../project.yml)
- Modify: [`Podfile`](../../Podfile)
- Create（空目录占位）:
  - `Sources/GiftEffect/Models/.gitkeep`
  - `Sources/GiftEffect/Center/.gitkeep`
  - `Sources/GiftEffect/Players/.gitkeep`
  - `Sources/GiftEffect/UI/.gitkeep`
  - `Sources/GiftEffect/IM/.gitkeep`
  - `Tests/HilyTests/GiftEffect/.gitkeep`

**不 TDD 理由**：Pod / xcodegen 是集成基建，无 unit 可测；验收靠 `xcodebuild build` 与 phase 数 sanity check。

- [ ] **Step 1**：编辑 `project.yml`，在 `targets.Hily.sources` 段追加 `Sources/GiftEffect`；`targets.HilyTests.sources` 追加 `Tests/HilyTests/GiftEffect`（若已经用 glob 匹配则跳过）

- [ ] **Step 2**：编辑 `Podfile`，`target 'Hily'` 段末尾加两行

  ```ruby
  pod 'SVGAPlayer', '~> 2.5'
  pod 'YYEVA', '~> 1.4'   # 若 1.4 不存在改 pod install 报错版本，本 step 内试跑锁定
  ```

- [ ] **Step 3**：创建目录占位文件（避免空目录被 git 丢）

  ```bash
  mkdir -p Sources/GiftEffect/{Models,Center,Players,UI,IM} Tests/HilyTests/GiftEffect
  touch Sources/GiftEffect/{Models,Center,Players,UI,IM}/.gitkeep Tests/HilyTests/GiftEffect/.gitkeep
  ```

- [ ] **Step 4**：提示用户关闭 Xcode Hily.xcworkspace（对齐 [`xcodegen-podinstall-binding.md`](../../.claude/rules/xcodegen-podinstall-binding.md) "Xcode 同时开着会让 pod install 静默失效"）

- [ ] **Step 5**：跑一条龙脚本

  ```bash
  ./bin/regen.sh
  ```

  Expected：脚本内含 `xcodegen generate && LANG=en_US.UTF-8 pod install`；末尾 sanity check 应报告 SVGAPlayer + YYEVA 已 Install。

  若 YYEVA `~> 1.4` 失败 → `pod search YYEVA` 拿到实际发布 tag → 改 Podfile 版本 → 重跑。

- [ ] **Step 6**：sanity check phase 名 + 命令行 build

  ```bash
  phase_names=$(grep 'name = "\[CP\]' Hily.xcodeproj/project.pbxproj | sort -u | wc -l | tr -d ' ')
  echo "Hily target CP phase 名唯一数：$phase_names （应 ≥2）"
  grep 'Copy XCFrameworks' Pods/Pods.xcodeproj/project.pbxproj | wc -l   # 应 ≥ 3
  xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
      -destination 'generic/platform=iOS' -configuration Debug \
      CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -5
  ```

  Expected：`BUILD SUCCEEDED`。

- [ ] **Step 7**：暂**不 commit**（等 Task 2 有真代码再一起提）

**验收条件**：命令行 build 通过；`import SVGAPlayer` / `import YYEVA` 在临时 test 文件里 build 无错（可在 Task 2 前借测试文件验一次）。

---

## Task 2 · Models 数据结构 + payload 双兼容 decoder（TDD）

**目的**：先建可测的纯数据层。`GiftEffectItem` / `MicroToastItem` / `GiftEffectSceneKey` + 一个从字典 payload decode 出 item 的 helper（`GiftEffectPayloadDecoder`）；后续 Task 3 Center + Task 9 Intake 都依赖这层。

**Files**:
- Create: `Sources/GiftEffect/Models/GiftEffectScene.swift`
- Create: `Sources/GiftEffect/Models/GiftEffectItem.swift`
- Create: `Sources/GiftEffect/Models/MicroToastItem.swift`
- Test: `Tests/HilyTests/GiftEffect/GiftEffectItemDecoderTests.swift`

- [ ] **Step 1**：写 failing tests（4 tc：Int giftId / String giftId 兼容 / 缺字段 nil / URL 后缀归一）

  ```swift
  // Tests/HilyTests/GiftEffect/GiftEffectItemDecoderTests.swift
  import XCTest
  @testable import Hily

  final class GiftEffectItemDecoderTests: XCTestCase {
      private let sceneKey = GiftEffectSceneKey(scene: .live, scopeId: "12345")
      private let mineAccid = "me_yx_001"

      func testDecodesIntGiftId() {
          let payload: [String: Any] = [
              "giftId": 8888,
              "giftName": "Rose",
              "giftNum": 5,
              "giftPrice": 100,
              "giftIcon": "https://cdn/x.svga",
              "giftSmallImg": "https://cdn/x_small.png",
              "senderYxAccid": "user_yx_A",
              "senderNickname": "Alice",
              "senderAvatar": "https://cdn/a.jpg"
          ]
          let item = GiftEffectPayloadDecoder.decode(
              sceneKey: sceneKey, payload: payload, mineYxAccid: mineAccid
          )
          XCTAssertNotNil(item)
          XCTAssertEqual(item?.giftId, 8888)
          XCTAssertEqual(item?.giftCount, 5)
          XCTAssertEqual(item?.animationUrl, "https://cdn/x.svga")
          XCTAssertEqual(item?.isSelfSent, false)
      }

      func testDecodesStringGiftId() {
          let payload: [String: Any] = [
              "giftId": "8888",
              "giftName": "Rose",
              "giftNum": "3",
              "giftIcon": "https://cdn/x.svga"
          ]
          let item = GiftEffectPayloadDecoder.decode(
              sceneKey: sceneKey, payload: payload, mineYxAccid: mineAccid
          )
          XCTAssertEqual(item?.giftId, 8888)
          XCTAssertEqual(item?.giftCount, 3)
      }

      func testMissingUrlReturnsNilAnimation() {
          let payload: [String: Any] = [
              "giftId": 1,
              "giftName": "X",
              "giftNum": 1,
              "giftIcon": "  "   // 空白应归一 nil
          ]
          let item = GiftEffectPayloadDecoder.decode(
              sceneKey: sceneKey, payload: payload, mineYxAccid: mineAccid
          )
          XCTAssertNil(item?.animationUrl)
      }

      func testDetectsSelfSent() {
          let payload: [String: Any] = [
              "giftId": 1,
              "giftName": "X",
              "giftNum": 1,
              "senderYxAccid": mineAccid
          ]
          let item = GiftEffectPayloadDecoder.decode(
              sceneKey: sceneKey, payload: payload, mineYxAccid: mineAccid
          )
          XCTAssertEqual(item?.isSelfSent, true)
      }
  }
  ```

- [ ] **Step 2**：跑 test，验证 FAIL（类型未定义）

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -only-testing:HilyTests/GiftEffectItemDecoderTests 2>&1 | tail -30
  ```
  Expected：`Compile error: cannot find type 'GiftEffectSceneKey'` 类。

- [ ] **Step 3**：写 `GiftEffectScene.swift`

  ```swift
  // Sources/GiftEffect/Models/GiftEffectScene.swift
  import Foundation

  public enum GiftEffectScene: String, Hashable, Sendable {
      case live, call, party, chat
  }

  public struct GiftEffectSceneKey: Hashable, Sendable {
      public let scene: GiftEffectScene
      public let scopeId: String

      public init(scene: GiftEffectScene, scopeId: String) {
          self.scene = scene
          self.scopeId = scopeId
      }
  }
  ```

- [ ] **Step 4**：写 `GiftEffectItem.swift`（含 `GiftEffectPayloadDecoder` 静态 helper）

  ```swift
  // Sources/GiftEffect/Models/GiftEffectItem.swift
  import Foundation

  public struct GiftEffectItem: Identifiable, Equatable, Sendable {
      public let id: UUID
      public let sceneKey: GiftEffectSceneKey
      public let senderYxAccid: String
      public let senderNickname: String
      public let senderAvatarUrl: String?
      public let giftId: Int64
      public let giftName: String
      public let giftCount: Int
      public let giftPrice: Int64
      public let animationUrl: String?
      public let staticImgUrl: String?
      public let timestamp: Int64
      public let isSelfSent: Bool

      public init(id: UUID = UUID(), sceneKey: GiftEffectSceneKey,
                  senderYxAccid: String, senderNickname: String, senderAvatarUrl: String?,
                  giftId: Int64, giftName: String, giftCount: Int, giftPrice: Int64,
                  animationUrl: String?, staticImgUrl: String?,
                  timestamp: Int64, isSelfSent: Bool) {
          self.id = id
          self.sceneKey = sceneKey
          self.senderYxAccid = senderYxAccid
          self.senderNickname = senderNickname
          self.senderAvatarUrl = senderAvatarUrl
          self.giftId = giftId
          self.giftName = giftName
          self.giftCount = giftCount
          self.giftPrice = giftPrice
          self.animationUrl = animationUrl
          self.staticImgUrl = staticImgUrl
          self.timestamp = timestamp
          self.isSelfSent = isSelfSent
      }
  }

  /// 从字典 payload 构造 GiftEffectItem（对齐 spec §1.2 + rule ios-decode-userid-compat）
  public enum GiftEffectPayloadDecoder {
      public static func decode(
          sceneKey: GiftEffectSceneKey,
          payload: [String: Any],
          mineYxAccid: String,
          now: () -> Int64 = { Int64(Date().timeIntervalSince1970 * 1000) }
      ) -> GiftEffectItem? {
          let giftId = decodeInt64(payload["giftId"]) ?? 0
          guard giftId != 0 else { return nil }
          let giftCount = decodeInt(payload["giftNum"] ?? payload["giftCount"]) ?? 1
          let giftPrice = decodeInt64(payload["giftPrice"]) ?? 0

          let senderYxAccid = decodeSender(payload) ?? ""
          let senderNickname = decodeString(payload["senderNickname"] ?? payload["nickname"]) ?? ""
          let senderAvatar = decodeString(payload["senderAvatar"] ?? payload["avatar"])?.nilIfEmpty
          let giftName = decodeString(payload["giftName"]) ?? ""

          let rawUrl = decodeString(payload["giftIcon"] ?? payload["giftImg"])?.nilIfEmpty
          let staticImg = decodeString(payload["giftSmallImg"] ?? payload["giftImg"])?.nilIfEmpty
          let animationUrl = normalizeAnimationUrl(rawUrl)

          return GiftEffectItem(
              sceneKey: sceneKey,
              senderYxAccid: senderYxAccid,
              senderNickname: senderNickname,
              senderAvatarUrl: senderAvatar,
              giftId: giftId,
              giftName: giftName,
              giftCount: giftCount,
              giftPrice: giftPrice,
              animationUrl: animationUrl,
              staticImgUrl: staticImg,
              timestamp: now(),
              isSelfSent: !senderYxAccid.isEmpty && senderYxAccid == mineYxAccid
          )
      }

      // MARK: - private helpers

      private static func normalizeAnimationUrl(_ url: String?) -> String? {
          guard let u = url,
                let parsed = URL(string: u) else { return nil }
          let ext = parsed.pathExtension.lowercased()
          return ["svga", "mp4"].contains(ext) ? u : nil
      }

      private static func decodeSender(_ payload: [String: Any]) -> String? {
          if let s = payload["senderYxAccid"] as? String { return s }
          if let dict = payload["senderInfo"] as? [String: Any],
             let s = dict["yxAccid"] as? String { return s }
          if let s = payload["fromAccid"] as? String { return s }
          return nil
      }

      private static func decodeInt64(_ any: Any?) -> Int64? {
          if let i = any as? Int64 { return i }
          if let i = any as? Int { return Int64(i) }
          if let n = any as? NSNumber { return n.int64Value }
          if let s = any as? String, let i = Int64(s) { return i }
          return nil
      }

      private static func decodeInt(_ any: Any?) -> Int? {
          if let i = any as? Int { return i }
          if let n = any as? NSNumber { return n.intValue }
          if let s = any as? String, let i = Int(s) { return i }
          return nil
      }

      private static func decodeString(_ any: Any?) -> String? {
          if let s = any as? String { return s.trimmingCharacters(in: .whitespaces) }
          return nil
      }
  }

  private extension String {
      var nilIfEmpty: String? { isEmpty ? nil : self }
  }
  ```

- [ ] **Step 5**：写 `MicroToastItem.swift`

  ```swift
  // Sources/GiftEffect/Models/MicroToastItem.swift
  import Foundation

  public struct MicroToastItem: Identifiable, Equatable, Sendable {
      public let id: UUID
      public let sceneKey: GiftEffectSceneKey
      public let imgUrl: String?
      public let giftName: String
      public let count: Int
      public let duration: TimeInterval

      public init(id: UUID = UUID(), sceneKey: GiftEffectSceneKey,
                  imgUrl: String?, giftName: String, count: Int,
                  duration: TimeInterval = 3.0) {
          self.id = id
          self.sceneKey = sceneKey
          self.imgUrl = imgUrl
          self.giftName = giftName
          self.count = count
          self.duration = duration
      }
  }
  ```

- [ ] **Step 6**：跑 tests，验证 PASS

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -only-testing:HilyTests/GiftEffectItemDecoderTests 2>&1 | tail -10
  ```
  Expected：`Test Suite 'GiftEffectItemDecoderTests' passed`。

- [ ] **Step 7**：暂不 commit（合并到 Task 3 提交，减少小 commit）

**验收条件**：4 个 tc 全 pass；Models 层无 SDK 依赖（`import Foundation` 即可编译）。

---

## Task 3 · GiftEffectCenter 状态机 + 单测（TDD）

**目的**：核心引擎——队列管理、场景 tag 过滤、上限 30、派对房 me 插队、场景切换硬中断、内存告警清理、logout reset。**Player 层用 FakePlayer 替身**（依赖倒置），真 SDK 在 Task 5 接入。

**Files**:
- Create: `Sources/GiftEffect/Players/GiftAnimationPlayer.swift`（先建 protocol）
- Create: `Sources/GiftEffect/Center/GiftEffectCenter.swift`
- Create: `Tests/HilyTests/GiftEffect/FakeGiftAnimationPlayer.swift`
- Test: `Tests/HilyTests/GiftEffect/GiftEffectCenterTests.swift`

- [ ] **Step 1**：先写 Player protocol（Task 3 依赖，Task 5 才有真实现）

  ```swift
  // Sources/GiftEffect/Players/GiftAnimationPlayer.swift
  import UIKit

  @MainActor
  public protocol GiftAnimationPlayer: AnyObject {
      /// 播放一段动画；onFinish 由播放器在**主线程**回调（幂等，被 stop 硬打断时也应 fire 一次）
      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void)
      /// 硬停：立即中断当前播放并清理，onFinish 会（同步或异步）被调一次
      func stop()
      /// SDK 实例销毁（logout 时调）
      func tearDown()
  }

  /// 默认路由抽象（Task 5 加两个具体 player 后 impl）
  @MainActor
  public protocol GiftPlayerRouting: AnyObject {
      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void)
      func stopAll()
      func tearDownPlayers()
      func warmupSVGA()
  }
  ```

- [ ] **Step 2**：写 FakePlayer（测试替身，直接同步 fire onFinish）

  ```swift
  // Tests/HilyTests/GiftEffect/FakeGiftAnimationPlayer.swift
  import UIKit
  @testable import Hily

  @MainActor
  final class FakeGiftPlayerRouter: GiftPlayerRouting {
      var playHistory: [GiftEffectItem] = []
      var stopAllCount = 0
      var tearDownCount = 0
      var warmupCount = 0
      /// 若 true，play 时不立即 fire onFinish，需外部调 `finishCurrent()`（模拟真异步）
      var manualFinish = false
      private var pendingFinish: (() -> Void)?

      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          playHistory.append(item)
          if manualFinish {
              pendingFinish = onFinish
          } else {
              onFinish()
          }
      }
      func finishCurrent() {
          let f = pendingFinish
          pendingFinish = nil
          f?()
      }
      func stopAll() {
          stopAllCount += 1
          let f = pendingFinish
          pendingFinish = nil
          f?()   // 硬停也 fire onFinish 一次（对齐 spec §5.3 幂等保护）
      }
      func tearDownPlayers() { tearDownCount += 1 }
      func warmupSVGA() { warmupCount += 1 }
  }
  ```

- [ ] **Step 3**：写 failing tests（覆盖 §3.2-§3.7）

  ```swift
  // Tests/HilyTests/GiftEffect/GiftEffectCenterTests.swift
  import XCTest
  @testable import Hily

  @MainActor
  final class GiftEffectCenterTests: XCTestCase {
      private var center: GiftEffectCenter!
      private var router: FakeGiftPlayerRouter!
      private let liveKey = GiftEffectSceneKey(scene: .live, scopeId: "live_1")
      private let partyKey = GiftEffectSceneKey(scene: .party, scopeId: "party_1")

      override func setUp() {
          super.setUp()
          router = FakeGiftPlayerRouter()
          center = GiftEffectCenter(playerRouter: router)
      }

      // 建 item 的便捷 helper
      private func item(key: GiftEffectSceneKey, self isSelf: Bool = false,
                        url: String? = "https://cdn/x.svga",
                        name: String = "Gift", id: Int64 = 0) -> GiftEffectItem {
          GiftEffectItem(
              sceneKey: key, senderYxAccid: isSelf ? "me" : "other",
              senderNickname: "N", senderAvatarUrl: nil,
              giftId: id == 0 ? Int64(Int.random(in: 1...1_000_000)) : id,
              giftName: name, giftCount: 1, giftPrice: 100,
              animationUrl: url, staticImgUrl: nil,
              timestamp: Int64(Date().timeIntervalSince1970 * 1000),
              isSelfSent: isSelf
          )
      }

      // 1. 未 setActiveScene 时 enqueue 被拒
      func testEnqueueRejectedWithoutActiveScene() {
          center.enqueue(item(key: liveKey))
          XCTAssertEqual(router.playHistory.count, 0)
          XCTAssertNil(center.current)
      }

      // 2. 场景 tag 不匹配的 item 被拒
      func testEnqueueRejectedWhenSceneMismatch() {
          center.setActiveScene(liveKey)
          center.enqueue(item(key: partyKey))
          XCTAssertEqual(router.playHistory.count, 0)
      }

      // 3. 匹配 tag 立刻触发 play
      func testEnqueuePlaysImmediatelyWhenIdle() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          XCTAssertEqual(router.playHistory.count, 1)
          XCTAssertEqual(center.current?.giftName, "A")
      }

      // 4. 上限 30 FIFO 淘汰最旧
      func testQueueOverflowDropsOldest() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          for i in 1...31 {
              center.enqueue(item(key: liveKey, name: "G\(i)"))
          }
          // 第 1 条正在播（G1）；pending 应留 30 条中的 [G2..G31]
          // 溢出 (31-1)-30 = 0，实际此时 pending=30（enqueue 时不含 current）
          // ↑ 复核：spec §3.4 逻辑 —— enqueue 后 pending.count > 30 才淘汰
          // 触发：再入队 G32，pending 变 31，触发淘汰 G2
          center.enqueue(item(key: liveKey, name: "G32"))
          // 现 pending 应含 [G3..G32]（30 条），G2 被淘汰
          // 验证：finish current 30 次，第 30 次播的应是 G31，之后剩 G32
          router.finishCurrent()   // G1 播完，next=G3（因 G2 已被淘汰）
          XCTAssertEqual(center.current?.giftName, "G3")
      }

      // 5. 派对房 me 送礼插队（不抢占正在播）
      func testPartySelfSentInsertsAtHead() {
          center.setActiveScene(partyKey)
          router.manualFinish = true
          center.enqueue(item(key: partyKey, name: "A"))   // 立刻播
          center.enqueue(item(key: partyKey, name: "B"))   // pending=[B]
          center.enqueue(item(key: partyKey, self: true, name: "ME"))   // pending=[ME, B]
          XCTAssertEqual(center.current?.giftName, "A")   // 未抢占
          router.finishCurrent()
          XCTAssertEqual(center.current?.giftName, "ME")  // 队首优先
          router.finishCurrent()
          XCTAssertEqual(center.current?.giftName, "B")
      }

      // 6. 直播场景 me 插队**不生效**（只派对房）
      func testLiveSelfSentDoesNotInsertAtHead() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.enqueue(item(key: liveKey, name: "B"))
          center.enqueue(item(key: liveKey, self: true, name: "ME"))   // Live 场景不插队
          router.finishCurrent()
          XCTAssertEqual(center.current?.giftName, "B")   // FIFO
      }

      // 7. 场景切换硬中断
      func testSetActiveSceneHardStopsCurrent() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.enqueue(item(key: liveKey, name: "B"))
          XCTAssertNotNil(center.current)

          center.setActiveScene(partyKey)   // 切换
          XCTAssertEqual(router.stopAllCount, 1)
          XCTAssertNil(center.current)
          // 新场景 enqueue 应正常
          center.enqueue(item(key: partyKey, name: "P1"))
          XCTAssertEqual(center.current?.giftName, "P1")
      }

      // 8. leaveScene 硬中断
      func testLeaveSceneClearsAll() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.leaveScene(liveKey)
          XCTAssertEqual(router.stopAllCount, 1)
          XCTAssertNil(center.current)
      }

      // 9. leaveScene 场景 key 不匹配时无操作
      func testLeaveSceneNoOpIfKeyMismatch() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.leaveScene(partyKey)   // 别的 key
          XCTAssertEqual(router.stopAllCount, 0)
          XCTAssertNotNil(center.current)
      }

      // 10. showMicroToast Chat 场景不启用
      func testMicroToastNotShownInChat() {
          let chatKey = GiftEffectSceneKey(scene: .chat, scopeId: "peer_A")
          center.setActiveScene(chatKey)
          let toast = MicroToastItem(
              sceneKey: chatKey, imgUrl: "https://x/x.png",
              giftName: "T", count: 1
          )
          center.showMicroToast(toast)
          XCTAssertEqual(center.microToasts.count, 0)
      }

      // 11. showMicroToast Live 场景 append + auto expire
      func testMicroToastAutoExpires() async throws {
          center.setActiveScene(liveKey)
          let toast = MicroToastItem(
              sceneKey: liveKey, imgUrl: "https://x/x.png",
              giftName: "T", count: 1, duration: 0.1
          )
          center.showMicroToast(toast)
          XCTAssertEqual(center.microToasts.count, 1)
          try await Task.sleep(nanoseconds: 200_000_000)
          XCTAssertEqual(center.microToasts.count, 0)
      }

      // 12. handleMemoryWarning 清 pending + microToasts，activeKey 保留
      func testMemoryWarningClearsInFlight() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.enqueue(item(key: liveKey, name: "B"))
          center.handleMemoryWarning()
          XCTAssertEqual(router.stopAllCount, 1)
          XCTAssertNil(center.current)
          // activeKey 未被清 → 新 enqueue 应能被接受
          center.enqueue(item(key: liveKey, name: "C"))
          XCTAssertEqual(center.current?.giftName, "C")
      }

      // 13. reset 完整清理 + tearDown players
      func testResetTearsDownPlayers() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.reset()
          XCTAssertEqual(router.stopAllCount, 1)
          XCTAssertEqual(router.tearDownCount, 1)
          XCTAssertNil(center.current)
          // reset 后 enqueue 应被拒（activeKey 已清）
          center.enqueue(item(key: liveKey, name: "B"))
          XCTAssertNil(center.current)
      }

      // 14. setActiveScene 幂等（同 key 不触发 stop）
      func testSetActiveSceneIdempotent() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          center.enqueue(item(key: liveKey, name: "A"))
          center.setActiveScene(liveKey)   // 同 key
          XCTAssertEqual(router.stopAllCount, 0)
          XCTAssertNotNil(center.current)
      }

      // 15. warmupSVGA 转发给 router
      func testWarmupForwards() {
          center.warmupSVGA()
          XCTAssertEqual(router.warmupCount, 1)
      }
  }
  ```

- [ ] **Step 4**：跑 test 验证 FAIL（`GiftEffectCenter` 未定义）

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -only-testing:HilyTests/GiftEffectCenterTests 2>&1 | tail -30
  ```

- [ ] **Step 5**：写 `GiftEffectCenter.swift` 最小 impl 让 15 tc 全 pass

  ```swift
  // Sources/GiftEffect/Center/GiftEffectCenter.swift
  import Foundation
  import UIKit
  import Combine
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "GiftEffectCenter")

  @MainActor
  public final class GiftEffectCenter: ObservableObject {

      // MARK: - Singleton

      /// 默认单例（生产用）。UT 时用 init(playerRouter:) 注入 fake。
      public static let shared: GiftEffectCenter = {
          // 真 router 在 Task 5 实现；此处先用 no-op 占位（Task 5 replace）
          GiftEffectCenter(playerRouter: NoopGiftPlayerRouter())
      }()

      // MARK: - Public State

      @Published public private(set) var current: GiftEffectItem?
      @Published public private(set) var microToasts: [MicroToastItem] = []

      // MARK: - Private State

      private var pending: [GiftEffectItem] = []     // Deque 语义用 [];removeFirst 是 O(n) 但 30 上限可接受
      private var activeKey: GiftEffectSceneKey?
      private let playerRouter: GiftPlayerRouting

      /// 承载 SVGA/YYEVA player 的 UIView（Task 6 由 GiftPlayerHostView 注册）
      private weak var hostView: UIView?

      private static let queueLimit = 30

      // MARK: - Init

      public init(playerRouter: GiftPlayerRouting) {
          self.playerRouter = playerRouter
          NotificationCenter.default.addObserver(
              self, selector: #selector(handleMemoryWarningNoti),
              name: UIApplication.didReceiveMemoryWarningNotification, object: nil
          )
      }

      // MARK: - Host 桥（Task 6 用）

      public func registerHostView(_ view: UIView) { hostView = view }
      public func unregisterHostView(_ view: UIView) {
          if hostView === view { hostView = nil }
      }

      // MARK: - Public API

      public func setActiveScene(_ key: GiftEffectSceneKey) {
          if activeKey == key { return }
          if activeKey != nil {
              stopCurrentImmediately()
              pending.removeAll()
              microToasts.removeAll()
          }
          activeKey = key
          logger.info("setActiveScene → \(key.scene.rawValue, privacy: .public):\(key.scopeId, privacy: .public)")
      }

      public func leaveScene(_ key: GiftEffectSceneKey) {
          guard activeKey == key else { return }
          stopCurrentImmediately()
          pending.removeAll()
          microToasts.removeAll()
          activeKey = nil
          logger.info("leaveScene ← \(key.scene.rawValue, privacy: .public):\(key.scopeId, privacy: .public)")
      }

      public func enqueue(_ item: GiftEffectItem) {
          guard let active = activeKey, active == item.sceneKey else {
              logger.warning("enqueue rejected: item=\(item.sceneKey.scene.rawValue, privacy: .public) active=\(self.activeKey?.scene.rawValue ?? "nil", privacy: .public)")
              return
          }
          if item.sceneKey.scene == .party && item.isSelfSent {
              pending.insert(item, at: 0)
              logger.info("party me-sent gift head-inserted: \(item.giftName, privacy: .public)")
          } else {
              pending.append(item)
          }
          while pending.count > Self.queueLimit {
              let dropped = pending.removeFirst()
              logger.info("queue overflow dropped: \(dropped.giftName, privacy: .public)")
          }
          playNextIfIdle()
      }

      public func showMicroToast(_ toast: MicroToastItem) {
          guard let active = activeKey, active == toast.sceneKey else { return }
          guard active.scene != .chat else { return }
          microToasts.append(toast)
          let id = toast.id
          Task { @MainActor [weak self] in
              try? await Task.sleep(nanoseconds: UInt64(toast.duration * 1_000_000_000))
              self?.microToasts.removeAll { $0.id == id }
          }
      }

      public func handleMemoryWarning() {
          stopCurrentImmediately()
          pending.removeAll()
          microToasts.removeAll()
          logger.warning("memory warning: cleared in-flight")
      }

      public func reset() {
          stopCurrentImmediately()
          pending.removeAll()
          microToasts.removeAll()
          activeKey = nil
          playerRouter.tearDownPlayers()
      }

      public func warmupSVGA() {
          playerRouter.warmupSVGA()
      }

      // MARK: - Private

      @objc private func handleMemoryWarningNoti() { handleMemoryWarning() }

      private func stopCurrentImmediately() {
          playerRouter.stopAll()
          current = nil
      }

      private func playNextIfIdle() {
          guard current == nil, !pending.isEmpty else { return }
          let next = pending.removeFirst()
          current = next
          guard let host = hostView else {
              // 没有 hostView（UT 环境 or Window 未挂）— 直接 fire finish 让队列继续
              // 生产：Window 未挂说明 login flag 未生效，treated as no-op
              logger.warning("no hostView: skipping player and treating as finished immediately")
              Task { @MainActor [weak self] in
                  self?.onPlayerFinished()
              }
              return
          }
          playerRouter.play(item: next, in: host) { [weak self] in
              Task { @MainActor [weak self] in self?.onPlayerFinished() }
          }
      }

      private func onPlayerFinished() {
          current = nil
          playNextIfIdle()
      }
  }

  // MARK: - No-op router（Task 5 前占位）

  @MainActor
  final class NoopGiftPlayerRouter: GiftPlayerRouting {
      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          onFinish()
      }
      func stopAll() {}
      func tearDownPlayers() {}
      func warmupSVGA() {}
  }
  ```

  ⚠️ **注意 Test 兼容点**：Test 用的 FakeRouter 手动 finish 模式；生产用真 router 异步 finish。Center 内 `playNextIfIdle` 里 `hostView == nil` 分支保证 UT 无 host 也能推进队列（Test 3 `testEnqueuePlaysImmediatelyWhenIdle` 触发的是 fake router 的 play；无 host 分支只在 UT 未提供 host 时才走）。**Test 3 里应先 `center.registerHostView(fakeHost)`** —— 加到 setUp 里。

- [ ] **Step 6**：修 Test setUp 加 fakeHost

  ```swift
  override func setUp() {
      super.setUp()
      router = FakeGiftPlayerRouter()
      center = GiftEffectCenter(playerRouter: router)
      center.registerHostView(UIView())   // UT 用 fake host
  }
  ```

- [ ] **Step 7**：跑 15 tc 验证 PASS

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -only-testing:HilyTests/GiftEffectCenterTests 2>&1 | tail -10
  ```
  Expected：15 tc 全 pass。

- [ ] **Step 8**：commit Task 1 + 2 + 3 一起

  ```bash
  git add project.yml Podfile Sources/GiftEffect/ Tests/HilyTests/GiftEffect/
  git commit -m "$(cat <<'EOF'
  feat: [礼物特效] 引入 SVGA/YYEVA Pod + Center 状态机骨架 + Models 双兼容 decoder

  - Podfile 引 SVGAPlayer ~>2.5 + YYEVA
  - Sources/GiftEffect/{Models,Center,Players,UI,IM} 新模块目录
  - GiftEffectCenter 单例：queue(30) / 场景 tag / 派对房 me 插队 / 硬中断 / 内存告警
  - GiftEffectPayloadDecoder 支持 String/Int 双兼容 decode
  - 20 tc 单测覆盖状态机 + payload 兼容

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  EOF
  )"
  ```

  ⚠️ **`NEVER commit unless explicitly asked`** —— 本 step 需**用户明示批准**才 commit。impl 期 subagent 遇到"未明示"应停下等用户。

**验收条件**：15+4 = 19 tc 全 pass；`Sources/GiftEffect/` 目录挂 xcodegen 成功；build 通。

---

## Task 4 · GiftPlayerRouter 骨架（依赖倒置 + URL 后缀路由）

**目的**：写 `GiftPlayerRouter` 实体类（`GiftPlayerRouting` 的实现），内部持有 `GiftAnimationPlayer` 双实例引用；此时**两个 Player 用空 stub**（Task 5 替换成真 SVGA/YYEVA）。**这样 Center.shared 可以早点绑定真 router，等 Task 5 完成时只替换两个内部 field，不动 Center**。

**Files**:
- Create: `Sources/GiftEffect/Players/GiftPlayerRouter.swift`
- Modify: `Sources/GiftEffect/Center/GiftEffectCenter.swift`（`.shared` 换成用真 router）

**不 TDD 理由**：路由逻辑本身简单（URL 后缀 switch），且核心分支已由 Center tests 间接覆盖（`animationUrl == nil` 分支）；Task 5 加真 SDK 后写集成测。

- [ ] **Step 1**：写 `GiftPlayerRouter.swift`

  ```swift
  // Sources/GiftEffect/Players/GiftPlayerRouter.swift
  import UIKit
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "GiftPlayerRouter")

  @MainActor
  public final class GiftPlayerRouter: GiftPlayerRouting {

      // 两个 player 声明为 var，Task 5 内部实现替换（保持外部接口不变）
      private lazy var svgaPlayer: GiftAnimationPlayer = StubGiftAnimationPlayer(kind: "SVGA")
      private lazy var yyevaPlayer: GiftAnimationPlayer = StubGiftAnimationPlayer(kind: "YYEVA")

      private var activePlayer: GiftAnimationPlayer?

      public init() {}

      public func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          guard let url = item.animationUrl,
                let parsed = URL(string: url) else {
              onFinish(); return
          }
          let ext = parsed.pathExtension.lowercased()
          let player: GiftAnimationPlayer? = {
              switch ext {
              case "svga": return svgaPlayer
              case "mp4":  return yyevaPlayer
              default: return nil
              }
          }()
          guard let p = player else {
              logger.warning("unsupported ext=\(ext, privacy: .public) skip")
              onFinish(); return
          }
          activePlayer = p
          p.play(item: item, in: host) { [weak self] in
              self?.activePlayer = nil
              onFinish()
          }
      }

      public func stopAll() {
          activePlayer?.stop()
          activePlayer = nil
      }

      public func tearDownPlayers() {
          svgaPlayer.tearDown()
          yyevaPlayer.tearDown()
          activePlayer = nil
      }

      public func warmupSVGA() {
          // Task 5 内 SVGAAnimationPlayer 有真实现；本 step 用 stub 不做事
      }
  }

  /// Task 5 前占位；真 SDK 集成后本 struct 只保留在 tests 里（若还用）
  @MainActor
  final class StubGiftAnimationPlayer: GiftAnimationPlayer {
      let kind: String
      init(kind: String) { self.kind = kind }
      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          // 2.5s 假动画，与 B 期 StubGiftAnimationPlayer 行为一致
          Task { @MainActor in
              try? await Task.sleep(nanoseconds: 2_500_000_000)
              onFinish()
          }
      }
      func stop() {}
      func tearDown() {}
  }
  ```

- [ ] **Step 2**：修 `GiftEffectCenter.swift` `.shared` 用真 router

  在 Center 顶部：

  ```swift
  public static let shared: GiftEffectCenter = GiftEffectCenter(playerRouter: GiftPlayerRouter())
  ```

  同时**移除**`NoopGiftPlayerRouter`（Task 3 遗留占位）。

- [ ] **Step 3**：重跑 Task 3 的 15 tc（保 Center 逻辑无回归）

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -only-testing:HilyTests/GiftEffectCenterTests 2>&1 | tail -10
  ```

- [ ] **Step 4**：暂不 commit（等 Task 5 真 SVGA/YYEVA 一起）

**验收条件**：19 tc 全 pass；`GiftPlayerRouter` 编译无 SDK 依赖。

---

## Task 5 · SVGAPlayer / YYEVAPlayer 真封装

**目的**：把 Task 4 的 stub 替换成真 SDK 调用；单例复用、onFinish 幂等（stop 也 fire 一次）、tearDown 销毁。

**Files**:
- Create: `Sources/GiftEffect/Players/SVGAAnimationPlayer.swift`
- Create: `Sources/GiftEffect/Players/YYEVAAnimationPlayer.swift`
- Modify: `Sources/GiftEffect/Players/GiftPlayerRouter.swift`（把 svgaPlayer/yyevaPlayer 换成真类）

**不 TDD 理由**：SDK 依赖硬编码 UIKit + 网络 + Metal，unit 层无法可靠测。真集成验证在 Task 12（真机 QA）。**Static analysis 保底**：run `xcodebuild build` 验证 API 签名正确。

- [ ] **Step 1**：写 `SVGAAnimationPlayer.swift`

  ```swift
  // Sources/GiftEffect/Players/SVGAAnimationPlayer.swift
  import UIKit
  import SVGAPlayer
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "SVGAPlayer")

  @MainActor
  final class SVGAAnimationPlayer: NSObject, GiftAnimationPlayer, SVGAPlayerDelegate {

      private var player: SVGAPlayer?
      private var parser: SVGAParser?
      private var currentFinish: (() -> Void)?

      func warmup() {
          if parser == nil { parser = SVGAParser() }
          logger.info("SVGA warmed up")
      }

      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          guard let urlStr = item.animationUrl,
                let url = URL(string: urlStr) else {
              onFinish(); return
          }
          // 保证一次播只有一个 finish
          finishOnce()
          currentFinish = onFinish

          let p = ensurePlayer(in: host)
          let parser = ensureParser()
          parser.parse(with: url) { [weak self, weak p] videoItem in
              guard let self, let p, let videoItem else {
                  self?.finishOnce()
                  return
              }
              p.videoItem = videoItem
              p.loops = 1
              p.startAnimation()
          } failureBlock: { [weak self] err in
              logger.warning("SVGA parse fail url=\(url.absoluteString, privacy: .public) err=\(err?.localizedDescription ?? "nil", privacy: .public)")
              self?.finishOnce()
          }
      }

      func stop() {
          player?.stopAnimation()
          player?.clear()
          finishOnce()
      }

      func tearDown() {
          player?.stopAnimation()
          player?.removeFromSuperview()
          player = nil
          parser = nil
          currentFinish = nil
      }

      // MARK: - private

      private func ensurePlayer(in host: UIView) -> SVGAPlayer {
          if let p = player, p.superview === host { return p }
          player?.removeFromSuperview()
          let p = SVGAPlayer(frame: host.bounds)
          p.contentMode = .scaleAspectFit
          p.delegate = self
          p.autoresizingMask = [.flexibleWidth, .flexibleHeight]
          host.addSubview(p)
          player = p
          return p
      }

      private func ensureParser() -> SVGAParser {
          if let p = parser { return p }
          let p = SVGAParser()
          parser = p
          return p
      }

      private func finishOnce() {
          let f = currentFinish
          currentFinish = nil
          f?()
      }

      // MARK: - SVGAPlayerDelegate

      func svgaPlayerDidFinishedAnimation(_ player: SVGAPlayer!) {
          finishOnce()
      }
  }
  ```

- [ ] **Step 2**：写 `YYEVAAnimationPlayer.swift`

  ⚠️ YYEVA 官方 API 具体符号（`YYEasyMetalPlayer` / delegate 事件枚举）需以 `Pods/YYEVA/` 头文件为准；本 step 落地时以 pod 头为准调整。骨架：

  ```swift
  // Sources/GiftEffect/Players/YYEVAAnimationPlayer.swift
  import UIKit
  import YYEVA
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "YYEVAPlayer")

  @MainActor
  final class YYEVAAnimationPlayer: NSObject, GiftAnimationPlayer {

      /// pod 头名待锁；下面按 YYEasyMetalPlayer 骨架
      private var player: YYEasyMetalPlayer?
      private var currentFinish: (() -> Void)?

      func play(item: GiftEffectItem, in host: UIView, onFinish: @escaping () -> Void) {
          guard let urlStr = item.animationUrl,
                let url = URL(string: urlStr) else {
              onFinish(); return
          }
          finishOnce()
          currentFinish = onFinish

          let p = ensurePlayer(in: host)
          // pod install 后按头文件调整（play(url:) or setVideoURL + startPlay）
          p.play(with: url)
      }

      func stop() {
          player?.stop()
          finishOnce()
      }

      func tearDown() {
          player?.stop()
          player?.removeFromSuperview()
          player = nil
          currentFinish = nil
      }

      // MARK: - private

      private func ensurePlayer(in host: UIView) -> YYEasyMetalPlayer {
          if let p = player, p.superview === host { return p }
          player?.removeFromSuperview()
          let p = YYEasyMetalPlayer(frame: host.bounds)
          p.delegate = self   // ⚠️ delegate protocol 名称待锁
          p.autoresizingMask = [.flexibleWidth, .flexibleHeight]
          host.addSubview(p)
          player = p
          return p
      }

      private func finishOnce() {
          let f = currentFinish
          currentFinish = nil
          f?()
      }
  }

  // MARK: - YYEasyMetalPlayerDelegate（api 名待锁）
  extension YYEVAAnimationPlayer: YYEasyMetalPlayerDelegate {
      func onEvent(_ evt: YYMetalPlayerEvent) {
          if evt.type == .finish || evt.type == .error {
              finishOnce()
          }
      }
  }
  ```

  ⚠️ **impl 期具体做法**：Pod install 后 `find Pods/YYEVA -name "*.h" | xargs grep -l "@interface"` 找出真类名 + delegate 方法签名，再改此文件。若 YYEVA 官方仓库停止维护 / iOS 17 兼容问题严重，退路 **降级为 AVPlayer 无 alpha 播放**（视觉降级但保留代码路径），下一 spec 补上。

- [ ] **Step 3**：改 `GiftPlayerRouter.swift`

  ```swift
  // 替换两行
  private lazy var svgaPlayer: GiftAnimationPlayer = SVGAAnimationPlayer()
  private lazy var yyevaPlayer: GiftAnimationPlayer = YYEVAAnimationPlayer()

  // warmupSVGA 转发
  public func warmupSVGA() {
      (svgaPlayer as? SVGAAnimationPlayer)?.warmup()
  }
  ```

  移除 `StubGiftAnimationPlayer`。

- [ ] **Step 4**：命令行 build

  ```bash
  xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
      -destination 'generic/platform=iOS' -configuration Debug \
      CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -5
  ```
  Expected：SUCCEEDED。若失败：pod 头名不匹配 → 按真头名调 YYEVAAnimationPlayer.swift。

- [ ] **Step 5**：重跑 GiftEffectCenterTests 15 tc（FakePlayer 独立，不受 SDK 影响）

- [ ] **Step 6**：**暂不 commit**（合并到 Task 8 一起）

**验收条件**：build 通、19 tc 全 pass；两 SDK 头文件 API 已在代码中正确 binding。

---

## Task 6 · UIWindow overlay + Root View + Central/MicroToast + HostView

**目的**：搭 SwiftUI + UIWindow 视觉层；本 Task 完成后**手动 mock enqueue 能在真机/模拟器看到动画**。

**Files**:
- Create: `Sources/GiftEffect/Center/GiftEffectOverlayWindow.swift`
- Create: `Sources/GiftEffect/UI/GiftEffectRoot.swift`
- Create: `Sources/GiftEffect/UI/CentralEffectLayer.swift`
- Create: `Sources/GiftEffect/UI/MicroToastLayer.swift`
- Create: `Sources/GiftEffect/UI/GiftPlayerHostView.swift`

**不 TDD 理由**：SwiftUI + UIWindow 视觉层 unit 测意义低（真机 QA 更可靠）；关键逻辑（Center 状态机 / payload decode）已由前面 tc 覆盖。

- [ ] **Step 1**：写 `GiftPlayerHostView.swift`

  ```swift
  // Sources/GiftEffect/UI/GiftPlayerHostView.swift
  import SwiftUI
  import UIKit

  /// 承载 SVGAPlayer / YYEasyMetalPlayer 的 UIView 桥；同一实例长期挂在 CentralEffectLayer
  struct GiftPlayerHostView: UIViewRepresentable {
      func makeUIView(context: Context) -> UIView {
          let host = UIView()
          host.backgroundColor = .clear
          host.isUserInteractionEnabled = false
          GiftEffectCenter.shared.registerHostView(host)
          return host
      }
      func updateUIView(_ uiView: UIView, context: Context) {
          // no-op：Player 由 Center 触发 play，UIView 只做容器
      }
      static func dismantleUIView(_ uiView: UIView, coordinator: ()) {
          GiftEffectCenter.shared.unregisterHostView(uiView)
      }
  }
  ```

- [ ] **Step 2**：写 `CentralEffectLayer.swift`

  ```swift
  // Sources/GiftEffect/UI/CentralEffectLayer.swift
  import SwiftUI

  struct CentralEffectLayer: View {
      @ObservedObject var center: GiftEffectCenter

      var body: some View {
          ZStack {
              GiftPlayerHostView()
                  .frame(maxWidth: .infinity, maxHeight: .infinity)
                  .opacity(center.current == nil ? 0 : 1)
                  .animation(.easeInOut(duration: 0.2), value: center.current?.id)
          }
      }
  }
  ```

- [ ] **Step 3**：写 `MicroToastLayer.swift`

  ```swift
  // Sources/GiftEffect/UI/MicroToastLayer.swift
  import SwiftUI

  struct MicroToastLayer: View {
      let toasts: [MicroToastItem]

      var body: some View {
          VStack {
              Spacer()
              if let latest = toasts.last {
                  HStack(spacing: 10) {
                      AsyncImage(url: URL(string: latest.imgUrl ?? "")) { phase in
                          switch phase {
                          case .success(let img):
                              img.resizable().aspectRatio(contentMode: .fit)
                          default:
                              Color.clear
                          }
                      }
                      .frame(width: 60, height: 60)
                      Text("×\(latest.count)")
                          .font(.system(size: 24, weight: .bold))
                          .foregroundColor(Color(red: 0.58, green: 0.20, blue: 0.92))
                  }
                  .padding(.bottom, 200)
                  .transition(.opacity)
                  .id(latest.id)
              }
          }
          .frame(maxWidth: .infinity, maxHeight: .infinity)
          .animation(.easeInOut(duration: 0.3), value: toasts.last?.id)
      }
  }
  ```

- [ ] **Step 4**：写 `GiftEffectRoot.swift`

  ```swift
  // Sources/GiftEffect/UI/GiftEffectRoot.swift
  import SwiftUI

  struct GiftEffectRoot: View {
      @ObservedObject private var center = GiftEffectCenter.shared

      var body: some View {
          ZStack {
              CentralEffectLayer(center: center)
              MicroToastLayer(toasts: center.microToasts)
          }
          .allowsHitTesting(false)
          .ignoresSafeArea()
      }
  }
  ```

- [ ] **Step 5**：写 `GiftEffectOverlayWindow.swift`

  ```swift
  // Sources/GiftEffect/Center/GiftEffectOverlayWindow.swift
  import UIKit
  import SwiftUI
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "GiftEffectWindow")

  @MainActor
  public final class GiftEffectOverlayWindow {
      public static let shared = GiftEffectOverlayWindow()
      private var window: UIWindow?

      private init() {}

      /// 登录后调用，创建 window
      public func show(on scene: UIWindowScene) {
          guard window == nil else { return }
          let w = UIWindow(windowScene: scene)
          w.windowLevel = .alert - 1
          w.backgroundColor = .clear
          w.isUserInteractionEnabled = false
          let host = UIHostingController(rootView: GiftEffectRoot())
          host.view.backgroundColor = .clear
          host.view.isUserInteractionEnabled = false
          w.rootViewController = host
          w.isHidden = false
          window = w
          logger.info("GiftEffectWindow shown")
      }

      /// logout / 内存告警 手动 hide 时调
      public func hide() {
          window?.isHidden = true
          window = nil
          logger.info("GiftEffectWindow hidden")
      }
  }
  ```

- [ ] **Step 6**：手动 mock 验证（可选 UI 测试）

  在 [`Sources/HilyApp.swift`](../../Sources/HilyApp.swift) 里加一个 `#if DEBUG` mock trigger（Task 7 定式挂 Window 后再验证；本 Task 只保证 build 通 + 视图能 render 空态）

  ```bash
  xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
      -destination 'generic/platform=iOS' -configuration Debug \
      CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -5
  ```

- [ ] **Step 7**：暂不 commit（合并到 Task 8）

**验收条件**：build 通；`import Sources/GiftEffect/UI/*` 编译无警告。

---

## Task 7 · scene modifier + HilyApp 集成 + logout reset

**目的**：把 Center + Window 接入 App 生命周期；提供 `.giftEffectScene(_:scopeId:)` API 供 4 场景 view 使用。

**Files**:
- Create: `Sources/GiftEffect/Center/GiftEffectSceneModifier.swift`
- Modify: [`Sources/HilyApp.swift`](../../Sources/HilyApp.swift)
- Modify: [`Sources/Auth/SessionStore.swift`](../../Sources/Auth/SessionStore.swift)

**不 TDD 理由**：SwiftUI modifier + App-level lifecycle 依赖 UIApplication.connectedScenes，UT 难以复现；集成到真机验证。

- [ ] **Step 1**：写 modifier

  ```swift
  // Sources/GiftEffect/Center/GiftEffectSceneModifier.swift
  import SwiftUI

  public extension View {
      /// 声明本 view 属于某个礼物特效场景
      /// - Parameters:
      ///   - scene: 场景类型
      ///   - scopeId: 场景内唯一标识（liveId / callId / partyRoomId / peerYxAccid）
      func giftEffectScene(_ scene: GiftEffectScene, scopeId: String) -> some View {
          modifier(GiftEffectSceneModifier(scene: scene, scopeId: scopeId))
      }
  }

  private struct GiftEffectSceneModifier: ViewModifier {
      let scene: GiftEffectScene
      let scopeId: String
      @Environment(\.scenePhase) private var scenePhase

      func body(content: Content) -> some View {
          content
              .onAppear {
                  GiftEffectCenter.shared.setActiveScene(
                      GiftEffectSceneKey(scene: scene, scopeId: scopeId)
                  )
              }
              .onDisappear {
                  // 对齐 rule swiftui-camera-preview.md §6：scenePhase=.background 时也会触发 onDisappear
                  guard scenePhase != .background else { return }
                  GiftEffectCenter.shared.leaveScene(
                      GiftEffectSceneKey(scene: scene, scopeId: scopeId)
                  )
              }
      }
  }
  ```

- [ ] **Step 2**：修 [`Sources/HilyApp.swift`](../../Sources/HilyApp.swift) 挂 Window + 5s 冷启预热

  找到 App 的 `.onAppear` / `.onChange(of: session.isLoggedIn)` 位置，加：

  ```swift
  .onChange(of: sessionStore.isLoggedIn) { logged in
      if logged, let scene = UIApplication.shared.connectedScenes
                    .compactMap({ $0 as? UIWindowScene })
                    .first {
          GiftEffectOverlayWindow.shared.show(on: scene)
          // 5s 空闲后预热 SVGA（对齐 H5）
          Task { @MainActor in
              try? await Task.sleep(nanoseconds: 5_000_000_000)
              GiftEffectCenter.shared.warmupSVGA()
          }
      } else {
          GiftEffectCenter.shared.reset()
          GiftEffectOverlayWindow.shared.hide()
      }
  }
  ```

  ⚠️ 若 HilyApp 已经用别的模式（比如 RootView 内挂），改到 RootView 而非 App level。**具体位置由 impl agent 查现有代码结构选**。

- [ ] **Step 3**：修 [`Sources/Auth/SessionStore.swift`](../../Sources/Auth/SessionStore.swift) `logout()` 加清理调用

  找到 `logout()` 函数，末尾（在其它 clear/reset 语句附近）加：

  ```swift
  GiftEffectCenter.shared.reset()
  GiftEffectOverlayWindow.shared.hide()
  ```

- [ ] **Step 4**：DEBUG mock trigger（临时验证）

  在 [`Sources/Home/HomeView.swift`](../../Sources/Home/HomeView.swift) 或临时 Debug 页加一个按钮

  ```swift
  #if DEBUG
  Button("Mock Gift Effect") {
      let key = GiftEffectSceneKey(scene: .live, scopeId: "mock")
      GiftEffectCenter.shared.setActiveScene(key)
      let item = GiftEffectItem(
          sceneKey: key,
          senderYxAccid: "test", senderNickname: "Tester", senderAvatarUrl: nil,
          giftId: 1, giftName: "Rose", giftCount: 1, giftPrice: 100,
          animationUrl: "https://svga-cdn/rose.svga",  // 需要一个真 URL；或改用 assets 内 svga 本地文件
          staticImgUrl: nil,
          timestamp: Int64(Date().timeIntervalSince1970 * 1000),
          isSelfSent: false
      )
      GiftEffectCenter.shared.enqueue(item)
  }
  #endif
  ```

  ⚠️ 真 SVGA 资源 URL —— 找一个 SVGAPlayer-iOS 官方示例 URL 或自己上传测试 SVGA 到 CDN。**推荐**：把 https://svgaplayer.github.io/website/svga/kanna.svga 类官方 sample 塞进 mock（会有依赖外网 CDN，仅 debug）。

- [ ] **Step 5**：模拟器跑一次

  ```bash
  xcodebuild -workspace Hily.xcworkspace -scheme Hily \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
      -configuration Debug build 2>&1 | tail -20
  ```

  然后手动跑一次 —— 打开模拟器 → 点 mock 按钮 → 应看到中央 SVGA 动画播放 2.5-5s 后消失。

- [ ] **Step 6**：全测试重跑（保 refactor 不破 unit tests）

  ```bash
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' 2>&1 | grep -E "Test Suite.*passed|Test Suite.*failed" | tail
  ```

- [ ] **Step 7**：暂不 commit（合并到 Task 8）

**验收条件**：模拟器上 mock 按钮能触发一次真 SVGA 动画播放；build 通；全 tests pass。

---

## Task 8 · GiftEffectIntake + payload 解析 + 单测（TDD）

**目的**：4 场景 IM 统一入口。负责判定"中央大动画 vs 微飘窗"、构造 item、调 Center。

**Files**:
- Create: `Sources/GiftEffect/IM/GiftEffectIntake.swift`
- Test: `Tests/HilyTests/GiftEffect/GiftEffectIntakeTests.swift`

- [ ] **Step 1**：写 failing tests

  ```swift
  // Tests/HilyTests/GiftEffect/GiftEffectIntakeTests.swift
  import XCTest
  @testable import Hily

  @MainActor
  final class GiftEffectIntakeTests: XCTestCase {
      private var center: GiftEffectCenter!
      private var router: FakeGiftPlayerRouter!
      private let liveKey = GiftEffectSceneKey(scene: .live, scopeId: "live_1")
      private let chatKey = GiftEffectSceneKey(scene: .chat, scopeId: "peer_A")
      private let partyKey = GiftEffectSceneKey(scene: .party, scopeId: "party_1")

      override func setUp() {
          super.setUp()
          router = FakeGiftPlayerRouter()
          center = GiftEffectCenter(playerRouter: router)
          center.registerHostView(UIView())
      }

      // 1. Live SVGA URL → 中央大动画（enqueue）
      func testLiveSvgaGoesCentralQueue() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          let ok = GiftEffectIntake.ingest(
              scene: .live, scopeId: "live_1",
              payload: [
                  "giftId": 1, "giftName": "R", "giftNum": 1,
                  "giftIcon": "https://cdn/x.svga"
              ],
              mineYxAccid: "me", into: center
          )
          XCTAssertTrue(ok)
          XCTAssertEqual(router.playHistory.count, 1)
          XCTAssertEqual(center.microToasts.count, 0)
      }

      // 2. Live MP4 URL → 中央大动画
      func testLiveMp4GoesCentralQueue() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          let ok = GiftEffectIntake.ingest(
              scene: .live, scopeId: "live_1",
              payload: ["giftId": 1, "giftName": "R", "giftNum": 1,
                        "giftIcon": "https://cdn/x.mp4"],
              mineYxAccid: "me", into: center
          )
          XCTAssertTrue(ok)
          XCTAssertEqual(router.playHistory.count, 1)
      }

      // 3. Live 静态图 URL → MicroToast
      func testLiveStaticImgGoesMicroToast() {
          center.setActiveScene(liveKey)
          router.manualFinish = true
          let ok = GiftEffectIntake.ingest(
              scene: .live, scopeId: "live_1",
              payload: ["giftId": 1, "giftName": "R", "giftNum": 1,
                        "giftImg": "https://cdn/x.png",
                        "giftSmallImg": "https://cdn/x.png"],
              mineYxAccid: "me", into: center
          )
          XCTAssertTrue(ok)
          XCTAssertEqual(router.playHistory.count, 0)
          XCTAssertEqual(center.microToasts.count, 1)
      }

      // 4. Chat 无动画资源 → 既不入队也不 MicroToast（气泡由现有 SystemGiftBubbleView 承担）
      func testChatWithoutAnimationSuppressesMicroToast() {
          center.setActiveScene(chatKey)
          router.manualFinish = true
          let ok = GiftEffectIntake.ingest(
              scene: .chat, scopeId: "peer_A",
              payload: ["giftId": 1, "giftName": "R", "giftNum": 1,
                        "giftImg": "https://cdn/x.png"],
              mineYxAccid: "me", into: center
          )
          XCTAssertTrue(ok)
          XCTAssertEqual(router.playHistory.count, 0)
          XCTAssertEqual(center.microToasts.count, 0)
      }

      // 5. Chat SVGA URL → 中央大动画（用户明示 Chat 也弹全屏）
      func testChatSvgaGoesCentralQueue() {
          center.setActiveScene(chatKey)
          router.manualFinish = true
          let ok = GiftEffectIntake.ingest(
              scene: .chat, scopeId: "peer_A",
              payload: ["giftId": 1, "giftName": "R", "giftNum": 1,
                        "giftIcon": "https://cdn/x.svga"],
              mineYxAccid: "me", into: center
          )
          XCTAssertTrue(ok)
          XCTAssertEqual(router.playHistory.count, 1)
          XCTAssertEqual(center.microToasts.count, 0)
      }

      // 6. Party me 送礼 → 队首插入 + isSelfSent=true 标记
      func testPartyMeSentFlaggedForHeadInsert() {
          center.setActiveScene(partyKey)
          router.manualFinish = true
          _ = GiftEffectIntake.ingest(
              scene: .party, scopeId: "party_1",
              payload: ["giftId": 1, "giftName": "A", "giftNum": 1,
                        "giftIcon": "https://cdn/x.svga",
                        "senderYxAccid": "other"],
              mineYxAccid: "me", into: center
          )
          _ = GiftEffectIntake.ingest(
              scene: .party, scopeId: "party_1",
              payload: ["giftId": 2, "giftName": "ME", "giftNum": 1,
                        "giftIcon": "https://cdn/x.svga",
                        "senderYxAccid": "me"],
              mineYxAccid: "me", into: center
          )
          // A 正在播，ME 应在队首（未抢占）
          router.finishCurrent()
          XCTAssertEqual(center.current?.giftName, "ME")
      }

      // 7. 缺 giftId → 返回 false，不入队
      func testMissingGiftIdRejected() {
          center.setActiveScene(liveKey)
          let ok = GiftEffectIntake.ingest(
              scene: .live, scopeId: "live_1",
              payload: ["giftName": "R"], mineYxAccid: "me", into: center
          )
          XCTAssertFalse(ok)
      }

      // 8. 场景 scopeId 不匹配 activeScene → enqueue 层拒（正确性由 Center 保证，此处验入口透传）
      func testMismatchedScopeIdRejected() {
          center.setActiveScene(liveKey)   // scopeId="live_1"
          router.manualFinish = true
          _ = GiftEffectIntake.ingest(
              scene: .live, scopeId: "live_999",   // 不同 scopeId
              payload: ["giftId": 1, "giftName": "R", "giftNum": 1,
                        "giftIcon": "https://cdn/x.svga"],
              mineYxAccid: "me", into: center
          )
          XCTAssertEqual(router.playHistory.count, 0)
      }
  }
  ```

- [ ] **Step 2**：跑 test 验证 FAIL

- [ ] **Step 3**：写 `GiftEffectIntake.swift`

  ```swift
  // Sources/GiftEffect/IM/GiftEffectIntake.swift
  import Foundation
  import os

  private let logger = Logger(subsystem: "com.anchor.livechat", category: "GiftEffectIntake")

  @MainActor
  public enum GiftEffectIntake {

      /// 4 场景 IM 层统一入口
      /// - Returns: true 表示成功识别为礼物消息（无论走队列还是 MicroToast）；false 表示 payload 不合法/缺关键字段
      @discardableResult
      public static func ingest(
          scene: GiftEffectScene,
          scopeId: String,
          payload: [String: Any],
          mineYxAccid: String,
          into center: GiftEffectCenter = .shared
      ) -> Bool {
          let key = GiftEffectSceneKey(scene: scene, scopeId: scopeId)
          guard let item = GiftEffectPayloadDecoder.decode(
              sceneKey: key, payload: payload, mineYxAccid: mineYxAccid
          ) else {
              logger.warning("intake failed: no valid giftId in payload; scene=\(scene.rawValue, privacy: .public)")
              return false
          }

          if item.animationUrl != nil {
              // 有动画资源 → 中央大动画
              center.enqueue(item)
          } else if scene != .chat {
              // 无动画资源 + 非 Chat → MicroToast
              let toast = MicroToastItem(
                  sceneKey: key,
                  imgUrl: item.staticImgUrl,
                  giftName: item.giftName,
                  count: item.giftCount
              )
              center.showMicroToast(toast)
          } else {
              // Chat 无动画 → 无操作（消息气泡由 SystemGiftBubbleView 承载）
          }
          return true
      }
  }
  ```

- [ ] **Step 4**：跑 tests 验证 PASS

- [ ] **Step 5**：**commit Task 1-8**（用户明示批准后）

  ```bash
  git add project.yml Podfile Sources/GiftEffect/ Tests/HilyTests/GiftEffect/ \
      Sources/HilyApp.swift Sources/Auth/SessionStore.swift
  git commit -m "$(cat <<'EOF'
  feat: [礼物特效] 引擎骨架完成（Center + Player + Window + Intake）

  - Pod: SVGAPlayer ~>2.5 + YYEVA
  - GiftEffectCenter 单例 + Deque(30) + 场景 tag + 派对房 me 插队
  - SVGAAnimationPlayer / YYEVAAnimationPlayer 池化封装
  - 独立 UIWindow overlay（windowLevel .alert-1，全局点击透传）
  - .giftEffectScene(_:scopeId:) modifier + HilyApp 集成
  - GiftEffectIntake 4 场景 IM 统一入口 + SVGA/MP4/静态图判定
  - SessionStore.logout 清理引擎 + Window hide

  27 tc 单测覆盖：payload 双兼容 / 状态机 / 派对房 me 插队 / MicroToast 场景过滤 / Intake 判定分支

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  EOF
  )"
  ```

  ⚠️ 无用户明示批准 → 不 commit（对齐 CLAUDE.md `NEVER commit unless explicitly asked`）

**验收条件**：27 tc 全 pass；build 通；模拟器 mock 按钮触发中央动画。

---

## Task 9 · Live 场景 IM 接线

**目的**：LiveRoomView 挂 modifier；NIMChatroomManager 收到礼物 attachType → 调 Intake。

**Files**:
- Modify: [`Sources/Live/LiveRoomView.swift`](../../Sources/Live/LiveRoomView.swift)
- Modify: [`Sources/Live/NIMChatroomManager.swift`](../../Sources/Live/NIMChatroomManager.swift)

**不 TDD 理由**：现有直播/IM 层已有接线；本 Task 只加两处调用点。集成正确性由真机 QA 验证。

- [ ] **Step 1**：Read [`Sources/Live/LiveRoomView.swift`](../../Sources/Live/LiveRoomView.swift)，找到 body 主 view 的 root（`ZStack` 或 `VStack`）

- [ ] **Step 2**：给 root view 挂 modifier（roomId 是 `Int?`，用 String 转换）

  ```swift
  .giftEffectScene(.live, scopeId: String(store.roomId ?? 0))
  ```

- [ ] **Step 3**：Read [`Sources/Live/NIMChatroomManager.swift`](../../Sources/Live/NIMChatroomManager.swift)，找 attachType 50 / 1（`SEND_GIFT`）的分发点

- [ ] **Step 4**：在分发点调 Intake

  ```swift
  // 在礼物消息 handler 内加
  GiftEffectIntake.ingest(
      scene: .live,
      scopeId: String(liveStore.roomId ?? 0),
      payload: giftPayload,   // 已解析的 dict
      mineYxAccid: sessionStore.mineYxAccid   // 若字段名不同，按实际调整
  )
  ```

  ⚠️ `mineYxAccid` 具体字段名以 SessionStore 实际 API 为准（grep 找）。若 SessionStore 没有直接暴露 → 加一个 `var mineYxAccid: String { get }` computed property（在 SessionStore 内已有 `mineInfo?.yxAccid` 类字段就 wrap）。

- [ ] **Step 5**：build 通

  ```bash
  xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
      -destination 'generic/platform=iOS' -configuration Debug \
      CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -5
  ```

- [ ] **Step 6**：手动真机 QA（准备一个真直播 → 让用户端送 SVGA 礼物 → 观察本机主播看到动画）

**验收条件**：真机 Live 场景收礼时看到中央 SVGA 动画。

---

## Task 10 · Call + Party 场景 IM 接线

**目的**：Call + Party 两个场景同类接线。

**Files**:
- Modify: [`Sources/Call/CallView.swift`](../../Sources/Call/CallView.swift)
- Modify: [`Sources/Call/CallStore.swift`](../../Sources/Call/CallStore.swift) 或 IM 处理层（找 attachType 4）
- Modify: [`Sources/Party/UI/PartyRoomView.swift`](../../Sources/Party/UI/PartyRoomView.swift)
- Modify: [`Sources/Party/NIM/PartyMessageRouter.swift`](../../Sources/Party/NIM/PartyMessageRouter.swift)

**不 TDD 理由**：同 Task 9。

- [ ] **Step 1**：CallView root 挂 modifier

  ```swift
  .giftEffectScene(.call, scopeId: callStore.currentCallId ?? "")
  ```

- [ ] **Step 2**：找 Call attachType=4 分发点，调 Intake

  ```swift
  GiftEffectIntake.ingest(
      scene: .call,
      scopeId: callStore.currentCallId ?? "",
      payload: giftPayload,
      mineYxAccid: sessionStore.mineYxAccid
  )
  ```

- [ ] **Step 3**：PartyRoomView root 挂 modifier

  ```swift
  .giftEffectScene(.party, scopeId: partyStore.roomInfo?.id ?? "")
  ```

- [ ] **Step 4**：找 PartyMessageRouter 里 `partyGiftCompressed` 的 gzip 解压后循环点，调 Intake

  ```swift
  // 解压后 gifts 数组循环
  for giftPayload in gifts {
      GiftEffectIntake.ingest(
          scene: .party,
          scopeId: partyStore.roomInfo?.id ?? "",
          payload: giftPayload,
          mineYxAccid: sessionStore.mineYxAccid
      )
  }
  ```

- [ ] **Step 5**：build 通 + 真机 QA

**验收条件**：Call / Party 场景真机收礼看到动画。

---

## Task 11 · Chat 场景 IM 接线

**目的**：ChatDetailView + P2P 消息接收路径接线；`attachType == "SEND_GIFT"`（字符串）分支。

**Files**:
- Modify: [`Sources/Message/Chat/UI/ChatDetailView.swift`](../../Sources/Message/Chat/UI/ChatDetailView.swift)
- Modify: [`Sources/Message/Chat/P2PChatStore.swift`](../../Sources/Message/Chat/P2PChatStore.swift) 或 P2P 消息路由处

- [ ] **Step 1**：ChatDetailView root 挂 modifier（`peerYxAccid` 名字按实际，可能是 `session.yxAccid` / `chatSession.id`）

  ```swift
  .giftEffectScene(.chat, scopeId: session.peerYxAccid)
  ```

- [ ] **Step 2**：找 P2P 消息收到 `attachType == "SEND_GIFT"` 的分发点，调 Intake

  ```swift
  if attachType == "SEND_GIFT" {
      GiftEffectIntake.ingest(
          scene: .chat,
          scopeId: session.peerYxAccid,
          payload: giftPayload,
          mineYxAccid: sessionStore.mineYxAccid
      )
  }
  ```

- [ ] **Step 3**：build 通

- [ ] **Step 4**：手动真机 QA（用户端 P2P 送 SVGA 礼物，本机主播私聊页看到中央动画 + 消息气泡）

**验收条件**：私聊页真机同时展示消息气泡 + 中央 SVGA 动画。

---

## Task 12 · 骨架文件迁移 + sanity check

**目的**：删 B 期骨架文件；4 场景真机 QA 全过。

**Files**:
- Delete: [`Sources/Live/GiftAnimation/GiftAnimationPlayerProtocol.swift`](../../Sources/Live/GiftAnimation/GiftAnimationPlayerProtocol.swift)
- Delete: [`Sources/Live/GiftAnimation/GiftAnimationQueue.swift`](../../Sources/Live/GiftAnimation/GiftAnimationQueue.swift)
- Delete: [`Sources/Live/GiftAnimation/GiftAnimationOverlay.swift`](../../Sources/Live/GiftAnimation/GiftAnimationOverlay.swift)
- Delete: [`Sources/Live/NIM/GiftAnimationQueueProtocol.swift`](../../Sources/Live/NIM/GiftAnimationQueueProtocol.swift)
- Modify: [`Sources/Live/NIM/GiftMessageRouter.swift`](../../Sources/Live/NIM/GiftMessageRouter.swift) —— 移除 `enqueueGift` / `reorderDuringPK` 方法（若无外部调用点）

- [ ] **Step 1**：grep 确认无引用

  ```bash
  grep -rn "GiftAnimationQueue\|GiftAnimationOverlay\|StubGiftAnimationPlayer\|RealSVGAPlayer\|GiftAnimationQueueProtocol\|enqueueGift\|reorderDuringPK" Sources/ Tests/ 2>&1 | grep -v "Sources/GiftEffect"
  ```
  Expected：空（若有引用需先改）

- [ ] **Step 2**：删 4 个骨架文件 + 修 GiftMessageRouter.swift

  ```bash
  rm Sources/Live/GiftAnimation/GiftAnimationPlayerProtocol.swift
  rm Sources/Live/GiftAnimation/GiftAnimationQueue.swift
  rm Sources/Live/GiftAnimation/GiftAnimationOverlay.swift
  rm Sources/Live/NIM/GiftAnimationQueueProtocol.swift
  # 若 GiftAnimation 目录空，删目录
  rmdir Sources/Live/GiftAnimation 2>/dev/null
  ```

  改 `Sources/Live/NIM/GiftMessageRouter.swift`：删 `enqueueGift(_:)` / `reorderDuringPK(maxQueueLength:)` 两方法（若 protocol 内定义要一起删，同时删所有 impl 内空 stub）。

- [ ] **Step 3**：`xcodegen generate && pod install` 走一遍（如有必要）

  ```bash
  ./bin/regen.sh
  ```

- [ ] **Step 4**：全 build + test

  ```bash
  xcodebuild build -workspace Hily.xcworkspace -scheme Hily \
      -destination 'generic/platform=iOS' -configuration Debug \
      CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | tail -5
  xcodebuild test -workspace Hily.xcworkspace -scheme HilyTests \
      -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' 2>&1 | tail -30
  ```

- [ ] **Step 5**：真机 QA · spec §9.4 全项走一遍

  - [ ] SVGA 官方 sample 在 Live/Call/Party/Chat 4 场景各能播一次
  - [ ] YYEVA 官方 sample MP4 alpha 能播（背景透明不黑）
  - [ ] 快速切换 Live→Call 10 次无崩溃
  - [ ] Debug Memory Graph 无 GiftEffectCenter 泄漏
  - [ ] Instruments 60s 无 fps 掉到 30
  - [ ] 30 条队列压测（mock 40 条 enqueue，log 里出现 10 次 "queue overflow"）
  - [ ] 派对房 me 插队 mock 验证（enqueue 3 条：A(other), B(other), ME(self)；观察 A 后应播 ME）

- [ ] **Step 6**：**commit 迁移 + 接线**（用户明示批准后）

  ```bash
  git add -A
  git commit -m "..." # 见 Task 8 类似格式
  ```

**验收条件**：4 场景真机 sanity check 全过；测试全 pass；无骨架残留。

---

## Task 13 · 性能验证 + Instruments profile

**目的**：spec §7 表格全部指标达标；发现问题就地评估 or 立 rule。

**不 TDD 理由**：性能测量属于 profiling 类型，用 Instruments 而非 XCTest。

- [ ] **Step 1**：Instruments 启动 Time Profiler + Core Animation + Allocations

  ```bash
  # 命令行触发（可选，也可以 Xcode Cmd+I 手工）
  xcrun xctrace record --template 'Time Profiler' \
      --launch -- ~/Library/Developer/Xcode/DerivedData/.../Hily.app
  ```

- [ ] **Step 2**：按 spec §7 表格逐项测

  | 指标 | 测量方式 |
  |---|---|
  | 首帧延迟 P95 ≤ 200ms（缓存） | 用 `os_signpost` 埋点 enqueue → SVGA startAnimation → 首帧渲染；Instruments 观察 |
  | SVGA fps ≥ 55 | Core Animation instrument |
  | YYEVA fps ≥ 55 | 同上 |
  | 单条 SVGA 峰值 < 30MB | Allocations instrument |
  | 单条 YYEVA 峰值 < 50MB | 同上 |
  | 冷启到 SVGA warmup < 6s | 冷启 log 时间戳 |
  | enqueue 全链路 < 5ms | os_signpost |

- [ ] **Step 3**：不达标项处理

  - 若首帧延迟高 → 检查 SVGAParser cache / URLCache 是否生效
  - 若 fps 低 → 检查 SVGA loops 参数 / MTKView-like 逻辑
  - 若峰值内存高 → 检查 SVGAVideoEntity 是否复用 / YYEVA texture 分辨率

  发现的每个不达标项：**根因先查证（对齐 [root-cause-investigation.md](../../.claude/rules/root-cause-investigation.md)），不下游打补丁**。

- [ ] **Step 4**：结果记录到 [`docs/plan/H-礼物特效引擎-性能验证-YYYYMMDDHHMM.md`](docs/plan/)（新文档）

- [ ] **Step 5**：impl 期若发现新坑（如 UIWindow 与 CallKit 冲突 / SVGA replay 崩溃），沉淀新 rule 到 [`.claude/rules/`](../../.claude/rules/)

**验收条件**：spec §7 指标全达标；不达标项有 root-cause 记录 + fix / 明示延后。

---

## 完成后

- [ ] 全 tests pass（`xcodebuild test` 全绿）
- [ ] 4 场景真机 QA 全过
- [ ] Instruments 性能达标
- [ ] Memory Graph 无 Center 泄漏
- [ ] 骨架文件迁移完成
- [ ] `.claude/rules/` 新增 rule（若 impl 期发现新坑）
- [ ] docs/plan/ 加"性能验证记录"文档
- [ ] 若真集成反悔项 ≥1 → spec 补 §12 红队章节 or 加 rule

---

## 反悔归类模板（对齐 [`feature-pipeline-complexity-tier.md`](../../.claude/rules/feature-pipeline-complexity-tier.md) §"step 3 反悔归类"）

| 方向 | 含义 | 修复路径 |
|---|---|---|
| spec 漏 case | §1-§4 未覆盖的反向 | 回 spec 起草期升级 spec |
| 状态机错 | Center 不变量违反 | 回 Task 3 改 Center + 加 tc |
| View 结构错 | UIWindow / SwiftUI 层交互 | 回 Task 6/7 改 |
| 通用知识缺失 | SDK 特性 / iOS API | 沉淀新 rule |
| 范围扩展 | 用户要求加钻石福袋等 | spec 升级 + 独立 spec |
