# 流水线试运行 Checklist — 首页顶部 tab cycle (Trial #1)

> **本文件用途**：流水线 trial #1 期间的进度跟踪 + 验收门记录 + retrospective 载体。
> **不是**：skill / rule / 永久流程规范。trial 完成后由用户决策是否 codify。
>
> **会话交接说明**：新会话开局，read 本文件即可知道当前在哪步、上次卡在哪。每完成一步必须更新「当前进度」行。

---

## 当前进度

- **当前步骤**：步 0 待启动
- **上次更新**：2026-06-24 15:00
- **最近卡点**：无（trial 尚未启动）

---

## Trial 元信息

| 字段 | 内容 |
|---|---|
| Trial 名 | 首页顶部 tab cycle |
| 范围明确边界 | Home 页内部的分类子 tab（推荐/关注/附近 之类），**不是** RootView 全局 TabBar |
| Spec 文档路径 | TBD（步 0 完成后回填） |
| 关联里程碑 | A 基建（Home 模块） |
| 选作 trial 的理由 | UI + 状态机 + 切换交互坑，是入口枢纽，trial 失败损失局部、成功复利全局 |
| 已知 trial 局限 | 不含 RTC SDK 集成 — 步 3 难触发真实 SDK 假设推翻，codify 决策可能延后 |

---

## 流水线步骤

每步的"验收门"必须 100% 满足才能进下一步。记录区在 trial 期间填写。

### 步 0 — Spec

**产出清单**：
- [ ] 业务概念词表（不锁 protocol 名）
- [ ] H5/安卓二次校验小节（含校验结论：文档 vs 源码冲突时的取舍）
- [ ] 验收清单（正向场景）
- [ ] 验收清单（反向/边界场景）
- [ ] 复用候选标记（哪些代码应落 Core/，理由）

**红队外审**：
- [ ] 用 Plan agent 评审 spec（独立 context，不带本会话偏见）
- [ ] 评审产出的漏洞已补回 spec

**验收门**：
- [ ] 用户审过 spec 并签字

**记录区**：
- 实际过程：
- 卡点：
- 红队外审拦下的问题：
- 验收门是否有效：

---

### 步 1a — Store + 状态机 + protocol

**产出清单**：
- [ ] 词表翻译为 protocol（命名 + 接口签名）
- [ ] 状态机定义
- [ ] 正向路径单测
- [ ] 反向/边界态单测（非法迁移、回调乱序、并发态）

**验收门**：
- [ ] 单测全过
- [ ] 边界态覆盖率自检：spec 反向清单的每一项都有对应单测

**记录区**：
- 翻译过程是否发现 spec 词表漏概念（回补步 0）：
- 边界态单测拦下的 bug：
- 验收门是否有效：

---

### 步 1b — UI 还原

**产出清单**：
- [ ] 用 `/restore-design` 还原顶部 tab + 联动内容区视觉
- [ ] View 接 1a 的 protocol，**不接具体 Store**
- [ ] PreviewProvider 覆盖 spec 列出的所有合法状态（含空、加载、错误、各 tab 切换中间态）

**验收门**：
- [ ] Preview 跑出 spec 列出的全部合法状态
- [ ] View init 签名只依赖 1a 的 protocol

**记录区**：
- /restore-design 输出是否需要手动调整：
- Preview 漏覆盖的状态：
- 验收门是否有效：

---

### 步 1c — API model + 数据层 protocol + Fakes

**产出清单**：
- [ ] API request/response Codable models
- [ ] 数据层桥接 protocol（顶部 tab cycle 仅含 HTTP 数据接口，无 RTC SDK）
- [ ] Fakes 实现正常行为
- [ ] Fakes 实现异常行为（网络超时、返回非法字段、空列表、接口降级）

**验收门**：
- [ ] 用 Fakes 跑通步 1a 的所有反向用例
- [ ] Fakes 异常行为清单与 spec 反向场景一一对应

**记录区**：
- Fakes 覆盖时发现 spec 漏 case：
- 验收门是否有效：

---

### 步 2 — 接线

**产出清单**：
- [ ] 真 Store + Fakes 数据层接入真 View
- [ ] 跑完 spec 验收清单全部用例（含正向 + 反向）

**验收门**：
- [ ] spec 验收清单 100% 通过（在 Preview 或集成测中）

**记录区**：
- 接线时发现 protocol 设计需要调整（回 1a）：
- View 签名需要改（回 1b）：
- 验收门是否有效：

---

### 步 3 — 真集成（接真后端接口）

**产出清单**：
- [ ] Fakes 替换为真 APIClient
- [ ] Fakes vs 真接口行为对照测（contract test）

**反悔机制**：
每次推翻假设必须强制产出一份反思，反思指向：
- spec 漏 case → 回步 0
- 状态机错 → 回步 1a
- View 结构错（参考 v5.x SwiftUI 坑模式） → 回步 1b
- 通用知识缺失 → 沉淀 `.claude/rules/` + 继续

**验收门**：
- [ ] 真接口下 spec 验收清单全部通过
- [ ] 每次推翻假设有对应反思记录

**记录区**：
- 推翻假设次数与每次反思：
- `.claude/rules/` 新增条目清单：
- 验收门是否有效：

---

### 步 4 — 真机验收

**产出清单**：
- [ ] 用户在真机走完 spec 验收清单
- [ ] 清单外发现的 bug 不直接修，先回补 spec 验收清单再回测

**验收门**：
- [ ] 用户签字通过

**记录区**：
- 清单内通过率：
- 清单外发现的 bug 清单：
- 已回补的 spec 验收项：
- 验收门是否有效：

---

### 步 5 — 代码位置 Review（弱化版）

**产出清单**：
- [ ] 步 0 标记的"应落 Core/"代码确实落在 Core/
- [ ] 步 0 未标记但实际通用的代码 → 补标记 + 移位
- [ ] 步 0 标记"应落 Core/"但实际证明只此里程碑用 → 移回业务模块

**验收门**：
- [ ] 代码位置与最新复用判断一致

**注**：真复用验证不在本步——等下一个里程碑用到时回看本里程碑代码是否能直接复用。

**记录区**：
- 位置调整记录：
- 验收门是否有效：

---

### 步 6 — Trial Retrospective

按下方模板完成。

---

## Retrospective 模板

完成步 0~5 后填写本节。

### 各验收门有效性逐项核对

| 步骤 | 验收门 | 是否拦下过问题 | 拦下的问题描述 |
|---|---|---|---|
| 步 0 | 用户审 spec 签字 | ☐ 是 / ☐ 否 | |
| 步 0 | Plan agent 红队外审 | ☐ 是 / ☐ 否 | |
| 步 1a | 单测全过 + 边界态覆盖率自检 | ☐ 是 / ☐ 否 | |
| 步 1b | Preview 跑全状态 + View 仅依赖 protocol | ☐ 是 / ☐ 否 | |
| 步 1c | Fakes 跑通反向用例 | ☐ 是 / ☐ 否 | |
| 步 2 | spec 验收清单 100% 通过 | ☐ 是 / ☐ 否 | |
| 步 3 | 真接口验收清单通过 + 反思记录完整 | ☐ 是 / ☐ 否 | |
| 步 4 | 用户真机签字 | ☐ 是 / ☐ 否 | |
| 步 5 | 代码位置 review | ☐ 是 / ☐ 否 | |

**注**：所有"否"的验收门要么删除（形式主义），要么修改（无法触发），不能原样保留。

### 应删/改/合的步骤建议

- 删除候选：
- 修改候选：
- 合并候选：
- 拆分候选：

### `.claude/rules/` 新增条目清单（步 3 沉淀）

（如果步 3 没真触发假设推翻，本项可能为空，需要在 codify 决策中说明）

### 流水线本身的反思

- 哪步实际价值低于预期：
- 哪步实际价值高于预期：
- 流水线是否有应该完全换一种做法的部分：

---

## Codify 决策表（trial 完成后填写）

5 个 codify 触发条件：

| # | 条件 | 是否满足 | 说明 |
|---|---|---|---|
| 1 | 0~6 步骤无大改动 | ☐ 是 / ☐ 否 | |
| 2 | 每个验收门至少拦下过一次问题 | ☐ 是 / ☐ 否 | |
| 3 | `.claude/rules/` 有新增条目 | ☐ 是 / ☐ 否 | |
| 4 | retrospective 未出现"应完全换做法"反思 | ☐ 是 / ☐ 否 | |
| 5 | 步 3 至少经历过 1 次真实假设推翻并沉淀 | ☐ 是 / ☐ 否 | |

**Claude 建议**（trial 完成后填）：
- 是否 codify：
- 推荐形态：
- 理由：

**用户最终决策**（用户填）：
- 是否 codify：
- 形态：
- 备注：

---

## 文档版本

| 日期 | 修改内容 |
|---|---|
| 2026-06-24 15:00 | 初版（流水线三次修订定稿） |
