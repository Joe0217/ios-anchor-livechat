# A-2 新主播注册流程一期 Implementation Plan (v2)

> **v2 修订日志（2026-07-08）** — Plan agent 独立评审 round 1 反馈：
> - **BLOCK-P1（T1c.5）**：`SessionStore.shared.deviceId` 幻觉字段 → 实际项目里 `DeviceInfo.deviceId` (定义于 `Sources/Networking/APIClient.swift:238-242`)；改直读
> - **BLOCK-P2（T1c.1）**：`suppressCodes` 仅 login 传；register.submit 不传（1005 语义只在未注册 login 出现），加文档 note
> - **MAJOR-M1（T1c.2）**：`save()` 内部处理 AuthToken.value；applyLogin 需先设 user 再 save() 顺序对齐现有 login()，加实施 note
> - **MAJOR-M2**：补 T1a.8 承接 `RegisterValidationTests` + `LanguagePickerLogicTests`（spec §8.1 明确验收 4 类 test，v1 plan 漏 2）
> - **MAJOR-M3（T0 段）**：抓包受阻 fallback → 先 mock 跑 unit + T1c.5 前必须回填抓包结果
> - **MAJOR-M4**：T1b.1 三个 sheet 合一粒度过粗 → 拆 T1b.1a/1b/1c 独立 task；LanguagePickerSheet 单独 task 且 TDD 先写 test（承接 MAJOR-M2 的 LanguagePickerLogicTests）
> - **MINOR-N4（关键）**：v1 plan T1b.7 RegisterFlowView 顶层 NavigationStack 与 spec §3.2 v3 "LoginView 顶层持 path" 不一致 → 定死方案：**LoginView 顶层持 NavigationStack + $path；RegisterFlowView 不套 NavigationStack，仅作 4 view 分派容器（或直接把 4 view 作 LoginView.navigationDestination 的分派目标，省掉 RegisterFlowView 中间层）**
> - **MINOR-N1**：Task 总数 30 (T0*3 + T1a*8 + T1b*8 + T1c*8 + Step 2-6*5 组)
> - **MINOR-N2/N3**：test 用关键字参数构造 AnchorInfo；sessionRef Swift 严格并发 note
> - **MINOR-N5**：T0.3 合并到 T1c.1 首个 step
>
> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）or `superpowers:executing-plans` 逐 task 执行。Steps 用 `- [ ]` 复选框追踪。
>
> **Spec 依赖**：[docs/plan/A-2-spec-新主播注册流程-202607082033.md](./A-2-spec-新主播注册流程-202607082033.md)（v3, 1015 行）。本 plan **只讲怎么做**，spec 讲**做什么/为什么**——两者互补，执行者两份都要读。

**Goal**：为 iOS 主播端从零实现新版三页注册流程（基本信息 → 必填资料 → 视频录制），支持首次注册（hostRegisterV2）+ 被拒重录（hostReSubmitView），含 20s 现场录制 + 二次压缩到 ≤6MB + 三语 L10n；升级 UI 视觉还原度 95%+ 对齐蓝湖设计稿。

**Architecture**：三页 NavigationStack path 驱动 + 单例 `RegisterStore` 收敛表单/上传/录制进度；`SessionStore.login` 加 `applyLogin` 抽取 + 1005 pathway 改造让 login catch 分流不被 APIClient session-invalidated 通知拦截；视频用独立 `AVCaptureMovieFileOutput` 录 1080p mov + `AVAssetWriter` 二次压缩到 ~2.5Mbps ≤6MB mp4；图片复用现有 `ImageUploader` avatar/moment 预设。

**Tech Stack**：Swift 5 / SwiftUI iOS 16+ / AVFoundation / PhotosPicker / AVAssetWriter+AVAssetReader / 现有 APIClient (AES-128-CBC/PKCS7 加解密) / 现有 ImageUploader + PublicVideoUploader / 现有 KeychainStore / L10n Localizable.strings + InfoPlist.strings 三语 (en/ar/tr).

**Task 总数**：30 个 task（T0×2 + T1a×8 + T1b×10 + T1c×8 + Step 2-6 各 1 组共 5），分 7 组对齐 spec §9 pipeline step 0/1a/1b/1c/2/3/4/5/6。

**关键 rule 参考**：
- 每次 Sources/ 增删源文件 → `./bin/regen.sh`（xcodegen-podinstall-binding.md 铁律）
- `.claude/rules/swiftui-camera-preview.md` §6 ScenePhase 陷阱 + §7 AVCaptureSession 中断
- `.claude/rules/cross-scene-component-reuse-preflight.md`
- `.claude/rules/session-scoped-store-refresh.md`
- `.claude/rules/ios-decode-userid-compat.md`
- `.claude/rules/api-http-method-strict.md`
- `.claude/rules/toast-vs-banner-consistency.md`

---

## File Structure

### 新增文件（21 个源码 + 4 个 tests + 3 个 lproj key 追加）

```
Sources/Core/
└─ KeychainKey.swift                                # 跨类型共享 Keychain key 常量 namespace

Sources/Register/
├─ RegisterPathHolder.swift                         # @MainActor 单例，@Published var path = NavigationPath()；给 4 view 提供 EnvironmentObject 下传（v2 MINOR-N4）
├─ RegisterStore.swift                              # @MainActor 单例，表单 + 进度 + 生命周期
├─ RegisterService.swift                            # A1/A2/A3 接口封装（getCountryList / registerV2 / reSubmitView）
├─ RegisterModels.swift                             # Country / LanguageOption / PhotoUploadTask / VideoRecordState / RegisterRoute
├─ RegisterAnalytics.swift                          # 埋点桩（no-op + os.Logger）
├─ BasicInfo/
│   ├─ RegisterBasicInfoView.swift                  # Page 1 (对应 注册1.png)
│   ├─ RegisterAvatarPickerView.swift               # 圆形头像 + 相机叠标 + PhotosPicker
│   ├─ BirthdayPickerSheet.swift                    # 底部 sheet DatePicker(.wheels)
│   └─ CountryPickerSheet.swift                     # 底部 sheet List + 国旗
├─ Required/
│   ├─ RegisterRequiredView.swift                   # Page 2 (对应 必填资料-*.png)
│   ├─ LanguagePickerSheet.swift                    # 底部 sheet 7-chip grid（对应 选择语言-选中2个.png）
│   ├─ RegisterPhotosGrid.swift                     # 6 宫格 + 独立上传进度 + 重试
│   ├─ VideoGuideSheet.swift                        # 底部 sheet 引导文案（对应 视频录制1.png）
│   └─ VideoSlotView.swift                          # 单视频占位卡片
└─ Camera/
    ├─ RegisterVideoRecordView.swift                # Page 3 (对应 视频录制2.png) 全屏录制
    ├─ RegisterVideoPreviewView.swift               # Page 4 (对应 视频录制5-录制完成.png) 预览+Rerecord/Upload
    ├─ RegisterVideoRecorder.swift                  # AVCaptureSession + MovieFileOutput（1080p, 前置, 20s 硬约束）
    ├─ RegisterVideoCompressor.swift                # AVAssetWriter + AVAssetReader，1080p 2.5Mbps 精确控码率
    └─ RegisterCameraPreview.swift                  # UIViewRepresentable(AVCaptureVideoPreviewLayer)

Tests/Register/
├─ RegisterStoreTests.swift                         # begin/hydrate/reset/首次 vs resubmit 分支
├─ RegisterValidationTests.swift                    # 8 种校验规则
├─ RegisterVideoCompressorTests.swift               # fixture 10MB → ≤6MB / duration ±100ms
└─ LanguagePickerLogicTests.swift                   # 1-4 选下限/上限

Sources/en.lproj/Localizable.strings                # 追加约 30 个 register.* key
Sources/ar.lproj/Localizable.strings                # 追加同 key 阿拉伯语
Sources/tr.lproj/Localizable.strings                # 追加同 key 土耳其语

Sources/en.lproj/InfoPlist.strings                  # 追加 NSPhotoLibraryUsageDescription
Sources/ar.lproj/InfoPlist.strings                  # 同
Sources/tr.lproj/InfoPlist.strings                  # 同
```

### 修改文件（5 个）

```
Sources/Networking/APIClient.swift                  # +func post(..., suppressCodes: Set<String>) 参数
Sources/Auth/SessionStore.swift                     # +pendingRegister/needsResubmit @Published + applyLogin helper
Sources/Auth/LoginView.swift                        # NavigationStack(path: pathHolder.path) + 4 register view 分派 + onChange pendingRegister/needsResubmit
Sources/Profile/ProfileModels.swift                 # AnchorInfo 扩 6 字段 (email/birthday/inviteCode/language/phone/countryId)
project.yml                                         # Sources/Register/ 加 sources + Info.plist NSPhotoLibraryUsageDescription
```

### xcodegen + pod install 铁律触发点

**任何添加/删除 .swift 源文件后**必须 `./bin/regen.sh`（关 Xcode → xcodegen generate → pod install → sanity check → 开 workspace）。以下 task 结束后必跑：
- T1a.1-5（新增 Sources/Register/ 5 个非 View 模块）
- T1b.1-10（新增 10 个 View 文件）
- T1c.1（新增 KeychainKey.swift）
- T1c.6（改 project.yml）

**Sanity check**（rule 里更硬的验证）：`xcodebuild build -workspace Hily.xcworkspace -scheme Hily -destination 'generic/platform=iOS' -configuration Debug CODE_SIGNING_ALLOWED=NO` 必须 BUILD SUCCEEDED。

---

## Step 0: 前置抓包 & Model 扩展（约 2h）

### Task T0.1: 抓包 hostRegisterV2 / hostReSubmitView 字段

**Files:**
- Reference: `/Users/joe/Desktop/HN/anchor-livechat-h5/src/api/user/index.ts`（拿字面 path，对齐 `api-http-method-strict.md`）
- Update: `docs/plan/A-2-spec-新主播注册流程-202607082033.md` §1.1 A2/A3 body 字段清单

- [ ] **Step 1**：在生产环境 H5 中开一个新账号跑首次注册流程，用 Charles/Proxyman 抓 `hostRegisterV2` 请求：body 密文（Base64 AES-128-CBC）解密后拿完整字段清单
- [ ] **Step 2**：登录一个已被拒的主播账号，触发 hostReSubmitView 抓包同上
- [ ] **Step 3**：登录一个"审核中"的主播账号，抓 login 响应拿 userType 实际值（H5 login/index.vue:75-82 只判 2/9，其它值即视为审核中/被拒）
- [ ] **Step 4**：把抓包字段清单回写到 spec §1.1 A2/A3 body 表；把 userType 值回写到 spec §1.3

**回写标准**：spec §1.1 A2/A3 body 表格从"待抓包"变为具体字段列表（如 `iconUrl` 还是 `icon`、`pics` 还是 `picList`、language 逗号 join 还是数组）。

---

### Task T0.2: 抓包 mineInfo 并扩 AnchorInfo model 6 字段

**Files:**
- Modify: `Sources/Profile/ProfileModels.swift`
- Update: `docs/plan/A-2-spec-新主播注册流程-202607082033.md` §10 R6 状态

- [ ] **Step 1**：抓包 `/api/anchor/userInfo`（H5 拉 mineInfo 的接口）拿完整字段清单，尤其确认以下 6 字段的**接口字段名 + 类型**：
  - email (String)
  - birthday (String "yyyy-MM-dd" 还是 timestamp？)
  - inviteCode (String)
  - language (逗号分隔 String 还是数组？)
  - phone (String)
  - countryId (是 locale 还是 en 名？是 String 还是 Int？)

- [ ] **Step 2**：修改 `Sources/Profile/ProfileModels.swift` 在 `struct AnchorInfo` 补 6 字段（全部 `Optional` + 对齐现有字段命名风格）：

```swift
// AnchorInfo 内部追加（位置：countryCode 后）
let email: String?              // A-2 hydrate 用；接口字段名以抓包为准
let birthday: String?           // "yyyy-MM-dd"；接口字段以抓包为准
let phone: String?
let inviteCode: String?
let language: String?           // 逗号分隔（对齐 H5 register.js formData.language 逗号 join）
let countryId: String?          // 若接口 Int 则改 Int? 并用 String/Int 兼容 decode
```

- [ ] **Step 3**：**若** countryId 后端返 Int，用 `.claude/rules/ios-decode-userid-compat.md` decoder 模板做 String/Int 兼容 decode（改 AnchorInfo Codable 手动实现 init(from:)）
- [ ] **Step 4**：spec §10 R6 状态改为"已解决 - AnchorInfo 已扩 6 字段"
- [ ] **Step 5**：`./bin/regen.sh`（虽只改 model 不新增文件，但保险起见验证 pod install phase 无缺）
- [ ] **Step 6**：Commit

```bash
git add Sources/Profile/ProfileModels.swift docs/plan/A-2-spec-新主播注册流程-202607082033.md
git commit -m "feat: [注册] 扩 AnchorInfo model 6 字段供 A-2 hydrate resubmit 使用"
```

---

### ~~Task T0.3~~（v2 MINOR-N5 合并到 T1c.1 首 step）

`APIError.code: String` 已在 `Sources/Networking/APIClient.swift:7` 验证过；spec §3.2 pattern match `where e.code == "1005"` 直接保留。**本 task 无输出，删除**。

### T0 段 fallback（v2 MAJOR-M3）

若 T0.1-T0.2 抓包受阻（H5 生产环境访问不了 / 无测试账号），T1a 全部 unit test 可 mock AnchorInfo 与 RegisterSubmitBody 字段跑通；**但 T1c.5 前必须回填抓包结果**，否则 registerV2/reSubmitView 真接口调用会 400。abstract 依赖：只有 T0.1-T0.2 完成才能 T1c.5-T1c.8。

---

## Step 1a: 非 View 模块 + Unit tests（约 6h）

### Task T1a.1: KeychainKey namespace（v3 NEW-1）

**Files:**
- Create: `Sources/Core/KeychainKey.swift`

- [ ] **Step 1**：创建文件：

```swift
// Sources/Core/KeychainKey.swift
import Foundation

/// Keychain key 常量 namespace。
///
/// 跨类型（SessionStore ↔ RegisterStore）共享的 Keychain key 集中放这里，
/// 避免 private let scattered 在各类内导致跨类型引用编译不过（A-2 spec v3 NEW-1）。
enum KeychainKey {
    /// A-2 resubmit 场景：login → register 短期传密（Submit 成功清 + logout 清）
    static let pendingRegisterPassword = "session.pending.password.v1"
}
```

- [ ] **Step 2**：`./bin/regen.sh`（新增源文件，触发 pbxproj 重生）
- [ ] **Step 3**：`xcodebuild build -workspace Hily.xcworkspace -scheme Hily -destination 'generic/platform=iOS' -configuration Debug CODE_SIGNING_ALLOWED=NO` verify BUILD SUCCEEDED
- [ ] **Step 4**：Commit

```bash
git add Sources/Core/KeychainKey.swift project.yml Hily.xcodeproj Hily.xcworkspace
git commit -m "feat: [注册] 新增 KeychainKey namespace 供跨类型 keychain 常量共享"
```

---

### Task T1a.2: RegisterModels（数据结构 + 枚举）

**Files:**
- Create: `Sources/Register/RegisterModels.swift`
- Test: `Tests/Register/RegisterModelsTests.swift`（若有纯逻辑；否则合并到 RegisterStoreTests）

- [ ] **Step 1**：创建 `Sources/Register/RegisterModels.swift`：

```swift
import Foundation

// MARK: - 导航路由

enum RegisterRoute: Hashable {
    case basicInfo
    case required
    case videoRecord
    case videoPreview
}

// MARK: - Country（getCountryList 响应元素）

struct Country: Codable, Identifiable, Hashable {
    let locale: String       // "ES" / "US" / ...
    let en: String           // "Spain" / "United States"
    var id: String { locale }
    var flagAssetName: String { locale.lowercased() }   // 对齐 H5 flag png
}

// MARK: - Language（Page 2 语言选择）

/// 7 项候选（对齐 spec §0.5 + 设计稿排序）
enum RegisterLanguage: String, CaseIterable, Identifiable {
    case english = "English"
    case french = "French"
    case spanish = "Spanish"
    case russian = "Russian"
    case arabic = "Arabic"
    case hindi = "Hindi"
    case german = "German"
    var id: String { rawValue }
}

// MARK: - 照片上传任务

struct PhotoUploadTask: Identifiable, Equatable {
    let id: UUID
    let localData: Data
    var state: State
    enum State: Equatable {
        case uploading(progress: Double)
        case succeeded(url: String)
        case failed(error: String)
    }
    var isSucceeded: Bool {
        if case .succeeded = state { return true }
        return false
    }
    var succeededUrl: String? {
        if case .succeeded(let url) = state { return url }
        return nil
    }
}

// MARK: - 视频录制状态

enum VideoRecordState: Equatable {
    case idle
    case preparing
    case ready                                     // preview 就绪未开录
    case recording(elapsed: TimeInterval)          // 0..20
    case finished(localUrl: URL)                   // 录完 mov
    case failed(RegisterVideoError)
}

enum RegisterVideoError: Error, Equatable {
    case cameraDenied
    case microphoneDenied
    case sessionInterrupted
    case configFailed(String)
    case fileWriteFailed(String)
    case compressFailed(String)
    case uploadFailed(String)
}

// MARK: - 表单提交 body（内部构造用）

struct RegisterSubmitBody {
    // spec §1.1 A2/A3 抓包结果回写后，此结构对齐真接口字段名
    // T0.1 前保留 placeholder；抓包后 T1c.5 时对齐
    var email: String
    var password: String                           // 已 MD5 两次 upper（同 login pwd 处理）
    var iconUrl: String
    var nickname: String
    var birthday: String
    var countryId: String
    var inviteCode: String
    var language: String                           // 逗号 join
    var picList: [String]                          // 6 OSS URLs
    var videos: [String]                           // 1 OSS URL
    var gender: Int = 2
    var deviceId: String
    var phone: String = ""

    /// 转 [String: Any] 供 APIClient.post
    func toDict() -> [String: Any] {
        return [
            "email": email,
            "password": password,
            "icon": iconUrl,                       // 字段名以 T0.1 抓包为准
            "nickname": nickname,
            "birthday": birthday,
            "countryId": countryId,
            "inviteCode": inviteCode,
            "language": language,
            "picList": picList,
            "videos": videos,
            "gender": gender,
            "deviceId": deviceId,
            "phone": phone
        ]
    }
}
```

- [ ] **Step 2**：`./bin/regen.sh`
- [ ] **Step 3**：`xcodebuild build ...` verify SUCCEEDED
- [ ] **Step 4**：Commit

```bash
git add Sources/Register/ project.yml Hily.xcodeproj Hily.xcworkspace
git commit -m "feat: [注册] 新增 RegisterModels 数据结构（Route/Country/Language/PhotoTask/VideoState/SubmitBody）"
```

---

### Task T1a.3: RegisterStore（表单 + 进度 + 生命周期）

**Files:**
- Create: `Sources/Register/RegisterStore.swift`
- Test: `Tests/Register/RegisterStoreTests.swift`

- [ ] **Step 1**：创建 `Tests/Register/RegisterStoreTests.swift`（TDD 先写 test）：

```swift
import XCTest
@testable import Hily

@MainActor
final class RegisterStoreTests: XCTestCase {

    override func setUp() {
        super.setUp()
        RegisterStore.shared.reset()
    }

    func test_begin_落位_email_password() {
        RegisterStore.shared.begin(email: "test@example.com", password: "raw_pwd")
        XCTAssertEqual(RegisterStore.shared.email, "test@example.com")
        XCTAssertEqual(RegisterStore.shared.password, "raw_pwd")
        XCTAssertFalse(RegisterStore.shared.isResubmit)
    }

    func test_reset_全清_所有字段() {
        RegisterStore.shared.begin(email: "a@b.c", password: "pwd")
        RegisterStore.shared.nickname = "Coco"
        RegisterStore.shared.iconUrl = "https://oss/1.jpg"
        RegisterStore.shared.picUrls = ["u1", "u2"]
        RegisterStore.shared.videoUrl = "vurl"
        RegisterStore.shared.isResubmit = true

        RegisterStore.shared.reset()

        XCTAssertEqual(RegisterStore.shared.email, "")
        XCTAssertEqual(RegisterStore.shared.password, "")
        XCTAssertEqual(RegisterStore.shared.nickname, "")
        XCTAssertNil(RegisterStore.shared.iconUrl)
        XCTAssertEqual(RegisterStore.shared.picUrls, [])
        XCTAssertNil(RegisterStore.shared.videoUrl)
        XCTAssertFalse(RegisterStore.shared.isResubmit)
    }

    func test_hydrate_from_mineInfo_全映射_6字段() {
        // 假设 T0.2 已扩 AnchorInfo model；用**关键字参数**构造避免顺序脆性（v2 MINOR-N2）
        let mineInfo = AnchorInfo(
            userId: 1000001877,
            nickname: "Coco",
            icon: "https://oss/a.jpg",
            sex: 2,
            age: 35,
            countryCode: "ES",
            signature: nil, signatureVaild: nil,
            level: nil, levelName: nil, callPrice: nil,
            upsNum: nil, fansNum: nil, friendsNum: nil,
            pictures: nil, videos: nil,
            greetMsgs: nil, callVideoUrl: nil, giftList: nil,
            chatBubble: nil, activeTycoon: nil,
            // T0.2 扩字段：**插入位置在末尾（activeTycoon 之后）**（v2 MINOR-N2 约束）
            email: "coco@example.com",
            birthday: "1987-12-26",
            phone: "",
            inviteCode: "258 634 12",
            language: "English,Russian",
            countryId: "CentralAfrica"
        )
        RegisterStore.shared.hydrate(from: mineInfo, cachedPassword: "cached_pwd")

        XCTAssertEqual(RegisterStore.shared.email, "coco@example.com")
        XCTAssertEqual(RegisterStore.shared.password, "cached_pwd")
        XCTAssertEqual(RegisterStore.shared.nickname, "Coco")
        XCTAssertEqual(RegisterStore.shared.iconUrl, "https://oss/a.jpg")
        XCTAssertEqual(RegisterStore.shared.birthday, "1987-12-26")
        XCTAssertEqual(RegisterStore.shared.countryCode, "ES")
        XCTAssertEqual(RegisterStore.shared.inviteCode, "258 634 12")
        XCTAssertEqual(RegisterStore.shared.languages.sorted(), ["English", "Russian"].sorted())
    }

    func test_hydrate_cachedPassword_nil_时_从_Keychain_读() {
        _ = KeychainStore.setString("kc_pwd", for: KeychainKey.pendingRegisterPassword)
        defer { _ = KeychainStore.remove(for: KeychainKey.pendingRegisterPassword) }

        let mineInfo = /* 同上 minimal AnchorInfo */
        RegisterStore.shared.hydrate(from: mineInfo, cachedPassword: nil)
        XCTAssertEqual(RegisterStore.shared.password, "kc_pwd")
    }
}
```

- [ ] **Step 2**：Run test → Expected FAIL（RegisterStore 未定义）
- [ ] **Step 3**：创建 `Sources/Register/RegisterStore.swift`：

```swift
import Foundation
import SwiftUI

@MainActor
final class RegisterStore: ObservableObject {
    static let shared = RegisterStore()
    private init() {}

    // MARK: - 表单持久字段（对齐 H5 register.js formData）

    @Published var email: String = ""
    @Published var password: String = ""
    @Published var iconUrl: String? = nil
    @Published var nickname: String = ""
    @Published var birthday: String = ""
    @Published var countryCode: String? = nil
    @Published var countryName: String? = nil
    @Published var inviteCode: String = ""
    @Published var languages: [String] = []
    @Published var picUrls: [String] = []
    @Published var videoUrl: String? = nil
    @Published var gender: Int = 2
    @Published var deviceId: String = ""

    // MARK: - 进行态

    @Published var isAvatarUploading: Bool = false
    @Published var picUploadTasks: [PhotoUploadTask] = []
    @Published var localVideoOriginalUrl: URL? = nil
    @Published var localVideoCompressedUrl: URL? = nil
    @Published var videoCompressProgress: Double? = nil
    @Published var isVideoUploading: Bool = false
    @Published var isSubmitting: Bool = false
    @Published var submitError: String? = nil

    // MARK: - 场景标记

    @Published var isResubmit: Bool = false

    // MARK: - 生命周期

    func begin(email: String, password: String) {
        self.email = email
        self.password = password
    }

    /// v3 NEW-5：cachedPassword nil 时从 Keychain 兜底
    func hydrate(from mineInfo: AnchorInfo, cachedPassword: String? = nil) {
        self.email = mineInfo.email ?? ""
        self.password = cachedPassword
            ?? KeychainStore.getString(for: KeychainKey.pendingRegisterPassword)
            ?? ""
        self.iconUrl = mineInfo.icon
        self.nickname = mineInfo.nickname ?? ""
        self.birthday = mineInfo.birthday ?? ""
        self.countryCode = mineInfo.countryCode
        self.countryName = nil    // 由 CountryPickerSheet 拉列表后回填
        self.inviteCode = mineInfo.inviteCode ?? ""
        self.languages = (mineInfo.language ?? "").split(separator: ",")
            .map { String($0).trimmingCharacters(in: .whitespaces) }
            .filter { !$0.isEmpty }
        // picList / videos 不 hydrate 到 picUrls / videoUrl（对齐 spec §0.14：一期 iOS 简化，picList 尾部作 video 的 hack 不移植）
        self.picUrls = []
        self.videoUrl = nil
        self.gender = mineInfo.sex ?? 2
        // deviceId 由外部注入（施工时 T1c.5 里 SessionStore.deviceId 已有）
    }

    func reset() {
        email = ""
        password = ""
        iconUrl = nil
        nickname = ""
        birthday = ""
        countryCode = nil
        countryName = nil
        inviteCode = ""
        languages = []
        picUrls = []
        videoUrl = nil
        gender = 2
        deviceId = ""

        isAvatarUploading = false
        picUploadTasks = []
        localVideoOriginalUrl = nil
        localVideoCompressedUrl = nil
        videoCompressProgress = nil
        isVideoUploading = false
        isSubmitting = false
        submitError = nil

        isResubmit = false
    }
}
```

- [ ] **Step 4**：`./bin/regen.sh` + Run test → Expected PASS (4/4)
- [ ] **Step 5**：Commit

```bash
git add Sources/Register/RegisterStore.swift Tests/Register/RegisterStoreTests.swift project.yml
git commit -m "feat: [注册] 新增 RegisterStore（表单/进度/生命周期）+ hydrate/reset unit tests"
```

---

### Task T1a.4: RegisterService（getCountryList/registerV2/reSubmitView 接口封装）

**Files:**
- Create: `Sources/Register/RegisterService.swift`

- [ ] **Step 1**：创建：

```swift
import Foundation

/// A1/A2/A3 三接口封装。加解密走 APIClient.post 现有链路。
///
/// ⚠️ A2/A3 body 字段名以 T0.1 抓包结果为准；本文件里若与真接口不符请以 spec §1.1 抓包回写为准调整。
enum RegisterService {

    /// A1: 国家列表
    static func fetchCountryList() async throws -> [Country] {
        let data = try await APIClient.shared.post("/api/index/getCountryList", body: nil)
        return try JSONDecoder().decode([Country].self, from: data)
    }

    /// A2: 首次注册
    static func registerV2(body: RegisterSubmitBody) async throws -> LoginResult {
        let data = try await APIClient.shared.post("/api/login/register", body: body.toDict())
        return try JSONDecoder().decode(LoginResult.self, from: data)
    }

    /// A3: 被拒重录
    static func reSubmitView(body: RegisterSubmitBody) async throws -> LoginResult {
        let data = try await APIClient.shared.post("/api/login/reSubmitView", body: body.toDict())
        return try JSONDecoder().decode(LoginResult.self, from: data)
    }
}
```

- [ ] **Step 2**：`./bin/regen.sh` + `xcodebuild build ...` verify
- [ ] **Step 3**：Commit

```bash
git add Sources/Register/RegisterService.swift project.yml
git commit -m "feat: [注册] 新增 RegisterService 封装 getCountryList/registerV2/reSubmitView 三接口"
```

---

### Task T1a.5: RegisterVideoRecorder（AVCaptureSession + MovieFileOutput）

**Files:**
- Create: `Sources/Register/Camera/RegisterVideoRecorder.swift`

- [ ] **Step 1**：创建（骨架先编译通过，业务在 Step 2 补）：

```swift
import Foundation
import AVFoundation
import Combine
import os

@MainActor
final class RegisterVideoRecorder: NSObject, ObservableObject {

    @Published private(set) var state: VideoRecordState = .idle
    @Published private(set) var elapsed: TimeInterval = 0

    let session = AVCaptureSession()
    private var movieOutput = AVCaptureMovieFileOutput()
    private var timer: Timer?
    private let maxDuration: TimeInterval = 20.0
    private let logger = Logger(subsystem: "com.anchor.livechat", category: "RegisterRecorder")

    // MARK: - Public API

    /// 权限申请 + configure session + 起 preview（后台线程 startRunning）
    func prepare() async {
        state = .preparing
        // 相机权限
        let camera = await AVCaptureDevice.requestAccess(for: .video)
        guard camera else { state = .failed(.cameraDenied); return }
        // 麦克风权限
        let mic = await AVCaptureDevice.requestAccess(for: .audio)
        guard mic else { state = .failed(.microphoneDenied); return }

        do {
            try configureSession()
            // 后台线程 start 避免主线程阻塞
            // v2 MINOR-N3：Swift 严格并发若报 sessionRef 非 Sendable，改用 `sessionQueue.async { session.startRunning() }` 传统 pattern，或用 `nonisolated(unsafe) let sessionRef = session`
            let sessionRef = session
            await Task.detached(priority: .userInitiated) {
                sessionRef.startRunning()
            }.value
            state = .ready
        } catch {
            state = .failed(.configFailed("\(error)"))
        }
    }

    func startRecording() {
        guard state == .ready else { return }
        let url = outputTempURL()
        movieOutput.maxRecordedDuration = CMTime(seconds: maxDuration, preferredTimescale: 600)
        movieOutput.startRecording(to: url, recordingDelegate: self)

        elapsed = 0
        state = .recording(elapsed: 0)
        timer = Timer.scheduledTimer(withTimeInterval: 0.05, repeats: true) { [weak self] _ in
            Task { @MainActor in
                guard let self else { return }
                self.elapsed = min(self.maxDuration, self.elapsed + 0.05)
                self.state = .recording(elapsed: self.elapsed)
                if self.elapsed >= self.maxDuration {
                    self.timer?.invalidate()
                    self.timer = nil
                    // SDK maxRecordedDuration 已自动 stop，delegate 会在 finishRecording 里触发
                }
            }
        }
    }

    func stopRecording() {
        // v3 spec 一期不允许提前停；本方法仅供内部/测试使用
        movieOutput.stopRecording()
        timer?.invalidate()
        timer = nil
    }

    func teardown() {
        timer?.invalidate()
        timer = nil
        if session.isRunning { session.stopRunning() }
        // TODO: remove observers（wasInterrupted 在下一 step 加）
    }

    // MARK: - Private

    private func configureSession() throws {
        session.beginConfiguration()
        defer { session.commitConfiguration() }

        session.sessionPreset = .hd1920x1080

        guard let camera = AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: .front) else {
            throw NSError(domain: "RegisterRecorder", code: 1, userInfo: [NSLocalizedDescriptionKey: "no front camera"])
        }
        let videoInput = try AVCaptureDeviceInput(device: camera)
        if session.canAddInput(videoInput) { session.addInput(videoInput) }

        if let mic = AVCaptureDevice.default(for: .audio) {
            let audioInput = try AVCaptureDeviceInput(device: mic)
            if session.canAddInput(audioInput) { session.addInput(audioInput) }
        }

        if session.canAddOutput(movieOutput) { session.addOutput(movieOutput) }
        movieOutput.connection(with: .video)?.videoOrientation = .portrait
    }

    private func outputTempURL() -> URL {
        let name = "register-record-\(UUID().uuidString).mov"
        return URL(fileURLWithPath: NSTemporaryDirectory()).appendingPathComponent(name)
    }
}

// MARK: - Recording Delegate

extension RegisterVideoRecorder: AVCaptureFileOutputRecordingDelegate {
    nonisolated func fileOutput(_ output: AVCaptureFileOutput,
                                didFinishRecordingTo outputFileURL: URL,
                                from connections: [AVCaptureConnection],
                                error: Error?) {
        Task { @MainActor in
            self.timer?.invalidate()
            self.timer = nil
            // 错误 code -11810 是 maxRecordedDuration 到点正常 stop；其它错误视为失败
            if let error = error as NSError?,
               error.domain != AVFoundationErrorDomain || error.code != -11810 {
                self.state = .failed(.fileWriteFailed(error.localizedDescription))
                return
            }
            self.state = .finished(localUrl: outputFileURL)
        }
    }
}
```

- [ ] **Step 2**：`./bin/regen.sh` + `xcodebuild build ...` verify
- [ ] **Step 3**：Commit

```bash
git add Sources/Register/Camera/RegisterVideoRecorder.swift project.yml
git commit -m "feat: [注册] 新增 RegisterVideoRecorder AVCaptureSession + MovieFileOutput 骨架（1080p 前置 20s 硬约束）"
```

- [ ] **Step 4**（补 wasInterrupted 处理）：追加中断监听（同一 file，`prepare()` 内在 startRunning 前挂 observer）：

```swift
// 在 configureSession 后 startRunning 前，加：
NotificationCenter.default.addObserver(
    self,
    selector: #selector(handleWasInterrupted(_:)),
    name: .AVCaptureSessionWasInterrupted,
    object: session
)

@objc private func handleWasInterrupted(_ note: Notification) {
    Task { @MainActor in
        // 注册场景：任意 reason 中断都视为失败（对齐 spec §4.1 v3；与直播中断策略不同）
        if case .recording = state {
            movieOutput.stopRecording()
            state = .failed(.sessionInterrupted)
            timer?.invalidate()
            timer = nil
        }
    }
}
```

teardown() 里补 `NotificationCenter.default.removeObserver(self, name: .AVCaptureSessionWasInterrupted, object: session)`。

- [ ] **Step 5**：Commit

```bash
git add Sources/Register/Camera/RegisterVideoRecorder.swift
git commit -m "feat: [注册] RegisterVideoRecorder 加 wasInterrupted 分支（中断即视为失败）"
```

---

### Task T1a.6: RegisterVideoCompressor（AVAssetWriter 精确控码率）+ Unit tests

**Files:**
- Create: `Sources/Register/Camera/RegisterVideoCompressor.swift`
- Test: `Tests/Register/RegisterVideoCompressorTests.swift`

- [ ] **Step 1**：先写 test（TDD）：

```swift
// Tests/Register/RegisterVideoCompressorTests.swift
import XCTest
import AVFoundation
@testable import Hily

final class RegisterVideoCompressorTests: XCTestCase {

    /// fixture 10MB 1080p mov → 压缩后 ≤6MB / duration [19.5, 20.5]（v3 §8.1）
    func test_compress_1080p_20s_目标规格() async throws {
        guard let fixtureUrl = Bundle(for: type(of: self))
            .url(forResource: "fixture-1080p-20s", withExtension: "mov") else {
            XCTFail("Missing fixture. 补 fixture 步骤见 test 注释")
            return
        }

        var lastProgress: Double = 0
        let outUrl = try await RegisterVideoCompressor.compress(sourceUrl: fixtureUrl) { progress in
            lastProgress = progress
        }
        defer { try? FileManager.default.removeItem(at: outUrl) }

        let attrs = try FileManager.default.attributesOfItem(atPath: outUrl.path)
        let size = attrs[.size] as? Int ?? 0
        XCTAssertLessThanOrEqual(size, 6 * 1024 * 1024, "压缩输出应 ≤6MB，实测 \(size)")

        let asset = AVURLAsset(url: outUrl)
        let duration = try await asset.load(.duration).seconds
        XCTAssertEqual(duration, 20.0, accuracy: 0.5, "duration 应在 [19.5, 20.5]")

        XCTAssertGreaterThanOrEqual(lastProgress, 0.95)
    }
}
```

⚠️ Fixture 准备：在真机录一段 20s 前置 1080p mov (~10MB)，命名 `fixture-1080p-20s.mov`，放 `Tests/Register/Fixtures/` 并加入 project.yml test target resources。若跑测试时报 "Missing fixture"，先录 fixture 再重跑。

- [ ] **Step 2**：Run test → Expected FAIL（RegisterVideoCompressor 未定义 or Missing fixture）
- [ ] **Step 3**：创建 `Sources/Register/Camera/RegisterVideoCompressor.swift`：

```swift
import Foundation
import AVFoundation

/// 二次压缩：1080p mov (~10MB) → 1080p H.264 ~2.5Mbps mp4 (~5-6MB)。
/// 保留分辨率（画质 80% 感受），降码率（体积压缩 50%+）。
enum RegisterVideoCompressor {

    /// - parameter sourceUrl: 录制产出的 mov
    /// - parameter progressHandler: 0.0 → 1.0 主线程回调
    /// - returns: 压缩后 mp4 临时路径
    static func compress(
        sourceUrl: URL,
        progressHandler: @escaping @MainActor (Double) -> Void
    ) async throws -> URL {

        let outputUrl = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent("register-compressed-\(UUID().uuidString).mp4")
        try? FileManager.default.removeItem(at: outputUrl)

        let asset = AVURLAsset(url: sourceUrl)
        guard let videoTrack = try await asset.loadTracks(withMediaType: .video).first else {
            throw RegisterVideoError.compressFailed("no video track")
        }
        let audioTrack = try? await asset.loadTracks(withMediaType: .audio).first

        // AVAssetReader
        let reader = try AVAssetReader(asset: asset)
        let videoOutput = AVAssetReaderTrackOutput(
            track: videoTrack,
            outputSettings: [kCVPixelBufferPixelFormatTypeKey as String: kCVPixelFormatType_420YpCbCr8BiPlanarVideoRange]
        )
        reader.add(videoOutput)

        var audioOutput: AVAssetReaderTrackOutput?
        if let audioTrack {
            let output = AVAssetReaderTrackOutput(track: audioTrack, outputSettings: nil)
            reader.add(output)
            audioOutput = output
        }

        // AVAssetWriter (mp4)
        let writer = try AVAssetWriter(outputURL: outputUrl, fileType: .mp4)

        // Video input: 1080x1920 H.264 2.5Mbps
        let naturalSize = try await videoTrack.load(.naturalSize)
        let transform = try await videoTrack.load(.preferredTransform)
        // 前置录制 transform 通常是 rotate 90°，输出保 portrait
        let outputWidth = 1080
        let outputHeight = 1920

        let videoInput = AVAssetWriterInput(mediaType: .video, outputSettings: [
            AVVideoCodecKey: AVVideoCodecType.h264,
            AVVideoWidthKey: outputWidth,
            AVVideoHeightKey: outputHeight,
            AVVideoCompressionPropertiesKey: [
                AVVideoAverageBitRateKey: 2_500_000,
                AVVideoProfileLevelKey: AVVideoProfileLevelH264HighAutoLevel,
                AVVideoExpectedSourceFrameRateKey: 30
            ]
        ])
        videoInput.expectsMediaDataInRealTime = false
        videoInput.transform = transform
        writer.add(videoInput)

        // Audio input: passthrough AAC 128kbps
        var audioInput: AVAssetWriterInput?
        if audioOutput != nil {
            let input = AVAssetWriterInput(mediaType: .audio, outputSettings: [
                AVFormatIDKey: kAudioFormatMPEG4AAC,
                AVSampleRateKey: 44_100,
                AVNumberOfChannelsKey: 2,
                AVEncoderBitRateKey: 128_000
            ])
            input.expectsMediaDataInRealTime = false
            writer.add(input)
            audioInput = input
        }

        // 启动 read & write
        guard reader.startReading() else {
            throw RegisterVideoError.compressFailed("reader start failed: \(reader.error?.localizedDescription ?? "-")")
        }
        guard writer.startWriting() else {
            throw RegisterVideoError.compressFailed("writer start failed: \(writer.error?.localizedDescription ?? "-")")
        }
        writer.startSession(atSourceTime: .zero)

        let totalDuration = try await asset.load(.duration).seconds
        let queue = DispatchQueue(label: "register.compress.\(UUID().uuidString)")

        // 分别拉视频 + 音频（并行）
        async let videoDone: Void = pumpTrack(
            input: videoInput, output: videoOutput, queue: queue,
            totalDuration: totalDuration, progressHandler: progressHandler
        )

        if let audioInput, let audioOutput {
            async let audioDone: Void = pumpTrack(
                input: audioInput, output: audioOutput, queue: queue,
                totalDuration: totalDuration, progressHandler: { _ in }   // 音频 progress 不上报
            )
            _ = try await (videoDone, audioDone)
        } else {
            _ = try await videoDone
        }

        await writer.finishWriting()
        if writer.status == .failed {
            throw RegisterVideoError.compressFailed(writer.error?.localizedDescription ?? "writer failed")
        }

        await MainActor.run { progressHandler(1.0) }
        return outputUrl
    }

    /// 把 output 里的 sample buffer 依次拷贝到 input
    private static func pumpTrack(
        input: AVAssetWriterInput,
        output: AVAssetReaderTrackOutput,
        queue: DispatchQueue,
        totalDuration: Double,
        progressHandler: @escaping @MainActor (Double) -> Void
    ) async {
        await withCheckedContinuation { (cont: CheckedContinuation<Void, Never>) in
            input.requestMediaDataWhenReady(on: queue) {
                while input.isReadyForMoreMediaData {
                    guard let sample = output.copyNextSampleBuffer() else {
                        input.markAsFinished()
                        cont.resume()
                        return
                    }
                    let pts = CMSampleBufferGetPresentationTimeStamp(sample).seconds
                    if totalDuration > 0 {
                        let p = min(1.0, pts / totalDuration)
                        Task { @MainActor in progressHandler(p) }
                    }
                    _ = input.append(sample)
                }
            }
        }
    }
}
```

- [ ] **Step 4**：`./bin/regen.sh` + Run test → Expected PASS（若 fixture 补齐）

**Fixture 准备提示**：若 test target 没有 fixture 目录，先 `mkdir -p Tests/Register/Fixtures` + 录 20s 1080p mov 放入 + `project.yml` test target 加 `resources: [Tests/Register/Fixtures/]`。

- [ ] **Step 5**：Commit

```bash
git add Sources/Register/Camera/RegisterVideoCompressor.swift Tests/Register/RegisterVideoCompressorTests.swift project.yml
git commit -m "feat: [注册] 新增 RegisterVideoCompressor（AVAssetWriter 精确控 2.5Mbps）+ fixture 单测"
```

---

### Task T1a.7: RegisterAnalytics 桩

**Files:**
- Create: `Sources/Register/RegisterAnalytics.swift`

- [ ] **Step 1**：

```swift
import os

/// 一期埋点桩：no-op + os.Logger 打日志。
/// J 期接 ThinkingData 后一处改：把 `Logger.info` 换为 `ThinkingData.track(...)`
enum RegisterAnalytics {
    private static let logger = Logger(subsystem: "com.anchor.livechat", category: "RegisterAnalytics")

    static func report(_ event: Event, extra: [String: Any] = [:]) {
        logger.info("[Analytics] \(event.rawValue, privacy: .public) \(extra.description, privacy: .public)")
    }

    enum Event: String {
        case signUp = "h_signUp"                    // login → 1005 → push register
        case reviewInf = "h_reviewInf"              // Page 1 → Page 2
        case videoInf = "h_videoInf"                // 视频上传完成
        case appSign = "app_sign"                   // 注册接口成功
    }
}
```

- [ ] **Step 2**：`./bin/regen.sh` + build + Commit

```bash
git add Sources/Register/RegisterAnalytics.swift project.yml
git commit -m "feat: [注册] 新增 RegisterAnalytics 埋点桩（J 期接 ThinkingData）"
```

---

### Task T1a.8: RegisterValidationTests + LanguagePickerLogicTests（v2 MAJOR-M2 补挂）

**Files:**
- Create: `Tests/Register/RegisterValidationTests.swift`
- Create: `Tests/Register/LanguagePickerLogicTests.swift`

对齐 spec §8.1 4 类 Unit tests；v1 plan T1a.3/T1a.6 只承接了 2 类，另 2 类本 task 补。

- [ ] **Step 1**：`RegisterValidationTests` —— 覆盖 spec §6.4 8 校验规则（对应 spec `components/registerForm.vue:129-153` H5 校验逻辑）：

```swift
import XCTest
@testable import Hily

@MainActor
final class RegisterValidationTests: XCTestCase {

    var validator: RegisterFormValidator!    // 纯函数抽出（Sources/Register/RegisterFormValidator.swift）

    override func setUp() { super.setUp(); validator = RegisterFormValidator() }

    // Page 1 校验
    func test_head_missing_avatar_返错() { XCTAssertEqual(validator.validatePage1(iconUrl: nil, nickname: "A", birthday: "1990-01-01", countryCode: "US"), .missingAvatar) }
    func test_head_missing_nickname_trim_返错() { XCTAssertEqual(validator.validatePage1(iconUrl: "u", nickname: "   ", birthday: "1990-01-01", countryCode: "US"), .missingNickname) }
    func test_head_missing_birthday_返错() { XCTAssertEqual(validator.validatePage1(iconUrl: "u", nickname: "A", birthday: "", countryCode: "US"), .missingBirthday) }
    func test_head_missing_country_返错() { XCTAssertEqual(validator.validatePage1(iconUrl: "u", nickname: "A", birthday: "1990-01-01", countryCode: nil), .missingCountry) }
    func test_head_page1_全填_返_ok() { XCTAssertEqual(validator.validatePage1(iconUrl: "u", nickname: "A", birthday: "1990-01-01", countryCode: "US"), .ok) }

    // Page 2 校验
    func test_p2_missing_language_返错() { XCTAssertEqual(validator.validatePage2(languages: [], picUrls: Array(repeating: "u", count: 6), videoUrl: "v"), .missingLanguage) }
    func test_p2_missing_photos_少于6_返错() { XCTAssertEqual(validator.validatePage2(languages: ["English"], picUrls: Array(repeating: "u", count: 5), videoUrl: "v"), .missingPhotos) }
    func test_p2_missing_video_返错() { XCTAssertEqual(validator.validatePage2(languages: ["English"], picUrls: Array(repeating: "u", count: 6), videoUrl: nil), .missingVideo) }
    func test_p2_全填_返_ok() { XCTAssertEqual(validator.validatePage2(languages: ["English"], picUrls: Array(repeating: "u", count: 6), videoUrl: "v"), .ok) }

    // Nickname 15 字符边界
    func test_nickname_15_emoji_通() { XCTAssertTrue(validator.isNicknameLenValid(String(repeating: "😀", count: 15))) }
    func test_nickname_16_char_不通() { XCTAssertFalse(validator.isNicknameLenValid(String(repeating: "a", count: 16))) }
}

enum RegisterValidationResult: Equatable {
    case ok
    case missingAvatar, missingNickname, missingBirthday, missingCountry
    case missingLanguage, missingPhotos, missingVideo
}
```

- [ ] **Step 2**：先跑 test → Expected FAIL（`RegisterFormValidator` 未定义）
- [ ] **Step 3**：创建 `Sources/Register/RegisterFormValidator.swift` 承接：

```swift
struct RegisterFormValidator {

    func validatePage1(iconUrl: String?, nickname: String, birthday: String, countryCode: String?) -> RegisterValidationResult {
        if iconUrl?.isEmpty ?? true { return .missingAvatar }
        if nickname.trimmingCharacters(in: .whitespaces).isEmpty { return .missingNickname }
        if birthday.isEmpty { return .missingBirthday }
        if countryCode?.isEmpty ?? true { return .missingCountry }
        return .ok
    }

    func validatePage2(languages: [String], picUrls: [String], videoUrl: String?) -> RegisterValidationResult {
        if languages.isEmpty { return .missingLanguage }
        if picUrls.count < 6 { return .missingPhotos }
        if videoUrl?.isEmpty ?? true { return .missingVideo }
        return .ok
    }

    func isNicknameLenValid(_ s: String) -> Bool {
        // Swift String.count 是 grapheme cluster 单位，一个 emoji = 1 count（对齐 H5 maxlength 15 语义）
        return s.count <= 15
    }
}
```

- [ ] **Step 4**：Run test → Expected PASS 11/11

- [ ] **Step 5**：`LanguagePickerLogicTests` —— 覆盖 spec §8.1 语言选 1-4 上下限：

```swift
import XCTest
@testable import Hily

final class LanguagePickerLogicTests: XCTestCase {

    /// 选中 <4 未选下允许选：返 true（可添加）
    func test_可添加_当已选3_返true() {
        XCTAssertTrue(LanguagePickerLogic.canAdd(current: ["English", "French", "Spanish"]))
    }

    /// 选中已 4 未选下不允许选：返 false（应 toast "You can only choose four"）
    func test_不可添加_当已选4_返false() {
        XCTAssertFalse(LanguagePickerLogic.canAdd(current: ["English", "French", "Spanish", "Russian"]))
    }

    /// Confirm 计数 = 已选数
    func test_confirm_计数_返_已选数() {
        XCTAssertEqual(LanguagePickerLogic.confirmCount(current: ["English", "Russian"]), 2)
        XCTAssertEqual(LanguagePickerLogic.confirmCount(current: []), 0)
    }
}
```

- [ ] **Step 6**：先跑 test → FAIL；创建 `Sources/Register/Required/LanguagePickerLogic.swift`：

```swift
enum LanguagePickerLogic {
    static let maxSelection = 4
    static func canAdd(current: [String]) -> Bool { current.count < maxSelection }
    static func confirmCount(current: [String]) -> Int { current.count }
}
```

- [ ] **Step 7**：Run test → PASS 3/3

- [ ] **Step 8**：`./bin/regen.sh` + Commit

```bash
git add Sources/Register/RegisterFormValidator.swift Sources/Register/Required/LanguagePickerLogic.swift Tests/Register/RegisterValidationTests.swift Tests/Register/LanguagePickerLogicTests.swift project.yml
git commit -m "feat: [注册] 补 RegisterFormValidator + LanguagePickerLogic 纯函数 + unit tests (14/14)"
```

---

## Step 1b: 10 个 View 组件（无真接口，可编译 + mock 走通 UI 流）（约 12h）

**通用规则**：
- 每个 view 用 `RegisterStore.shared` 作 `@ObservedObject` 或 `@EnvironmentObject`
- 每个 view 完成后 `./bin/regen.sh`（新增源码）
- 每 3 个 view 一次 commit（避免 commit 太散）

### Task T1b.1a: BirthdayPickerSheet（v2 MAJOR-M4 拆分）

**Files:**
- Create: `Sources/Register/BasicInfo/BirthdayPickerSheet.swift`

- [ ] **Step 1**：完整实现 DatePicker(.wheel) + range 1970-今 + 确认按钮 + `presentationDetents([.height(320)])`（对齐 spec §2.2 Page 1 birthday field）：

```swift
struct BirthdayPickerSheet: View {
    @Binding var isPresented: Bool
    @Binding var birthday: String   // "yyyy-MM-dd"
    @State private var date: Date = Date()

    private let dateRange: ClosedRange<Date> = {
        let start = DateComponents(calendar: .init(identifier: .gregorian), year: 1970, month: 1, day: 1).date!
        return start...Date()
    }()

    private let formatter: DateFormatter = {
        let f = DateFormatter()
        f.dateFormat = "yyyy-MM-dd"
        return f
    }()

    var body: some View {
        VStack {
            DatePicker("", selection: $date, in: dateRange, displayedComponents: .date)
                .datePickerStyle(.wheel)
                .labelsHidden()
            HStack {
                Button(L10n.commonCancel) { isPresented = false }
                Spacer()
                Button(L10n.commonConfirm) {
                    birthday = formatter.string(from: date)
                    isPresented = false
                }
            }
            .padding()
        }
        .presentationDetents([.height(320)])
    }
}
```

- [ ] **Step 2**：`./bin/regen.sh` + build + Commit

```bash
git add Sources/Register/BasicInfo/BirthdayPickerSheet.swift project.yml
git commit -m "feat: [注册] BirthdayPickerSheet 底部 sheet DatePicker(.wheel) range 1970-今"
```

---

### Task T1b.1b: CountryPickerSheet（v2 MAJOR-M4 拆分）

**Files:**
- Create: `Sources/Register/BasicInfo/CountryPickerSheet.swift`

- [ ] **Step 1**：布局对齐 H5 `components/countrys.vue`（列表 + 国旗 asset + tap 回填）：

```swift
struct CountryPickerSheet: View {
    @Binding var isPresented: Bool
    let onPick: (Country) -> Void

    @State private var countries: [Country] = []
    @State private var isLoading = true
    @State private var loadError: String? = nil

    var body: some View {
        NavigationStack {
            Group {
                if isLoading {
                    ProgressView()
                } else if let err = loadError {
                    VStack {
                        Text(err).foregroundStyle(.red)
                        Button("Retry") { Task { await fetch() } }
                    }
                } else {
                    List(countries) { country in
                        Button {
                            onPick(country)
                            isPresented = false
                        } label: {
                            HStack {
                                if let img = UIImage(named: country.flagAssetName) {
                                    Image(uiImage: img).resizable().frame(width: 24, height: 16)
                                }
                                Text(country.en)
                                Spacer()
                            }
                        }
                    }
                }
            }
            .task { await fetch() }
            .navigationTitle("Select country")
            .toolbar { ToolbarItem(placement: .topBarLeading) { Button("Cancel") { isPresented = false } } }
        }
    }

    @MainActor
    func fetch() async {
        isLoading = true; loadError = nil
        defer { isLoading = false }
        do {
            countries = try await RegisterService.fetchCountryList()
        } catch {
            loadError = error.localizedDescription
        }
    }
}
```

**⚠️ 依赖**：flag png asset 是否已在 Assets.xcassets？若无，从 H5 `src/assets/flag/*.png` 拷 68 张过来，或改用 Unicode flag emoji fallback。

- [ ] **Step 2**：`./bin/regen.sh` + build + Commit

```bash
git add Sources/Register/BasicInfo/CountryPickerSheet.swift project.yml
git commit -m "feat: [注册] CountryPickerSheet 列表 + flag asset + retry"
```

---

### Task T1b.1c: LanguagePickerSheet（v2 MAJOR-M4 拆分，先 TDD 承接 T1a.8 LanguagePickerLogic）

**Files:**
- Create: `Sources/Register/Required/LanguagePickerSheet.swift`

- [ ] **Step 1**：布局对齐 `选择语言-选中2个.png`：3-col LazyVGrid，7 chip，选中粉底✓ / 未选深底；顶部 Cancel/Language/Confirm(N)。

```swift
struct LanguagePickerSheet: View {
    @Binding var isPresented: Bool
    @Binding var selected: [String]

    let all = RegisterLanguage.allCases

    var body: some View {
        VStack(spacing: 20) {
            HStack {
                Button(L10n.commonCancel) { isPresented = false }
                Spacer()
                Text("Language").font(.headline)
                Spacer()
                Button("Confirm ( \(LanguagePickerLogic.confirmCount(current: selected)) )") {
                    isPresented = false
                }.foregroundStyle(Color.pink)
            }
            LazyVGrid(columns: [.init(), .init(), .init()], spacing: 12) {
                ForEach(all) { lang in
                    LanguageChip(
                        text: lang.rawValue,
                        selected: selected.contains(lang.rawValue),
                        onTap: { toggle(lang.rawValue) }
                    )
                }
            }
            Spacer()
        }
        .padding()
        .presentationDetents([.fraction(0.45)])
    }

    func toggle(_ lang: String) {
        if let idx = selected.firstIndex(of: lang) {
            selected.remove(at: idx)   // 取消已选
        } else if LanguagePickerLogic.canAdd(current: selected) {
            selected.append(lang)      // 未达上限则添加
        } else {
            // 上限达 4：toast "You can only choose four"（走 T1c.7 L10n key: register.error.languageMax）
        }
    }
}

struct LanguageChip: View { /* 粉底选中态 / 深底未选态 */ }
```

- [ ] **Step 2**：`./bin/regen.sh` + build + Commit

```bash
git add Sources/Register/Required/LanguagePickerSheet.swift project.yml
git commit -m "feat: [注册] LanguagePickerSheet 3-col grid + confirm(N) + 1-4 上下限"
```

---

### Task T1b.2: RegisterAvatarPickerView

**Files:**
- Create: `Sources/Register/BasicInfo/RegisterAvatarPickerView.swift`

- [ ] **Step 1**：圆形头像 + 右下角相机叠标 + `PhotosPicker(maxSelectionCount: 1)`。选中 → 通过 `ImageUploader.shared.upload(rawData:preset:.avatar)` → 存 `RegisterStore.shared.iconUrl`。上传中显示 ProgressView 遮罩。视觉对齐 `注册1.png` / `注册-填写好状态.png`。
- [ ] **Step 2**：build + Commit

---

### Task T1b.3: RegisterBasicInfoView（Page 1）

**Files:**
- Create: `Sources/Register/BasicInfo/RegisterBasicInfoView.swift`

- [ ] **Step 1**：布局对齐 `注册1.png`：CustomBackBar / Avatar / Nickname(0/15) / Birthday+Country 并排 / RequiredFieldsRow（chevron） / InviteCode(optional) / 底部 "Sign Up" 按钮。RequiredFieldsRow tap → `path.append(.required)` 通过 `@EnvironmentObject var flowPath: NavigationPath`（在 T1b.7 flow view 里注入）。
- [ ] **Step 2**：Sign Up tap 时做 Page 1 验证（对齐 spec §2.2 Page 1 验证规则），失败 toast，成功 append(.required)。
- [ ] **Step 3**：build + Commit

---

### Task T1b.4: RegisterPhotosGrid + VideoSlotView

**Files:**
- Create: `Sources/Register/Required/RegisterPhotosGrid.swift`
- Create: `Sources/Register/Required/VideoSlotView.swift`

- [ ] **Step 1**：`RegisterPhotosGrid` —— 2 行 3 列 LazyVGrid，cell size 约 100pt。每格：占位 `+` / 上传中 ProgressView / 完成 KFImage 缩略图 / 失败 ❌+重试。`PhotosPicker(maxSelectionCount: 6-filled)` 触发添加。
- [ ] **Step 2**：`VideoSlotView` —— 单卡片 ~150×200pt。未录 → 灰底 + 摄像机 SF Symbol；已录 → `AsyncImage(url: videoThumbUrl)` + 播放按钮 + 右上 ×。tap → 弹 VideoGuideSheet。
- [ ] **Step 3**：build + Commit

---

### Task T1b.5: VideoGuideSheet + RegisterRequiredView（Page 2）

**Files:**
- Create: `Sources/Register/Required/VideoGuideSheet.swift`
- Create: `Sources/Register/Required/RegisterRequiredView.swift`

- [ ] **Step 1**：`VideoGuideSheet` —— 对齐 `视频录制1.png` 半透明 sheet；文案走 `L10n.registerVideoGuideBody`；`Go to Record` 按钮 dismiss + `path.append(.videoRecord)`。
- [ ] **Step 2**：`RegisterRequiredView` —— 对齐 `必填资料-未填时.png` 布局：3 sections（Language / Photos / Video）+ 底部 "Upload" / "Edit" 按钮。tap Upload → 验证 → `RegisterStore.shared.submit()`（T1c.5 里接线）。
- [ ] **Step 3**：build + Commit

---

### Task T1b.6: RegisterCameraPreview + RegisterVideoRecordView（Page 3）

**Files:**
- Create: `Sources/Register/Camera/RegisterCameraPreview.swift`
- Create: `Sources/Register/Camera/RegisterVideoRecordView.swift`

- [ ] **Step 1**：`RegisterCameraPreview` —— `UIViewRepresentable`，makeUIView 里创建 UIView + `AVCaptureVideoPreviewLayer(session: recorder.session)` 添加为 sublayer，`videoGravity = .resizeAspectFill`；updateUIView no-op。dismantleUIView 无需 tearDown（recorder 生命周期由 view 管理，见 T1b.6 step 3）。

```swift
struct RegisterCameraPreview: UIViewRepresentable {
    let recorder: RegisterVideoRecorder

    func makeUIView(context: Context) -> UIView {
        let view = UIView()
        let layer = AVCaptureVideoPreviewLayer(session: recorder.session)
        layer.videoGravity = .resizeAspectFill
        layer.frame = UIScreen.main.bounds
        view.layer.addSublayer(layer)
        context.coordinator.previewLayer = layer
        return view
    }

    func updateUIView(_ uiView: UIView, context: Context) {
        context.coordinator.previewLayer?.frame = uiView.bounds
    }

    func makeCoordinator() -> Coordinator { Coordinator() }
    final class Coordinator {
        var previewLayer: AVCaptureVideoPreviewLayer?
    }
}
```

- [ ] **Step 2**：`RegisterVideoRecordView` —— 对齐 `视频录制2.png`：全屏 `RegisterCameraPreview` + `ZStack` overlay 顶部 back / 顶部倒计时胶囊 / 底部圆形录制按钮。用 `@StateObject var recorder = RegisterVideoRecorder()`。onAppear `Task { await recorder.prepare() }`。

- [ ] **Step 3**（v3 MAJOR-3 修）：onDisappear 加 ScenePhase 守卫：

```swift
@Environment(\.scenePhase) private var scenePhase
// ...
.onDisappear {
    // v3 MAJOR-3：切后台走 wasInterrupted 已 stop，preview 由 iOS 挂起；仅真正离开页面才 tearDown
    guard scenePhase != .background else { return }
    recorder.teardown()
}
```

- [ ] **Step 4**：Back tap → 若 recorder.state == .recording → alert "Discard recording?"；yes → recorder.stopRecording() + pop
- [ ] **Step 5**：录完（recorder.state == .finished）→ `RegisterStore.shared.localVideoOriginalUrl = url` + `path.append(.videoPreview)`；同时后台 Task 触发 `RegisterVideoCompressor.compress(...)` → 存 `localVideoCompressedUrl`
- [ ] **Step 6**：build + Commit

---

### Task T1b.7: RegisterVideoPreviewView + PathHolder（Page 4，v2 MINOR-N4 定死顶层容器方案）

**Files:**
- Create: `Sources/Register/Camera/RegisterVideoPreviewView.swift`
- Create: `Sources/Register/RegisterPathHolder.swift`
- **不新建** `RegisterFlowView`（v1 有此文件设计；v2 撤销——为对齐 spec §3.2 v3 "LoginView 顶层持 NavigationStack" 的设计）

**顶层容器方案定死**（v2 MINOR-N4）：
- **LoginView** 顶层 `NavigationStack(path: $path)` + `.navigationDestination(for: RegisterRoute.self) { switch route ... }` 4 case
- **RegisterFlowView 撤销**（避免"两层 NavigationStack 嵌套"陷阱）；4 个 register view 直接作 LoginView.navigationDestination 分派目标
- `path` 由 `RegisterPathHolder`（`@MainActor final class RegisterPathHolder: ObservableObject { @Published var path = NavigationPath() }`）单例持有，作 `@EnvironmentObject` 下传给所有 register view；子 view append 时 `pathHolder.path.append(.required)` 即可

- [ ] **Step 1**：`RegisterPathHolder`：

```swift
import SwiftUI

@MainActor
final class RegisterPathHolder: ObservableObject {
    static let shared = RegisterPathHolder()
    @Published var path = NavigationPath()

    /// 注册流程结束（Submit 成功 or 用户主动全 pop 回 login）时清 path，让下次进注册重新从 basicInfo 起
    func reset() { path = NavigationPath() }
}
```

- [ ] **Step 2**：`RegisterVideoPreviewView` —— 对齐 `视频录制5-录制完成.png`：

```swift
struct RegisterVideoPreviewView: View {
    @EnvironmentObject var store: RegisterStore
    @EnvironmentObject var pathHolder: RegisterPathHolder

    var body: some View {
        VStack {
            /* CustomBackBar */
            /* VideoPlayer(url: store.localVideoCompressedUrl ?? store.localVideoOriginalUrl) */
            /* 压缩中显示 ProgressView(value: store.videoCompressProgress) */
            HStack {
                Button("Re-record") {
                    store.localVideoOriginalUrl = nil
                    store.localVideoCompressedUrl = nil
                    store.videoCompressProgress = nil
                    pathHolder.path.removeLast()   // pop 回 videoRecord
                }
                Button("Upload") {
                    Task { await uploadVideo() }
                }.disabled(store.videoCompressProgress != 1.0)
            }
        }
    }

    @MainActor
    func uploadVideo() async {
        guard let compressedUrl = store.localVideoCompressedUrl,
              let data = try? Data(contentsOf: compressedUrl) else { return }
        store.isVideoUploading = true
        defer { store.isVideoUploading = false }
        do {
            let ossUrl = try await PublicVideoUploader.shared.upload(videoData: data, fileExtension: "mp4")
            store.videoUrl = ossUrl
            RegisterAnalytics.report(.videoInf)
            // pop 两次回 Required（.videoRecord → .videoPreview 两级）
            pathHolder.path.removeLast(2)
        } catch {
            // toast register.error.uploadFailed
        }
    }
}
```

- [ ] **Step 3**：`./bin/regen.sh` + build + Commit

```bash
git add Sources/Register/RegisterPathHolder.swift Sources/Register/Camera/RegisterVideoPreviewView.swift project.yml
git commit -m "feat: [注册] RegisterVideoPreviewView 预览+重录+Upload；PathHolder 单例持 NavigationPath（v2 MINOR-N4 定死顶层容器）"
```

---

### Task T1b.8: mock 走通 UI 流验收

- [ ] **Step 1**：编一个 preview scaffold 或临时入口（`HilyApp.swift` 里加一个 debug button "Debug → Register"）绕过 LoginView 直接 `RegisterPathHolder.shared.path.append(.basicInfo)` + `RegisterStore.shared.begin(email:"dev@t.co", password:"dev")`；LoginView 主体保留但套一层 NavigationStack path 让分派生效
- [ ] **Step 2**：真机跑：能否从 Page 1 → sheet → Page 2 → sheet → Page 3 录制 → Page 4 预览 → 回 Page 2 全流程 UI 走通
- [ ] **Step 3**：拿真机截图对比 10 张设计稿主图，粗校对（不做像素级对齐，那是 step 5 事）
- [ ] **Step 4**：清 debug 入口。**不 commit**（临时入口用完弃）

---

## Step 1c: 接口串联 + APIClient/SessionStore 改造 + LoginView 接线（约 6h）

### Task T1c.1: APIClient suppressCodes 参数

**Files:**
- Modify: `Sources/Networking/APIClient.swift`

**语义边界**（v2 BLOCK-P2 note）：`suppressCodes` **仅 SessionStore.login 传** `["1005"]`；`RegisterStore.submit`（走 registerV2/reSubmitView）**不传**，因 1005 语义只在"未注册 login"出现；已登录状态调 register 接口正常 401 走 handleSessionInvalidated logout 是**正确**行为。施工者不要在 register.submit 里画蛇添足传 suppressCodes。

- [ ] **Step 0**：grep `APIError` 确认 `let code: String`（v1 T0.3 合并到这里，MINOR-N5）：`grep -nE "struct APIError|let code" Sources/Networking/APIClient.swift` 应命中 L6-8。
- [ ] **Step 1**：修改 `post(...)` 函数签名，加 `suppressCodes: Set<String> = []` 参数：

```swift
// APIClient.swift 已有 3 个重载（post/get/delete），全部加 suppressCodes 参数
func post(_ path: String, body: [String: Any]? = nil, token: String? = nil,
          suppressCodes: Set<String> = []) async throws -> Data {
    // 转到 postJSON
}

private func postJSON(_ path: String, jsonBody: Any?, token: String?,
                      suppressCodes: Set<String>) async throws -> Data {
    // 现有实现
    // ...
    // L82 附近：
    if code == "1004" || code == "1005", !suppressCodes.contains(code) {
        NotificationCenter.default.post(...)
    }
    throw APIError(code: code, message: ...)
}
```

同理修改 GET/DELETE 路径的 code 分流逻辑（L175 附近同款）。

- [ ] **Step 2**：build + 跑现有其它模块的接口测试（比如 login 用旧签名 catch 1004 是否还正常）
- [ ] **Step 3**：Commit

```bash
git add Sources/Networking/APIClient.swift
git commit -m "feat: [注册] APIClient +suppressCodes 参数（A-2 spec v3 BLOCK-1）"
```

---

### Task T1c.2: SessionStore.applyLogin helper 抽出

**Files:**
- Modify: `Sources/Auth/SessionStore.swift`

**顺序 note**（v2 MAJOR-M1）：现有 `save()` 依赖 `guard let user = user else { return }`（L152-155）—— **applyLogin 必须先 `user = result` 再 `save()`**，反了 save 会 no-op。对齐现有 `login()` L83-85 顺序即可。

- [ ] **Step 1**：抽出现有 `login()` L83-93 的"设 user + save + 埋点 + AnchorInfoStore.refresh + AppConfigStore.activate"为独立 helper（对齐 spec §3.3）：

```swift
@discardableResult
func applyLogin(_ result: LoginResult) async -> Bool {
    guard let token = result.token, !token.isEmpty else { return false }
    user = result
    isLoggedIn = true
    save()
    AppLogger.auth.info("[LOGIN OK] userId=\(result.userId ?? -1, privacy: .private)")
    Task { await AnchorInfoStore.shared.refresh() }
    Task { await AppConfigStore.shared.activate() }
    return true
}
```

`login()` 内改为调 `applyLogin(result)` + 保留 token 空守卫（若 applyLogin 返 false 则展示 errorMessage）。

- [ ] **Step 2**：build + Commit

```bash
git add Sources/Auth/SessionStore.swift
git commit -m "refactor: [Auth] 抽 SessionStore.applyLogin helper 供 login + register 复用（A-2 spec v3 MAJOR-4）"
```

---

### Task T1c.3: SessionStore.pendingRegister / needsResubmit + login 1005/审核态分支

**Files:**
- Modify: `Sources/Auth/SessionStore.swift`

- [ ] **Step 1**：加 @Published + struct + login 分支扩展（对齐 spec §3.2 v3 完整代码）：

```swift
// 新增
struct PendingRegister: Equatable {
    let email: String
    let password: String
}

@Published var pendingRegister: PendingRegister? = nil
@Published var needsResubmit: (loginResult: LoginResult, mineInfo: AnchorInfo)? = nil

// login() 内 do{} 里：
let data = try await APIClient.shared.post(
    "/api/login/v4/login",
    body: ["email": email, "password": pwd],
    suppressCodes: ["1005"]                                 // v3 BLOCK-1
)
let result = try JSONDecoder().decode(LoginResult.self, from: data)

guard await applyLogin(result) else {
    errorMessage = L10n.authErrorNoToken
    return
}

if result.userType != 2 && result.userType != 9 {
    _ = KeychainStore.setString(password, for: KeychainKey.pendingRegisterPassword)
    await AnchorInfoStore.shared.refresh()                  // 显式 await 而非 fire-and-forget（v3 NEW-4）
    if let mineInfo = AnchorInfoStore.shared.mine {
        needsResubmit = (result, mineInfo)
    }
}

// catch 分支加：
catch let e as APIError where e.code == "1005" {
    pendingRegister = PendingRegister(email: email, password: password)
}
```

- [ ] **Step 2**：logout() 里加 `_ = KeychainStore.remove(for: KeychainKey.pendingRegisterPassword)` + `RegisterStore.shared.reset()`
- [ ] **Step 3**：build + Commit

```bash
git add Sources/Auth/SessionStore.swift
git commit -m "feat: [Auth] SessionStore +pendingRegister/needsResubmit + 1005/审核态分支（A-2 spec §3.2 v3）"
```

---

### Task T1c.4: LoginView NavigationStack + 4 register view 分派（v2 MINOR-N4）

**Files:**
- Modify: `Sources/Auth/LoginView.swift`

对齐 spec §3.2 v3 完整代码 + T1b.7 定死方案：**LoginView 唯一持 NavigationStack**，4 个 register view 作 navigationDestination 分派目标；path 通过 `RegisterPathHolder.shared` 提供。

- [ ] **Step 1**：改造 `LoginView` 外层套 `NavigationStack(path: $pathHolder.path)`：

```swift
struct LoginView: View {
    @EnvironmentObject var session: SessionStore
    @StateObject var pathHolder = RegisterPathHolder.shared
    @StateObject var registerStore = RegisterStore.shared

    var body: some View {
        NavigationStack(path: $pathHolder.path) {
            LoginContent(/* ... */)  // 原 LoginView body 抽 subview
                .onChange(of: session.pendingRegister) { pending in
                    if let p = pending {
                        registerStore.begin(email: p.email, password: p.password)
                        pathHolder.path.append(RegisterRoute.basicInfo)
                        session.pendingRegister = nil                    // 消费
                    }
                }
                .onChange(of: session.needsResubmit) { rs in
                    if let (_, mineInfo) = rs {
                        let cachedPwd = KeychainStore.getString(for: KeychainKey.pendingRegisterPassword)
                        registerStore.hydrate(from: mineInfo, cachedPassword: cachedPwd)
                        registerStore.isResubmit = true
                        pathHolder.path.append(RegisterRoute.basicInfo)
                        session.needsResubmit = nil
                    }
                }
                .navigationDestination(for: RegisterRoute.self) { route in
                    Group {
                        switch route {
                        case .basicInfo: RegisterBasicInfoView()
                        case .required: RegisterRequiredView()
                        case .videoRecord: RegisterVideoRecordView()
                        case .videoPreview: RegisterVideoPreviewView()
                        }
                    }
                    .environmentObject(registerStore)
                    .environmentObject(pathHolder)
                }
        }
    }
}
```

- [ ] **Step 2**：build + Commit

```bash
git add Sources/Auth/LoginView.swift
git commit -m "feat: [Auth] LoginView 顶层 NavigationStack + 4 register view 分派（v2 MINOR-N4 定死）"
```

---

### Task T1c.5: RegisterStore.submit + Country 拉取接线

**Files:**
- Modify: `Sources/Register/RegisterStore.swift`
- Modify: `Sources/Register/BasicInfo/CountryPickerSheet.swift`
- Modify: `Sources/Register/Required/RegisterRequiredView.swift`

- [ ] **Step 1**：`RegisterStore` 加 `submit()`：

```swift
func submit() async {
    guard !isSubmitting else { return }
    isSubmitting = true
    defer { isSubmitting = false }

    // 构 body
    let body = RegisterSubmitBody(
        email: email,
        password: CryptoUtil.loginPassword(password),   // 两次 upper MD5
        iconUrl: iconUrl ?? "",
        nickname: nickname,
        birthday: birthday,
        countryId: countryCode ?? "",                    // 以 T0.1 抓包字段值为准（可能是 en 名 or locale）
        inviteCode: inviteCode,
        language: languages.joined(separator: ","),
        picList: picUrls,
        videos: [videoUrl].compactMap { $0 },
        gender: gender,
        deviceId: DeviceInfo.deviceId,                   // v2 BLOCK-P1 修：SessionStore.deviceId 幻觉字段；实际是 DeviceInfo.deviceId (Sources/Networking/APIClient.swift:238-242)
        phone: ""
    )

    do {
        let result = try await (isResubmit
            ? RegisterService.reSubmitView(body: body)
            : RegisterService.registerV2(body: body))

        guard await SessionStore.shared.applyLogin(result) else {
            submitError = L10n.authErrorNoToken
            return
        }
        _ = KeychainStore.remove(for: KeychainKey.pendingRegisterPassword)
        RegisterAnalytics.report(.appSign)
        reset()
    } catch let e as APIError {
        submitError = e.message
    } catch {
        submitError = String(format: L10n.authErrorNetworkFormat, error.localizedDescription)
    }
}
```

- [ ] **Step 2**：`CountryPickerSheet` onAppear 拉 `RegisterService.fetchCountryList()`；错误显示 Retry。
- [ ] **Step 3**：`RegisterRequiredView` Upload 按钮 tap → 验证 → `Task { await store.submit() }`；观察 `store.submitError` 显示 banner。
- [ ] **Step 4**：Commit

---

### Task T1c.6: project.yml + InfoPlist.strings 三语（v3 MAJOR-2）

**Files:**
- Modify: `project.yml`
- Modify: `Sources/en.lproj/InfoPlist.strings`
- Modify: `Sources/ar.lproj/InfoPlist.strings`
- Modify: `Sources/tr.lproj/InfoPlist.strings`

- [ ] **Step 1**：`project.yml` Info.plist section 加 `NSPhotoLibraryUsageDescription`（对齐现有 `NSCameraUsageDescription` 语法）：

```yaml
info:
  path: Sources/Info.plist
  properties:
    NSPhotoLibraryUsageDescription: $(PRODUCT_BUNDLE_IDENTIFIER)
```

（或直接写死字符串，看现有配置风格）

- [ ] **Step 2**：三个 InfoPlist.strings 加：

```
// en
NSPhotoLibraryUsageDescription = "Access to your photo library is required to upload your avatar and profile photos during registration.";
// ar
NSPhotoLibraryUsageDescription = "يتطلب الوصول إلى مكتبة الصور لتحميل صورتك الرمزية وصور ملفك الشخصي أثناء التسجيل.";
// tr
NSPhotoLibraryUsageDescription = "Kayıt sırasında profil resminizi ve profil fotoğraflarınızı yüklemek için fotoğraf kitaplığına erişim gereklidir.";
```

- [ ] **Step 3**：`./bin/regen.sh` → 真机装应用触发相册 → 应看到系统权限弹窗+新描述
- [ ] **Step 4**：Commit

```bash
git add project.yml Sources/*.lproj/InfoPlist.strings
git commit -m "feat: [注册] +NSPhotoLibraryUsageDescription 三语 InfoPlist.strings"
```

---

### Task T1c.7: Localizable.strings 三语 30 keys

**Files:**
- Modify: `Sources/en.lproj/Localizable.strings`
- Modify: `Sources/ar.lproj/Localizable.strings`
- Modify: `Sources/tr.lproj/Localizable.strings`
- Modify: `Sources/L10n.swift`（新 key 的 accessor）

- [ ] **Step 1**：spec §7.1 30 keys 全部加入 en.lproj/Localizable.strings（先英文，ar/tr T4 全批一起翻）
- [ ] **Step 2**：`L10n.swift` 加 accessors（`static var registerTitleBasicInfo: String { NSLocalizedString("register.title.basicInfo", comment: "") }`）
- [ ] **Step 3**：所有 view 里硬编码文案换 L10n 引用（BasicInfo / Required / Video 三批 view）
- [ ] **Step 4**：build + verify en 环境 UI 文案显示 OK（ar/tr 走 T4）
- [ ] **Step 5**：Commit

---

### Task T1c.8: I1 全链路首次注册跑通

- [ ] **Step 1**：创新账号（H5 环境 dev）→ iOS 输入 email/password → 登录挂 1005 → 应触发 pathHolder.path.append(.basicInfo)
- [ ] **Step 2**：Page 1 → 头像 上传成功 → 昵称 / 生日 / 国家 / 邀请码 → Sign Up
- [ ] **Step 3**：Page 2 → 语言选 3 项 → 6 张相册照片全上传成功 → 视频录制 20s → 二次压缩完成 → Upload 视频成功
- [ ] **Step 4**：Page 2 底部 Upload tap → registerV2 接口返 200 → SessionStore.applyLogin → 跳首页
- [ ] **Step 5**：验证首页拉主播数据（AnchorInfoStore.refresh 触发）+ 新账号看到"审核中"状态
- [ ] **Step 6**：若 I1 通，Commit 一个 tag：`git tag v-A2-M1-e2e-passing`

---

## Step 2: 真集成 I2-I12 12 case（约 8h）

对每个 case 走真机手动验证，问题回归到 spec §9 step 3 反悔归类里。

- [ ] **Step 1**：I2 被拒重录全链路（新账号 dev 后端把 userType 改为审核中/被拒模拟）
- [ ] **Step 2**：I3-I5 三个权限拒绝路径（Settings 里手动关权限重跑）
- [ ] **Step 3**：I6 录制切后台（录到 10s 切后台 → 回前台 → 应显示 toast "录制中断请重试"）
- [ ] **Step 4**：I7 录制中 tap Back → alert → discard
- [ ] **Step 5**：I8 单张照片上传断网重试
- [ ] **Step 6**：I9 mock Submit 500（Charles 篡改响应）→ banner + 保持 Upload 可点
- [ ] **Step 7**：I10 视频压缩规格：录一段真机 20s → 检查输出 mp4 ≤6MB + `AVAsset` duration [19.5, 20.5]
- [ ] **Step 8**：I11 三语切（Settings > 语言切 en/ar/tr 三种），ar 环境验证 leading/trailing 镜像
- [ ] **Step 9**：I12 昵称 15 字符边界（15 emoji / 15 汉字 / 15 英文各测一次）

**验收标准**：12 case 全过。任一挂请回归到 step 3 反悔归类修完再重跑。

---

## Step 3: Red team 反悔归类 & 修复

- [ ] **Step 1**：把 I1-I12 挂掉的 case 按 spec §9 step 3 五方向归类（spec 漏 case / 状态机错 / View 结构错 / 通用知识缺失 / 范围扩展）
- [ ] **Step 2**：对每一挂逐个修 + 重跑对应 case verify
- [ ] **Step 3**：若挂点属"通用知识缺失"类，沉淀到 `.claude/rules/`（step 6 会执行）
- [ ] **Step 4**：全 pass 后 Commit

---

## Step 4: L10n ar/tr 全量 + ar 镜像视觉

- [ ] **Step 1**：调 `/i18n translate` skill（or 手翻）把 30 个 register.* keys 从英文翻 ar / tr，写入对应 lproj
- [ ] **Step 2**：真机切 ar 环境重跑 I11：
  - 布局 leading/trailing 是否正确镜像（用 `.leading`/`.trailing` 而非 `.left`/`.right`）
  - Confirm(N) 数字格式
  - 长句 wrap（阿拉伯语加长约 30%）
- [ ] **Step 3**：修任何镜像/文案挂点
- [ ] **Step 4**：Commit

```bash
git add Sources/ar.lproj/ Sources/tr.lproj/
git commit -m "feat: [注册] L10n ar+tr 全量翻译 30 keys + ar 镜像验证"
```

---

## Step 5: 视觉回归 10 场景对比设计稿

- [ ] **Step 1**：真机跑到 10 个截图基线场景（spec §8.3 列表），每个截图（Xcode Instruments Screenshot 或 Simulator ⌘S）
- [ ] **Step 2**：对比设计稿主图（`/Users/joe/Downloads/注册UI/*.png`）用图像对比工具（PixelSnap / Sketch Compare）
- [ ] **Step 3**：像素误差 >5% 的场景列 punch list：
  - 字号 / 行距 / 颜色 / spacing / 圆角
- [ ] **Step 4**：逐条修（Theme.Palette 补色 + font size / padding 精调）
- [ ] **Step 5**：重跑 verify <5%
- [ ] **Step 6**：Commit

---

## Step 6: 沉淀 & 归档

- [ ] **Step 1**：step 3 归类到"通用知识缺失"的挂点写成 `.claude/rules/*.md`
- [ ] **Step 2**：更新 `docs/plan/路线图执行追踪-*.md` 里 A 里程碑：A-2 完成
- [ ] **Step 3**：spec §12 完成定义 checklist 全打勾
- [ ] **Step 4**：spec 归档 → `docs/plan/archive/A-2-spec-新主播注册流程-202607082033.md`（对齐现有 archive 命名）
- [ ] **Step 5**：本 plan 归档 → `docs/plan/archive/A-2-plan-新主播注册流程-202607082033.md`
- [ ] **Step 6**：最后 Commit

```bash
git add docs/plan/ .claude/rules/
git commit -m "docs: [注册] A-2 完成 - 归档 spec/plan + 更新路线图追踪 + 沉淀新 rule"
```

---

## 关键实施纪律

1. **每次 Sources/ 加删源文件后必 `./bin/regen.sh`**（xcodegen-podinstall-binding.md 铁律）；skip 会导致 `No such module 'AgoraRtcKit'` 或 `Switch must be exhaustive` 误报
2. **改代码前先 read**（CLAUDE.md 用户偏好）；改完 read 回校验中文完整性（encoding-check.md）
3. **cross-scene-component-reuse-preflight**：任何"这个组件好像可以复用"念头出现时，先 grep 目标文件 `@StateObject|CameraManager|ignoresSafeArea` 判自持依赖
4. **root-cause-investigation**：改 2 次失败 → 停手看 log 头部证据链，不再下游打补丁
5. **api-http-method-strict**：所有 API 引用先 grep H5 `src/api/user/index.ts` 拿字面 path，不推理
6. **Never commit unless explicitly asked**（用户 CLAUDE.md 铁律）：本 plan 里所有 `git commit` step 只在用户明示"提交"时执行；开发过程可先局部改 → 用户 review → 用户说 commit 才 commit
7. **swiftui-camera-preview §6/§7**：录制页 onDisappear 加 ScenePhase 守卫；wasInterrupted 分支 stop 录制

---

## Task 依赖图

```
T0.1 → T0.2 → T1a.2 (RegisterModels)
              ↓
T0.3 →      T1a.1 (KeychainKey) → T1a.3 (RegisterStore) → T1a.4 (RegisterService)
                                                        → T1a.5 (Recorder)
                                                        → T1a.6 (Compressor)
                                                        → T1a.7 (Analytics)
                                                        ↓
      T1b.1 (Sheets) + T1b.2 (Avatar) + T1b.3 (BasicInfo) + T1b.4 (Grid/Slot) +
      T1b.5 (Guide+Required) + T1b.6 (Record) + T1b.7 (Preview+Flow) + T1b.8 (mock 走通)
                                                        ↓
      T1c.1 (APIClient) + T1c.2 (applyLogin) + T1c.3 (login 分支) + T1c.4 (LoginView) +
      T1c.5 (submit 接线) + T1c.6 (InfoPlist) + T1c.7 (L10n) + T1c.8 (I1 e2e)
                                                        ↓
      Step 2 (I2-I12) → Step 3 (反悔) → Step 4 (L10n ar/tr) → Step 5 (视觉) → Step 6 (沉淀)
```

估计总耗时（对齐 spec §9）：约 8-10 person-day。

---

## 完成定义（同 spec §12）

- [ ] step 0-6 全过（含反悔归类完成 + step 6 沉淀）
- [ ] Unit tests + Integration tests I1-I12 全通
- [ ] 视觉回归 10 场景 <5% 误差
- [ ] 三语（en/ar/tr）L10n 齐 + ar 镜像正确
- [ ] 冷启动 + logout + 首次注册 + 被拒重录 4 大路径真机跑通
- [ ] 视频压缩指标达标（≤6MB / 20s ±0.5s）
- [ ] 路线图追踪文档 A 里程碑更新 A-2 完成
- [ ] 反悔类沉淀新 rule（若有）
