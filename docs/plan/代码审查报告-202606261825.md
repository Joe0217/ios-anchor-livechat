# Hily iOS 主播端 代码审查报告

> 审查时间：2026-06-26 18:25
> 审查范围：本会话 IM 场景门未提交部分（8 个 swift 文件）—— [IMSceneFilter.swift](../../Sources/Live/NIM/IMSceneFilter.swift) / [IMSceneGate.swift](../../Sources/Live/NIM/IMSceneGate.swift) / [NIMService.swift](../../Sources/Live/NIM/NIMService.swift) / [LiveStore.swift](../../Sources/Live/LiveStore.swift) / [CallStore.swift](../../Sources/Call/CallStore.swift) / [PartyStore.swift](../../Sources/Party/PartyStore.swift) / [SessionStore.swift](../../Sources/Auth/SessionStore.swift) / [IMSceneFilterTests.swift](../../Tests/HilyTests/IMSceneFilterTests.swift)
> 审查方式：5 维并行 finder（3 维 API 失败，2 维成功）+ 3 票对抗验证 + 强制二次复查（[code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§6/§7）+ 自审补足失败维度
> 审查依据：CLAUDE.md + .claude/rules/*

---

## 一、概览

| 等级 | 数量 | 说明 |
|---|---|---|
| **P0（必须改）** | 0 | — |
| **P1（应该改）** | 0 | — |
| **P2（可以改）** | **1** | concurrency（Task @MainActor 显式标） |
| **超出 scope 剔除** | 5 | finder 输出 5 项均不在本会话改动 hunk 内 |

**对照**：finder 原始输出 5 项（全 P2）→ 二次复查后 **0 项必修 / 1 项建议（自审补足）**。

---

## 二、本会话改动唯一发现

### 建议 1 — IMSceneGate.exit 内 Task 漏显式 @MainActor，与工程约定不一致

- 文件：[Sources/Live/NIM/IMSceneGate.swift](../../Sources/Live/NIM/IMSceneGate.swift):58
- 维度：concurrency（finder 失败维度，自审补出）
- 复查证据：
  - CallStore 内 `Task { @MainActor [weak self] in` 模式出现 **11 次（全部显式）**——是工程统一约定
  - 当前 IMSceneGate:58 `Task { [weak self] in` 未显式标，依赖 Swift 5 默认 actor inheritance（编译通过 ✓ build SUCCEEDED 验证）
  - **触发场景真实**：每次通话挂断（state→.idle 后 8s grace）都创建 Task → Task body 内 `self.active.remove(scene)` 写 main actor isolated 字段
  - Swift 6 严格并发模式下会编译报错；当前 Swift 5 是 silent UB 风险
- 风险等级：**建议**（当前 Swift 5 build SUCCEEDED + tests 全过实际运行 OK；但破坏工程约定一致性 + Swift 6 升级时会出错）
- 修复（≤2 行）：

```swift
// Sources/Live/NIM/IMSceneGate.swift:58
pendingExitTasks[scene.rawValue] = Task { @MainActor [weak self] in
    try? await Task.sleep(nanoseconds: UInt64(delay * 1_000_000_000))
    guard !Task.isCancelled else { return }
    // ... 余下不变
}
```

---

## 三、Workflow finder 输出二次复查（全部剔除）

按 [code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §3 + 用户限定「仅查本会话改动且未提交的部分」，以下 5 项均不在本会话改动 hunk 内。

| Finder ID | finder 描述 | 真实位置 | 在本会话 hunk 内？ | 复查证据 | 判定 |
|---|---|---|---|---|---|
| P2-1 | SessionStore followIncrementCount / lastAuditStatus @Published 触发广播 | [SessionStore.swift](../../Sources/Auth/SessionStore.swift):16-18 字段定义 + line 116-128 函数 | ❌ | git diff 显示 `+followIncrementCount` 等是从上轮累积起的累积 hunk，本会话只追加了 logout 末尾 1 行 `IMSceneGate.shared.resetAll()` | **超出 scope 剔除** |
| P2-2 | POCDebugView 整 CallStore @ObservedObject | [POCDebugView.swift](../../Sources/Home/POCDebugView.swift):10 | ❌ | POCDebugView.swift 不在本会话 scope 8 文件清单内；finder 越界自由扩展上下文 | **finder 越界剔除** |
| P2-3 | RootView 整 CallStore @StateObject 1Hz 重算 | [RootView.swift](../../Sources/Home/RootView.swift):7-24 | ❌ | RootView.swift 不在 scope；finder 越界 | **finder 越界剔除** |
| P2-4 | LiveStore.warn() Task 未标 @MainActor 多余 actor hop | [LiveStore.swift](../../Sources/Live/LiveStore.swift):222-230 | ❌ | warn() 函数及其 Task 写法是上轮累积；本会话改动只有 `state` 的 didSet block（约 line 14-26） | **超出 scope 剔除** |
| P2-5 | PartyRoomView 整 PartyStore @ObservedObject lastGiftEvent 穿透 | [PartyRoomView.swift](../../Sources/Party/UI/PartyRoomView.swift):25,53 | ❌ | PartyRoomView.swift 不在 scope；finder 越界 | **finder 越界剔除** |

**复查注**：finder 越界（P2-2/P2-3/P2-5 文件根本不在 scope）+ 上轮累积误判为本会话产出（P2-1/P2-4）是 workflow 5 维 finder 自由探索上下文的常见副作用。按 rule §7「工具产出 ≠ 二次复查」，必须按 scope 精切。

---

## 四、自审 3 个失败维度（finder API 失败，主流程补足）

Workflow 3 维 finder 失败（security / concurrency / ios-spec，API Connection closed），主流程按 skill 验收标准对本会话改动手动补审：

### concurrency 维度

| 检查项 | 结果 |
|---|---|
| IMSceneGate.exit Task 风格统一 | ❌ **见上方 建议 1** |
| IMSceneFilter 纯函数线程安全 | ✅ 纯函数无并发依赖 |
| NIMService.dispatch 改造 @MainActor 保持 | ✅ 原 @MainActor 上下文未破坏 |
| 4 store didSet 内 sync 调 gate | ✅ @MainActor class 内 didSet 串行 |
| IMSceneGate.resetAll Task cancel | ✅ 所有 cancel 在 main actor 串行 |

### security 维度

| 检查项 | 结果 |
|---|---|
| IMSceneFilter 输入敏感性 | ✅ enum + Date，零 PII |
| IMSceneGate 日志隐私（scene/grace/delay）| ✅ Int / Bool / TimeInterval 全 .public 合理 |
| NIMService.dispatch logger（attachType.raw / context）| ✅ 无 userId / token / nickname 敏感字段 |
| mustReceive 集合泄漏风险 | ✅ 编译期常量，无运行时注入 |

### ios-spec 维度

| 检查项 | 结果 |
|---|---|
| Task.sleep(nanoseconds:) iOS 兼容 | ✅ iOS 16+ 通用 API |
| Date() / addingTimeInterval | ✅ 无新 API 引入 |
| @MainActor + ObservableObject 用法 | ✅ 符合 SwiftUI 模式 |
| Privacy Manifest 数据采集影响 | ✅ 无新数据采集，PrivacyInfo.xcprivacy 不需更新 |

---

## 五、复查统计

| 项 | 数量 |
|---|---|
| Workflow finder 原始输出 | 5（全 P2） |
| 二次复查后必修 | **0** |
| 二次复查后建议 | **1**（自审补出，IMSceneGate Task 显式 @MainActor） |
| 二次复查后信息 | 0 |
| 超出 scope 剔除 | 5（finder 越界 3 + 上轮累积 2） |
| Finder API 失败维度 | 3（security / concurrency / ios-spec）→ 主流程自审补足 |

---

## 六、建议处理

**唯一项**：[Sources/Live/NIM/IMSceneGate.swift](../../Sources/Live/NIM/IMSceneGate.swift):58 Task 加显式 `@MainActor`，与 CallStore 11/11 显式标的工程约定一致 + 防 Swift 6 strict 升级时编译错。改动 ≤ 2 行，可在下次小批 commit 一并修。

**不修**：5 项 finder 输出全部超出 scope，按用户限定「仅本会话改动」不动；若用户后续要审上轮累积 / 顶层架构问题，可另开 `/review deep diff` 全文件范围审查。

---

## 七、说明

- 本报告由 `/review deep 仅查 本会话改动且未提交的部分` 自动生成
- Workflow 2/5 维成功（swiftui-perf / memory）+ 主流程自审补 3 失败维度（concurrency / security / ios-spec）
- 二次复查按 [code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §1/§6/§7 强制
- finder 越界（POCDebugView / RootView / PartyRoomView 不在 scope）暴露 workflow 上下文扩展副作用——已在剔除决策里说明
