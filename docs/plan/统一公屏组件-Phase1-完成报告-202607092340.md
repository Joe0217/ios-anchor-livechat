# 统一公屏组件 · Phase 1 完成报告（2026-07-09）

## 交付摘要

Phase 1（直播接入）已完成 T1-T11 共 11 个任务。**BUILD SUCCEEDED + 12 单测全绿**。剩余 T12（i18n）+ T13（真机验收）交接下次会话。

## 已交付内容

### 新模块 [Sources/PublicChat/](../../Sources/PublicChat/)

```
Sources/PublicChat/
├─ Model/
│   ├─ Enums.swift                          # NicknameColor / PartyRole / AnnouncementKind / ModeSwitchKind / Mention / PublicChatDiamondGiftSubType / GameWinPayload
│   ├─ RichSegment.swift                    # PK notify 富文本片段（text/iconURL/highlight）
│   ├─ SenderProfile.swift                  # 跨场景昵称/等级/VIP/Host/勋章/角色
│   ├─ PublicChatVariant.swift              # 17 变体枚举 + Discriminator
│   └─ UnifiedPublicChatMessage.swift       # id + timestamp + sender + variant
├─ Store/
│   └─ UnifiedPublicChatFeed.swift          # append/appendBatch/replace/clear/trim (limit 200)
├─ UI/
│   ├─ PublicChatTheme.swift                # .live（Phase 2/3 待接入 .call/.party）
│   ├─ PublicChatListView.swift             # ScrollViewReader + LazyVStack + suffix(80) + autoScroll
│   ├─ PublicChatRow.swift                  # 3 组 @ViewBuilder 分派避免 SwiftUI type-check timeout
│   ├─ Rows/                                # 13 个 Row（H5 file:line 严格对齐）
│   │   ├─ RowRegularText.swift             # H5 L367 灰半透气泡 + Lv/VIP + 青绿昵称
│   │   ├─ RowAnchor.swift                  # H5 L348 紫渐变 anchor-box + host + 粉昵称
│   │   ├─ RowGift.swift                    # H5 L486 灰半透 + gift 20pt + xN
│   │   ├─ RowLuckyGift.swift               # H5 L474 粉黄渐变 h50w268 + 富文本
│   │   ├─ RowEnterRoom.swift               # H5 L565 tier 渐变 + Lv/VIP + 昵称 + 座驾
│   │   ├─ RowOfficialBoostEnter.swift      # H5 L586 蓝渐变 + Official Boost✨ #FFE600
│   │   ├─ RowPKNotify.swift                # H5 L519 暗红 D33901/30 + RichSegment
│   │   ├─ RowRpsWin.swift                  # H5 L549 紫渐变 rounded-14 + 双行 medal
│   │   ├─ RowWheelRes.swift                # H5 L529 橙 rounded-24 + 结果 #FFED68
│   │   ├─ RowAnnouncement.swift            # H5 L652 蓝紫 + 📢
│   │   ├─ RowWinnerBroadcast.swift         # H5 L607 双形态大卡/跑马灯
│   │   ├─ RowWishlistEffect.swift          # H5 L599 粉紫渐变
│   │   └─ RowDiamondGift.swift             # H5 L524 4 子类内嵌 switch
│   └─ Atoms/                               # 5 个共享 View（PublicChat 前缀命名空间）
│       ├─ PublicChatLevelBadge.swift       # Lv.N 分档背景（tier→色）
│       ├─ PublicChatVipBadge.swift         # 金色 VIP crown
│       ├─ PublicChatHostBadge.swift        # HOST 粉底标
│       ├─ PublicChatSenderCapsule.swift    # 组合昵称+Lv+VIP+Host
│       └─ PublicChatGiftIconBubble.swift   # icon + xN + CachedAsyncImage
├─ Adapters/
│   └─ LivePublicChatAdapter.swift          # PublicChatMessage → UnifiedPublicChatMessage
└─ Utils/
    └─ LevelTierResolver.swift              # H5 setLevelStyle 对齐
```

### 修改的既有文件

- [`Sources/Live/LiveRoomView.swift`](../../Sources/Live/LiveRoomView.swift) —— 顶层加 `@StateObject publicChatFeed`；`LiveRoomChatList` → `PublicChatListView(feed:.live)`；删 `PublicScreenList` / `LiveRoomChatRow` / `LiveRoomChatList` 3 处旧结构
- [`Sources/Live/NIMChatroomManager.swift`](../../Sources/Live/NIMChatroomManager.swift) —— `PublicChatMessage struct` 迁到独立文件（下述）
- [`Sources/Live/PublicScreen/LivePublicChatPayload.swift`](../../Sources/Live/PublicScreen/LivePublicChatPayload.swift) —— **新** · 承载 `PublicChatMessage struct`（纯 Foundation，入 HilyTests 白名单）
- [`Sources/Call/CallView.swift`](../../Sources/Call/CallView.swift) —— `LevelBadge`/`VipBadge` → `PublicChatLevelBadge`/`PublicChatVipBadge`（跨场景复用）
- [`Sources/Live/UserCard/UserCardPopup.swift`](../../Sources/Live/UserCard/UserCardPopup.swift) —— 同款迁移
- [`Sources/Live/Marquee/EnterRoomFloat.swift`](../../Sources/Live/Marquee/EnterRoomFloat.swift) —— 同款迁移
- [`project.yml`](../../project.yml) —— HilyTests 白名单加 PublicChat 纯 Swift 文件 + LivePublicChatPayload + PublicChatMessageType

### 删除的旧文件

- **`Sources/Live/PublicScreen/UI/ChatRowSubviews.swift`（635 行）** · 已删
- `Sources/Live/PublicScreen/PublicChatMessageType.swift` · **保留**（adapter 读 `messageType` 派 variant；未来 Phase 4 unification 时删）

### 顺手修的问题

- `Sources/Party/List/PartyListService.swift`（用户 WIP 新文件）违反 [single-target-public-hygiene.md](../../.claude/rules/single-target-public-hygiene.md)，去 `public` 修饰词
- `Tests/HilyTests/EditProfile/FakeEditProfileService.swift` 缺 `picList: nil` 编译错，Task 0 前补齐

## 测试基线

**BUILD SUCCEEDED**（`generic/platform=iOS` Debug + `CODE_SIGNING_ALLOWED=NO`）

**12 个新单测全绿**（`iPhone 16 Pro` simulator）：
- `UnifiedPublicChatFeedTests`（5 tests：append/trim/appendBatch/clear/replace）
- `LevelTierResolverTests`（2 tests：tier 边界 + 负数防御）
- `LivePublicChatAdapterTests`（7 tests：system/anchor/gift/luckyGift/officialBoost/diamondGift/regular）

**pre-existing 失败测试 4 个**（与本 Phase 无关，用户 WIP 未修）：
- `MatchStoreTests.test_R21_nonMatchV4Source_forceEnded / test_R21b_nilSource_forceEnded`
- `MessageSessionStoreTests.test_delegate_update_during_loading_enqueued_and_merged / test_load_success_empty_stays_loaded_with_empty_array`

## Git 分支状态

10 个 Phase 1 commits 在 `main` 上（用户明示"就在当前分支开发"）：

```
9d54fa2 chore: [公屏组件] Phase1-T11 删除旧 ChatRowSubviews.swift + 迁移 4 处外部引用 + Party rule fix
f7cba0f feat: [公屏组件] Phase1-T10 LiveRoomView 切换到 PublicChatListView
36225dc feat: [公屏组件] Phase1-T8+T9 LivePublicChatPayload 拆出 + LivePublicChatAdapter + 7 单测全绿
d086af9 feat: [公屏组件] Phase1-T7 PublicChatRow 分派 + PublicChatListView 容器
e9c437e feat: [公屏组件] Phase1-T6c 5 个 Row（RpsWin/WheelRes/WinnerBroadcast/Wishlist/DiamondGift）
b4bd256 feat: [公屏组件] Phase1-T6b 4 个 Row（EnterRoom/OfficialBoost/PKNotify/Announcement）
44ecced feat: [公屏组件] Phase1-T6a 4 个 Row（RegularText/Anchor/Gift/LuckyGift）
b1c7b4b feat: [公屏组件] Phase1-T2/3/4/5 Store + Utils + Theme + 5 Atoms + 单测
ed53cee feat: [公屏组件] Phase1-T1 建统一 model 层（16 变体 enum + SenderProfile + RichSegment）
```

净 diff：**+1152 行 / -737 行**（新增 26 个文件 / 删除 1 个文件 / 修改 6 个文件）

## 已知偏差 vs 原 plan

1. **命名前缀**：所有 Atoms + `DiamondGiftSubType` 加 `PublicChat` 前缀以避免与旧 `ChatRowSubviews.swift` + `CallView.swift` 内同名符号冲突。删除旧文件后 CallView/UserCardPopup/EnterRoomFloat 一并迁移到新前缀。
2. **rawPayload 字段推迟**：原 plan Task 8 计划为 `PublicChatMessage` 加 `rawPayload: LiveRawPayload?` 并修改 15+ 构造点。**实际改为 adapter 直接读 `messageType`**（旧 UI 分派枚举），避免大范围修改。`PublicChatMessageType.swift` 保留（未来 Phase 4 全场景 unification 时删）。
3. **占位切图**：VIP badge / lucky-gift icon / rouletteMsg icon / luck-gift icon 用 SF Symbol 占位（`crown.fill` / `sparkles` / `circle.hexagongrid.fill` / `hand.raised.fill`）。真机验收时用户可视觉判断是否需要替换真切图（`1758252289621.webp` / `luck-gift.png` / `rouletteMsg.webp` 等）。

## 交接：T12 + T13

### T12 · i18n keys（未完成）

所有 Row 内文案目前**英文硬编码**（如 "Entered Room" / "Official Boost✨" / "Welcome ... to the Platform..." / "wins RPS" / "get" / "hit" / "on the wheel" / "Winner Got" / "sent a Diamond Gift Pack" / "claimed" / "Top: X won Y" / "refunded" 等）。**未接入 `L10n.PublicChat` namespace + en/ar/tr 3 语言 keys**。

**接下来该做**：
1. 扫 H5 i18n 源（`anchor-livechat-h5/src/i18n/lang/en.js / ar.js / tr.js` + `livechat-h5/src/i18n/lang/`）找可复用 key
2. 加 `L10n.PublicChat.enterRoomTemplate` / `.officialBoostEnter` / `.pkNotifyPrefix` / `.rpsWinTemplate` / `.wheelResHitTemplate` / `.announcementPrefix` / `.winnerBroadcastDays` / `.wishlistEffectTemplate` / `.diamondGiftSendTemplate` / `.diamondGiftClaimTemplate` / `.diamondGiftSettledTemplate` / `.diamondGiftExpiredTemplate` / `.giftCountFormat`
3. 3 语言 `.strings` 同步补齐
4. 各 Row 硬编码字符串替换为 `L10n.PublicChat.xxx`

### T13 · 真机验收（**需要用户真机 + 主播账号**）

真机 checklist（同 [plan 文档 Task 13.3](../../docs/plan/统一公屏组件-plan-Phase1-202607092230.md)）：

- [ ] 主播文本（.anchor）—— 紫渐变 anchor-box + 粉昵称 #FE00DE
- [ ] 用户普通文本（.text）—— 灰底 + 青绿昵称 #1AFFCD + Lv/VIP 徽章
- [ ] 送礼（.gift）—— 灰底 + 20pt gift icon + xN
- [ ] 幸运礼物（.luckyGift）—— 268×50 粉黄渐变 + 富文本
- [ ] 进房（.enterRoom）—— tier 渐变 + Lv/VIP + 座驾
- [ ] 官方推荐进房（.officialBoostEnter）—— 蓝渐变 + Official Boost✨
- [ ] PK 通知（.pkNotify）—— 暗红 D33901/30 气泡
- [ ] 猜拳获胜（.rpsWin）—— 紫渐变 + medal + hours
- [ ] 转盘结果（.wheelRes）—— 橙 rounded-24 + 结果 #FFED68
- [ ] 公告（.announcement）—— 蓝紫 + 📢
- [ ] 活动中奖（.winnerBroadcast）—— 大卡 / 简版跑马灯
- [ ] 心愿单登顶（.wishlistEffect）—— 粉紫渐变
- [ ] 钻石盲盒 4 子（.diamondGift）—— 蓝紫渐变，4 子样式差异
- [ ] 系统消息（.system）—— 兜底走 announcement 样式

真机注入方式（DEBUG build）：主播端登录直播房，公屏区**三连击** → `PublicChatDebugInjector.injectAll()` 批量注入 15 类 mock 消息（已存在于 `Sources/Live/PublicScreen/PublicChatDebugInjector.swift`）

## Phase 2/3 展望

Phase 1 完成后：
- **Phase 2**（通话接入）：`CallPublicChatAdapter` + `RowBonus` + `VipChatBubbleBackground`（borderImage）
- **Phase 3**（派对房接入）：`PartyPublicChatAdapter` + `RowGameWinNotify` + `RowPartyModeSwitch` + Party 送礼进公屏

Phase 2/3 各自跑独立 spec + plan（避免 Phase 1 impl 反悔波及）。
