# Hily iOS 主播端 代码审查报告

> 审查时间：2026-07-07 00:00 → 11:20（含 2 次复查修订）
> 审查范围：本会话 v5.5 空闲清理机制（4 个 Sources/*.swift 文件；含 pre-existing 修复）
> 审查方式：Workflow 5 维并行 finder + 3 票对抗验证 →（Workflow finder 全部 stalled，未产出）→ **主流程手工二次复查兜底** → **用户第 3 轮复查筛除过度防御**
> 审查依据：CLAUDE.md + .claude/rules/*

## 审查范围（4 文件）

- [Sources/Message/MessageSessionStore.swift](../../Sources/Message/MessageSessionStore.swift) — 新增常量 + `isCleaningUp` guard + `performIdleCleanupWithProtection` + `isProtected`；`cancellables` `private` → 默认 `internal`
- [Sources/Message/MessageSessionStore+IdleCleanup.swift](../../Sources/Message/MessageSessionStore+IdleCleanup.swift) — 新建 extension：`attachAutoOfflineObserver` + `performIdleCleanup`（读 `CallStore.shared`）
- [Sources/Message/MessageSessionStoreShared.swift](../../Sources/Message/MessageSessionStoreShared.swift) — `shared` 构造后调 `attachAutoOfflineObserver()`
- [Sources/Match/MatchStore.swift](../../Sources/Match/MatchStore.swift) — pre-existing typo 修复（L #3d in-progress code line 483）

**说明**：Workflow 5 个 finder agent 全部 stalled（每 6 次重试 180s 无进展，共烧 2.59M tokens 无产出）。改由主流程手工复查兜底。用户第 3 轮复查明示"不要过度优化和防御，应该处理的就处理"→ 按此原则筛除伪问题。

## 一、概览（用户 3 轮复查后终版）

| 分级 | 数量 | 说明 |
|---|---|---|
| P0 必修 | 0 | 无 blocker |
| **已处理** | **2** | 应改就改（本轮直接改） |
| 已剔除 | 1 | 假设建立在错误的 SDK 顺序模型上，无实际触发链路 |
| 信息（不改） | 3 | 已知设计取舍 / 无收益洁癖 |

## 二、P0（必修）

无。核心清理路径 + 保护清单 + 并发 guard + 单测覆盖均无 blocker。

## 三、已处理（本轮直接改）

### ✅ 处理-1 `isProtected` 传参统一 `now`

- **原判定**：信息（P2-1）
- **升级理由**：不是防御洁癖，是**代码清晰性 + 未来防呆**——若未来阈值改分钟/小时级会命中漂移；一次 cleanup 内时间基准应统一
- **文件**：[Sources/Message/MessageSessionStore.swift](../../Sources/Message/MessageSessionStore.swift) `isProtected` 签名 + `performIdleCleanupWithProtection` 3 处调用点
- **修复内容**：`isProtected` 增加 `now: Int64` 参数，caller 传统一 `let now = Int64(Date().timeIntervalSince1970 * 1000)`（原本就有此变量）
- **成本**：签名 +1 参数，3 处调用改，约 4 行

### ✅ 处理-2 删除冗余 `recomputeSystemInbox()`

- **原判定**：信息（P2-3）
- **升级理由**：保护清单 #2 已排除系统账号 → idsToRemove 永不含它们 → 调用后 systemInboxEntries 结果不会变。保留即是"防御性一致性"的过度防御
- **文件**：[Sources/Message/MessageSessionStore.swift](../../Sources/Message/MessageSessionStore.swift) 原 line 479
- **修复内容**：删除该行，加注释说明"保护清单 #2 排除系统账号"
- **成本**：-1 行

## 四、已剔除（假设错误 / 过度防御）

### ❌ 剔除-1 for-await SDK delete 循环期间 `.update` 竞态

- **原判定**：建议（P1-1）
- **原假设**：SDK 在 delete 命令未返回前推同 session `.update` 会导致 state 复活孤儿
- **剔除理由**：NIMSDK 顺序模型上，`NIMConversationManager.delete` 命令内部去队列/持久化 → `didRemove` 是 delete 的 final callback，中间不会推同一 session 的 update/add。假设建立在错误的 SDK 内部行为理解上，**无实际触发链路**——过度防御，剔除

## 五、信息（不改）

### ℹ️ 信息-1 200 条 SDK delete 串行未测性能

- **状态**：日志已含 `elapsed=Xms`，dev 阶段会话 <30 不触发规则 B，无实测数据不预优化
- **决策**：**不改**，生产半年后观察日志决定

### ℹ️ 信息-2 AutoOffline 弹窗依赖服务端 `max_no_use_app_reminder_time > 0`

- **状态**：配置为 0 时清理永不触发。属产品/后端决策，客户端无解
- **决策**：**不改**

### ℹ️ 信息-3 sink `[weak self]` 对 `shared` 单例多余

- **状态**：无害通用防御模式；改 strong self 只省一次 unwrap
- **决策**：**不改**——无收益的洁癖不改

## 六、pre-existing 修复审计（本会话顺手改，需你事后决策）

### PX-1 `Sources/Match/MatchStore.swift:483` 移除多余 `guard let self`

- **背景**：L 里程碑 #3d 别人 in-progress code，outer closure line 469 `[weak self]` + line 472 已 `guard let self` → line 483 再 `guard let self` 让 self 实际类型是 non-optional `MatchStore`（compile error `initializer for conditional binding must have Optional type`）
- **修复内容**：line 483 由 `guard let self, !Task.isCancelled, ...` 改为 `guard !Task.isCancelled, ...`
- **合理性**：修复正确（Swift 5.7+ shadow 语义），最小化改动
- **越界性质**：属他人 in-progress 代码，按 [.claude/rules/code-review-discipline.md](../../.claude/rules/code-review-discipline.md) §3 应用户批准
- **状态**：**已在实施报告告知用户，待你确认是否保留或 revert**

### PX-2 `project.yml` 补 `FaceDetectionService.swift` 到 HilyTests sources

- **背景**：L 里程碑 #3d 新加的 `FaceDetectionServiceProtocol` / `FaceDetectionServiceStub` 位于 `Sources/Match/FaceDetectionService.swift`（`??` untracked），`MatchStore.swift` 已引用但 `project.yml` HilyTests target 白名单未加 → HilyTests build fail
- **修复内容**：补一行 `- path: Sources/Match/FaceDetectionService.swift`
- **合理性**：修复正确，让 HilyTests target 编译通过
- **越界性质**：pre-existing 环境问题，非本任务，同样按 §3 应用户批准
- **状态**：**已在实施报告告知用户，待你确认**

## 七、复查统计

| 项目 | 数量 |
|---|---|
| Workflow finder 原始产出 | 0（5 agent 全 stalled） |
| 主流程首轮手工复查产出 | 6 |
| 用户第 3 轮复查后终版 |  |
| — 必修 | 0 |
| — 已处理 | **2**（isProtected 传 now + 删除冗余 recomputeSystemInbox） |
| — 剔除（过度防御） | 1（.update 竞态假设错误） |
| — 信息（不改） | 3（未测性能 / 服务端配置依赖 / weak self 单例） |
| 越界修复（pre-existing） | 2（等你决策） |
| 单测状态 | **721/0 pass**（处理-1/-2 修改后回归通过） |
| 主 target build | **SUCCEEDED** |

## 八、本次审查方式反思

1. **Workflow 5 finder 全部 stalled**：4 文件小范围审查烧 2.59M tokens 无产出。教训：小范围审查（<10 文件）应跳过 Workflow 直接手工——本次手工复查覆盖度更高
2. **首轮手工复查过度防御**：原报告 P1/P2 共 6 条，其中 4 条属于"理论存在 + 无触发证据 + 修复防御性"类。用户明示筛除后剩 2 条真该改的（1 是清晰性 + 1 是明确冗余），沉淀新纪律：**代码 review 找的应该是"该改的"，不是"能改的"**
